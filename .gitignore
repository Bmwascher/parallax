__pycache__/
.pytest_cache/
.claude/*
!.claude/settings.json
# drift watch local state (tools/check-drift.ps1) - reports are the only
# copy of a week's findings, but they are machine-local, not project history
tools/drift-snapshot.json
tools/drift-pending.json
tools/drift-reports/

# Not ours: a broken mechanical rebrand of CLAUDE.md that calls this a
# "Codex plugin" and gives two update commands that do not exist. Ignored
# so a `git add -A` cannot re-track it, which is exactly how it reached the
# branch in the first place. The preflight still SEES it: that enumeration
# uses --others WITHOUT --exclude-standard, so ignoring does not blind the
# back-channel check.
AGENTS.md
