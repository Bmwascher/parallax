`JINN/...` below means the exact pinned clone root supplied in the brief; `PROBE/...` means the supplied probe-fixture root.

1. **C1 — adoption correct; patch shape incomplete.** The current preflight and test cover only `AGENTS.md` (`skills/multi-model-verify/SKILL.md:50-62`; `evals/multi-model-verify/test_multi_model_verify.py:162-186`). The probe demonstrates full ingestion of the planted `.agents` skill (`PROBE/transcript1.txt:16-18`, `PROBE/transcript1.txt:82-96`), and the minimal pathspec covers jinn’s actual symlink surface (`JINN/.agents/skills/showcase-video-capture:1`). Adopt option (a), but first add a test asserting the literal `.agents/*` pathspec and STOP semantics, then update the pending-adoption note (`skills/multi-model-verify/references/model-prompting-notes.md:159-174`). Do not add unprobed `.codex/`. **FIX**

2. **C2 — adoption correct; consumer/test sweep incomplete.** The dated record and raw error establish the deterministic signature and absent reply (`skills/multi-model-verify/references/model-prompting-notes.md:137-146`; `PROBE/transcript2.txt:1`). Jinn recognizes it and automatically restarts without consent (`JINN/packages/jinn/src/engines/codex.ts:363-367`, `JINN/packages/jinn/src/engines/codex.ts:759-767`). Amend the immediate-gate list and catch-all as well as the session-loss section (`skills/multi-model-verify/references/fallbacks.md:27-48`, `skills/multi-model-verify/references/fallbacks.md:98-103`); add a dedicated exact-signature regression test because the current session-loss test only pins eventual gating (`evals/multi-model-verify/test_multi_model_verify.py:355-370`); and add the missing-rollout no-retry branch to the public fallback diagram/summary (`README.md:82-99`). **FIX**

3. **C3 — adoption correct; test locking is insufficient.** The dated probe supports adding `CODEX_HOME` (`skills/multi-model-verify/references/model-prompting-notes.md:110-128`), and the six named consumers are indeed the current contract surfaces (`tools/check-drift.ps1:539-548`; `evals/tools/run_behavioral_evals.py:477-489`; `commands/doctor.md:42-50`; `README.md:100-112`). However, the drift test’s tuple omits it, the runner test merely checks that a denylist symbol exists, and the doctor anchors do not pin any environment variable (`evals/multi-model-verify/test_multi_model_verify.py:1292-1304`, `evals/multi-model-verify/test_multi_model_verify.py:1321-1328`, `evals/multi-model-verify/test_multi_model_verify.py:1391-1408`). Add exact four-variable assertions for drift, runner, and doctor before changing the consumers. **FIX**

4. **C4 — adoption correct; output contract and README consumer are missing.** The dated probe records the experimental handshake and returned quota fields (`skills/multi-model-verify/references/model-prompting-notes.md:184-194`), matching jinn’s five-second, stdin-held-open implementation (`JINN/packages/jinn/src/shared/engine-limits.ts:370-453`). The doctor currently requires every table row to use `OK / STALE / BROKEN` (`commands/doctor.md:5-7`), so “UNAVAILABLE but never failure” needs an explicit representation. Fold quota state into the transport row’s state while leaving its verdict determined solely by transport, or define a non-failing `N/A` verdict. Pin that exact behavior—not merely a quota token—in `TestDoctorCommand` (`evals/multi-model-verify/test_multi_model_verify.py:1385-1408`), and update the README’s enumerated doctor capabilities (`README.md:176-179`). **FIX**

5. **C5 — correctly rejected.** Jinn bypasses Codex sandbox/approval enforcement for fresh, resume, and interactive calls (`JINN/packages/jinn/src/engines/codex.ts:334-355`; `JINN/packages/jinn/src/engines/codex-interactive.ts:385-395`) and seeds broad `curl`, `rm`, write, and edit permissions (`JINN/packages/jinn/src/cli/setup.ts:645-658`). Parallax explicitly pins and verifies `read-only` (`skills/multi-model-verify/SKILL.md:78-99`). **PASS**

6. **C6 — correctly rejected.** Jinn treats any non-null exit with a thread id and nonempty result as success (`JINN/packages/jinn/src/engines/codex.ts:745-756`). Parallax classifies nonzero exit or stale/empty output as transport failure (`skills/multi-model-verify/SKILL.md:88-99`; `skills/multi-model-verify/references/fallbacks.md:43-49`). **PASS**

7. **C7 — rejection correct, wording inaccurate.** The restart is automatic and lacks explicit user consent, violating the continuity gate (`JINN/packages/jinn/src/engines/codex.ts:759-767`; `skills/multi-model-verify/references/fallbacks.md:3-6`, `skills/multi-model-verify/references/fallbacks.md:98-103`). It is not literally silent: jinn emits a warning (`JINN/packages/jinn/src/engines/codex.ts:762-764`). Replace “silent” with “warning-logged but automatic and unconsented.” **FIX**

8. **C8 — rejection stands only after narrowing the premise.** Setup-time version probing is informational (`JINN/packages/jinn/src/cli/setup.ts:72-78`, `JINN/packages/jinn/src/cli/setup.ts:395-425`) and lacks parallax’s weekly scheduling, snapshots, version-change handling, and pending lifecycle (`tools/check-drift.ps1:13-25`, `tools/check-drift.ps1:188-245`, `tools/check-drift.ps1:274-320`). But “jinn’s engine probing is `--version` only” contradicts its structured quota probe (`JINN/packages/jinn/src/shared/engine-limits.ts:370-453`), already adopted by C4. Rewrite this as “jinn’s setup-time engine-version/drift check.” **FIX**

9. **C9 — factual premise refuted.** CI does run only typecheck/build/test (`JINN/.github/workflows/ci.yml:8-45`), but `pnpm test` executes Vitest (`JINN/packages/jinn/package.json:21-27`), and those tests parse frontmatter and assert tool/content contracts across shipped skills (`JINN/packages/jinn/src/shared/__tests__/template-company-doctrine.test.ts:210-230`, `JINN/packages/jinn/src/shared/__tests__/template-company-doctrine.test.ts:232-330`). PUT validation is separately tested against malformed frontmatter (`JINN/packages/jinn/src/gateway/__tests__/skills-api.test.ts:148-182`). Parallax remains broader through generic lint, security, routing, and structural gates (`.github/workflows/skill-evals.yml:1-44`; `README.md:181-191`), so retain rejection but replace the false rationale with: “jinn has targeted domain tests, but no repo-wide generic all-skill lint/security/routing gate comparable to parallax.” **FIX**

10. **C10 — finding valid, record wording/path need correction.** The release skill explicitly directs automation-token publishing, bypassing interactive login/OTP, and pushes main/tags (`JINN/.claude/skills/release-jinn-cli/SKILL.md:61-77`); the showcase skill is repository-local instructional content (`JINN/.claude/skills/showcase-video-capture/SKILL.md:1-15`). The symlink is specifically `.agents/skills/showcase-video-capture`, targeting the showcase skill—not “the `.agents/skills` symlink” (`JINN/.agents/skills/showcase-video-capture:1`). Record these as repository-local agent-instruction surfaces, not generically “auto-loading”; the intake charter requires noting such files as hostile-reference findings (`commands/intake.md:11-27`). **FIX**

**UNVERIFIED:** C10’s historical assertion that nothing was followed or executed. The supplied files establish the charter and instruction surfaces, but no retained intake transcript or execution log proves that negative. Record it as the intake session’s assertion, not independently verified evidence.

**Overall verdict: FIX BEFORE RULE TEXT.** C1–C4 are sound adoptions in substance, but their implementation contracts need the amendments above. C5–C6 stand. C7–C10 require corrected wording or rationale; C9 is the material factual error.

