Round 2. Evidence rules and verdict grammar as before. Position changes
since round 1 — all ten of your findings were verified against the cited
lines this session and are ACCEPTED; none refuted, none struck:

- C1: accepted as amended. Adoption order: test first (assert the literal
  `.agents/*` pathspec + STOP semantics in TestTransportContract), then
  SKILL.md preflight, then rewrite the pending-adoption note. `.codex/`
  stays out until probed.
- C2: accepted as amended. The signature joins the immediate-gate list at
  fallbacks.md:27-48 AND the session-loss section :98-103; dedicated
  exact-signature test added to TestFallbacks; README mermaid gains the
  missing-rollout no-retry edge next to quota-exhausted (README.md:87).
- C3: accepted as amended. Tests-first with exact four-variable
  assertions at test_multi_model_verify.py:1303 (drift), a strengthened
  runner assertion at :1321-1328 pinning the tuple content, and a doctor
  anchor pinning `CODEX_HOME`; then the six consumers change together.
- C4: accepted as amended, with a concrete resolution for the verdict
  grammar: doctor check 1 already emits a non-failing
  `N/A (GitHub install — ...)` verdict (commands/doctor.md:17-18), so
  quota becomes its OWN row whose verdict may be `OK` or
  `N/A (experimental surface unavailable)` — never BROKEN by itself, and
  never folded into the transport row's verdict. TestDoctorCommand pins
  the N/A-permitted behavior, README doctor capabilities updated
  (README.md:176-179).
- C7: accepted — wording becomes "warning-logged but automatic and
  unconsented" (verified at JINN codex.ts:762-764).
- C8: accepted — premise narrowed to "jinn's setup-time engine
  version/drift probing" (its quota probe is separately adopted via C4).
- C9: accepted — your citation verified (template-company-doctrine
  test.ts:210-330 asserts MCP-tool content contracts across shipped
  skills); rejection retained with corrected rationale: "targeted domain
  tests, but no repo-wide generic all-skill lint/security/routing gate."
- C10: accepted — record the surfaces precisely (repo-root
  .claude/skills/* as Claude-session-local instruction surfaces; the
  .agents/skills/showcase-video-capture symlink specifically), and the
  "nothing followed or executed" line is recorded as this session's
  assertion, per your UNVERIFIED note.

No new risks raised. Verdict requested per claim on the amended plan:
PASS / FIX / ESCALATE.
