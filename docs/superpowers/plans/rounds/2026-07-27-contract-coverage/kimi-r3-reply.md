# Round 3 review — contract coverage checker, revision 2

I re-read the brief, both named documents in full, and every live file the plan touches: `docs/superpowers/specs/2026-07-27-contract-coverage-design.md`, `docs/superpowers/plans/2026-07-27-contract-coverage.md`, `skills/multi-model-verify/references/backup-lane.md`, `skills/multi-model-verify/references/panels.md`, `skills/multi-model-verify/references/fallbacks.md`, `agents/fable-panel-reviewer.md`, `evals/multi-model-verify/test_backup_lane.py`, `evals/multi-model-verify/test_seat_reshuffle.py`, plus `agents/implementer.md`, `agents/flash-implementer.md`, `CLAUDE.md`, `README.md`, `.claude-plugin/plugin.json`.

I did not re-litigate the six findings. I looked for what the fixes broke or missed.

## New finding (the only FIX)

**F1. Task 7 documents the refuted revision-1 pin definition into CLAUDE.md.** The plan's Global Constraints define a pin as "a string literal in one of exactly two positive-presence shapes" (docs/superpowers/plans/2026-07-27-contract-coverage.md:18), and the design devotes a table to the three excluded shapes (docs/superpowers/specs/2026-07-27-contract-coverage-design.md:126-133). But the text Task 7 appends to CLAUDE.md says a pin is "a string inside an `assert`" (docs/superpowers/plans/2026-07-27-contract-coverage.md:1333-1336). That is exactly the revision-1 overclaim that finding 4 refuted — an assertion's failure message is a string inside an `assert`, and the design itself says it "checks nothing about the text" (design:131). CLAUDE.md is the standing instruction file future editors read; an author following it could write `assert p.is_file(), "<region text>"` believing the region is locked, or extend a pin into a `not in` comparison. This is precisely this repo's recorded pattern: a defect inside the fix, in the last-written, least-checked artifact. **Fix: rewrite that sentence in Task 7 Step 1 to name the two shapes** — e.g. "where a pin is a string literal on the left of `"literal" in body` or an argument to `body.count("literal")`; strings in failure messages, under `not`, in `not in`, or in `==` comparisons do not count."

## Per-claim verdicts

**C1 — PASS.** Whole-region containment removes the mis-split bug class structurally rather than bounding it. The friction cost is real and is stated on the record, not waved away: "No weakening valve, deliberately" with the reasoning that this repo's failure mode is weakening a perpetually-red test (design:213-217), and the author-sizes-the-region rule is explicit (design:51-55). The friction I raised in round 1 is larger under one-region-one-pin, but the refuted alternative is what produced the live silent pass; the trade is stated honestly, which is what the brief asks. PASS.

**C2 — PASS, with the census UNVERIFIED.** The census numbers (331 `In`, 105 `Call`, 71 `Eq`, 64 `BoolOp`, 26 `NotIn`, 14 `Name`, 9 `not`) are a measurement I cannot re-derive without a shell — UNVERIFIED. But the plan would still be correct if the numbers were off, because the failure direction of every dropped shape is a red, never false coverage, and the plan demonstrates the recovery path (extend the assertion) in Tasks 4–6. Against the live files I can read: `BoolOp` cases are not dropped at all — `_collect_from` recurses into children, so `"a" in body and "b" in body` is collected (plan:570-571); the one genuinely-locking dropped shape I found live is the Name-bound pin at evals/multi-model-verify/test_seat_reshuffle.py:97-102, which the design names explicitly as the single accepted instance (design:190-197). No live shape that locks document text is silently dropped.

**C3 — PASS.** `_collect_from` returns without descending on `not` and on any `NotIn` comparison (plan:558-562). Dropping a positive check nested in a negation can only produce a false RED (region reads uncovered), never false green — and semantically it is correct anyway: `assert not ("x" in body and "y" in body)` does not guarantee `"y"` is present. Confirmed against the code; nothing that matters is dropped.

**C4 — PASS, one hardening note.** The anchoring claim verifies against the live tree: `agents/implementer.md:13,28` and `agents/flash-implementer.md:19,34` carry only `shared-contract:` markers, and `MARKERISH = <!--\s*contract:` (plan:275) cannot match them. I grepped both scanned trees: no stray `contract:` comments exist today. I hunted for a marker-shaped input that still vanishes silently and found one residual: MARKERISH requires the colon immediately after `contract`, so `<!-- contract : start id=x -->` (space before colon) matches neither MARKERISH nor the strict patterns and is ignored. It does NOT produce a silent deletion in practice: a single-sided typo is caught by the partner marker ("end with no start" / "never closed", plan:317-325, 338-339), and a double-sided typo during region addition is caught by the declared-inventory red (plan:859-867), which the task order always populates first. Not a defect; worth one sentence in the design's accepted limits if you want the grammar's edge stated.

**C5 — PASS, simulation claims UNVERIFIED but self-checking.** The control/defect structure is sound and the anti-vacuity test (plan:784-794) plus the three per-instance tests asserting exactly `["hist-N-defect"]` (plan:766-781) make a wrong-reason red structurally impossible: a fixture whose pins failed to load reports two misses and fails `len(misses) == 1`. Internally consistent details I could verify: fixture 12's defect body (plan:727-732) does not contain the bare phrase, so the historical bare-phrase pin cannot cover it; fixture 11's control equals the historical fragment pin, so it is covered iff the defect is not. The historical-commit claims themselves ("simulated against real git history", plan:666-667) are a measurement I cannot re-run — UNVERIFIED — but if the simulation were wrong, these very tests go red at Task 3 Step 5, which is the correct failure direction for a gate.

**C6 — PASS, re-confirmed.** The inventory compares both directions (plan:863-870), so deleting a region with its markers still goes red via the declared set. Nothing about it changed since round 1; I re-verified the mechanism against the code rather than from memory.

**C7 — PASS.** The selection rule is as PASSed before. Both named entries verify live: `panel-lane-loss` is `###`-headed at skills/multi-model-verify/references/fallbacks.md:190 and `panel-lane-unavailable` at fallbacks.md:210. The three-way split of `panel-lane-unavailable` matches the document exactly: principle (fallbacks.md:220-221), procedure (fallbacks.md:221-224), invariant (fallbacks.md:224-226), with the `An unavailable lane is recorded` sentence correctly left outside (fallbacks.md:226-228). Nine ids total (3+2+4) matches the design's inventory (design:251-263).

**C8 — PASS, as the zero-judgment implementer.** I executed the plan by reading. Every "Replace:" block matches the live file byte-for-byte: the two rotation-guard pins (test_backup_lane.py:141-142, 152), both seat-reshuffle pairs (test_seat_reshuffle.py:78-80, 134-136), and the fallbacks fragment (test_seat_reshuffle.py:173). Every normalized region body the plan dictates matches the live document: rotation guard (backup-lane.md:53-70), panels floor (panels.md:73-77), agent floor (agents/fable-panel-reviewer.md:30-33). The residual-gap "already covered" claim verifies (test_backup_lane.py:163-165). The one variable-bound pin the design concedes exists where it says it does (test_seat_reshuffle.py:97-102). `body` in `test_backup_lane_evidence_pins` is `_norm`-read (test_backup_lane.py:117), so the extended multi-line pins will match. Test-count arithmetic is internally consistent: 16+13+4+2 new tests over a 170 baseline gives 186/199/203/205 (plan:1393-1399). Task 7's anchors exist (CLAUDE.md:41 `## Skill editing rules`; README.md:95-107 component table; plugin.json:3 is `"0.14.4"`). The only step where I would hesitate as that implementer is Task 5 Step 3's "indent to match the surrounding text" — but the exact normalized target body is given and Steps 5/7 verify it, so no invention is required.

## UNVERIFIED (measurements, no shell)

- The assert-expression census (C2 numbers) and the 715→375 / 192 / 19 / 9 counts (design:129-137, plan:548-553). Internally consistent across both documents; note the brief's "194" vs the documents' "192" failure-message strings — a brief paraphrase, not a document defect; the documents agree with each other.
- The 170-passed baseline and all intermediate counts. Arithmetic is internally consistent; absolute baseline not re-derivable.
- The historical-commit simulations in Task 3 and the "will be reported / must NOT be reported" predictions in Tasks 4-6. Partially verified by reading (no existing pin contains the three `panel-unavailable-*` sentences — I grepped the evals tree and found none); the rest is self-checking at execution time.

## Overall verdict

**FIX — one specific fix: Task 7 Step 1's CLAUDE.md sentence "a pin is a string inside an `assert`" (docs/superpowers/plans/2026-07-27-contract-coverage.md:1335) must be rewritten to name the two positive-presence shapes before this plan executes.** With that change, the plan is safe to execute: both rounds of mechanism fixes are faithfully reflected in the code and tests, every live anchor I could check matches, and I found no other defect introduced by the fixes.

To resume this session: kimi -r 049ce8a8-b55a-4be2-a41d-d3d81104faba
