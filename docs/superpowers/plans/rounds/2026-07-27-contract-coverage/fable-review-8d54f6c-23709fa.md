# Fable whole-branch review — range `8d54f6c..23709fa`

Seat: `agents/fable-reviewer.md`, dispatched fresh by the controlling
session. Required input to the mode-diff debate under SKILL.md. Raw reply,
verbatim, retained unedited.

---

### Strengths

- **The classifier's restraint is real and correctly implemented.** `_clause_pins` (evals/multi-model-verify/contract_coverage.py:207-258) matches only complete clauses and returns `set()` on anything unrecognized; the count branch's arity, keyword, int-not-bool, and positive-bound guards (contract_coverage.py:244-259) each map to a documented exclusion, and every exclusion I traced fails toward UNCOVERED, never toward false coverage. The reversed comparison, chained comparison, walrus, and `all(...)` shapes all fall through to `set()` without special cases, exactly as the plan's complete-clause constraint requires.
- **No-reword claim independently confirmed, all four documents.** Method: exhaustive hunk-pair comparison in the controller's -U10 diff — a reword must surface as a removed/added line pair, and diffs cannot hide a changed line outside a hunk. In every hunk of skills/multi-model-verify/references/backup-lane.md:54-76, references/fallbacks.md:196-238, references/panels.md:74-81, and agents/fable-panel-reviewer.md:30-35, the removed and added lines are word-identical with only line-break repositioning around the inserted markers. The plan's marked-body normalization targets (plan lines 1322, 1341, and Task 6's three bodies) match the marked text exactly.
- **Both pin-extension commits strictly strengthen.** Every old fragment is a substring of its replacement, so nothing previously locked is released: test_backup_lane.py:144-149 contains the old "if after the call the file is SMALLER…" fragment; test_backup_lane.py:160-165 contains "That is a route-attribution failure"; test_seat_reshuffle.py:80-85 contains both old panel-floor-agent fragments; test_seat_reshuffle.py:141-146 contains both old panel-floor-reference fragments; test_seat_reshuffle.py:187-196 contains the old "the panel cannot silently convene without it" fragment. Each new pin normalizes to exactly its region body, so containment holds by identity, not coincidence.
- **Inventory is exact and two-directional.** Nine ids in `DECLARED_REGIONS` (test_contract_coverage.py:526-536); exactly nine live marked regions confirmed by repo-wide grep (backup-lane.md:54,61,72; panels.md:74; fallbacks.md:196,222,226,232; fable-panel-reviewer.md:30). `test_declared_regions_match_the_documents` (test_contract_coverage.py:539-550) fails on both missing and extra, so the inventory cannot drift silently in either direction. The `hist-*` fixture ids live under evals/ and the design spec's example markers under docs/, both outside `DOC_PATHS` (test_contract_coverage.py:513-517), so neither collides.
- **The history fixtures reproduce the real defect shapes, not incidental failures.** Instance 10's control is covered by instance-10-pins.py:142-143 while no pin contains the disposition region whole; instance 11's control is covered by the "IS transient" pin (instance-11-pins.py:1522-1523 region of the diff) while nothing runs through "would produce clean evidence"; instance 12's control "Claude Code 2.1.216" is pinned twice while nothing pins the floor's operative sentence. `test_history_fixtures_are_not_vacuous` (test_contract_coverage.py:498-508) requires exactly one miss per fixture, which rules out the failed-to-load-pins false red.
- **The sweep exclusion is correctly scoped.** test_backup_lane.py:522-523,532-533 excludes only the fixture directory, only from the `BACKUP_ID` single-source sweep. `DOC_PATHS` and `PIN_PATHS` never reach the fixture directory, so no coverage or route check is blinded. The fixtures do contain `BACKUP_ID` (instance-10-pins.py:20), so the exclusion was necessary, and by-directory scoping means future fixtures do not reopen it for this file.

### Issues

#### Critical

None.

#### Important

1. **One-third of the range is off the frozen plan, and the reviews that authorized it are not retained.** Commits f872b34 (widens `DOC_PATHS` from the plan's references+agents to skills/\*\*+agents+commands — plan line 1097-1100 vs test_contract_coverage.py:513-517), 8a6a9fb (five tests beyond the plan, including direct `_clause_pins` access, test_contract_coverage.py area of diff lines 2240-2251), 4ec80b1 (edits the frozen plan itself in-range, plan lines 21 and 205), 8d313b9, and 23709fa have no basis in plan revision 7. The ledger reconstruction (docs/superpowers/plans/rounds/2026-07-27-contract-coverage/ledger-reconstruction.md:55-94) attributes them to an Opus whole-branch review and a scoped re-review, but neither artifact exists in the rounds directory (listing shows only plan-phase round replies), the original SDD ledger is deleted, and the debate record covers mode-plan rounds 1-2 only — the directory holds replies for rounds 1, 2, 5, and 6 and nothing for 3-4. I verified each off-plan commit's content directly and all five strengthen (the widening closes a genuine inert-scanner hole; the 4ec80b1 wording fix is correct — `body.count("x") > 0` does lock, and "a zero or negative count comparison" contradicted the stated `> n, n ≥ 0` bound in all three sites). The code is sound; the evidence chain for these commits rests entirely on session claims. Remedy is a record commit: retain the two Opus review artifacts if they survive, or state on the record that they do not, and fold rounds 3-6 into debate-record.md.

#### Minor

1. **Region-only trailing shrink can stay green under a cross-target pin.** `uncovered` (contract_coverage.py:288-300) pools pins with no binding between a pin and the document its assertion actually reads, and pins are collected from assertions against non-document receivers too (e.g. the evals.json `joined` pins, test_backup_lane.py's eval-case test). If a region's tail is deleted in the document while its markers stay, the shrunk body remains a substring of the old pin; when that pin targets the same document the pin's own assertion goes red, but when the containing pin targets a different artifact nothing does. This is a sharper variant of the design's accepted "Cross-region coverage" limit and is not covered by the new lockstep-shrink entry (design spec:301-309), which requires the pin to move too. Still needs a real coincidence; worth one tagged line in the design's limits list.
2. **instance-10-doc.md:2 overstates the historical defect.** It says "the disposition sentence had no pin," but instance-10-pins.py:146 pins the fragment "not a reason to re-read from zero," which is inside that sentence. What the fixture actually reproduces — correctly — is fragment-not-whole coverage. The comment on evidence should match the evidence.
3. **The pin-grammar paragraph in CLAUDE.md:47-77 is itself unlocked.** CLAUDE.md is outside `DOC_PATHS` and no test pins this paragraph, yet it is the instruction surface the suite's own docstrings say drifted from the code twice before, and it drifted once more inside this very range (the Task 7 CRITICAL). Acceptable to ride, but it is the one contract document the mechanism cannot see.
4. **Fixture verbatim-ness against 4d8a121 / 8eacc8a / f9fd9b9 is not re-verifiable under my tool grant** (no git access). The plan's Step 3 grep protocol plus the ledger's claims are the only verification; I checked internal consistency (controls covered, defects uncovered, historical pre-I2 wording present in instance-12) and found nothing contradicting them. Named as a gap, not a finding.

### Ledger minors triage

- **No fenced-code-block awareness** — ride. The failure direction is loud (a fenced marker parses as a real region and errors or demands a pin); a fence exemption would rebuild the silent-skip hole. No scanned document currently fences a marker (grep confirms markers exist only in the four live documents plus unscanned docs/ and evals/).
- **`PIN_PATHS` self-exclusion by exact filename only** — ride. The excluded module's region-body literals sit in equality comparisons and fixture-driven asserts that the clause rule would not collect anyway; the exclusion is belt-and-braces.
- **`test_flash_implementer.py` sweep lacks the fixture exclusion** — ride, verified currently safe: the sweep at test_flash_implementer.py:141-158 covers `evals/**/*.py`, and zero flash-literal matches exist in the fixture directory today. Fix opportunistically the next time that file is edited; a kimi- or flash-quoting fixture would trip it.
- **Design spec's false-coverage count reads as past tense** — ride. Docs-only, and the spec explicitly argues against maintaining a live total.
- **Known gap: debate record covers rounds 1-2 only** — fix-before-merge in spirit; since the merge already happened, fold into the Important 1 record commit rather than riding silently.

### Assessment

**Ready to merge: With fixes** — the fix being a record commit, not a code change. Every code-level property I could test holds: the checker's exclusions all fail safe, the four documents are word-identical inside markers, the extended pins are strict supersets that equal their regions, the inventory is exact, the fixtures reproduce their historical defects non-vacuously, and the sweep exclusion blinds nothing; but five of twelve commits sit outside the frozen plan with their authorizing reviews existing only as an unverifiable session claim, and closing that evidence gap is precisely what this after-the-fact debate exists to do.
