# Cross-vendor review ΓÇö parallax 0.16.0, range `c6b7c85..7ddb871`

I read the brief, the full diff (`0160-diff.txt`), and every changed file in its final state, and attacked the four areas the brief names. Findings first, then what held.

## Findings (all minor, preference-class ΓÇö none block merge)

**1. Minor / follow-up: the sibling mutation lane still ships the `Write(**)` rule this branch proves invalid.** This branch removes `Write(**)` from the drift runner because "the CLI rejects it" (tools/check-drift.ps1:490-496, rule removed at tools/check-drift.ps1:497-499) and asserts its absence there (evals/multi-model-verify/test_multi_model_verify.py:1408). But `MUTATION_ALLOWED_TOOLS = "Skill,Read(**),Glob,Grep,Edit(**),Write(**)"` survives in evals/tools/run_behavioral_evals.py:113, and a test in the same suite still asserts its *presence* (evals/multi-model-verify/test_multi_model_verify.py:1060-1061). Both lanes are internally consistent today, so nothing is red, and the branch's claims are explicitly scoped to the drift runner ΓÇö but the same no-op warning now fires on every behavioral-eval mutation run by this branch's own evidence. One-line follow-up, not a merge blocker.

**2. Minor / record imprecision: the "three scenarios added" enumeration names the wrong third scenario.** docs/superpowers/plans/2026-07-27-0150-backlog.md:72-75 lists "one proves a verdict-less run is recorded as a generic runner failure" as one of three *added* scenarios, but `no-verdict` pre-existed ΓÇö this branch only added one assertion to it (evals/tools/drift_statemachine_tests.ps1:458). The third genuinely new scenario is `blocked-crash` (evals/tools/drift_statemachine_tests.ps1:461-477), which the record does name separately at docs/superpowers/plans/2026-07-27-0150-backlog.md:69-70. All claimed coverage exists; only the enumeration is loose.

**3. Minor / robustness edge: a non-ASCII lock label strands the lane for up to 45 minutes.** Acquire writes the lock via `Set-Content -Encoding ASCII` (tools/kimi-lane-lock.ps1:234), which mangles non-ASCII label characters to `?`; the owner's own later release then fails the case-sensitive compare (tools/kimi-lane-lock.ps1:187). Self-heals at the staleness threshold and `-Force` is the documented remedy, and the contract's label format `"<debate>-<round>"` is ASCII (skills/multi-model-verify/references/backup-lane.md:85). Preference, not defect ΓÇö a one-line label charset check on acquire would close it.

## What I attacked and found holding

- **Lock ownership.** A bare `-Release` is refused against a labelled lock, against `label: 0`/`null`/blank/missing, and against a case-variant label (tools/kimi-lane-lock.ps1:174-192); acquire requires a nonblank label (tools/kimi-lane-lock.ps1:204-208); the 45-minute threshold is a bare constant with no flag or env override (tools/kimi-lane-lock.ps1:73). Under the brief's attacker model: a hand-written stale stamp can free a held lane, but that is the documented cost of age-based staleness stated at tools/kimi-lane-lock.ps1:10-23 and backup-lane.md:89-91 ΓÇö and no single hand-written file can hold the lane forever, since future stamps (tools/kimi-lane-lock.ps1:134), non-string stamps (tools/kimi-lane-lock.ps1:110), and unparseable stamps (tools/kimi-lane-lock.ps1:124-127) all read as infinitely old and breakable. The JSON-scalar edges (`0`, `false`, `5`) all resolve breakable-or-refused, never wedged.
- **The age routine.** DateTimeOffset parsing makes age a function of the instant (tools/kimi-lane-lock.ps1:122-128); sentinel ages are described, never printed as measurements (tools/kimi-lane-lock.ps1:159, 241). The test suite pins every one of these against the defect it names, including both UTC-`Z` halves and the throw-on-non-string wedge (evals/multi-model-verify/test_kimi_lane_lock.py:352-417).
- **Three-state classification in check-drift.ps1.** I enumerated every exit from the auto-triage block (tools/check-drift.ps1:412-796): timeout, gate failure, commit failure, untrusted run, and worktree failure each set `$autotriageFailure`; clean-exit BLOCKED sets only `$autotriageBlocked` (tools/check-drift.ps1:734-752); the two trusted outcomes clear `$manualToast`. The comment's invariant ΓÇö exactly one variable non-empty on every enabled manual path, both empty only under `-NoAutoTriage` (tools/check-drift.ps1:380-389) ΓÇö holds against the code, the toast precedence reports exactly one state (tools/check-drift.ps1:803-813), and the pending entry's `failure` semantics match commands/drift-triage.md:31-37, including empty-for-BLOCKED. The blocked-crash and credits-death scenarios exercise the two failure shapes end-to-end (evals/tools/drift_statemachine_tests.ps1:461-499).
- **Session-block attribution.** The rule binds by order, is fail-closed on truncation, and discloses its residual (skills/multi-model-verify/references/backup-lane.md:56-80); the self-collision case is closed by the per-user lane lock whose path is checkout-independent (tools/kimi-lane-lock.ps1:57-60), and a false-pass interleave would require a foreign session sharing parallax's exact containment yaml ΓÇö which is the case the lock already serializes. The duplicate-evidence disposition gap from the earlier review is reconnected at backup-lane.md:51.
- **Contract coverage.** All 13 `DECLARED_REGIONS` (evals/multi-model-verify/test_contract_coverage.py:624-641) match the live markers in the scanned trees exactly, with `rotation-guard-residual-gap` surviving only in an unscanned historical plan. Each new/renamed region sits whole inside one valid `"literal" in body` pin (evals/multi-model-verify/test_backup_lane.py:138-144, 147-151, 154-158, 166-179, 239-244), and the two restoration guards are correctly declared non-pins (evals/multi-model-verify/test_backup_lane.py:186-190).
- **Attestation message.** The reject reason names each failing field (tools/verify-attestation.ps1:52-78); the all-fields-valid guard is genuinely unreachable since both call sites only invoke it after `Test-AttestationPasses` returned false on the same three fields (tools/verify-attestation.ps1:195, 219). The tests assert the failing field is named *and* the passing fields are absent (0160-diff.txt:507-562).
- **Prior rounds.** The fable review's I1/I2 and the Sol reply's Q1/Q2/Q5 (0160-diff.txt:377-491) are all addressed in the final state; the record's "41 tests" claim matches the pytest case count in evals/multi-model-verify/test_kimi_lane_lock.py (30 functions + 5 + 6 parametrized).

## UNVERIFIED ΓÇö excluded from the verdict

I cannot run the suites. The record's "Full suite: ALL SCENARIOS PASS" (docs/superpowers/plans/2026-07-27-0150-backlog.md:75), the live-log verification of block attribution (backup-lane.md:65-67), and the on-disk rotation evidence (backup-lane.md:113-123) are runtime claims I read but did not reproduce.

Overall verdict: **PASS**

