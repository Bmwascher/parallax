1. The wildcard write is fixed by `-LiteralPath`. The read-back verifies the literal destination; protection against an additional sibling write comes from `-LiteralPath` and the sibling assertion. [tools/write-attestation.ps1:289](C:/Users/Brandon/AppData/Local/Temp/pxmr/tools/write-attestation.ps1:289), `tools/write-attestation.ps1:296`; [evals/multi-model-verify/test_mirror_reaper.py:588](C:/Users/Brandon/AppData/Local/Temp/pxmr/evals/multi-model-verify/test_mirror_reaper.py:588).

   The regression test exercises the bracketed-path problem and would reject e9d2713’s wildcard write to the sibling. It does not recreate the old false-success branch because it plants no previous record in the bracketed repository. The sibling has a copied `.git`, and its assertion checks parsed JSON equality, not byte equality. `evals/multi-model-verify/test_mirror_reaper.py:78`, `evals/multi-model-verify/test_mirror_reaper.py:575`, `evals/multi-model-verify/test_mirror_reaper.py:580`, `evals/multi-model-verify/test_mirror_reaper.py:588`.

   One comparison defect remains: **`-ne` is case-insensitive**, so unequal record contents can pass. I evaluated the live expression on both hosts with `{"participants":"Session/Reviewer"}` versus `{"participants":"session/reviewer"}`; both returned false for the rejection condition. Use `[string]::Equals($writtenText, $json, [System.StringComparison]::Ordinal)` and reject its negation. Add a comparison case differing only in casing. `tools/write-attestation.ps1:300`.

   The declared non-ASCII repository-name consequence is correctly classified: ASCII replacement already produced a repository field the verifier rejected. The new comparison makes that failure immediate. `tools/write-attestation.ps1:249`, `tools/write-attestation.ps1:289`; [tools/verify-attestation.ps1:40](C:/Users/Brandon/AppData/Local/Temp/pxmr/tools/verify-attestation.ps1:40). **FIX — make the content comparison ordinal.**

2. The inspection catches now distinguish missing entries from other errors. A non-absence exception names the sidecar, includes the unattempted-bridge note, and exits 3 before bridge removal. Reading the control flow confirms this repair. I have no independently verified non-administrator Windows failure mechanism to add; the declared inspection-failure coverage remains reading-only. `tools/write-attestation.ps1:319`, `tools/write-attestation.ps1:323`, `tools/write-attestation.ps1:359`. **PASS.**

3. The implementation checks attributes after deletion and reaches the success message only through a missing-entry catch. A surviving entry or another inspection error exits 3 with the bridge note. `tools/write-attestation.ps1:333`, `tools/write-attestation.ps1:343`, `tools/write-attestation.ps1:351`, `tools/write-attestation.ps1:356`.

   The test’s ordering pin is only a string-position assertion. Moving the failure text into an uncalled function before the success statement would satisfy it; the happy-path test could still pass without a runtime read-back. The current implementation is correct. A named test follow-up is to drive a post-delete read-back failure and assert exit 3, no success announcement, and an untouched bridge. `evals/multi-model-verify/test_mirror_reaper.py:592`, `evals/multi-model-verify/test_mirror_reaper.py:604`. **PASS for the repair; the pin does not lock runtime ordering.**

4. The inserted paragraph explicitly supersedes the defective sidecar recipe and identifies the shipped inspection handling, deletion read-back, literal record write, and content read-back. Keeping the earlier block as historical evidence satisfies round 1’s requested correction or explicit supersession. [docs/superpowers/plans/2026-09-13-mirror-reaper.md:968](C:/Users/Brandon/AppData/Local/Temp/pxmr/docs/superpowers/plans/2026-09-13-mirror-reaper.md:968). **PASS.**

5. The diff contains exactly the three named files. The emitter count is **34 additions and 4 deletions**; the diffstat’s `38` counts both. The test module adds 42 lines, including `import json`, and the plan adds two lines. The changed regions match the claimed scope. `tools/write-attestation.ps1:287`, `tools/write-attestation.ps1:319`, `tools/write-attestation.ps1:339`; `evals/multi-model-verify/test_mirror_reaper.py:19`, `evals/multi-model-verify/test_mirror_reaper.py:569`; `docs/superpowers/plans/2026-09-13-mirror-reaper.md:968`. **PASS.**

6. Removal/write sweep: **none further**, beyond claim 1’s comparison defect. The changed record write has a read-back, and sidecar deletion has an absence check. Upward-discovering git reads: **none**; this diff introduces no git reads. I inspected the complete emitter, test-module, and plan changes in `e9d2713..fafc4ce`. `tools/write-attestation.ps1:287`, `tools/write-attestation.ps1:343`; `evals/multi-model-verify/test_mirror_reaper.py:569`; `docs/superpowers/plans/2026-09-13-mirror-reaper.md:968`. **PASS.**

**UNVERIFIED:** Disposable-repository execution of the new tests and their failure against e9d2713; the session’s ACL probe; the reported 66/66 results and lint/scanner results. The sandbox remains read-only. The case-comparison probe ran without filesystem writes under both PowerShell hosts. Unverified material does not contribute to the verdict.

No file instruction caused a pause, refusal, or scope change.

**Range verdict: FIX — replace the case-insensitive record comparison with ordinal equality.** `tools/write-attestation.ps1:300`.