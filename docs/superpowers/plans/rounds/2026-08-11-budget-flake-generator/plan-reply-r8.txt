1. “Assert the shadow and what casts it” is honest diagnosis, but it is not the right permanent oracle. The test requires the redundant source anchor to remain and requires its mutants to keep producing no differences (`evals/multi-model-verify/test_skill_report_shapes.py:313-340`). That locks today’s topology without proving the fallback works.

The arithmetic is deliberate defense in depth: its comment explicitly rejects relying on the preceding rule after prior defects (`tools/codex-context-probe.ps1:184-188`). Test that purpose by fault injection:

- Keep the direct tests proving `Hide-KnownContainer` rejects malformed shapes.
- Add a test-only baseline that bypasses `Hide-KnownContainer` only for `skills_instructions`, representing the primary guard failing open.
- First require the unchanged downstream arithmetic to classify the generated arrangements correctly under that fault.
- Apply each of the three arithmetic mutants to that baseline and require a generated case to kill it.

This remains test-only, consistent with Task 6’s no-production-change boundary (`docs/superpowers/plans/2026-08-11-budget-flake-generator.md:492-507`). **FIX — replace survivor assertions with explicit upstream-failure injection and kill all three downstream mutants.**

2. Item 9 is not closed yet. The frozen plan requires one mutant per named defense and says every mutant must be killed (`docs/superpowers/plans/2026-08-11-budget-flake-generator.md:528-542`). Naming three survivors is more honest than hiding them, but it does not satisfy that completion rule.

Once the fault-injection lane kills those three, item 9 may close without a residual, retaining the already-agreed narrow scope: generated coverage for these two parsers, not parser correctness across the repository. If the fault-injection work does not land, item 9 remains open; “closed with a named residual” would require another explicit plan amendment. **FIX — do not close item 9 until all three downstream defenses are killed under the declared fail-open fault model.**

3. There is no missing production input shape that reaches the arithmetic with malformed skills boundaries:

- `skills_instructions` is in the known-container list (`tools/codex-context-probe.ps1:398-402`).
- `Get-SkillReport` runs every value through `Hide-KnownContainer`; a throw sets `Ambiguous` before the later arithmetic (`tools/codex-context-probe.ps1:206-220`).
- The masking function admits only no pair or exactly one correctly ordered pair; all other counts and close-before-open throw (`tools/codex-context-probe.ps1:450-472`).
- The later arithmetic runs only when no such throw occurred and repeats that same predicate (`tools/codex-context-probe.ps1:222-243`).

Length-preserving masking replaces bodies with spaces, so it cannot manufacture a malformed delimiter arrangement (`tools/codex-context-probe.ps1:466-478`). Fault injection—not another generated input—is therefore the correct way to exercise the fallback. **PASS**

No additional substantive finding emerged. This is not an adjudicated dry round because the survivor treatment requires a fix. The ledger is correct: unit 9 is spent and one authorized unit remains (`skills/multi-model-verify/references/debate-protocol.md:79-90`).

UNVERIFIED:

- The reported two-host executions and printed mutation results; their test structure is present, but I did not rerun them because no Python executable was available on PATH.
- The claimed live `skill_lint` output of zero errors and zero warnings; the 5250/5500 implementation and exact byte-oracle corrections are present on disk.