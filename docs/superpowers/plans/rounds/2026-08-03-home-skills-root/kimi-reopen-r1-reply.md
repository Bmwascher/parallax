# Backup lane (Kimi K3), reopened debate, round 1 reply

Session evidence: validator `status: clean`, write-probe PASS on all three
legs in the same debate home, mirror delta 0 against a 212-entry baseline,
mirror at HEAD `694f323`. This file carries the reviewer's FINAL REPLY only.
The client's tool-call narration is not retained: it is 80 KB of the reviewer
paging through files already in this repo, and the repo is public.

---

I have read the probe record in full, the frozen plan in full, both contract files, the fixture, both agent files, the pin suites, and the region registry. Everything below is grounded in those reads; where I reason rather than cite, I say so.

  ## q1 — Claim 2 is the overreach

  **Claim 2 states more than the measurement supports, in two distinct ways.**

  First, "suppresses all four roots" is measured for exactly ONE root. The cell table (probe-record.md:39-45) planted a canary in only two places across the whole run: `~/.agents/skills/` (cells B, C, D) and `<debate-home>/skills/` (cell E). The flag-on cell, C, carried a canary at the home root only, and its suppression is what the frozen comparison measured (probe-record.md:127-134). The two project roots — `.kimi-code/skills/` and `.agents/skills/` — held nothing in any cell this run, so their suppression under the flag is UNOBSERVED, not measured. It is inferred from the replacement semantics the home-root cell establishes plus the client's own help text ("instead of auto-discovered user and project directories", quoted at plan:84) — which is documentation, and which plan:84 itself labeled UNVERIFIED before this probe. The probe verified replacement for the root that was canaried; it did not verify the help text's "project directories" clause.

  Second, and this is a logical slip, not just an evidentiary one: the flag does not suppress all four roots because the fourth root IS the flag. The four roots are enumerated at backup-lane.md:329-330. `--skills-dir <debate-home>/skills` makes `<debate-home>/skills/` the discovery root — it selects that root, and cell E confirms that root is discovered (probe-record.md:82, :212-213). What the flag suppresses is the OTHER THREE: the two project roots and the home root. "Suppresses all four" describes a client that ignores its own flag argument.

  The honest claim 2 is: "--skills-dir REPLACES discovery with its target directory — measured directly for `~/.agents/skills/` (C against D, C matching the calibrated not-found exactly, probe-record.md:197-201), inferred for the two project roots from the replacement semantics plus the help text, and by definition selecting rather than suppressing its own target."

  Two lesser qualifications, not refutations:

  - **Claim 1** stands but is version-bound. The record answers reachability for kimi-code 0.31.1 (probe-record.md:17, :208-210), one canary, named exactly, with `Skill` offered and invoked. The plan's own never-shipped disposition text knew to bound this way (plan:616-622: "bound to what the probe reached: one skill, named exactly, at one root, on kimi-code 0.31.1"). Your claim omits the bound; in context I'll accept it as shorthand, but the contract text must carry it.
  - **Claim 3** is the best-supported of the four, and I independently verified its negative: the builder creates `<debate-home>/skills` empty (tools/new-kimi-lane-home.ps1:902, with `extra_skill_dirs = []` at :874), and the round validator contains no skill-related check at all — grep for `skill|Skill` over tools/read-kimi-round-evidence.ps1 returns nothing, and the per-round evidence section (backup-lane.md:176-273) enumerates every check without touching the skills directory. One caveat: "suppresses the home root ONLY because that directory is empty" is an inference — no cell ran flag-on with a POPULATED target. It follows from the flag's measured replacement semantics plus its documented purpose, and I judge it sound, but it was not exercised.
  - **Claim 4** reaches the right conclusion with a slightly wrong support structure. Cells A and B BOTH had the flag passed AND `Skill` denied (probe-record.md:41-42, :76-77), so they cannot attribute the no-fire to either layer — they measure the conjunction. The flag's suppressive power was measured only under the probe agent, a configuration the lane never runs. The precise statement is: the deny list alone is measured to close the hole as shipped; the flag-plus-empty-dir is a measured control in a configuration the lane doesn't use, held in reserve. That is still two independent layers, so I let claim 4 stand with that note.

  ## q2 — Replacement wording

  Constraint check first: the SKILL.md passage at lines 70-73 sits OUTSIDE any contract region (the nearest markers are `enumeration-depth-asymmetry` at lines 79/88), and no test pins its text — grep over evals/ for "indistinguishable", "suppresses nothing", "PRIMARY control" finds no pin on SKILL.md. The backup-lane.md passage at :345-354 also sits outside contract markers (no region covers the config-surface section; DECLARED_REGIONS at test_contract_coverage.py:651-671 contains none for it), but it IS pinned by three asserts at test_backup_lane.py:861-867, which must change FIRST per the standing constraint (plan:59). My replacement introduces no contract markers, so no DECLARED_REGIONS edit is needed; the pins become ordinary string asserts. No backslashes anywhere, satisfying the references/ rule.

  **SKILL.md, replacing lines 70-74** ("Probed 2026-07-31 ... not defence in depth."):

  > Probed 2026-07-31 with canaries planted at both project roots: a run with `--skills-dir` and a run without it were indistinguishable, and neither loaded either canary — a CONFOUNDED result, because `Skill` is on the backup reviewer agent's deny list, so that probe measured the deny list rather than the flag. Re-probed 2026-08-03 on kimi-code 0.31.1 with `Skill` offered (docs/superpowers/plans/rounds/2026-08-03-home-skills-root/probe-record.md): the flag REPLACES discovery with its target directory — a canary in `~/.agents/skills/` loaded with the flag omitted and returned the calibrated not-found with it passed. That control is conditional (its target must stay empty) and version-bound, and a planted SKILL.md remains readable as ordinary file content whatever discovery does — so this enumeration is the PRIMARY control for the reviewed tree's skills and agents, not defence in depth.

  **backup-lane.md, replacing lines 345-354:**

  > `--skills-dir` is a MEASURED control, promoted from mitigation on 2026-08-03 (docs/superpowers/plans/rounds/2026-08-03-home-skills-root/probe-record.md). Measured on kimi-code 0.31.1 with the `Skill` tool offered and invoked by exact name: a canary planted in `~/.agents/skills/` loaded with the flag omitted, and with the flag passed the lookup returned the calibrated not-found result exactly — the flag REPLACES discovery with its target directory rather than adding to it. That suppression was measured directly for the home root; the two project roots rest on the replacement semantics and the client's own help text, and preflight-3 remediation clears them in the mirror regardless. The 2026-07-31 probe that found runs indistinguishable was CONFOUNDED: `Skill` is on the reviewer agent's deny list, so it measured the deny list, not the flag. The control is CONDITIONAL: it suppresses the other roots only while `<debate-home>/skills/` is EMPTY, the builder creates it empty, and no per-round check re-verifies that at dispatch. The load-bearing control as the lane ships is the deny list — a discovered skill cannot be invoked — with preflight-3 remediation and this flag as measured second layers. Keep passing `--skills-dir` on every call, fresh and resumed, and claim for it exactly what was measured: replacement, conditional on an empty target, on 0.31.1.

  Note what both replacements preserve deliberately: the enumeration's primacy and the keep-passing instruction survive; what dies is only "unmeasurable" and "claim nothing."

  ## q3 — Should the enumeration survive?

  **For keeping it.** The disposition is version-bound by its own terms — even the plan's never-shipped text said "a client whose skill delivery changes shape retires this measurement rather than inheriting it" (plan:651-657). An enumeration recorded before round 1 is the tripwire that notices when the answer changes: the root held 27 directories at this probe (probe-record.md:24), the largest instruction surface on the reviewer's machine, and a count that moves between debates is the cheapest drift signal available. The primary lane already records the analogous environment note (`~/.codex/AGENTS.md`, backup-lane.md:310-312), so deleting this one creates an asymmetry the record would have to explain. And the flag's conditionality (q1, claim 3) means the configuration's safety rests on facts the enumeration documents.

  **Against keeping it.** The original rationale — "unprobed territory, recorded as such rather than assumed absorbed by the tool allowlist" (backup-lane.md:342-344) — is dead, and an instruction whose rationale is gone but whose mechanics survive is exactly how this repo accumulates stale records (backlog items 10 and 17 both). The measurement shows the root is out of the discovery listing as the lane dispatches, so the enumeration feeds no decision: nothing branches on it. It is also an information hazard — the probe record deliberately carries "no directory listing of the user's home skills root" (probe-record.md:262-264), yet "record what it holds" invites exactly that into per-debate records of a public repo. And drift is already bounded: per-round hash continuity makes a client change visible in the record (backup-lane.md:240-251), and the evidence rules fail closed.

  **Recommendation: keep it, reduced.** Enumerate before round 1, record the COUNT and never the listing, and replace the disposition sentence with the measured one:

  > Enumerate that root before round 1 and record its count, never its contents — the repo is public. A non-empty one is REACHABLE territory, measured 2026-08-03 on kimi-code 0.31.1 (docs/superpowers/plans/rounds/2026-08-03-home-skills-root/probe-record.md): delivered when `--skills-dir` is omitted, suppressed when it is passed with an empty target, and held closed as the lane ships by the `Skill` deny list. The measurement is version-bound; a changed count or a client upgrade reopens it.

  This keeps the tripwire, kills the hazard, and states the measured truth. The pin at test_backup_lane.py:848-853 changes first.

  ## q4 — The clause, and the search

  **How the clause should have read.** The defect is at plan:523, :525 and :530 ("a matching `tool.result` carrying the nonce" / "its result carries the nonce"). Replacement:

  > Cell E produces a `tool.call` for the exact canary name AND a `tool.result` whose `toolCallId` matches that call AND the nonce somewhere in the cell's wire slice, in any record type. The record carrying the nonce is named verbatim in the probe record. No clause may read a tool's effect out of that tool's own result record without a per-tool measurement that the effect lives there: a Grep result's output IS the answer (`evals/multi-model-verify/fixtures/kimi-round/fresh-wire.jsonl:14`), but a Skill result is a fixed confirmation string and the body arrives as a separate `context.append_message` whose `origin.kind` is `skill_activation` (probe-record.md:138-150).

  This keeps the unconditional invocation requirement (which rounds 1-2 fought for, plan:23-25) while detaching the positive readout from the result record specifically. Readout 1 already searched the whole wire slice (plan:509), so the gate branch needed only to point at it.

  **The search — I looked, and here is everywhere I looked:**

  - `tools/`: grep for `tool\.result` returns NOTHING. The round validator never reads result content — its checks are counts, hashes, and exact-list equality (backup-lane.md:213-239). Clean.
  - `evals/multi-model-verify/evals.json:16, :27, :67`: three behavioral expectations DO read a tool's effect from its own `tool_result` — but each names a specific tool (a Read bearing the startup header, a git-diff call bearing the hunk, Read/Grep read-backs bearing file content) whose result genuinely carries that content on that lane. They are per-tool, not generalized. They are the same SHAPE of oracle, though: if one were ever templated onto a new tool the defect recurs. I flag the shape, not the instances — they stand.
  - `evals/multi-model-verify/test_multi_model_verify.py:926-994, :1383` and `evals/tools/run_behavioral_evals.py:362-425`: these test and implement tool_result id-binding, ERROR detection, and truncation in the transcript renderer — evidence plumbing, not tool-effect oracles. Clean.
  - The fixtures (`fresh-wire.jsonl:14`, `resume-wire.jsonl:14`): Grep results, accurate for Grep. The SOURCE of the generalization, not themselves wrong — the probe record confirms the fixture matches the live client record-for-record (probe-record.md:170-175).
  - The generalization's other habitats are all in the debate record itself: it entered from the review lane (sol-plan-r1-reply.md:56, sol-plan-r2-reply.md:28), and — worth noting honestly — the Kimi lane ANTICIPATED this exact failure mode (kimi-r1-reply.md:139: "What if the Skill tool result does NOT echo the skill body") and accepted it as fail-closed VOID. It was fail-closed, which is why the run survived; but the plan adopted the assumption as fact at measured fact 6b (plan:81) and three gate branches.

  Conclusion: the generalization sits in no other LIVE oracle. The repository's remaining tool_result readers are tool-specific and correct per tool.

  ## q5 — Describe, or require a check?

  **The strongest case against a per-round check.** (1) There is no measured writer to guard against: the debate home is built once per debate by the builder (backup-lane.md:47-73), nothing in the lane copies anything into it, and the single measured exception — cell E's canary copy — was probe-only (plan:569). A check for content nobody introduces guards a hypothetical. (2) The flag is the SECOND layer; the deny list alone is measured to close the shipped lane (probe-record.md:216-222). A per-round check would be a guard on the precondition of a backup control — third-order machinery. (3) This repo's own adjudication names the surviving defect class: "a check that passes either way" (plan:811). Every new check is a new false-clean surface that itself needs pins and mutation tests; the plan's standard for a REAL guard is Task 2's transactional, rollback-protected, positive-controlled harness (plan:302-322), and a per-round emptiness check built to that standard against an unthreatened precondition is scope creep; built cheaply, it is the defect class itself. (4) The effective surface is already pinned per round — hashes, toolCount, exact-list equality (backup-lane.md:213-251) — and a client that changed discovery semantics would move those surfaces or the version bound.

  **What I would ship.** Description, plus one build-time self-check — not a per-round oracle. The builder is the ONLY writer of `<debate-home>/skills` (it creates it at new-kimi-lane-home.ps1:902), so one assertion inside the builder that the directory it just created is empty is a self-check on its own act: near-free, fail-closed at the single moment content could enter, and no new per-round surface. The contract text then DESCRIBES the precondition (my q2 wording: "no per-round check re-verifies that at dispatch" — which remains true), and the q3 count-only enumeration is the drift tripwire. Per-round verification of an invariant nobody can violate mid-debate — the home holds one session plus the write-probe's, by construction (backup-lane.md:59-64) — buys nothing and costs a pinned, mutation-tested oracle.

  ## Unresolved points

  1. Whether suppression of the two PROJECT roots is accepted on replacement-semantics-plus-help-text inference, or demands its own canary. I say the inference suffices; the record itself already speaks of replacement generally (probe-record.md:212-213), which is wider than its cells.
  2. Whether evals.json:16/:27/:67 should carry a "per-tool, do not template" note. The instances are correct; the shape is the hazard.
  3. Task 5's frozen replacement text (plan:606-622) asserts the canary "was NOT reachable" — factually false post-probe — and must not ship; the pin block at test_backup_lane.py:842-853 and the two DECLARED_REGIONS additions it planned (plan:625) need fresh text, which this debate supplies in q2/q3 but nothing has executed.
  4. Whether the builder self-check I propose in q5 needs its own pin and mutation test to escape the "passes either way" class — I believe one assert watched to fail once is proportionate, but it is a judgment call.
  5. Claim 4's support structure: A/B measured the conjunction of both layers, not each independently (probe-record.md:76-77). "No hole today" stands; which layer holds it as shipped is measured only for the deny list.
