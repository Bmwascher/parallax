Round 3. Three asks, then I think we are converged.

First, a concession I owe you and am acting on. Your unresolved point 2 is correct and it is my error, not a historical curiosity. The probe record says the flag "is promoted from an unmeasurable mitigation to a verified control" and that "what the flag does is REPLACE discovery, not add to it". Neither survives your round-1 refutation. I am appending a dated CORRECTION section to that record rather than editing the claims in place, because the record is evidence and silently rewriting evidence is worse than carrying a correction. Say if you think in-place correction is better here and why.

ASK 1, the deliverable. Give me the COMPLETE final body of both contract regions, ready to paste, with nothing left as direction. Constraints, all of them hard:

- Files under `references/` are checked for the ABSENCE of backslashes. Use forward slashes everywhere.
- Each region's text must sit WHOLE inside a SINGLE test pin. A region too long for one pin is two regions, and we only have two frozen ids. So each body must be short enough that one string literal can carry it. Judge that yourself and tell me if either body is too long, because splitting would need a third id and that is a scope change the user has not authorized.
- `home-skill-root-disposition` carries the measured reachability and the enumeration instruction. `home-skill-root-disposition-limit` carries what the disposition does NOT establish, including your version-bound system-prompt sentence and the flag's unverified-until-checked precondition.
- Both must read as instructions to a driver, not as a lab report.

ASK 2, the evidence form for the per-call emptiness check, which is your own unresolved point 1. Pick one and justify:

  (a) prose instruction in the dispatch step, driver records a line in the debate record;
  (b) a new switch on an existing tool that prints a machine-readable result the driver must check;
  (c) folded into `tools/read-kimi-round-evidence.ps1` as a PRE-call mode.

You already argued the validator is the wrong home because it runs after the call. That argument kills (c) only if the validator cannot also be run before. Say whether it can. I lean (b), because this repo has repeatedly found that a prose-only check is a check nobody can prove ran, but I have not tested that against you.

ASK 3, scope, and this is a decision I will take to the user rather than make alone, so give me a recommendation with its cost. What we have agreed is no longer a text edit. It is: two rewritten contract regions, a SKILL.md edit, four new failure-direction tests for a check that does not exist yet, the check itself, replacement pins for four stale strings in `evals/multi-model-verify/test_backup_lane.py`, and `DECLARED_REGIONS` edits. The frozen plan's Task 5 was scoped as "write the measured disposition" and nothing else, and its Task 6 is backlog and version bookkeeping.

Is this an AMENDMENT to the frozen plan, or a NEW plan that supersedes Task 5? Argue for one. If amendment, say exactly which tasks change and whether the plan's Verification status survives. If new plan, say what the frozen plan's remaining tasks become.

End with anything still unresolved. If nothing is, say so plainly rather than manufacturing a point.
