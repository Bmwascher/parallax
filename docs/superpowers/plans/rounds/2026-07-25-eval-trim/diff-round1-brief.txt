<role>Same reviewer, new mode: DIFF-MODE verification of the implemented range.
Evidence rules and verdict grammar as before.</role>

<task>The frozen plan you debated (docs/superpowers/plans/2026-07-25-behavioral-eval-trim.md,
Verification status FULL, deviation A1 recorded) is implemented on branch
feat/0110-eval-trim. Verify SPEC FIDELITY of the committed range against the
frozen plan: the implementer makes zero judgment calls, so any unrecorded
drift is a finding. Non-port work - no port-fidelity lane. End with PASS,
FIX, or ESCALATE on the range.</task>

<range>
Base: 3364342 (merge-base with main)
Head: 73d49f0 (working tree == head)
Commits: 2bc264d spec, 8eaba28 plan+spec-amend, 77013d3 round-1 fixes,
babc629 freeze, de26c26 task 1, 9e6f269 task 2, 4d83982 task 3,
6a5ac43 round-3 record, 0955460 amendment a1, 8354cb2 bump,
73d49f0 fix wave.
Diff packages on disk (read them rather than re-deriving):
- C:/Users/Brandon/Documents/parallax/.superpowers/sdd/2026-07-25-behavioral-eval-trim/final-review-3364342..8354cb2.diff
  (commit list, stat, full diff with rounds/ retention files excluded from the body)
- C:/Users/Brandon/Documents/parallax/.superpowers/sdd/2026-07-25-behavioral-eval-trim/review-8354cb2..73d49f0.diff
  (the fix wave)
You may additionally read any file at head directly in your cwd.
</range>

<claims>
1. The range implements exactly plan Tasks 1-3, Task 4's version bump,
recorded amendment A1, and the recorded final-review fix wave - nothing else.
Every changed path appears in the plan's File Structure section, the
deviation record, or the retention/spec docs.

2. The A1 canonical string is byte-identical between the clause-lock test
(test_no_manufactured_objections_grades_outcome_not_label) and
evals.json expectations[0] of no-manufactured-objections, and matches the
string recorded in the plan's deviation table row A1.

3. The surface table shipped as debated: SKILL.md in all 7 cases;
fallbacks.md on degraded-consent-gate; application-checkpoint.md on both
fix-* cases; model-prompting-notes.md AND debate-protocol.md (your F2) on
the three debate cases; missing-reference-refusal SKILL.md-only; semantic
pins test enforces all of it.

4. select_cases semantics are as debated: surface-glob match with backslash
normalization; grading-view entry diff excluding the surface key; your F1
TypeError catch in parse_base_entries with the {"evals": null} test;
base_entries None selects all.

5. CLI wiring as planned: --changed after the --list block and before the
CLI tool checks; mutual exclusion with --case immediately after parse_args;
zero selections prints "no behavioral surface touched" exits 0; --list
untouched.

6. Post-freeze deltas are ONLY A1 (debated, round 3) and fix-wave 73d49f0
(three final-review minors: loader consolidation, plan-table blank line,
subprocess timeout - scoped re-review verdicted all addressed, no
breakage). Ledger at
.superpowers/sdd/2026-07-25-behavioral-eval-trim/progress.md records
every completion, deferral, and parked ruling.

7. Live evidence: four offline gates green (pytest 133 passed 1 skipped at
head); 3 consecutive stability PASSes on the final string
(scratchpad eval-trim-stability/a1-run1, a1-run2, a1-run3); run 3 via
--changed printed 6 SKIPPED + exactly 1 SELECTED (entry-diff) then PASS
(a1-run3.log). Quota discipline recorded: 91% cycle start, 96% post-live.
</claims>

<boundaries>The plan's content was settled in rounds 1-3 and is not
re-litigated here; this round verifies the RANGE against it. User decisions
(reword-not-split, run-now timing, skills/ frozen) stand.</boundaries>

<final-check>List anything you could not verify against files you read as
UNVERIFIED - do not fold unverified material into your verdict.</final-check>
