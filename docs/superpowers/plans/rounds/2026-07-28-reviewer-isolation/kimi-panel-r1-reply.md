I have completed the review. My findings follow, in the verdict grammar the brief specifies.

## Findings

1. **Major — tools/codex-context-probe.ps1:80,673 — the pass-2 absence proof reads quoted prose inside the user-authored `<INSTRUCTIONS>` body as block presence.** `Get-SkillReport` gates `BlockPresent` on a raw `$text.Contains("<skills_instructions>")` (line 80), and the pass-2 verdict blocks whenever `BlockPresent` is true (lines 673-677). Everywhere else the parser masks user-authored free-text regions before judging them (that is the entire two-stage pipeline, lines 337-348), but the suppression verdict does not. The branch's own test blesses a global `AGENTS.md` house rule quoting a `<skills_instructions>` marker as a legitimate configuration that must not block (evals/multi-model-verify/test_codex_context_probe.py:786-801), and SKILL.md:153 documents that the global `AGENTS.md` survives suppression by design. On such a machine, pass 2 renders that same body, the raw `Contains` fires, and the probe blocks with "the skills block is still present after suppression … suppression did not take" — a wrong diagnosis with no remediation short of editing the user's own instruction file. The same fixture used as pass 2 (evals/multi-model-verify/fixtures/codex-prompt-input/suppressed.json) carries no such quote, so no test reaches this path. Minimal fix: compute the pass-2 `BlockPresent`/entry facts on text with known-container bodies masked (or with the validated `<INSTRUCTIONS>` span excluded), so the absence proof respects the same masking the scan stages already apply.

2. **Minor — tools/codex-context-probe.ps1:155,619 — the project-doc delimiter is checked on the unmasked instruction segment.** `$seg.Contains("--- project-doc ---")` (line 155) runs over the whole `<INSTRUCTIONS>` span, which carries the user's global `AGENTS.md` verbatim. A global file that merely quotes the delimiter (e.g. documenting this mechanism) falsely reports a project doc and blocks at lines 619-622 with "the reviewed tree's AGENTS.md is being ingested" — wrong tree, wrong diagnosis. Same class and same fix direction as finding 1; fail-closed, so minor.

3. **Minor — tools/new-review-mirror.ps1:100,123 — quoted porcelain paths reject a legitimate run.** `Get-ManifestSubject` strips quoting with `$rest.Trim('"')` but never unescapes git's octal-escaped quoting, which porcelain applies by default (`core.quotepath`) to non-ASCII or control-character filenames. A repo carrying an untracked or ignored non-ASCII file produces a baseline entry like `?? "caf\303\251.txt"`; the trimmed path resolves to nothing, and `Get-ContentManifest` returns "baseline path … has no file behind it" (line 123) → BLOCKED. Fail-closed, but it rejects a legitimate mirror build with a misleading reason. Minimal fix: decode the quoted form (strip quotes, then unescape octal/standard sequences) before resolving, or pass `-z`/`-c core.quotepath=false` on the status capture and split on NUL.

4. **Minor — tools/codex-context-probe.ps1:747,770 — the `global_agents_md` report field measures block presence, not the file.** Both the clean and measured-only reports set `global_agents_md = $instructions.BlockPresent`, i.e. "the `<INSTRUCTIONS>` block rendered", while the field name and the non-JSON output (line 780, "global AGENTS.md present:") claim a fact about the user's file. Only `global_agents_md_path` (lines 632-638) actually measures the file. Whether the block renders on a machine with no global `AGENTS.md` is renderer behavior I cannot verify from this tree (INFERRED risk: the field could read True with no file present, or a bare machine could hit the missing-block stop at lines 462-465). commands/doctor.md:174 consumes this field as an environment note, so the blast radius is reporting fidelity, not gating.

## Unverified

- Renderer behavior of `codex debug prompt-input` on a machine with **no** global `AGENTS.md` (whether `<INSTRUCTIONS>` renders empty, or is absent and trips the shape stop at tools/codex-context-probe.ps1:462-465). Requires running codex; I have no shell.
- Actual execution of the pytest suites and PowerShell scripts. All test-vs-code matching above was verified by reading literals (probe messages vs. assertions at e.g. test_codex_context_probe.py:398, 621, 728, 891; mirror messages vs. test_review_mirror.py:74, 344, 477), not by running them.
- robocopy junction/reparse-point behavior when the stale `-Force` target was not produced by this script itself (tools/new-review-mirror.ps1:217); the overlap guards (lines 167-184) cover the reviewed-tree hazard, and I found no reachable wrong-tree delete.
- The claim that single-quoted TOML values survive Windows PowerShell 5.1 native argument passing (tools/codex-context-probe.ps1:424-429 asserts both hosts were probed 2026-07-28); consistent with the known 5.1 quoting behavior, not re-verifiable here.

## Verdicts

1. **PASS** — Amendments A1-A14 are each present where declared: single-quoted literals (A2, tools/codex-context-probe.ps1:442), in-process stub/UTF-8 pin/join (A3, lines 504-528), tracked-only staging plus directory pruning (A4, tools/new-review-mirror.ps1:234-272), `-OverrideOut` on the child call (A5, line 320), SKILL.md ordering (A6, skills/multi-model-verify/SKILL.md:85 vs 120), the `client-probe-scope-limit` region (A7, SKILL.md:149-163), provider-path resolution and `-SkipProbe` exit 1 (A8, new-review-mirror.ps1:157,343-348), guarded write/resolve plus exactness and malformed-entry rules (A9, probe:338-344,583-595,701-714), A10-A14's masking order, case rules, count rule, two-stage mask and close-after-open ordering (probe:189,331,339,240-255,337,363-374). No undeclared drift found. Finding 1 is a defect, not drift.
2. **PASS** — Both transport blocks re-read the file, check SHA-256 with `-cne`, strict-decode UTF-8, and pass `$override` with both flags (SKILL.md:180-184, 225-229); a tampered file throws (SKILL.md:182,227); the only other `codex exec` invocations in the tree are doctor/drift reachability probes, not review dispatches (commands/doctor.md:70, commands/drift-triage.md:80).
3. **PASS** — Within the declared grammar I found no path to `clean` without a successful suppression pass, an absent post-suppression block, a written artifact, and validated fields (probe:548-724, 734-758). Findings 1-2 are false-**block**, the opposite direction.
4. **PASS** — Scope is stated identically in all four places: SKILL.md:149-163 ("Do not call a passing probe full reviewer isolation"), README.md:165-180, docs/superpowers/specs/2026-07-28-reviewer-isolation-design.md:347-368, docs/superpowers/plans/2026-07-27-0150-backlog.md:343-369. No overstatement found.
5. **PASS** — Tests assert real literals that exist in the code and fixtures (spot-verified throughout; e.g. the single-quote pin matches A2 at test_codex_context_probe.py:176-193; replace-based fixtures would fail rather than pass vacuously if their target literal vanished; `assert_built` at test_review_mirror.py:77-90 requires exit 1 AND the specific skip line).
6. **FIX** — The destructive side is well guarded (overlap guards, provider-path resolution, stale-artifact check before any work, all test-pinned), but finding 3 rejects a legitimate run on quoted porcelain paths.

OVERALL: FIX — finding 1 blocks a configuration the branch itself declares legitimate, in the feature's central verdict path, with a wrong diagnosis and no remediation.

To resume this session: kimi -r 80e4075c-c7e7-4c3d-af42-c91955203080
