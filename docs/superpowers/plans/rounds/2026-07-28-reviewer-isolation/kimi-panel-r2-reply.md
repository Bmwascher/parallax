Panel round 2, head `42c942143b7c3d1c319e407c542991bb8fa0637c`.

## Fix-by-fix verification

**Fix 1** — Verified as applied. `Get-SkillReport($text, $structural)` takes presence from the masked text and entries from raw (tools/codex-context-probe.ps1:75-98); `Get-StructuralText` wraps `Hide-KnownContainer` with a caught-throw-to-blocked fallback (:411-430); both call sites pass it (:614-615, :735-736). Masking blanks bodies only, leaving container delimiters (:294-296), so a real top-level block is still seen — polarity-pinned by `test_a_real_surviving_block_is_still_seen_through_the_masking` (evals/multi-model-verify/test_codex_context_probe.py:1125). **But see Finding 1: the fix opens a false-clean path the raw-text check used to close.**

**Fix 2** — Verified. The delimiter is matched only alone on its line via `(?m)^[ \t]*--- project-doc ---[ \t]*\r?$` (tools/codex-context-probe.ps1:184-185), which tolerates indentation, trailing spaces, and CRLF. I attacked the anchor: leading/trailing content on the delimiter line is the only evasion, and the design pins that the renderer emits it alone (docs/superpowers/specs/2026-07-28-reviewer-isolation-design.md, diff hunk at FIX.diff:20-29). The repo-agents fixture's delimiter remains matched, locked by `test_repo_agents_fixture_reports_a_project_doc` calling the real function (evals/multi-model-verify/test_codex_context_probe.py:120-126), plus both new polarity tests (:1134, :1150). I find no in-grammar input that hides a real project doc. PASS.

**Fix 3** — Verified. Both pathname captures route through `Invoke-GitLines` with `-c core.quotepath=false`, a UTF-8 `OutputEncoding` pin, and a `finally` restore (tools/new-review-mirror.ps1:28-61, consumers :83-87 and :107-108). `Test-Tracked` (:89-92) and `rev-parse` (:352) capture no pathnames, correctly outside the helper. A still-quoted entry is an explicit stop in both consumers: remediation (:293-300, exit 1 before any delete) and manifest (:151-156, after the rename-strip and RD checks so a quoted rename destination is still caught). `Test-GitQuotedPath` is pinned in both directions (evals/multi-model-verify/test_review_mirror.py:582). No defect inside.

**Fix 4** — Verified. `global_agents_md` now derives from the file resolution (:691-697) at both report sites (:823, :846) and the text output (:857). The false-when-absent direction is tested for the first time (evals/multi-model-verify/test_codex_context_probe.py:455-462). Minor: `Test-Path` lacks `-PathType Leaf`, so a *directory* named `AGENTS.md` reports `True` — pathological, cosmetic, noted not filed.

**Fix 5** — Verified. The suppression pass keeps `$instructions2` (tools/codex-context-probe.ps1:734) and blocks on its `ProjectDoc` (:754-757), after the two suppression rules (precedence pinned by test at evals/multi-model-verify/test_codex_context_probe.py:1092), before the artifact write (:777-790). Re-audited the entire second-render-to-clean path for other discarded measurements (task 4): `$pass2.ExitCode` (:716), `$text2` parse (:720-725), `Test-PromptShape`'s internal shape/feature/unknown checks (:507-537), `$skills2.BlockPresent` (:741), `$after` (:746), override path and hash (:793-800). `$skills2.Malformed` is unchecked but unreachable as a signal: entries are only parsed when `BlockPresent` (:102), and `BlockPresent` blocks first (:741). No other measured-then-discarded value found.

## Findings

**1. SEVERITY: HIGH (false-clean direction, per the branch rule "a FIX at any severity"); reachability INFERRED low — tools/codex-context-probe.ps1:96-98 with :218-222 and :270.** Fix 1 moved presence to the masked text, and masking is order-dependent: `INSTRUCTIONS` and `permissions instructions` are masked *before* `skills_instructions` (:218-222). If the suppression render carries the real skills container **nested inside `<INSTRUCTIONS>` or `<permissions instructions>`**, the outer body is blanked first (:294-296), the nested delimiters go with it, the count for `skills_instructions` is then 0/0 and silently skipped (:270), the structural text contains no `<skills_instructions>`, `BlockPresent` is false, entry parsing is skipped (:102), `$after` is 0, and the run reaches `status: clean`, exit 0 (:741-749, :837-859) — while the raw render advertises a full skills block, potentially repo-scoped. Every other gate passes: the quiet mask and validating mask in `Get-UnknownPromptBlock` blank the same body (:367, :378), so the nested tags never reach the exactness or unknown scans. Before fix 1 this exact input blocked, because raw `.Contains` was position-blind (:741 would have fired). The design's own contract says a marker appearing in a shape the parser does not recognise is a transport failure (design spec, quoted at FIX.diff:31-32); this is now a clean. Pass 1 still blocks the nested shape (:620-624), so exploitation requires the renderer to relocate the container between the two renders — INFERRED unlikely in practice, but the guard that caught it was deleted by this fix. Minimal fix: in the suppression pass, when structural presence is false, block if the raw text still carries the full block signature (`<skills_instructions>` … `### Available skills` … `</skills_instructions>`); a user prose-quoting an entire verbatim block is textually indistinguishable from a nested real block, and blocking it is the fail-closed direction.

**2. SEVERITY: LOW (fail-closed, wrong diagnosis, no remediation) — tools/codex-context-probe.ps1:190-196 and :517-521.** Fix 1's own rationale — a container name "is an ordinary thing to write inside a house rule" (:81-92) — was applied only to skills presence. `Get-FeatureReport` still tests raw text, so a global `AGENTS.md` house rule naming `<plugins_instructions>`, `<recommended_plugins>`, or `<apps_instructions>` (or a skill *description* doing so — free text per the A14 amendment) blocks pass 1 with "the plugin or apps feature is advertising itself despite --disable plugins --disable apps" (:518-521): the same defect class A15 declares fixed, one function away, still reachable on both renders. The features check has no structural-text variant and the exactness scan cannot save it because it runs after (:522) and on masked text. Minimal fix: run the feature presence tests on the structural text, as fix 1 did for skills. (Pre-dates this fix range; filed because fix 1 is incomplete against its stated rule.)

## Unverified

- Exact byte context around the `--- project-doc ---` in `repo-agents.json`: the fixture is one >2000-char line, beyond my read truncation. Behaviorally locked by `test_repo_agents_fixture_reports_a_project_doc` (evals/multi-model-verify/test_codex_context_probe.py:120-126), which the driver reports green on both hosts.
- Whether the mirror's final `Write-Output` of the baseline/manifest (tools/new-review-mirror.ps1:401-403), emitted *after* the encoding restore, can mojibake an accented path on PS 5.1 under a non-UTF-8 console code page. The driver's live probe shows the accented file reaching both artifacts on both hosts; I cannot probe consoles myself.
- git-for-Windows argv decoding for non-ASCII path *arguments* (`Test-Tracked`, `git add`, tools/new-review-mirror.ps1:90, :324) on PS 5.1 — INFERRED correct (git reads the UTF-16 command line), not verified live.
- The 422-passed/1-skipped counts, the override sha256, and the revert-to-red evidence are the driver's; I have no shell to re-run them.

## Verdicts (against head `42c942143b7c3d1c319e407c542991bb8fa0637c`)

1. Implementation fidelity — **PASS**. All five fixes are present in code, plan row A15 (FIX.diff:9) and the design rows (FIX.diff:37-55) match what the code does; no undeclared drift found.
2. Dispatch integrity — **PASS**. The write/hash/verify chain is intact and unchanged (tools/codex-context-probe.ps1:777-800); fix 5's new check fires before the artifact write (:754-757 vs :777).
3. No false-clean path — **FIX**. The late-project-doc hole is genuinely closed (:734-757), but fix 1 opened the nested-container path of Finding 1: within the parser's flattened-text grammar an input now reaches `clean` while a real skills container survives suppression.
4. Honest scope — **PASS**. The new design rows describe the limits and the false-coverage direction honestly; nothing in the range overstates the guarantee.
5. Test integrity — **PASS**. `rewritten()` asserts its anchors before replacing (evals/multi-model-verify/test_codex_context_probe.py:1065-1073), every new check has a polarity guard (:1125, :1150), the quoting predicate is pinned both directions (test_review_mirror.py:582), and the café tests assert deletion, manifest hash, and an untouched real tree (:513-548). Soft spot, not a defect: the `bucket_counts` family still calls `Get-SkillReport` with one argument, exercising the `$structural=$null` fallback (tools/codex-context-probe.ps1:96-97) the script never uses; the full-script tests cover the real path, so nothing passes vacuously today.
6. Destructive or rejecting bugs — **PASS**. The quoted-entry stop precedes any deletion (tools/new-review-mirror.ps1:293-300), the overlap and stale-artifact guards are untouched, and the accented back-channel test asserts the real tree survives (:524-528).

OVERALL: **FIX** — one false-clean path inside fix 1 (Finding 1), one same-class false block left behind by fix 1 (Finding 2), against head `42c942143b7c3d1c319e407c542991bb8fa0637c`.

To resume this session: kimi -r 80e4075c-c7e7-4c3d-af42-c91955203080
