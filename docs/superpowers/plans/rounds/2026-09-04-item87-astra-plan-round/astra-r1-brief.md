<role>Adversarial reviewer, equal weight, in a two-model debate. You are GPT-6 Astra running under codex exec in a read-only sandbox. The other advisor is the Claude session that wrote the plan under review.</role>

<task>Refute or confirm each numbered claim below about the frozen implementation plan at docs/superpowers/plans/2026-09-04-item87-astra-reviewer-swap.md, which moves this repository's primary cross-vendor reviewer lane from gpt-5.6-sol to gpt-6-astra, declares Sol as an explicit alternate, and renames the lane's live labels. Read the plan in full first, then the files each claim cites. Then run the class sweep in the rules. This is round 1 of at most 4.</task>

<rules>
1. This round is NON-INTERACTIVE. Nobody can answer a question you ask. If something you need is missing or ambiguous, do not ask: record it under UNVERIFIED in the final check and give your best verdict on what you could read.
2. Cite path:line for every claim you make or contest, using paths relative to the working directory. Uncited claims will be struck.
3. Do not manufacture objections. If a claim stands, say PASS and move on. A plan converging in one round is the system working.
4. Per claim, end with exactly one of: PASS, FIX (with the specific change to the plan text), or ESCALATE (a decision only the user can make, with both positions stated).
5. Distinguish a defect in the PLAN from a defect in the REPO that the plan walks past. The second kind is a finding too, but label it "pre-existing" and do not fold it into a claim's verdict.
6. Class sweep, required: the plan renames the lane label "Sol" to "Astra" wherever the label is LIVE (a seat table, a composition list, a task-name convention, a transport list, a fallback-class sentence, a fixture header) and leaves HISTORICAL citations alone (a parenthetical like "Sol review 2026-07-12", a probe record with a date, a round table, anything under docs/superpowers/plans/rounds/). Search skills/, commands/, agents/, hooks/, tools/*.ps1, README.md and CLAUDE.md for every live label the plan's Task 3 file list misses. Report the SHAPES you searched for (at least: the word Sol, GPT-5.6, 5.6, the lowercase token sol inside identifiers or filenames) and either the instances found with path:line or an explicit "none found for shape X".
7. Output format: a numbered section per claim in order, then a section "Class sweep" per rule 6, then "UNVERIFIED", then one line "Overall: PASS | FIX | ESCALATE" with a one-sentence reason.
</rules>

<claims>
1. The reviewer model id is single-sourced. The only literal lives at skills/multi-model-verify/references/model-prompting-notes.md:152 (Canonical model id) with the effort at :154. Every executable parses it at runtime: evals/tools/run_behavioral_evals.py:813-814, tools/check-drift.ps1:1010, and commands/doctor.md:58-63 directs the doctor to read it. The sweep at evals/multi-model-verify/test_multi_model_verify.py:781-829 forbids the literal and the flag shape "-m <vendor>-<digit>" everywhere else. Therefore Task 2's one-line change of the declaration is complete for every executable surface.

2. Adding the two lines "Alternate codex reviewer model id: `gpt-5.6-sol`" and "Alternate codex reviewer effort: `high`" directly after the canonical pair (plan Task 2 step 4) breaks no parser, because every parser anchors on the exact phrase "Canonical model id: `" or "Canonical reasoning effort: `" or "Canonical backup reviewer model id:" (evals/multi-model-verify/test_seat_reshuffle.py:296-299, evals/multi-model-verify/test_route_parser_shapes.py:79, evals/tools/run_behavioral_evals.py:813-814, tools/check-drift.ps1:1010, evals/multi-model-verify/test_backup_lane.py). Check specifically for any regex or substring match anywhere in evals/ or tools/ that would match "model id: `" loosely and so pick up the alternate line first.

3. After Task 2's notes edit, the literal "gpt-5.6-sol" inside the new test function in evals/multi-model-verify/test_multi_model_verify.py is legal under test_reviewer_id_has_single_source, because its markers are the string "-m gpt-" and the CURRENTLY declared canonical id (test_multi_model_verify.py:791-793), and its shape regex at :802 requires a preceding "-m". The plan's Global Constraints say so and Task 2 step 3 names the sweep's transient red before the notes edit as expected.

4. Task 2 step 3 runs only evals/multi-model-verify/test_multi_model_verify.py with a -k filter, so the transient red of evals/multi-model-verify/test_contract_coverage.py during the red phase (the background-task-naming pin says Astra while the region still says Sol) is not observed there, and step 6 runs test_contract_coverage.py after both edits, where it is green again. The plan's ordering of pin-before-text is therefore sound for this contract region.

5. Task 3's rename list is complete for live labels: README.md:27,48,66,258,267; skills/multi-model-verify/references/panels.md:4,12,14,49; skills/multi-model-verify/SKILL.md:213; skills/multi-model-verify/references/fallbacks.md:217,236; skills/multi-model-verify/references/frozen-plan-format.md:103; CLAUDE.md:134; evals/tools/run_behavioral_evals.py:237; evals/multi-model-verify/test_seat_reshuffle.py:131-134; plus the background-task-naming region in the notes and its pin at test_multi_model_verify.py:1321-1340 (Task 2). Rule 6's sweep is the test of this claim.

6. The pins in test_seat_reshuffle.py:131-134 read panels.md RAW, so panels.md lines 12 and 14 must stay single physical lines; the plan says so in Global Constraints and Task 3 step 3. The background-task-naming pin at test_multi_model_verify.py:1327 is a whitespace-normalized read, so that region may wrap. Confirm both readings from the test code.

7. SKILL.md is at roughly 6456 tokens against a 6500 hard ceiling (python evals/tools/skill_lint.py, measured 2026-09-04 by the session). Task 3 changes one word there, "Sol" to "Astra" at SKILL.md:213, and the plan requires the lint to be re-run and the count recorded. No other SKILL.md edit is planned. Confirm no other line in SKILL.md names the lane by the label Sol.

8. The rewritten reviewer section (plan Task 2 step 4) keeps every claim within its evidence: it says the six-element brief shape and the lean-brief advice were on OpenAI's GPT-5.6 page fetched 2026-07-16 and are NOT on the GPT-6 page fetched 2026-09-04, and it keeps them as this repo's own convention; it states Astra's clarification tendency as the guide's claim and marks it UNMEASURED under codex exec; it states the config.toml default as read on 2026-09-04 and the per-call pin as the rule; and it carries effort `high` over per the migration advice. You cannot fetch the web in this sandbox, so do NOT verify the page contents; verify only that every sentence in the replacement text is phrased at the width the plan's Global Constraints require (a claim about a page is dated and attributed, an unmeasured thing is called unmeasured).

9. The alternate is opt-in only and not a fallback class: the plan text says "It is not a fallbacks.md class and no consent gate offers it", and Task 3 changes fallbacks.md only at :217 and :236 (label renames). Confirm that nothing in the plan makes Sol reachable through references/fallbacks.md's substitution machinery, and that this is consistent with how references/panels.md and references/fallbacks.md describe lanes today.

10. Task 4 (plan revisions 2 and 3) exists because this round's own mirror build was BLOCKED by tools/codex-context-probe.ps1 on codex-cli 0.153.4: two client-authored prompt families the probe did not name (`collaboration_mode`, `multi_agent_role`), and then a self-quoting delimiter inside the `<collaboration_mode>` body that tripped the count rule at tools/codex-context-probe.ps1:429-470. The fix names the two families in both lists (tools/codex-context-probe.ps1:395-414) and masks ONE exact case-sensitive backtick-wrapped literal before the count, with three tests holding the refusing directions (plan Task 4, Steps 2b and 5b). Claim: this keeps the probe's fail-closed contract as evals/multi-model-verify/test_codex_context_probe.py states it (an unknown family still blocks; a non-exact known tag still blocks; the literal mask cannot hide a real second container). Check specifically: (a) the mask runs before the INSTRUCTIONS body is masked, so the same literal inside a user's AGENTS.md is blanked a moment earlier than the rest of that body, and whether that ordering can change any count or verdict; (b) the `$only` subset path of Hide-KnownContainer sees the same masked text; (c) whether `multi_agent_role`, which tells the model it can `spawn_agent`, widens the reviewer's effective tool surface in a way the tool-surface probe (skills/multi-model-verify/references/model-prompting-notes.md, the tool-surface-probe region) does not measure, and whether that is a pre-existing gap or one this plan must state.

11. This round runs gpt-6-astra at effort high through the installed 0.30.1 dispatch tool, with the model literal supplied by the session rather than read from the installed notes, whose canonical declaration still names gpt-5.6-sol. The session records that as a deliberate, one-round deviation in the debate record. Claim: nothing in the dispatch or evidence contract (the round-dispatch-* regions and the codex brief binding in skills/multi-model-verify/references/model-prompting-notes.md) depends on the model id, so the round's evidence is as strong as a canonical-model round's; confirm or refute from those regions and tools/dispatch-round.ps1.

12. The plan's debate record declares Verification status DEGRADED with class plan-not-debated. This round is the debate. If this round ends PASS or converges with amendments, the session will rewrite that record as FULL with your session id and this brief's path; if it ends ESCALATE, the record stays DEGRADED. Confirm that this handling matches skills/multi-model-verify/references/frozen-plan-format.md's debate-record appendix and skills/multi-model-verify/references/fallbacks.md's degraded rules.
</claims>

<boundaries>
Already decided by the user on 2026-09-04 and not under debate: the lane moves to gpt-6-astra as the default; Sol stays as an alternate that runs only when the user names it; both run at effort high; the lane's live label becomes Astra and historical citations stay. Also not under debate: the version bump happens after the diff debate, not in this plan; the installed plugin (0.30.1) still dispatches Sol until the bump lands. Do not propose changing the transport, the isolation flags, the per-round evidence binding, or the dispatch tool.
</boundaries>

<final-check>List under UNVERIFIED every claim or sub-claim you could not verify against files you read in this working directory, with the reason. Do not fold unverified material into any verdict. Then give the Overall line.</final-check>
