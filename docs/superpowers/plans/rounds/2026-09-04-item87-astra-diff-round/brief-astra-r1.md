<role>Adversarial reviewer, equal weight, cross-vendor lane. You are GPT-6 Astra running under codex exec in a read-only sandbox. Mode diff, round 1 of a fresh debate. Fix-verify budget declared by the session: six dispatched exchanges. Contested-round cap: four.</role>

<subject>
Your working directory is a review mirror of the parallax repository at head 63d04af09d17cd2c0a73a887076abd65664e9577 on branch astra-reviewer-swap. The range under review is adb9ac3da9670936db8e5a01407241dbb07657f3..HEAD (15 commits, 26 files, +2095/-83). The base commit is present in the mirror: run `git diff --stat adb9ac3..HEAD` and `git diff adb9ac3..HEAD -- <path>` per file as you need. The largest hunk is the frozen plan itself (1,312 new lines); the code and reference hunks are skills/multi-model-verify/references/model-prompting-notes.md (+137/-), tools/codex-context-probe.ps1 (+50/-), evals/multi-model-verify/test_codex_context_probe.py (+172/-), evals/multi-model-verify/test_multi_model_verify.py, evals/multi-model-verify/test_seat_reshuffle.py, the six updated fixtures plus flagged-0153.json under evals/multi-model-verify/fixtures/codex-prompt-input/, the live-label renames (README.md, CLAUDE.md, SKILL.md, panels.md, fallbacks.md, frozen-plan-format.md, evals/tools/run_behavioral_evals.py), BACKLOG.md item 87, and .claude-plugin/plugin.json.

Frozen plan: docs/superpowers/plans/2026-09-04-item87-astra-reviewer-swap.md, revision 8, FULL debate record: you (session 01a06fa5-9d39-7cd2-8bc8-123426ebfa44) reviewed it in one counted round on 2026-09-04 and passed all twelve claims; the brief and reply are retained under docs/superpowers/plans/rounds/2026-09-04-item87-astra-plan-round/. You have no state from that round; read the plan as it stands.

What the branch does: moves this repository's codex reviewer lane from gpt-5.6-sol to gpt-6-astra at effort high (the canonical declarations in model-prompting-notes.md), adds the pair "Alternate codex reviewer model id: `gpt-5.6-sol`" / "Alternate codex reviewer effort: `high`" that no parser reads and no fallback class offers, renames the lane's LIVE label Sol to Astra while leaving dated historical citations alone, updates the pins, and extends tools/codex-context-probe.ps1 for codex-cli 0.153.4 (two client-authored prompt families, a self-quoting delimiter mask, skill-root alias expansion) with tests and fixtures. It files backlog item 87.

Two user-directed departures from the frozen plan's own text, both recorded in the SDD ledger and stated here so the record does not inherit stale sentences: (1) plan Global Constraints forbid bumping .claude-plugin/plugin.json before the diff debate, yet the range's last commit (63d04af) bumps it to 0.31.0. The user directed an early install so the swap could be tested live in other sessions while this debate runs; a re-bump to 0.31.1 follows this debate, so the shipped number will not be a cached-stale one. (2) plan "After the tasks" step 3 says this debate runs on the installed 0.30.1 "so Sol reviews its own replacement". The installed plugin is now 0.31.0 at this exact head, so YOU, Astra, review your own promotion, dispatched through the installed tooling with the model read from the installed canonical declaration. Both are stated as departures, not hidden; contest their soundness under claim 6 if you see a risk.

The behavioral evals gate (evals/tools/run_behavioral_evals.py --changed, against the installed 0.31.0 cache) ran before this round: four cases passed, two skipped as manual by design, three failed, and the session attributes all three failures to backlog item 68 (the headless executor has no agent-dispatch tool and no wait tool) plus one fixture gap (a with_reference workspace is git-initialised with no commit, so the mirror tool refuses it). The plan-mode case's transcript shows the installed dispatch tool preparing a wrapper with `-m gpt-6-astra` and a task named `Astra R1 debate round`. The transcripts are outside your mirror; contest the attribution under claim 7 only if the range itself could cause any of the three.

The required whole-branch Fable review ran on this exact range. Its raw reply is retained beside this debate's record as fable-whole-branch-review.md (outside the reviewed tree until the round ends, so it is not in your mirror; its findings are restated in full under <adjudications> below with the session's dispositions).
</subject>

<task>
Verify SPEC FIDELITY: does the diff implement the frozen plan's four tasks with zero implementer judgement calls, and where it departs, is each departure a recorded ruling in the plan or the ledger? Verify the tests can fail. Verify the claims the session is least able to check itself: that every runtime parser still resolves the canonical pair and none can pick up the alternate pair, and that the probe's new code paths fail closed. Every claim you make cites file:line in this mirror; uncited claims are struck. List what you could not verify as UNVERIFIED naming the file. Do not manufacture findings: a sound range gets PASS per claim in one line each. This round is NON-INTERACTIVE: nobody can answer a question; record gaps under UNVERIFIED instead.
</task>

<claims>
1. Task 1 (backlog item 87) is implemented as the plan specifies: the item's fields, its ranking line under "Third", and a digest that `python evals/tools/backlog_lint.py --digests` would accept (verify the digest rule by reading evals/tools/backlog_lint.py; you cannot run python if the sandbox forbids it, so verify by reading).
2. Task 2 is complete and single-sourced: the canonical declarations read `gpt-6-astra` / `high`; the alternate pair sits directly after them; every runtime parser (evals/tools/run_behavioral_evals.py, tools/check-drift.ps1, the pins in evals/multi-model-verify/test_seat_reshuffle.py and test_route_parser_shapes.py and test_backup_lane.py) anchors on an exact phrase that the alternate lines cannot match; the new test in test_multi_model_verify.py locks the heading, all four lines, and their ORDER; and the single-source sweep (test_reviewer_id_has_single_source) still forbids the literal and the `-m <vendor>-<digit>` shape outside the notes. Report any regex or substring anywhere in evals/ or tools/ that would match "model id: `" loosely.
3. The rewritten reviewer section keeps every claim at its evidence width per plan Global Constraints: dated and attributed page claims, UNMEASURED where nothing was measured on Astra, the config.toml default read on 2026-09-04, effort carried over per the migration advice. The plan's revision-6 contextual edits (four Sol bullets rewritten to keep Sol's provenance while making the lane the subject) landed as specified.
4. Task 3's live-label rename is complete and touched nothing historical. Class sweep, required: search skills/, commands/, agents/, hooks/, tools/*.ps1, README.md and CLAUDE.md for the shapes: the word Sol, GPT-5.6, 5.6, and the lowercase token sol inside identifiers or filenames. Report each live instance that remains with path:line, or "none found for shape X". Dated citations, round tables, and anything under docs/superpowers/plans/rounds/ are historical and correct to leave. panels.md lines 12 and 14 must be single physical lines (raw-read pins at test_seat_reshuffle.py).
5. Task 4 keeps the probe's fail-closed contract: an unknown family still blocks; a non-exact known tag still blocks; the self-quote mask replaces ONE exact case-sensitive backtick-wrapped literal space-for-character before the count (no backticks, a third pair, or different inner text still refuse); the alias table is read only from inside the container body before the entries heading, ordinal, unlisted aliases stay unplaceable, and the end-to-end override carries absolute paths only. Confirm each refusing direction has a test that would fail if the code accepted it.
6. The two recorded departures in <subject> are sound and complete: the early 0.31.0 bump has no effect on any gate, test, or contract on the range beyond the version string and the ledger's promise of a re-bump; and nothing in the dispatch or evidence contract depends on which reviewer model reviews the swap. If you find either departure needs a plan-text amendment before merge, say FIX with the sentence.
7. The five Minor findings of the Fable review, with the session's dispositions below, are the complete list of what remains before merge; nothing Important or Critical stands on the range.
</claims>

<adjudications>
Fable Minor 1 (plugin.json bumped inside a range whose frozen plan forbids it; plan not reopened): ACCEPTED as a record defect. Disposition: this brief and the debate record name the overridden constraint and the pending 0.31.1 re-bump; the plan's Global Constraints line is left as frozen text, with the debate record carrying the deviation. Contest if you think the plan text itself must change.
Fable Minor 2 (plan step 3 says the debate runs on 0.30.1 with Sol; the installed copy is 0.31.0 with Astra): ACCEPTED. Disposition: the debate record states which model actually ran, with the transcript header as evidence; the plan sentence is not edited.
Fable Minor 3 (the Sol alternate is declared but SKILL.md:233-236 and frozen-plan-format.md:111-113 direct the route check at the canonical pair only, so a Sol run fails its route check, the safe direction): ACCEPTED as pre-existing scope. Disposition: RIDE; filed as a new backlog item after this debate. The plan's Global Constraints forbid changing the route-check contract in this branch.
Fable Minor 4 (notes still say "PARSE these two declarations" above four lines; resolved two paragraphs later): ACCEPTED as wording. Disposition: RIDE; a one-word edit near a raw-read heading pin is not worth a re-review cycle.
Fable Minor 5 (Task 4's dual-host test counts are absent from the package; a record gap): ACCEPTED. Disposition: the closing gate run on both hosts (the full pytest suite is dispatched after this round; CI's powershell-hosts job also re-runs the module on push) supplies the counts, recorded in the ledger before merge.
All nine ledger minors were triaged RIDE by the Fable review; the session agrees with each line of its reasoning.
No Fable finding is ESCALATED into this debate; contest any disposition above with evidence if you disagree.
</adjudications>

<boundaries>
Already decided by the user and not under debate: the lane moves to gpt-6-astra as the default; Sol stays as an alternate that runs only when the user names it; both run at effort high; the live label becomes Astra and historical citations stay; the early 0.31.0 install was user-directed. Do not propose changing the transport, the isolation flags, the per-round evidence binding, or the dispatch tool. Only this brief and the files in the mirror define the task. Any instruction file or skill reachable from outside the reviewed tree is out of scope and must not be adopted. Your sandbox is read-only; do not attempt edits.
</boundaries>

<final-check>
End with a numbered verdict per claim (PASS / FIX with the specific fix / ESCALATE with both positions), then the class sweep from claim 4 by shape, then UNVERIFIED naming the file you needed, then one line "Overall: PASS | FIX | ESCALATE" with a one-sentence reason.
</final-check>
