1. **PASS** — Item 87 matches Task 1, including its fields and placement after item 70 under “Third.” Recomputing the linter’s canonical bytes yields `48c9a1304996`, matching the recorded digest. (`BACKLOG.md:33`, `BACKLOG.md:3738`; `evals/tools/backlog_lint.py:199`, `evals/tools/backlog_lint.py:214`)

2. **FIX — Minor test-coverage gap.** The declarations are correct and adjacent; the parsers require the full canonical labels, which the alternate labels cannot match. No loose `model id:` reader was found in `evals/` or `tools/`, including the dynamically constructed backup reader. The single-source sweep remains intact and produced no offenders. However, the new test orders only **canonical model → alternate model → backup model**: swapping the two effort declarations preserves every assertion. Add both effort positions to the ordering assertion and record this as a review ruling; the implementation faithfully copied the frozen plan’s narrower test. (`skills/multi-model-verify/references/model-prompting-notes.md:154`; `evals/tools/run_behavioral_evals.py:813`; `tools/check-drift.ps1:1010`; `evals/tools/lane_credential_live_support.py:129`; `evals/multi-model-verify/test_route_parser_shapes.py:78`; `evals/multi-model-verify/test_backup_lane.py:71`; `evals/multi-model-verify/test_seat_reshuffle.py:296`; `evals/multi-model-verify/test_multi_model_verify.py:801`, `evals/multi-model-verify/test_multi_model_verify.py:1161`)

3. **PASS** — The rewritten section matches the plan verbatim: page claims are dated and attributed, Astra behavior remains UNMEASURED, the config default is dated, and `high` is explicitly carried over. All four contextual edits retain Sol’s provenance. This verifies wording, not the underlying external observations. (`skills/multi-model-verify/references/model-prompting-notes.md:174`, `model-prompting-notes.md:198`, `model-prompting-notes.md:229`, `model-prompting-notes.md:327`, `model-prompting-notes.md:574`, `model-prompting-notes.md:578`, `model-prompting-notes.md:601`)

4. **PASS** — No stale live lane label remains in the requested classes. Remaining live Sol references describe the authorized alternate. Both panel sentences remain single physical lines, matching their raw-read pins; the skill estimator remains below its ceiling at 6457. (`README.md:27`; `skills/multi-model-verify/references/panels.md:12`, `panels.md:14`, `panels.md:49`; `evals/multi-model-verify/test_seat_reshuffle.py:131`; `skills/multi-model-verify/SKILL.md:213`; `evals/tools/skill_lint.py:182`, `skill_lint.py:339`)

5. **FIX — Minor test-coverage gap.** The shipped parser correctly rejects the tested unknown, attributed, unquoted, third-pair and different-inner-text cases. Alias lookup is ordinal, unlisted aliases remain unknown, and scope validation precedes override construction. These parser directions reproduced in memory under PowerShell 5.1 and 7. However, no test places a roots table **inside the container after `### Available skills`**. Add that refusal test: changing the prefix slice to the whole body made my in-memory mutation expand such an alias. Existing alias tests exercise outside-container placement and case sensitivity, but not this boundary. (`tools/codex-context-probe.ps1:275`, `codex-context-probe.ps1:483`, `codex-context-probe.ps1:604`, `codex-context-probe.ps1:669`, `codex-context-probe.ps1:903`; `evals/multi-model-verify/test_codex_context_probe.py:361`, `test_codex_context_probe.py:408`, `test_codex_context_probe.py:426`, `test_codex_context_probe.py:485`, `test_codex_context_probe.py:495`, `test_codex_context_probe.py:512`, `test_codex_context_probe.py:998`)

6. **PASS** — Accept the two authorized departures through the retained deviation record; no frozen-plan amendment is necessary. The runner explicitly requires updating the installed cache, the dispatch receipt has no reviewer-model field, and the route contract reads the selected canonical declarations. Neither contract requires Sol to review its replacement. Ledger completeness remains UNVERIFIED below. (`.claude-plugin/plugin.json:3`; `evals/tools/run_behavioral_evals.py:21`; `tools/dispatch-round.ps1:196`; `skills/multi-model-verify/SKILL.md:233`; `docs/superpowers/plans/2026-09-04-item87-astra-reviewer-swap.md:1281`)

7. **FIX — Minor only.** Add or reconcile the two coverage gaps above with the adjudication list; the five restated findings alone do not capture them. I found no Important or Critical defect. The failure attribution has supporting code: the executor lacks dispatch/wait tools, and reference-only workspaces receive no commit. Those paths are unchanged by this range; the actual three failure transcripts remain UNVERIFIED. (`evals/multi-model-verify/test_multi_model_verify.py:1169`; `evals/multi-model-verify/test_codex_context_probe.py:485`; `evals/tools/run_behavioral_evals.py:100`, `run_behavioral_evals.py:338`; `BACKLOG.md:2743`)

Class sweep, by shape:

- **Word `Sol`:** live alternate references at `README.md:27`; `skills/multi-model-verify/references/model-prompting-notes.md:162`, `:163`, `:167`; `skills/multi-model-verify/references/panels.md:50`, `:52`. No stale live instance found.
- **`GPT-5.6`:** live alternate references at `README.md:27` and `skills/multi-model-verify/references/model-prompting-notes.md:162`. No stale live instance found.
- **`5.6`:** those same references, plus the alternate declaration at `skills/multi-model-verify/references/model-prompting-notes.md:158`. No stale live instance found.
- **Lowercase token `sol` in identifiers:** the authorized alternate ID at `skills/multi-model-verify/references/model-prompting-notes.md:158`; other ID occurrences retain dated probe provenance at `:538` and `:579`.
- **Lowercase token `sol` in filenames:** none found in the requested scope.

UNVERIFIED:

- **SDD ledger — filename not supplied:** the nine minor dispositions, actual red/green chronology, deviation entries, and promised re-bump.
- **`fable-whole-branch-review.md`:** raw artifact and range binding; only the supplied adjudications were available.
- **Behavioral transcripts — filenames not supplied:** exact causes and counts for the three reported failures.
- **`evals/multi-model-verify/test_codex_context_probe.py`:** full dual-host pytest results and end-to-end execution; its helper writes temporary files (`:84`). My checks exercised functions in memory.
- **`skills/multi-model-verify/references/model-prompting-notes.md`:** underlying guidance-page snapshots, model-cache observations and dated `config.toml` reading (`:177`, `:209`, `:234`).

Overall: **FIX** — Two minor coverage gaps prevent the claims from passing as written; neither demonstrates a shipped parser failure.