1. The stale-state evidence is confirmed: entry 1 calls itself second, the status block admits repeated drift, and five open items are omitted (`docs/superpowers/plans/2026-07-27-0150-backlog.md:11`, `docs/superpowers/plans/2026-07-27-0150-backlog.md:46`, `docs/superpowers/plans/2026-07-27-0150-backlog.md:167`). IDs-only ranking plus set equality mechanically eliminates omissions and ranking prose (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:64`, `docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:174`). It does not prove the ranking agrees semantically with `Cost`: order is explicitly human judgment and the checker does not interpret bodies (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:41`, `docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:197`). Impact: medium; structural consistency improves, but the goal overclaims semantic consistency.

**FIX — Say the ranking cannot omit, duplicate, or attach stale prose to an item; leave agreement between Cost and position explicitly human-reviewed.**

2. Rule 7 is vacuous as evidence of reading. A session can set every `Verified` field to today; the rule checks only date ordering, and same-day edits require no further change (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:103`, `docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:181`). Before the initial rewrite is committed, `BACKLOG.md` has no history, yet only “not a repository/git unavailable” has defined handling; after commit, today’s dates pass even though every span changed (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:181`, `docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:185`). Reorder and heading-rename cases are not required tests; the test requirement merely says “creates and commits” a temporary repository (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:263`). Impact: high; the advertised freshness control can certify an unread bulk rewrite.

**FIX — Replace date freshness with a reviewed-content hash covering the item plus ranking location, describe it as an attestation rather than proof of reading, and add initial-file, dirty reorder, heading-rename, and same-day second-edit fixtures.**

3. The Stop condition is branch-scoped, not session-scoped. Once any earlier session changes `BACKLOG.md`, every later session on that branch passes because both sides are compared to the same merge base (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:216`). A direct committed change on clean main also passes explicitly (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:223`). The trigger calls `evals/` and `README.md` shipped surfaces while omitting actual release/enforcement surfaces `.claude-plugin/`, `.githooks/`, `.github/`, and `CLAUDE.md` (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:217`; `CLAUDE.md:17`; `CLAUDE.md:158`; `.githooks/pre-push:1`). A malformed or unrelated date bump escapes Stop because it tests only whether the file differs; PostToolUse lint is explicitly non-blocking (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:208`, `docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:220`). Detached HEAD and missing-main failures have no specified disposition. Impact: high; most multi-session branches become permanently exempt.

**FIX — Record a per-session baseline at SessionStart, compare both governed changes and backlog content to it, run lint before allowing Stop, include the real release/enforcement paths, and specify ordered `origin/main`/`main`/no-base handling.**

4. Merge topology is the wrong discriminator. Squash and fast-forward pushes evade the clause entirely, while a docs-only merge is blocked because the test asks only whether any merge exists and whether `BACKLOG.md` changed (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:234`). A backlog edit pushed earlier also fails the later merge’s `remote..local` path test despite already being on main (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:235`). This contradicts the existing “docs/chore pushes stay friction-free” intent (`.githooks/pre-push:6`). A temporary extracted tree also conflicts with rule 7’s requirement for Git history: outside a repository, lint deliberately exits non-clean (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:185`, `docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:238`). Impact: high; material pushes bypass it and trivial merges block.

**FIX — Trigger on governed paths in every main update, independent of merge topology; add a linter `--revision <sha>` mode using Git objects directly, and define a range-bound attestation for backlog updates made in an earlier push.**

5. The stated item-65 counterexample does not refute condensation. Item 65 is `PARTIALLY CLOSED`, its mechanical remainder is still open, and the design retains full text for partial items (`docs/superpowers/plans/2026-07-27-0150-backlog.md:5379`, `docs/superpowers/plans/2026-07-27-0150-backlog.md:5425`; `docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:113`). That is why `CLAUDE.md` can still direct readers to its full record (`CLAUDE.md:186`). Once closed, the design retains both Git history and a record path or merge commit (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:117`). I found no demonstrated category absent from both locations. Impact: none established.

**PASS.**

6. Item 35 does not close by construction. `-PriorStateFile` appears in the command before the skill explains that it must be captured immediately before dispatch (`skills/multi-model-verify/SKILL.md:208`, `skills/multi-model-verify/SKILL.md:252`). The parameter is merely a nullable string, not a mandatory typed inventory (`tools/dispatch-round.ps1:130`, `tools/dispatch-round.ps1:144`). The tool accepts and hashes any readable bytes; it checks neither inventory schema nor capture time (`tools/dispatch-round.ps1:498`). That preserves the original top-to-bottom failure: the capture instruction remains after dispatch (`docs/superpowers/plans/2026-07-27-0150-backlog.md:3476`). Impact: high—an arbitrary or stale file can bind while the actual pre-dispatch inventory remains unrecoverable.

**FIX — Keep item 35 open; put the concrete inventory-capture command before `-Prepare`, then validate its schema and binding to the current pre-dispatch state.**

7. At least the rationale for 75’s unpaired status does not follow from its text. The spec asserts nothing shares its file or gate (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:128`), but item 75 says its fix is undecided and uncosted and identifies mirror construction and the back-channel sweep as relevant surfaces (`docs/superpowers/plans/2026-07-27-0150-backlog.md:548`, `docs/superpowers/plans/2026-07-27-0150-backlog.md:622`). Item 76 concerns that same mirror/sweep machinery, although it explicitly chooses item 38 as its build partner (`docs/superpowers/plans/2026-07-27-0150-backlog.md:670`, `docs/superpowers/plans/2026-07-27-0150-backlog.md:719`). Impact: medium; the chosen no-pair result may stand, but its factual justification does not.

**FIX — Retain `Pairs: none` as the decided outcome, but justify it by item 75’s unresolved design/cost, not by nonexistent file/gate exclusivity.**

8. The first proposed new item already has an open owner. Item 34 owns truncated reply-side captures, explicitly includes the reply file as a candidate surface, and explains that a truncated reply can parse as a short review (`docs/superpowers/plans/2026-07-27-0150-backlog.md:3414`, `docs/superpowers/plans/2026-07-27-0150-backlog.md:3438`, `docs/superpowers/plans/2026-07-27-0150-backlog.md:3453`). The newer Fable evidence says precisely that no check proves a raw retained reply reached its final section (`skills/multi-model-verify/references/model-prompting-notes.md:52`). Impact: medium; duplicate ownership fragments one failure class.

**FIX — Amend and rerank item 34 with the Fable raw-reply completeness case; file only the refusal, alias/effort, and killed-round-resume items as new.**

9. Project scope is conceptually consistent with keeping repository maintenance out of the versioned plugin cache (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:202`; `CLAUDE.md:158`). But `.claude/` is ignored wholesale, so the proposed “tracked `.claude/settings.json`” cannot be added normally (`.gitignore:3`). The shipped hook manifest currently demonstrates only PostToolUse shapes, not Stop blocking (`hooks/hooks.json:3`). Impact: high; the enforcement may never enter the repository.

**FIX — Ignore `.claude/*` instead, explicitly unignore `.claude/settings.json`, and require a checked-in integration fixture proving the project-scope Stop response schema before implementation.**

10. Rule 8 is only a heuristic. Rule 3 still permits arbitrary group-header text, so ranking narrative remains structurally possible there; item-body wording is trivially substitutable (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:76`, `docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:188`). The checker otherwise deliberately avoids interpreting bodies (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:197`). Impact: low; false confidence and occasional false positives, not structural corruption.

**FIX — Keep it, label it an evadable migration heuristic, and narrow it to ranking group headers plus open/partial bodies.**

### New risks

- PostToolUse observes only Edit and Write, so shell commands or scripts can modify `BACKLOG.md` without same-turn lint despite the goal of checking every edit (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:34`, `docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:206`). Add unconditional lint at Stop whenever the backlog differs. Impact: medium.
- Replacing the old file with three lines makes existing `old-path:<line>` references resolve to meaningless locations; retained evidence contains such citations (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:155`; `docs/superpowers/plans/rounds/2026-09-03-item74-diff-debate/sol-diff-r3-reply.md:24`). Existing coverage explicitly excludes the backlog (`evals/multi-model-verify/test_contract_coverage.py:611`, `evals/multi-model-verify/test_contract_coverage.py:873`). Inventory live inbound line citations and rewrite them to item IDs or commit-bound references before moving the file. Impact: medium.

### Final check

- **UNVERIFIED, claim 2:** exact `git log -L` ancestry behavior across reorder/rename; needed the planned `evals/multi-model-verify/test_backlog_lint.py` fixtures (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:263`).
- **UNVERIFIED, claim 3:** exact detached-HEAD/no-main outcome; needed the unimplemented Stop script and its planned tests (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:265`).
- **UNVERIFIED, claim 4:** actual Windows temporary-index execution; needed the revised `.githooks/pre-push` and disposable-clone test (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:268`).
- **UNVERIFIED, claim 9:** whether the proposed Stop block JSON is a supported Claude Code hook shape; needed checked-in hook-schema documentation or an integration fixture, neither present beside the existing manifest (`hooks/hooks.json:1`).