1. Most accepted edits landed: corrected Problem bullets (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:16`), valid example (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:90`), prose-free ranking and rule 12 (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:63`, `docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:259`), narrowed rule 8 (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:242`), explicit control classification (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:272`), stronger re-attestation predicate (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:282`), and strengthened body rules (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:252`).

Two contradictions remain. Goal 2 still says a session “cannot finish” without touching the backlog, while Part 3 explicitly says stopping twice, missing baseline, or missing Git allows exactly that (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:36`, `docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:272`). Rule 8 also says an eight-word header makes ranking narrative impossible, although “Previously second, now first after 74 closed” is an eight-word renumbering story that avoids every banned phrase (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:242`, `docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:249`). Impact: high; both sentences promise more than their mechanisms deliver.

**FIX — Rewrite Goal 2 to promise a one-shot session reminder plus hard merge controls, and describe rules 8/12 as limiting ranking narrative rather than making it impossible.**

2. Editing the frozen plan is permitted: the no-edit rule applies specifically to raw round artifacts, while corrections belong in synthesized documents (`docs/superpowers/plans/rounds/2026-07-28-reviewer-isolation/README.md:10`; `docs/superpowers/plans/2026-07-27-0150-backlog.md:3577`). Section 1e records that distinction correctly (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:204`).

The universal resolver is unsound. The raw artifact `fable-review-c6b7c85-efe4fa0.md` cites backlog lines 160–162 for “There was no innocent case” (`docs/superpowers/plans/rounds/2026-07-28-0160-backlog/fable-review-c6b7c85-efe4fa0.md:30`). At the commit that added that artifact, those lines describe an old Kimi log (`docs/superpowers/plans/2026-07-27-0150-backlog.md@7a89084:160`); at the reviewed head in its filename, they discuss a rotation observation (`docs/superpowers/plans/2026-07-27-0150-backlog.md@efe4fa0:160`). Thus `git log -1 -- <citing file>` does not recover the cited layout, contrary to section 1e (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:194`). Impact: high; at least one raw citation appears bound to an uncommitted intermediate tree and has no resolving committed revision.

**FIX — Inventory each raw citation against its recorded subject snapshot; store the actual resolving commit where one exists, and mark citations from uncommitted layouts unresolved instead of prescribing the citing file’s commit.**

3. Part 3 itself honestly calls Stop a reminder and admits that a session can finish after a second Stop or when baseline/Git is unavailable (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:272`). However, Goal 2 still makes the opposite absolute promise (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:36`).

The claim that pre-push plus CI are hard controls is also too broad. Pre-push checks the governed-range/re-attestation relationship, but CI is specified only to run `backlog_lint.py` against the resulting file; an unchanged but valid backlog passes CI after a governed change merged without the local hook (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:350`, `docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:376`). Impact: critical; bypassing or not installing the local pre-push hook leaves no server-side range control despite the “nothing reaches main” promise.

**FIX — Remove the absolute session claim and add the same governed-range/re-attestation comparison to CI using the push or pull-request base SHA.**

4. The revised predicate rejects preamble-only or other unrelated-byte edits because it requires a changed `Verified` line belonging to an `OPEN` or `PARTIAL` item (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:282`, `docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:333`). It still can be satisfied by changing only the date on such a line: `Verified` itself is excluded from the digest, so no canonical item content need change and no reading is demonstrated (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:111`). That may be an explicit re-attestation act, but it does not mechanically establish the reading that section 3b says the hook exists to force (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:344`).

Consequently, “wrong item” is not the only residual. Re-attesting the nominally correct item without reading is another; rule 7 admits this generally, but the Part 3 residual statement names only wrong ownership (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:117`, `docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:289`). Impact: medium; the structural predicate is sound as an attestation check, but not as a reading control.

**FIX — State in Part 3 that the hook proves only that someone changed an eligible item’s attestation; explicitly list both wrong-item selection and unread re-attestation as irreducible residuals.**

### New risks

- Rule 9 can be satisfied by twenty filler words, and rule 10 by an unrelated existing path or arbitrary resolvable commit; neither proves meaningful remaining work or a relevant record (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:252`, `docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:254`). Label both as shape checks, not semantic controls; rely on the prescribed second-reader review for resolution accuracy and extend that review to partial-item remainder text (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:413`).
- Canonical digest construction still does not define encoding, newline normalization, separators between components, or terminal-newline treatment, despite claiming identical behavior in working trees and Git-object revision mode (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:111`, `docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:233`). Specify a byte-exact UTF-8/LF framing and test CRLF working-tree content against `--revision`.

### Final check

- **UNVERIFIED, claim 2:** the stated count of 83 affected raw records and how many have a resolving committed snapshot; needed the full retained branch inventory promised by section 1e (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:185`, `docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:208`).
- **UNVERIFIED, claim 3:** actual Claude Code `stop_hook_active` behavior; needed the checked-in hook-shape fixture required before wiring (`docs/superpowers/specs/2026-09-04-backlog-rewrite-design.md:300`).