1. **CONFIRMED — merge-blocking: the 8.3 character restrictions remain incomplete.**  
   The length limits work, but the basename excludes only spaces and periods, and the extension excludes only spaces. On **both hosts**, the helper still refused `a+b~1`, `a,b~1`, `a=b~1`, `a[b]~1`, and `ABC~1.+`. ([Predicate](C:/Temp/pxd1/tools/new-review-mirror.ps1:1376))

   I independently called Windows’ `CheckNameLegalDOS8Dot3W` in memory on both hosts. Every call succeeded; all five names returned **not legal DOS names**. Your four positive short-name examples returned legal. Microsoft also documents replacing this punctuation with underscores during short-name generation. ([Native check](https://learn.microsoft.com/en-us/windows/win32/api/winbase/nf-winbase-checknamelegaldos8dot3w), [generation rules](https://learn.microsoft.com/en-us/windows-hardware/drivers/ddi/ntifs/nf-ntifs-rtlgenerate8dot3name))

   **Change:** constrain characters in both basename and extension, or combine the native legality check with the tilde policy. Add these accepted-name cases. The promised false-refusal correction remains incomplete.

2. **CONFIRMED — merge-blocking validation fix: the new acceptance test can pass after construction fails.**  
   `test_names_that_8_3_cannot_produce_are_accepted` checks only absence of the short-name diagnostic and existence of the destination directory. A failure after directory creation satisfies both assertions. The script creates that directory before subsequent source-identity and copy checks. ([Assertions](C:/Temp/pxd1/evals/multi-model-verify/test_review_mirror.py:1975), [construction order](C:/Temp/pxd1/tools/new-review-mirror.ps1:1972))

   This does establish passage beyond the early spelling refusal, but its docstring claims a successful build. **Change:** call the existing `assert_built(proc)` inside the loop. That helper requires the specific completion diagnostic and exit code. ([Claim](C:/Temp/pxd1/evals/multi-model-verify/test_review_mirror.py:1971), [existing oracle](C:/Temp/pxd1/evals/multi-model-verify/test_review_mirror.py:130))

3. **CONFIRMED — nonblocking editorial cleanup: some corrections were appended without correcting the original claims.**  
   The debate’s original doubled-backslash paragraph still says **every** component check was dead and nothing else would catch it; the new section says those sentences were narrowed. Amend the original paragraph or explicitly mark it superseded. ([Original](C:/Temp/pxd1/docs/superpowers/plans/rounds/2026-09-05-mirror-identity-window/diff-debate.md:252), [correction claim](C:/Temp/pxd1/docs/superpowers/plans/rounds/2026-09-05-mirror-identity-window/diff-debate.md:338))

   Likewise, the category test retains the false explanation about an output assertion making a doubled fixture impossible to pass, immediately before saying that comment “used to” make the claim. The **new fixture assertions are correct**; delete the obsolete explanation. ([Old explanation](C:/Temp/pxd1/evals/multi-model-verify/test_review_mirror.py:2350), [new explanation and checks](C:/Temp/pxd1/evals/multi-model-verify/test_review_mirror.py:2359))

   Two record details also need narrowing:
   - Item 98 still says the guards “close the ALIAS class,” contradicting the newly documented tilde-free limitation. ([Backlog claim](C:/Temp/pxd1/BACKLOG.md:4291), [limitation](C:/Temp/pxd1/tools/new-review-mirror.ps1:1361))
   - The new round-4 summary says two findings concerned legitimate-input refusals. There was one such finding; the other was a suspected **accepted alias**. ([Summary](C:/Temp/pxd1/docs/superpowers/plans/rounds/2026-09-05-mirror-identity-window/diff-debate.md:301), [retained finding](C:/Temp/pxd1/docs/superpowers/plans/rounds/2026-09-05-mirror-identity-window/diff-astra-r4-reply.md:20))

4. **SUSPECTED — backlog follow-up: file the tilde-free alias gap.**  
   The helper still accepts `LONGFILE.TXT`, and now accurately acknowledges that spelling checks cannot distinguish an assigned alias from an ordinary name. Microsoft documents assigning exactly this kind of alias. ([Helper limitation](C:/Temp/pxd1/tools/new-review-mirror.ps1:1361), [assignment example](https://learn.microsoft.com/en-us/windows-server/administration/windows-commands/fsutil-file))

   **My adjudication:** defer implementation, but file it rather than leaving it solely in a comment. Record the missing experiment: assign an alias to a disposable directory and exercise overlapping construction on both hosts. I still could not perform that filesystem setup here. This remains follow-up work, not an additional merge blocker for this bounded change.

The **source-root ancestor fix holds**. Its check precedes both destructive statements. On both hosts, the construction prefix now refuses the `My Documents` source spelling, while the direct `Documents` spelling reaches the end of that check. On this machine the refusal comes from denied attribute access to `My Documents`, so my probe proves refusal, not the specific junction diagnostic. The new fixture separately asserts that diagnostic and source survival. ([Ancestor check](C:/Temp/pxd1/tools/new-review-mirror.ps1:1812), [attribute failure](C:/Temp/pxd1/tools/new-review-mirror.ps1:1765), [first removal](C:/Temp/pxd1/tools/new-review-mirror.ps1:1961), [regression](C:/Temp/pxd1/evals/multi-model-verify/test_review_mirror.py:1954))

Yes, this deliberately rejects a legitimate **junction spelling**. The direct target path provides a route past the new guard; I did not establish a complete build through that route. I consider that restriction defensible under the tool’s refusal policy. Automatic canonicalization can be a usability follow-up. ([Refusal and remedy](C:/Temp/pxd1/tools/new-review-mirror.ps1:1814))

The other substantive corrections hold: the code’s backslash explanation and followed-target ordering comment are narrowed; both fixtures positively require their codepoints; removal-simulation provenance now identifies round 4; and the round-1 account names the runtime-category test. The duplicate ordinal is corrected, although one accurate “third instance” remains. ([Backslash explanation](C:/Temp/pxd1/tools/new-review-mirror.ps1:1322), [ordering](C:/Temp/pxd1/tools/new-review-mirror.ps1:1793), [control fixture](C:/Temp/pxd1/evals/multi-model-verify/test_review_mirror.py:2320), [category fixture](C:/Temp/pxd1/evals/multi-model-verify/test_review_mirror.py:2366), [provenance](C:/Temp/pxd1/BACKLOG.md:4281), [test name](C:/Temp/pxd1/docs/superpowers/plans/rounds/2026-09-05-mirror-identity-window/diff-debate.md:77), [remaining ordinal](C:/Temp/pxd1/evals/multi-model-verify/test_review_mirror.py:3129))

The final sweep searched absence/nonzero assertions, creation-versus-completion checks, path mutations and guard call sites, escape literals, and measurement/count claims. Beyond the findings above, I found no additional operand omission or escaping-created dead code. I did not rerun pytest or behavioral evals, perform real removal, create junction/alias fixtures, or exhaustively test network and drive aliases.

**Verdict: FIX — fix the remaining 8.3 false refusals and add `assert_built` before merge; the editorial cleanup and documented alias-resolution gap are follow-up work.**