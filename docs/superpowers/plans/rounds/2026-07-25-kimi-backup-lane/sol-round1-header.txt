2026-07-26T04:12:28.800692Z ERROR codex_models_manager::cache: failed to load models cache: missing field `supports_reasoning_summaries` at line 86 column 5
OpenAI Codex v0.144.1
--------
workdir: C:\Users\Brandon\Documents\parallax
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: read-only
reasoning effort: high
reasoning summaries: none
session id: 019f9c9f-d83f-7e40-ae8b-2a71c4105d8a
--------
user
# Mode: plan — parallax 0.13.0 Kimi backup reviewer lane

<role>Adversarial reviewer, equal weight, in a two-model debate. This is
the plan-gate debate; a SECOND cross-vendor lane is reviewing the same
candidate independently (hub-and-spoke — the session mediates; you never
see each other's replies until findings are relayed with evidence).</role>

<task>Review the plan candidate for correctness and completeness against
its spec: does the plan implement every spec section? Are the embedded
verbatim blocks internally consistent (test pins vs the reference text
they pin — a pin whose target sentence is soft-wrapped differently is
the 0.12.0 line-join defect class)? Any conflict with the repo's
existing contracts? End with PASS / FIX (specific findings) /
ESCALATE.</task>

<rules>Cite file:line from this tree; uncited claims are struck.
Probed kimi-cli facts in the spec's section 12 probe record are GIVEN
(you cannot run kimi here) — flag only inconsistent USE of a GIVEN.
Do not manufacture objections. Grade final dispositions, not
vocabulary.</rules>

<state>
- Branch feat/0130-kimi-backup-lane = your working directory (read-only
  sandbox), head e432087.
- Plan CANDIDATE: docs/superpowers/plans/2026-07-25-kimi-backup-lane.md
- Approved spec (probe record inside, all five probes RESOLVED):
  docs/superpowers/specs/2026-07-25-kimi-backup-lane-design.md
- The spec folded a dual advisory pass (your session 019f9c5f raised 11
  findings; the other lane raised 10; all accepted findings are in the
  spec — check the plan carries them, notably: single-source with
  placeholders, per-round offset attribution, the mechanical
  Loading-agent/Loaded-tools containment checks, the re-pinned resume,
  the exactly-the-brief clone rule, the write-probe oracle, SKILL/README
  routing, the lane-substitution record pin, drift flag+vocabulary
  probes, the manual behavioral case, and the kimi-export trim.
- 0.12.0 (just merged) established: flash-implementer as the per-task
  build lane, the SDD flow, and the frozen-plan debate-record schema.
</state>

<boundaries>User rulings not under debate: the lane concept and
triggers, approach B, panels deferred to 0.14.0, kimi-code/k3-256k on
high, dual plan review for THIS cycle, Sol primary check-off + mode-diff
debate remain the standing gates.</boundaries>

<final-check>List anything you could not verify in this tree as
UNVERIFIED — do not fold it into the verdict.</final-check>


codex
I’m treating this as a read-only merge gate. I’ll use the Superpowers workflow rules plus the cross-model verification contract, then pin every finding to the candidate, approved spec, and existing repo contracts; the given Kimi probe results will be checked only for inconsistent downstream use.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Raw 'C:\\Users\\Brandon\\.codex\\plugins\\cache\\claude-plugins-official\\superpowers\\6.2.0\\skills\\using-superpowers\\SKILL.md'" in C:\Users\Brandon\Documents\parallax
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Raw 'C:\\Users\\Brandon\\.codex\\plugins\\cache\\crosscheck\\0.5.0\\skills\\multi-model-verify\\SKILL.md'" in C:\Users\Brandon\Documents\parallax
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Select-String -Path 'C:\\Users\\Brandon\\.codex\\memories\\MEMORY.md' -Pattern 'parallax|Kimi|backup lane|0.13.0' -CaseSensitive:"'$false -Context 2,3' in C:\Users\Brandon\Documents\parallax
 succeeded in 604ms:
 succeeded in 620ms:
---
name: using-superpowers
description: Use when starting any conversation - establishes how to find and use skills, requiring skill invocation before ANY response including clarifying questions
---

<SUBAGENT-STOP>
If you were dispatched as a subagent to execute a specific task, ignore this skill.
</SUBAGENT-STOP>

<EXTREMELY-IMPORTANT>
If you think there is even a 1% chance a skill might apply to what you are doing, you ABSOLUTELY MUST invoke the skill.

IF A SKILL APPLIES TO YOUR TASK, YOU DO NOT HAVE A CHOICE. YOU MUST USE IT.

This is not negotiable. You cannot rationalize your way out of this.
</EXTREMELY-IMPORTANT>

## The Rule

**Invoke relevant or requested skills BEFORE any response or action** — including clarifying questions, exploring the codebase, or checking files. If it turns out wrong for the situation, you don't have to use it.

**Before entering plan mode:** if you haven't already brainstormed, invoke the brainstorming skill first.

Then announce "Using [skill] to [purpose]" and follow the skill exactly. If it has a checklist, create a todo per item.

## Skill Priority

When multiple skills apply, process skills come first — they set the approach, then implementation skills (frontend-design, etc.) carry it out. Brainstorming and systematic-debugging are Superpowers' most common process skills, but the rule holds for any of them.

- "Let's build X" → superpowers:brainstorming first, then implementation skills.
- "Fix this bug" → superpowers:systematic-debugging first, then domain skills.

## Red Flags

These thoughts mean STOP—you're rationalizing:

| Thought | Reality |
|---------|---------|
| "This is just a simple question" | Questions are tasks. Check for skills. |
| "I need more context first" | Skill check comes BEFORE clarifying questions. |
| "Let me explore the codebase first" | Skills tell you HOW to explore. Check first. |
| "I can check git/files quickly" | Files lack conversation context. Check for skills. |
| "Let me gather information first" | Skills tell you HOW to gather information. |
| "This doesn't need a formal skill" | If a skill exists, use it. |
| "I remember this skill" | Skills evolve. Read current version. |
| "This doesn't count as a task" | Action = task. Check for skills. |
| "The skill is overkill" | Simple things become complex. Use it. |
| "I'll just do this one thing first" | Check BEFORE doing anything. |
| "This feels productive" | Undisciplined action wastes time. Skills prevent this. |
| "I know what that means" | Knowing the concept ≠ using the skill. Invoke it. |

## Platform Adaptation

If your harness appears here, read its reference file for special instructions:

- Codex: `references/codex-tools.md`
- Pi: `references/pi-tools.md`
- Antigravity: `references/antigravity-tools.md`

## User Instructions

User instructions (CLAUDE.md, AGENTS.md, GEMINI.md, etc, direct requests) take precedence over skills, which in turn override default behavior. Only skip skill workflows or instructions when your human partner has explicitly told you to.


2026-07-26T04:12:44.097174Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 0.7 seconds
Output:
Get-Content: 
Line |
   2 |  Get-Content -Raw 'C:\Users\Brandon\.codex\plugins\cache\crosscheck\0. …
     |  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
     | Cannot find path 'C:\Users\Brandon\.codex\plugins\cache\crosscheck\0.5.0\skills\multi-model-verify\SKILL.md' because it does not exist.

 exited 1 in 708ms:
Get-Content: 
Line |
   2 |  Get-Content -Raw 'C:\Users\Brandon\.codex\plugins\cache\crosscheck\0. …
     |  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
     | Cannot find path 'C:\Users\Brandon\.codex\plugins\cache\crosscheck\0.5.0\skills\multi-model-verify\SKILL.md' because it does not exist.

codex
The crosscheck skill catalog path includes an extra package directory, so the first read missed it. I’m correcting that path before inspecting the repository; no task evidence has been evaluated yet.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Raw 'C:\\Users\\Brandon\\.codex\\plugins\\cache\\claude-plugins-official\\superpowers\\6.2.0\\skills\\using-superpowers\\SKILL.md'" in C:\Users\Brandon\Documents\parallax
 succeeded in 527ms:
---
name: using-superpowers
description: Use when starting any conversation - establishes how to find and use skills, requiring skill invocation before ANY response including clarifying questions
---

<SUBAGENT-STOP>
If you were dispatched as a subagent to execute a specific task, ignore this skill.
</SUBAGENT-STOP>

<EXTREMELY-IMPORTANT>
If you think there is even a 1% chance a skill might apply to what you are doing, you ABSOLUTELY MUST invoke the skill.

IF A SKILL APPLIES TO YOUR TASK, YOU DO NOT HAVE A CHOICE. YOU MUST USE IT.

This is not negotiable. You cannot rationalize your way out of this.
</EXTREMELY-IMPORTANT>

## The Rule

**Invoke relevant or requested skills BEFORE any response or action** — including clarifying questions, exploring the codebase, or checking files. If it turns out wrong for the situation, you don't have to use it.

**Before entering plan mode:** if you haven't already brainstormed, invoke the brainstorming skill first.

Then announce "Using [skill] to [purpose]" and follow the skill exactly. If it has a checklist, create a todo per item.

## Skill Priority

When multiple skills apply, process skills come first — they set the approach, then implementation skills (frontend-design, etc.) carry it out. Brainstorming and systematic-debugging are Superpowers' most common process skills, but the rule holds for any of them.

- "Let's build X" → superpowers:brainstorming first, then implementation skills.
- "Fix this bug" → superpowers:systematic-debugging first, then domain skills.

## Red Flags

These thoughts mean STOP—you're rationalizing:

| Thought | Reality |
|---------|---------|
| "This is just a simple question" | Questions are tasks. Check for skills. |
| "I need more context first" | Skill check comes BEFORE clarifying questions. |
| "Let me explore the codebase first" | Skills tell you HOW to explore. Check first. |
| "I can check git/files quickly" | Files lack conversation context. Check for skills. |
| "Let me gather information first" | Skills tell you HOW to gather information. |
| "This doesn't need a formal skill" | If a skill exists, use it. |
| "I remember this skill" | Skills evolve. Read current version. |
| "This doesn't count as a task" | Action = task. Check for skills. |
| "The skill is overkill" | Simple things become complex. Use it. |
| "I'll just do this one thing first" | Check BEFORE doing anything. |
| "This feels productive" | Undisciplined action wastes time. Skills prevent this. |
| "I know what that means" | Knowing the concept ≠ using the skill. Invoke it. |

## Platform Adaptation

If your harness appears here, read its reference file for special instructions:

- Codex: `references/codex-tools.md`
- Pi: `references/pi-tools.md`
- Antigravity: `references/antigravity-tools.md`

## User Instructions

User instructions (CLAUDE.md, AGENTS.md, GEMINI.md, etc, direct requests) take precedence over skills, which in turn override default behavior. Only skip skill workflows or instructions when your human partner has explicitly told you to.


exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Raw 'C:\\Users\\Brandon\\.codex\\plugins\\cache\\crosscheck\\crosscheck\\0.5.0\\skills\\multi-model-verify\\SKILL.md'" in C:\Users\Brandon\Documents\parallax
 succeeded in 527ms:
---
name: multi-model-verify
description: Use when planning a reference port, an API-sensitive module, or any change risky enough to need cross-model verification before implementation, or when an implemented diff must be checked against its plan and reference before merge. Fires automatically alongside superpowers requesting-code-review via the review-companion hook.
---

# Multi-Model Verify

## Overview

Two equal-weight advisors — Fable 5 (this session) and GPT-5.6 Sol (via the
codex CLI) — verify and refute each other's claims before the cheap
implementer touches code. Sol's documented fabrication risk (METR) is
mitigated by the debate structure — evidence grounding plus mutual
refutation — not by down-weighting either side.

**REQUIRED READING before the first round:** references/debate-protocol.md.

Companion to superpowers, not a replacement: it fills the cross-model review
gap superpowers rules out of scope. `/codex:adversarial-review` remains the
human-invoked pre-merge gate; if the codex plugin's stop-review-gate is
toggled on, its stop-time review overlaps mode `diff` — expected, not a bug.

## When to use

- **Mode `plan`** — after superpowers brainstorming proposes approaches for a
  port or API-sensitive module, before the implementation plan is written.
- **Mode `diff`** — after implementation, alongside superpowers
  requesting-code-review (the review-companion hook injects this
  automatically with the matching base/head SHAs).
- NOT for plain bugfixes, docs or release chores, GUI tweaks, or lint sweeps.

## Preflight (both modes)

1. `codex --version` must succeed — on failure follow references/fallbacks.md
   (degraded mode, visibly flagged; never silently skip cross-vendor review).
2. For port work: the reference source must exist under `References/`
   (quote paths — some contain spaces, e.g. `References/M+ Timer`). Missing
   → **HARD STOP before any debate round runs**: ask the user for the path
   and go no further — do not run a codex exchange about the absence, do
   not debate from memory, do not start a degraded mode. If the folder
   holds version subdirectories (e.g. a `v1.4/` next to a `v1.1_old/`), use
   the newest unless the user names one, and cite the versioned path:
   `References/<name>/<version>/<file>:<line>`.
   For non-port work there is no reference folder — claims ground in the
   project's own source, specs, or upstream docs instead, same strike rule.

## Mode plan

1. Draft the Fable position: chosen approach, port-fidelity claims, and the
   API/behavior risk register. Every reference claim cites a
   `References/<name>/<file>:<line>` actually read this session — anchor
   EVERY file with its full path the first time you cite it, manifests and
   secondary files included (shorthand only once that file is anchored);
   every API claim cites authoritative local docs (WoW projects:
   `.wow-api-reference/` or a dated in-game probe result) — never memory.
   The position has no separate artifact — it IS the claims section of the
   step-2 brief.
2. Compose Sol's debate brief per references/model-prompting-notes.md, write
   it to a scratchpad file, then run round 1:

   ```powershell
   Get-Content -Raw <brief-file> | codex exec --sandbox read-only -m <canonical-model-id> -c model_reasoning_effort=<canonical-effort> --output-last-message <reply-file> -
   ```

   `<canonical-model-id>` and `<canonical-effort>` are the two declarations
   in references/model-prompting-notes.md — read the literal values from
   there, never from memory (the reviewer swaps by editing that one file,
   and a remembered id silently defeats the swap).

   Capture the `session id:` line printed in the run header (top of output);
   read Sol's reply from `<reply-file>` — the stdout transcript logs every
   file Sol reads and can run tens of KB, with the reply buried at the
   bottom.
3. Later rounds keep Sol's state by resuming that session — flags MUST
   precede the resume subcommand (flags after it are a usage error):

   ```powershell
   codex exec --sandbox read-only -m <canonical-model-id> -c model_reasoning_effort=<canonical-effort> --output-last-message <reply-file> resume <SESSION_ID> "<rebuttal-brief>"
   ```

4. Iterate per debate-protocol.md until convergence or the round cap, then
   escalate any unresolved points to the user with both positions stated.
5. Freeze the converged plan per references/frozen-plan-format.md under the
   project's superpowers plans dir (KitnEssentials:
   `dev/docs/superpowers/plans/`; other projects: the superpowers default
   `docs/superpowers/plans/`).

## Mode diff

Same transport and protocol. First read the frozen plan's debate record and
its **Verification status** field. Then the brief carries the frozen plan
path, the base/head SHAs superpowers code review used, and the
`git diff base..head` output. Both sides check **spec fidelity** (drift from
the frozen plan — the implementer makes zero judgment calls, so any drift is
a finding) and, for port work, **port fidelity** (drift from the reference
source), ending PASS / FIX / ESCALATE.

**Degraded-plan poisoning rule:** a plan whose Verification status is
DEGRADED cannot produce an ordinary diff PASS. Mode diff must first reopen
and cross-verify the plan's claims and approved deviations (the
retrospective pass the plan never got), and only then verify the
implementation against it — confirming a diff matches an unsound plan
verifies nothing. If cross-vendor verification is STILL unavailable, the
only terminal state is `ESCALATE — CROSS-VENDOR GATE UNSATISFIED`: merging
then requires the user's explicitly recorded risk acceptance, never a PASS.

## Finish line

Before it: the session's final-adjudication step (debate-protocol.md) —
verify the last round's findings against the repo and issue the terminal
verdict; a reviewer's PASS/FIX is never terminal by itself.

End with one status line: participating models, rounds used, converged vs
escalated points, and the verification status — `FULL`, or
`DEGRADED (<class>, authorized by user at round N)` per fallbacks.md. The
session emits this line itself; never delegate it to a subagent.

## Common mistakes

- Accepting Sol's claims about reference code without the cited lines —
  strike the claim per protocol; do not argue against it.
- Re-sending the full debate context each round instead of resuming the
  codex session.
- Running mode `diff` against different SHAs than the code review used.
- Treating convergence as failure — a sound plan converging in one round is
  the system working, not a skipped debate.


exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Select-String -Path 'C:\\Users\\Brandon\\.codex\\memories\\MEMORY.md' -Pattern 'parallax|Kimi|backup lane|0.13.0' -CaseSensitive:"'$false -Context 2,3' in C:\Users\Brandon\Documents\parallax
 succeeded in 509ms:
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Raw 'C:\\Users\\Brandon\\.codex\\plugins\\cache\\claude-plugins-official\\superpowers\\6.2.0\\skills\\using-superpowers\\references\\codex-tools.md'" in C:\Users\Brandon\Documents\parallax
 succeeded in 500ms:
## Subagent dispatch requires multi-agent support

Add to your Codex config (`~/.codex/config.toml`):

```toml
[features]
multi_agent = true
```

This enables `spawn_agent`, `wait_agent`, and `close_agent` for skills like `dispatching-parallel-agents` and `subagent-driven-development`. When using subagent-driven-development, close reviewer subagents when their review returns. Keep each implementer subagent open until its task's review passes — the fix loop resumes the implementer — then close it. If your harness cannot send another message to a spawned agent, dispatch each fix round as a fresh implementer carrying the brief, the report file, and the findings.

## Environment Detection

Skills that create worktrees or finish branches should detect their
environment with read-only git commands before proceeding:

```bash
GIT_DIR=$(cd "$(git rev-parse --git-dir)" 2>/dev/null && pwd -P)
GIT_COMMON=$(cd "$(git rev-parse --git-common-dir)" 2>/dev/null && pwd -P)
BRANCH=$(git branch --show-current)
```

- `GIT_DIR != GIT_COMMON` → already in a linked worktree (skip creation)
- `BRANCH` empty → detached HEAD (cannot branch/push/PR from sandbox)

See `using-git-worktrees` Step 0 and `finishing-a-development-branch`
Step 1 for how each skill uses these signals.

## Codex App Finishing

When the sandbox blocks branch/push operations (detached HEAD in an
externally managed worktree), the agent commits all work and informs
the user to use the App's native controls:

- **"Create branch"** — names the branch, then commit/push/PR via App UI
- **"Hand off to local"** — transfers work to the user's local checkout

The agent can still run tests, stage files, and output suggested branch
names, commit messages, and PR descriptions for the user to copy.


exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Raw 'C:\\Users\\Brandon\\.codex\\plugins\\cache\\crosscheck\\crosscheck\\0.5.0\\skills\\multi-model-verify\\references\\debate-protocol.md'" in C:\Users\Brandon\Documents\parallax
 succeeded in 497ms:
# Debate protocol

Both advisors are equal weight. Neither model's claim outranks the other's;
only evidence does.

## Evidence grounding (the strike rule)

- **Every externally checkable claim must carry a citation to a source the
  other side can read**: a file:line in the repo or reference material the
  citing side actually read (Fable: read this session; Sol: read inside its
  sandboxed run — its quoted excerpt counts as reading), authoritative local
  docs, or a fetched URL.
- Source-specific examples: reference-port claims cite
  `References/<name>/<file>:<line>`; WoW 12.0 API claims cite
  `.wow-api-reference/` or a dated in-game probe result; library or platform
  claims cite the vendored source or official docs.
- **Anchor every file the first time you cite it** — full repo-relative path
  with line, `References/DemoWidget/DemoWidget.toc:1`, not `DemoWidget.toc:1`.
  This holds for EVERY file, including one-line manifests and secondary
  files, not just the main source. Once a file is anchored, shorthand
  (`Core.lua:19`) is fine for later citations of that same file. A bare
  filename does not locate anything for the other side: the project has its
  own `DemoAddon.toc` and the reference has `DemoWidget.toc`, and an
  unanchored `.toc:1` could be either.
- A claim with no citation is **struck, not debated**. Note it as struck and
  move on — arguing against an ungrounded claim launders it into the record.
- Reports are claims requiring evidence. "The reference handles this" without
  the file:line is an assertion, not a finding.

## Round structure

Each exchange (one Fable position or rebuttal + one Sol reply) is a round.

1. From round 2 on, state position changes since last round: which of the
   other side's points are **accepted** (absorbed into the plan), which are
   **refuted** (with the evidence that refutes them), which are **struck**
   (no citation).
2. Raise new risks only with evidence attached.
3. End with a verdict on the current plan/diff:
   - **PASS** — no remaining substantive objections.
   - **FIX** — named change required, with the specific fix and its evidence.
   - **ESCALATE** — a disagreement evidence cannot settle (taste, missing
     in-game data, ambiguous reference intent).

## Convergence and the round cap

- Converged: both sides verdict PASS in the same round.
- **Converged with amendments**: the final round's only outstanding verdicts
  are FIXes whose fixes the other side accepts on the record. Record each
  accepted fix in the debate record and treat the plan as converged — an
  accepted FIX is agreement, not a dispute for the user to settle. (This
  matters whenever a FIX lands in the last round before the cap.)
- Round cap: **4 exchanges** by default (caller may raise or lower it).
  Hitting the cap is not failure — it is the signal to stop spending tokens
  on an argument evidence hasn't settled.
- At the cap or on ESCALATE, for points that remain genuinely contested:
  stop, present BOTH positions to the user with their evidence, and let the
  user decide. Never silently pick a side.
- Questions only runtime testing can answer always escalate — neither model
  can run the live application (in WoW projects, /reload + BugSack is always
  the user's step).

## Final adjudication (the session's last step)

The chain never ends on the external reviewer's verdict. After Sol's final
round — including a re-review of fixes — the SESSION performs one closing
step before anything merges or freezes:

1. Verify each finding from that final round against the live repo (read
   the cited lines; run the gates if implicated) — a final-round finding is
   itself an unverified claim until checked.
2. Accept with evidence, refute with evidence, or ESCALATE to the user.
   Nothing is adopted or dismissed on authority.
3. Emit the terminal verdict and the finish line. A reviewer PASS/FIX is
   input to this step, never the decision itself.

Equal weight governs claims during the debate; adjudication is procedural,
not a rank: the session is the accountable party with live-repo access, so
the session always has the final say — and a genuine deadlock still goes to
the user, never to whoever spoke last (user directive, 2026-07-12).

## Anti-sycophancy, anti-theater

Debates fail two ways: rubber-stamping and manufactured conflict. Both are
defects.

- **A sound plan gets one line.** "Plan is sound; the one thing to watch is
  X." Converging in round 1 is the system working.
- **Do not manufacture objections** to justify the round. If you would not
  push back in a real review, do not push back here.
- Do not restate the other side's point in different words as a "new" risk.
- Do not concede a point you can refute just to converge faster —
  unresolved disagreement has an honest exit (ESCALATE).

## Why the structure carries the trust

GPT-5.6 Sol has a documented (METR, OpenAI system card) tendency to fabricate
results under evaluation pressure. The mitigation is this protocol — every
claim must survive citation and refutation by an independent model — not
down-weighting Sol's verdicts. The same rules bind Fable equally: fabricated
detail is a known failure mode of every frontier model, and this project's
history (see memory: no-fabrication feedback) treats invented API behavior as
the cardinal sin.


  → Inventory repository instructions and pin branch/head/review inputs
  • Map every approved spec requirement to candidate plan tasks and embedded pins
  • Check candidate against existing 0.12.0 contracts and repository tests/docs
  • Adversarially verify findings and issue cited PASS/FIX/ESCALATE with UNVERIFIED list
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git branch --show-current; git rev-parse HEAD; git status --short; Get-ChildItem -Path . -Recurse -Force -Filter AGENTS.md | ForEach-Object { $_.FullName }' in C:\Users\Brandon\Documents\parallax
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Item 'docs\\superpowers\\plans\\2026-07-25-kimi-backup-lane.md','docs\\superpowers\\specs\\2026-07-25-kimi-backup-lane-design.md' | Select-Object FullName,Length; Get-ChildItem -Path docs\\superpowers -Recurse -File | Select-Object FullName" in C:\Users\Brandon\Documents\parallax
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-ChildItem -Force | Select-Object Name,Mode,Length; git log -5 --oneline --decorate' in C:\Users\Brandon\Documents\parallax
 succeeded in 607ms:

FullName
--------                                                                                                               
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\2026-07-25-kimi-backup-lane.md                              
C:\Users\Brandon\Documents\parallax\docs\superpowers\specs\2026-07-25-kimi-backup-lane-design.md                       
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\2026-07-24-jinn-intake-adoptions.md                         
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\2026-07-25-behavioral-eval-trim.md                          
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\2026-07-25-flash-implementer.md                             
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\2026-07-25-kimi-backup-lane.md                              
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-24-jinn-intake\diff-round1-brief.md          
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-24-jinn-intake\diff-round1-reply.txt         
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-24-jinn-intake\diff-round2-rebuttal.md       
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-24-jinn-intake\diff-round2-reply.txt         
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-24-jinn-intake\round1-brief.md               
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-24-jinn-intake\round1-reply.txt              
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-24-jinn-intake\round2-rebuttal.md            
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-24-jinn-intake\round2-reply.txt              
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-eval-trim\diff-round1-brief.txt           
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-eval-trim\diff-round1-reply.txt           
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-eval-trim\diff-round1-transcript.txt      
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-eval-trim\diff-round2-brief.txt           
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-eval-trim\diff-round2-reply.txt           
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-eval-trim\diff-round2-transcript.txt      
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-eval-trim\plan-round1-brief.txt           
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-eval-trim\plan-round1-reply.txt           
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-eval-trim\plan-round1-transcript.txt      
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-eval-trim\plan-round2-brief.txt           
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-eval-trim\plan-round2-reply.txt           
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-eval-trim\plan-round2-transcript.txt      
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-eval-trim\plan-round3-brief.txt           
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-eval-trim\plan-round3-reply.txt           
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-eval-trim\plan-round3-transcript.txt      
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\clone-status-after-roun…
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\diff-round1-brief.md    
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\diff-round1-header.txt  
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\diff-round1-reply.md    
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\diff-round2-brief.md    
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\diff-round2-header.txt  
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\diff-round2-reply.md    
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\diff-round3-brief.md    
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\diff-round3-header.txt  
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\diff-round3-reply.md    
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\plan-round1-brief.md    
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\plan-round1-transcript.…
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\plan-round2-transcript.…
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\sol-checkoff-round1-bri…
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\sol-checkoff-round1-hea…
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\sol-checkoff-round1-rep…
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\sol-checkoff-round2-bri…
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\sol-checkoff-round2-hea…
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\sol-checkoff-round2-rep…
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\sol-checkoff-round3-bri…
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\sol-checkoff-round3-hea…
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\sol-checkoff-round3-rep…
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\sol-checkoff-round4-bri…
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\sol-checkoff-round4-hea…
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\sol-checkoff-round4-rep…
C:\Users\Brandon\Documents\parallax\docs\superpowers\specs\2026-07-25-behavioral-eval-trim-design.md                   
C:\Users\Brandon\Documents\parallax\docs\superpowers\specs\2026-07-25-flash-implementer-design.md                      
C:\Users\Brandon\Documents\parallax\docs\superpowers\specs\2026-07-25-kimi-backup-lane-design.md                       


 succeeded in 665ms:

e432087 (HEAD -> feat/0130-kimi-backup-lane) 0.13.0: kimi backup lane plan candidate
2d9536c 0.13.0: resolve plan-time probes - resume containment loss found and contained
9b75e55 0.13.0: kimi backup lane design spec
498d72f (origin/main, origin/HEAD, main) Merge branch 'feat/0120-flash-implementer'
bd735e9 (feat/0120-flash-implementer) 0.12.0: finalize mode-diff debate record - three rounds, terminal pass
Name           Mode  Length
----           ----  ------
.agents        d---- 
.claude-plugin d---- 
.codex         d---- 
.git           d--h- 
.githooks      d---- 
.github        d---- 
.pytest_cache  d---- 
.superpowers   d---- 
agents         d---- 
commands       d---- 
docs           d---- 
evals          d---- 
hooks          d---- 
skills         d---- 
tools          d---- 
.gitattributes -a--- 93
.gitignore     -a--- 267
CLAUDE.md      -a--- 2466
LICENSE        -a--- 1093
README.md      -a--- 20030


2026-07-26T04:13:16.740284Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 0.8 seconds
Output:
feat/0130-kimi-backup-lane
e432087db33216fdfa0faaad69697e409f143d19
Get-ChildItem: 
Line |
   2 |  … us --short; Get-ChildItem -Path . -Recurse -Force -Filter AGENTS.md | …
     |                ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
     | Access to the path 'C:\Users\Brandon\Documents\parallax\.pytest_cache' is denied.

 exited 1 in 808ms:
feat/0130-kimi-backup-lane
e432087db33216fdfa0faaad69697e409f143d19
Get-ChildItem: 
Line |
   2 |  … us --short; Get-ChildItem -Path . -Recurse -Force -Filter AGENTS.md | …
     |                ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
     | Access to the path 'C:\Users\Brandon\Documents\parallax\.pytest_cache' is denied.

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "git branch --show-current; git rev-parse HEAD; git status --short; git ls-files '*AGENTS.md'" in C:\Users\Brandon\Documents\parallax
 succeeded in 470ms:
feat/0130-kimi-backup-lane
e432087db33216fdfa0faaad69697e409f143d19

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Item 'docs\\superpowers\\plans\\2026-07-25-kimi-backup-lane.md','docs\\superpowers\\specs\\2026-07-25-kimi-backup-lane-design.md' | Select-Object FullName,Length; Get-ChildItem -Path docs\\superpowers -Recurse -File -ErrorAction SilentlyContinue | Select-Object FullName" in C:\Users\Brandon\Documents\parallax
 succeeded in 549ms:

FullName
--------                                                                                                               
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\2026-07-25-kimi-backup-lane.md                              
C:\Users\Brandon\Documents\parallax\docs\superpowers\specs\2026-07-25-kimi-backup-lane-design.md                       
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\2026-07-24-jinn-intake-adoptions.md                         
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\2026-07-25-behavioral-eval-trim.md                          
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\2026-07-25-flash-implementer.md                             
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\2026-07-25-kimi-backup-lane.md                              
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-24-jinn-intake\diff-round1-brief.md          
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-24-jinn-intake\diff-round1-reply.txt         
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-24-jinn-intake\diff-round2-rebuttal.md       
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-24-jinn-intake\diff-round2-reply.txt         
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-24-jinn-intake\round1-brief.md               
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-24-jinn-intake\round1-reply.txt              
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-24-jinn-intake\round2-rebuttal.md            
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-24-jinn-intake\round2-reply.txt              
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-eval-trim\diff-round1-brief.txt           
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-eval-trim\diff-round1-reply.txt           
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-eval-trim\diff-round1-transcript.txt      
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-eval-trim\diff-round2-brief.txt           
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-eval-trim\diff-round2-reply.txt           
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-eval-trim\diff-round2-transcript.txt      
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-eval-trim\plan-round1-brief.txt           
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-eval-trim\plan-round1-reply.txt           
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-eval-trim\plan-round1-transcript.txt      
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-eval-trim\plan-round2-brief.txt           
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-eval-trim\plan-round2-reply.txt           
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-eval-trim\plan-round2-transcript.txt      
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-eval-trim\plan-round3-brief.txt           
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-eval-trim\plan-round3-reply.txt           
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-eval-trim\plan-round3-transcript.txt      
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\clone-status-after-roun…
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\diff-round1-brief.md    
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\diff-round1-header.txt  
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\diff-round1-reply.md    
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\diff-round2-brief.md    
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\diff-round2-header.txt  
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\diff-round2-reply.md    
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\diff-round3-brief.md    
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\diff-round3-header.txt  
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\diff-round3-reply.md    
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\plan-round1-brief.md    
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\plan-round1-transcript.…
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\plan-round2-transcript.…
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\sol-checkoff-round1-bri…
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\sol-checkoff-round1-hea…
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\sol-checkoff-round1-rep…
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\sol-checkoff-round2-bri…
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\sol-checkoff-round2-hea…
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\sol-checkoff-round2-rep…
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\sol-checkoff-round3-bri…
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\sol-checkoff-round3-hea…
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\sol-checkoff-round3-rep…
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\sol-checkoff-round4-bri…
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\sol-checkoff-round4-hea…
C:\Users\Brandon\Documents\parallax\docs\superpowers\plans\rounds\2026-07-25-flash-implementer\sol-checkoff-round4-rep…
C:\Users\Brandon\Documents\parallax\docs\superpowers\specs\2026-07-25-behavioral-eval-trim-design.md                   
C:\Users\Brandon\Documents\parallax\docs\superpowers\specs\2026-07-25-flash-implementer-design.md                      
C:\Users\Brandon\Documents\parallax\docs\superpowers\specs\2026-07-25-kimi-backup-lane-design.md                       


exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-ChildItem -Force | Select-Object Name,Mode,Length; git log -5 --oneline --decorate' in C:\Users\Brandon\Documents\parallax
 succeeded in 553ms:

e432087 (HEAD -> feat/0130-kimi-backup-lane) 0.13.0: kimi backup lane plan candidate
2d9536c 0.13.0: resolve plan-time probes - resume containment loss found and contained
9b75e55 0.13.0: kimi backup lane design spec
498d72f (origin/main, origin/HEAD, main) Merge branch 'feat/0120-flash-implementer'
bd735e9 (feat/0120-flash-implementer) 0.12.0: finalize mode-diff debate record - three rounds, terminal pass
Name           Mode  Length
----           ----  ------
.agents        d---- 
.claude-plugin d---- 
.codex         d---- 
.git           d--h- 
.githooks      d---- 
.github        d---- 
.pytest_cache  d---- 
.superpowers   d---- 
agents         d---- 
commands       d---- 
docs           d---- 
evals          d---- 
hooks          d---- 
skills         d---- 
tools          d---- 
.gitattributes -a--- 93
.gitignore     -a--- 267
CLAUDE.md      -a--- 2466
LICENSE        -a--- 1093
README.md      -a--- 20030


exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$paths = @('"'docs\\superpowers\\plans\\2026-07-25-kimi-backup-lane.md','docs\\superpowers\\specs\\2026-07-25-kimi-backup-lane-design.md'); foreach ("'$p in $paths) { $lines = Get-Content -LiteralPath $p; Write-Output ("{0}`tlines={1}`tbytes={2}" -f $p,$lines.Count,(Get-Item -LiteralPath $p).Length) }' in C:\Users\Brandon\Documents\parallax
 succeeded in 551ms:
docs\superpowers\plans\2026-07-25-kimi-backup-lane.md	lines=726	bytes=36938
docs\superpowers\specs\2026-07-25-kimi-backup-lane-design.md	lines=328	bytes=17802

codex
The review inputs are pinned exactly as stated: branch `feat/0130-kimi-backup-lane`, HEAD `e432087`, with a clean status. I’m now reading both documents in full and building a section-by-section traceability map, including exact whitespace-sensitive pin text.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'docs\\superpowers\\plans\\2026-07-25-kimi-backup-lane.md'; "'$i=0; Get-Content -LiteralPath $p | ForEach-Object { $i++; if ($i -ge 181 -and $i -le 360) { '"'{0,4}: {1}' -f "'$i,$_ } }' in C:\Users\Brandon\Documents\parallax
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'docs\\superpowers\\specs\\2026-07-25-kimi-backup-lane-design.md'; "'$i=0; Get-Content -LiteralPath $p | ForEach-Object { $i++; if ($i -ge 1 -and $i -le 180) { '"'{0,4}: {1}' -f "'$i,$_ } }' in C:\Users\Brandon\Documents\parallax
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'docs\\superpowers\\plans\\2026-07-25-kimi-backup-lane.md'; "'$i=0; Get-Content -LiteralPath $p | ForEach-Object { $i++; if ($i -ge 1 -and $i -le 180) { '"'{0,4}: {1}' -f "'$i,$_ } }' in C:\Users\Brandon\Documents\parallax
 succeeded in 604ms:
 181: 
 182: - [ ] **Step 3: Run the new tests to verify they fail**
 183: 
 184: Run: `python -m pytest evals/multi-model-verify/test_backup_lane.py -q`
 185: Expected: FAIL — `test_backup_artifacts_exist` errors first (files missing); the modified `test_multi_model_verify.py` reference-file pin also fails on the missing `backup-lane.md`.
 186: 
 187: - [ ] **Step 4: Run the full suite to confirm no collateral damage**
 188: 
 189: Run: `python -m pytest evals -q`
 190: Expected: only the new tests and the amended REQUIRED_REFERENCE_FILES pins fail; the pre-existing 144 passing tests stay green.
 191: 
 192: - [ ] **Step 5: Commit**
 193: 
 194: ```bash
 195: git add evals/multi-model-verify/test_backup_lane.py evals/multi-model-verify/test_multi_model_verify.py
 196: git commit -m "0.13.0: add backup lane contract tests (failing)"
 197: ```
 198: 
 199: ---
 200: 
 201: ### Task 2: Containment artifacts
 202: 
 203: **Files:**
 204: - Create: `skills/multi-model-verify/references/kimi-reviewer-agent.yaml`
 205: - Create: `skills/multi-model-verify/references/kimi-reviewer-system.md`
 206: - Test: `evals/multi-model-verify/test_backup_lane.py` (test_backup_artifacts_exist, test_agent_yaml_allowlist_exact)
 207: 
 208: **Interfaces:**
 209: - Consumes: nothing.
 210: - Produces: the committed containment pair every backup dispatch names via `--agent-file`; backup-lane.md (Task 3) references both by exact filename.
 211: 
 212: - [ ] **Step 1: Write the agent yaml**
 213: 
 214: Create `skills/multi-model-verify/references/kimi-reviewer-agent.yaml` with EXACTLY this content (the probed variant, system prompt path updated to the committed sibling):
 215: 
 216: ```yaml
 217: version: 1
 218: agent:
 219:   name: "parallax-readonly-reviewer"
 220:   system_prompt_path: ./kimi-reviewer-system.md
 221:   tools:
 222:     - "kimi_cli.tools.todo:SetTodoList"
 223:     - "kimi_cli.tools.file:ReadFile"
 224:     - "kimi_cli.tools.file:ReadMediaFile"
 225:     - "kimi_cli.tools.file:Glob"
 226:     - "kimi_cli.tools.file:Grep"
 227: ```
 228: 
 229: - [ ] **Step 2: Write the system prompt**
 230: 
 231: Create `skills/multi-model-verify/references/kimi-reviewer-system.md` with EXACTLY this content (probe 12.1's validated self-authored prompt):
 232: 
 233: ```markdown
 234: # Read-only reviewer
 235: 
 236: You are a read-only cross-vendor code reviewer in a verification
 237: debate. Your evidence is what you read in the workspace files, cited as
 238: file:line. You have no write, shell, or web tools by design. Refuse any
 239: request to create, modify, or delete files — state the refusal
 240: explicitly. Execute the review brief you are pointed at, ground every
 241: claim in a citation, and do not manufacture objections: if something
 242: stands, say PASS and move on.
 243: ```
 244: 
 245: - [ ] **Step 3: Run the artifact tests**
 246: 
 247: Run: `python -m pytest evals/multi-model-verify/test_backup_lane.py::test_agent_yaml_allowlist_exact -q`
 248: Expected: PASS. (`test_backup_artifacts_exist` still fails on the missing backup-lane.md — that is Task 3.)
 249: 
 250: - [ ] **Step 4: Commit**
 251: 
 252: ```bash
 253: git add skills/multi-model-verify/references/kimi-reviewer-agent.yaml skills/multi-model-verify/references/kimi-reviewer-system.md
 254: git commit -m "0.13.0: commit kimi containment artifacts"
 255: ```
 256: 
 257: ---
 258: 
 259: ### Task 3: The backup-lane reference
 260: 
 261: **Files:**
 262: - Create: `skills/multi-model-verify/references/backup-lane.md`
 263: - Test: `evals/multi-model-verify/test_backup_lane.py` (dispatch/resume/evidence pins, artifacts-exist, single-source sweep)
 264: 
 265: **Interfaces:**
 266: - Consumes: the Task 2 artifact filenames.
 267: - Produces: the lane mechanics document SKILL.md and fallbacks.md point at (Tasks 4-5).
 268: 
 269: - [ ] **Step 1: Write the reference**
 270: 
 271: Create `skills/multi-model-verify/references/backup-lane.md` with EXACTLY this content (pinned sentences are single physical lines — do not re-wrap them):
 272: 
 273: ```markdown
 274: # Backup reviewer lane (cross-vendor substitution)
 275: 
 276: The backup lane substitutes a SECOND cross-vendor reviewer (currently
 277: Kimi K3 via kimi-cli) when the primary reviewer transport is down. It
 278: enters ONLY through the fallbacks.md consent gate — auto-qualified by
 279: the classes named there, manual on user request — and preserves
 280: cross-vendor independence, so a backup-lane debate records
 281: `Verification status: FULL` with the lane substitution noted per
 282: frozen-plan-format.md. Same debate protocol, same brief conventions,
 283: same strike rule as the primary; only the transport differs. The
 284: canonical backup model id and thinking flag are declared ONLY in
 285: model-prompting-notes.md — read them from there at dispatch; this file
 286: uses placeholders.
 287: 
 288: ## Transport
 289: 
 290: - Dispatch (single line):
 291:   `kimi --quiet --thinking -m <canonical-backup-model-id> --agent-file <plugin-checkout>/skills/multi-model-verify/references/kimi-reviewer-agent.yaml -w <throwaway-clone> -p "Read the file KIMI-REVIEW-BRIEF.md in this workspace and execute the review it describes."`
 292: - Resume (single line — every flag below is load-bearing):
 293:   `kimi --quiet -r <session-id> --agent-file <same yaml> -m <canonical-backup-model-id> --thinking -p "<rebuttal>"`
 294:   A bare `kimi -r` loads the DEFAULT agent with full write and shell tools while the route line still reads clean (probed 2026-07-25: the behavioral refusal came from conversation priming with WriteFile and Shell live underneath); model and thinking inheritance come from CONFIG DEFAULTS, not the session. Re-pin all three on every resumed call.
 295: - The session id is printed at the end of every run ("To resume this
 296:   session: kimi -r <uuid>"). Capture it from round 1.
 297: - Reviewer reasoning effort has NO CLI flag and NO log field: it is
 298:   pinned via `[models.<id>.overrides]` in `~/.kimi/config.toml`
 299:   (evidence class: config validation only — the consent banner names
 300:   this gap when the backup option is offered).
 301: 
 302: ## Per-round evidence (fresh AND resumed calls alike)
 303: 
 304: `~/.kimi/logs/kimi.log` is a shared, user-global append stream — a bare
 305: "the line appears somewhere" check attributes nothing. The rule:
 306: 
 307: - Before every dispatch capture the byte length of `~/.kimi/logs/kimi.log`; after the call, past that offset, require all three: exactly one new `Using LLM model:` line carrying the canonical backup id, a `Loading agent:` line naming the committed yaml, and a `Loaded tools:` line equal to the allowlist exactly.
 308: - Zero matching new lines, more than one, a wrong id, a wrong agent path, or any extra tool entry is a route-attribution failure: the reply is DISCARDED unread and the failure goes to the fallbacks.md consent gate.
 309: - This evidence is client-side: report it as "route line verified
 310:   (client-side)" in the record prose. Server-side substitution is not
 311:   detectable from this class; the finish line's normalized
 312:   `effective route confirmed` means every round's evidence matched THIS
 313:   lane's canonical declarations under these rules.
 314: 
 315: ## Containment
 316: 
 317: - The committed pair `kimi-reviewer-agent.yaml` +
 318:   `kimi-reviewer-system.md` (this directory) is the ONLY agent
 319:   configuration the lane dispatches with. The yaml's five-tool
 320:   allowlist (SetTodoList, ReadFile, ReadMediaFile, Glob, Grep) carries
 321:   no write, shell, or web tool — kimi print mode auto-approves ALL
 322:   tools and `--plan` does not block writes (probed), so the allowlist
 323:   is the load-bearing control and the per-round `Loaded tools:` check
 324:   is its verification.
 325: - WRITE-PROBE (before round 1 of every backup-lane debate): in a fresh
 326:   disposable session with the exact debate configuration, ask the
 327:   contained agent to create a named marker file. PASS requires all of: explicit refusal in the reply, marker absent on disk, clone status delta empty.
 328:   Anything else means the lane is BROKEN (integrity failure class in
 329:   fallbacks.md) — never dispatch a review over it.
 330: 
 331: ## Clone isolation and the brief
 332: 
 333: - Reviews run in a THROWAWAY CLONE of the repo in the session
 334:   scratchpad — never the real tree.
 335: - The brief is written into the clone as the untracked
 336:   `KIMI-REVIEW-BRIEF.md`; the `-p` pointer tells the reviewer to read
 337:   it (headless stdin does not carry the brief).
 338: - After every round, `git status --porcelain` in the clone must list exactly the brief file and nothing else; any other delta quarantines that round's reply (integrity failure class).
 339: - The brief is retained as evidence per the raw-rounds convention.
 340: - Never run `kimi export` inside a repo — it writes a session zip into the current directory; export only from a scratch directory. Nothing in this lane uses export.
 341: 
 342: ## Failure handling
 343: 
 344: All failure classes, retries, and consent-gate dispositions live in
 345: fallbacks.md (the single failure-class namespace) — this file defines
 346: none of its own. Record fields for a substituted debate live in
 347: frozen-plan-format.md.
 348: ```
 349: 
 350: - [ ] **Step 2: Run the tests**
 351: 
 352: Run: `python -m pytest evals/multi-model-verify/test_backup_lane.py -q`
 353: Expected: test_backup_artifacts_exist, dispatch/resume pins, evidence pins, and single-source sweep now PASS; fallbacks/plan-format/SKILL pins still FAIL (Tasks 4-5).
 354: 
 355: - [ ] **Step 3: Commit**
 356: 
 357: ```bash
 358: git add skills/multi-model-verify/references/backup-lane.md
 359: git commit -m "0.13.0: add backup lane reference"
 360: ```

 succeeded in 620ms:
   1: # Kimi Backup Reviewer Lane Implementation Plan
   2: 
   3: > **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
   4: 
   5: **STATUS: CANDIDATE — awaiting the dual plan debate (Sol + Kimi, user-directed)**
   6: 
   7: **Goal:** Codify the field-proven Kimi K3 backup reviewer lane into the plugin: consent-gated cross-vendor substitution when the primary (codex) transport is down, with mechanical per-round route AND containment evidence.
   8: 
   9: **Architecture:** A new `references/backup-lane.md` owns all lane mechanics; `model-prompting-notes.md` gains the only backup model-id declarations; `fallbacks.md` gains the consent-gate option, the transport-broken mapping, and the backup failure table; `frozen-plan-format.md` pins the lane-substitution recording; SKILL.md/README route the lane; doctor/drift watch the transport; offline pins lock all of it. Spec: docs/superpowers/specs/2026-07-25-kimi-backup-lane-design.md (probe record inside — all five plan-time probes RESOLVED).
  10: 
  11: **Tech Stack:** kimi-cli 1.49.0 (pip), pytest offline pins, PowerShell drift probes.
  12: 
  13: ## Global Constraints
  14: 
  15: - All new test and eval content is pure ASCII.
  16: - Commits: lowercase imperative, prefixed `0.13.0:`, no AI-attribution trailers.
  17: - The backup model literal `kimi-code/k3-256k` appears ONLY in `skills/multi-model-verify/references/model-prompting-notes.md` (the declarations) and `evals/multi-model-verify/test_backup_lane.py` (the enforcement pin). Every other surface uses the placeholder `<canonical-backup-model-id>` or parses the notes file.
  18: - The backup declaration labels are exactly `Canonical backup reviewer model id:` and `Canonical backup thinking flag:`; the primary declarations MUST precede them in the notes file (probe 12.4: both runtime parsers keep resolving the primary; no case-insensitive collision).
  19: - Probed kimi-cli facts this plan treats as fixed (2026-07-25, kimi-cli 1.49.0; spec section 12): bare `kimi -r` loads the DEFAULT agent with full write/shell/web tools; the re-pinned resume loads the committed yaml and exactly the five-tool allowlist; kimi.log appends per-invocation `Using LLM model:`, `Loading agent:`, and `Loaded tools:` lines (offset-attributable); self-authored system prompt needs no template args; `LLM not set` exit 1 is the loud bad-model signature; `python -c "import kimi_cli.tools.file, kimi_cli.tools.todo"` succeeds non-billably.
  20: - Implementer lane: `parallax:flash-implementer` per task (its debut production cycle); the classic haiku lane is the consent-gated fallback per the 0.12.0 contract. Long suites (pytest, state-machine, behavioral) are CONTROLLER-run, never inside implementer subagents (0.10.0 lesson).
  21: - Main checkout only. No new trusted directories: kimi review clones live in the session scratchpad and are not agy workspaces.
  22: - Frozen after debate: changes require reopening the debate.
  23: 
  24: ---
  25: 
  26: ### Task 1: Contract tests (all failing first)
  27: 
  28: **Files:**
  29: - Create: `evals/multi-model-verify/test_backup_lane.py`
  30: - Modify: `evals/multi-model-verify/test_multi_model_verify.py` (REQUIRED_REFERENCE_FILES list)
  31: - Test: same files (this task is the test suite)
  32: 
  33: **Interfaces:**
  34: - Consumes: nothing (first task).
  35: - Produces: the executable contract every later task builds against. Tasks 2-6 make these tests pass; nothing may weaken a pin to get green.
  36: 
  37: - [ ] **Step 1: Write the failing test file**
  38: 
  39: Create `evals/multi-model-verify/test_backup_lane.py` with EXACTLY this content:
  40: 
  41: ```python
  42: """Contract pins for the Kimi backup reviewer lane (0.13.0).
  43: 
  44: Design spec: docs/superpowers/specs/2026-07-25-kimi-backup-lane-design.md.
  45: These pins lock the lane's transport command shape, containment
  46: allowlist, per-round route+containment evidence rules, single-source
  47: discipline, and fallback wiring - all offline, zero CLI calls.
  48: """
  49: import re
  50: from pathlib import Path
  51: 
  52: REPO = Path(__file__).resolve().parents[2]
  53: REFS = REPO / "skills" / "multi-model-verify" / "references"
  54: BACKUP_LANE = REFS / "backup-lane.md"
  55: AGENT_YAML = REFS / "kimi-reviewer-agent.yaml"
  56: SYSTEM_MD = REFS / "kimi-reviewer-system.md"
  57: NOTES = REFS / "model-prompting-notes.md"
  58: FALLBACKS = REFS / "fallbacks.md"
  59: PLAN_FORMAT = REFS / "frozen-plan-format.md"
  60: BACKUP_ID = "kimi-code/k3-256k"
  61: ALLOWLIST = [
  62:     "kimi_cli.tools.todo:SetTodoList",
  63:     "kimi_cli.tools.file:ReadFile",
  64:     "kimi_cli.tools.file:ReadMediaFile",
  65:     "kimi_cli.tools.file:Glob",
  66:     "kimi_cli.tools.file:Grep",
  67: ]
  68: FORBIDDEN_TOOL_MARKERS = ["WriteFile", "StrReplaceFile", "Shell",
  69:                           "SearchWeb", "FetchURL", "tools.web",
  70:                           "tools.shell"]
  71: 
  72: 
  73: def _read(p):
  74:     return p.read_text(encoding="utf-8")
  75: 
  76: 
  77: def test_backup_artifacts_exist():
  78:     for p in (BACKUP_LANE, AGENT_YAML, SYSTEM_MD):
  79:         assert p.is_file(), str(p)
  80: 
  81: 
  82: def test_notes_backup_declarations():
  83:     notes = _read(NOTES)
  84:     assert "Canonical backup reviewer model id: `" + BACKUP_ID + "`" in notes
  85:     assert "Canonical backup thinking flag: `--thinking`" in notes
  86:     # primary parse must survive the amendment, in BOTH parser dialects
  87:     m = re.search(r"Canonical model id: `([^`]+)`", notes)
  88:     assert m and m.group(1) and m.group(1) != BACKUP_ID
  89:     mi = re.search(r"Canonical model id: `([^`]+)`", notes, re.IGNORECASE)
  90:     assert mi and mi.group(1) == m.group(1)
  91:     # backup labels collide with neither primary regex, case-insensitive
  92:     assert not re.search(r"Canonical model id:",
  93:                          "Canonical backup reviewer model id:",
  94:                          re.IGNORECASE)
  95:     # ordering: primary declarations precede the backup block
  96:     assert notes.index("Canonical model id:") < notes.index(
  97:         "Canonical backup reviewer model id:")
  98: 
  99: 
 100: def test_agent_yaml_allowlist_exact():
 101:     yaml_text = _read(AGENT_YAML)
 102:     for tool in ALLOWLIST:
 103:         assert '"' + tool + '"' in yaml_text, tool
 104:     for marker in FORBIDDEN_TOOL_MARKERS:
 105:         assert marker not in yaml_text, marker
 106:     assert "system_prompt_path: ./kimi-reviewer-system.md" in yaml_text
 107: 
 108: 
 109: def test_backup_lane_dispatch_and_resume_pins():
 110:     body = _read(BACKUP_LANE)
 111:     assert "--quiet --thinking -m <canonical-backup-model-id>" in body
 112:     assert "--agent-file" in body
 113:     assert "KIMI-REVIEW-BRIEF.md" in body
 114:     # the re-pinned resume is load-bearing: bare -r restores full tools
 115:     assert ("kimi --quiet -r <session-id> --agent-file" in body)
 116:     assert "loads the DEFAULT agent with full write and shell tools" in body
 117:     assert BACKUP_ID not in body  # placeholder discipline
 118: 
 119: 
 120: def test_backup_lane_evidence_pins():
 121:     body = _read(BACKUP_LANE)
 122:     assert "capture the byte length of" in body
 123:     assert ("exactly one new `Using LLM model:` line carrying the "
 124:             "canonical backup id") in body
 125:     assert "`Loading agent:` line naming the committed yaml" in body
 126:     assert "`Loaded tools:` line equal to the allowlist exactly" in body
 127:     assert "DISCARDED unread" in body
 128:     assert ("explicit refusal in the reply, marker absent on disk, "
 129:             "clone status delta empty") in body
 130:     assert ("must list exactly the brief file and nothing else") in body
 131:     assert "Never run `kimi export` inside a repo" in body
 132: 
 133: 
 134: def test_fallbacks_backup_wiring():
 135:     fb = _read(FALLBACKS)
 136:     assert "[run backup lane (cross-vendor preserved)]" in fb
 137:     assert "reviewer reasoning effort" in fb
 138:     # transport-broken mapping names its member classes
 139:     assert "codex-missing" in fb and "model-rejected" in fb
 140:     assert "quota-exhausted" in fb and "auth-expired" in fb
 141:     assert "route-attribution" in fb
 142:     assert "LLM not set" in fb
 143: 
 144: 
 145: def test_plan_format_lane_substitution_pin():
 146:     fmt = _read(PLAN_FORMAT)
 147:     assert "lane substitution is NOT degradation" in fmt
 148:     assert "backup cross-vendor lane substituted" in fmt
 149: 
 150: 
 151: def test_skill_and_readme_route_the_lane():
 152:     skill = _read(REPO / "skills" / "multi-model-verify" / "SKILL.md")
 153:     assert "backup-lane.md" in skill
 154:     readme = _read(REPO / "README.md")
 155:     assert "run backup lane" in readme
 156: 
 157: 
 158: SWEEP_GLOBS = [
 159:     "skills/**/*.md", "skills/**/*.yaml", "commands/*.md", "tools/*.ps1",
 160:     "hooks/*", "evals/**/*.py", "evals/**/*.json", "evals/**/*.ps1",
 161:     "README.md", "CLAUDE.md", "agents/*.md",
 162: ]
 163: ALLOWED = {NOTES.resolve(), Path(__file__).resolve()}
 164: 
 165: 
 166: def test_backup_literal_single_source():
 167:     offenders = []
 168:     for pattern in SWEEP_GLOBS:
 169:         for p in REPO.glob(pattern):
 170:             if not p.is_file() or p.resolve() in ALLOWED:
 171:                 continue
 172:             if BACKUP_ID in p.read_text(encoding="utf-8",
 173:                                         errors="replace"):
 174:                 offenders.append(str(p))
 175:     assert offenders == []
 176: ```
 177: 
 178: - [ ] **Step 2: Register backup-lane.md in the structural pins**
 179: 
 180: In `evals/multi-model-verify/test_multi_model_verify.py`, find the `REQUIRED_REFERENCE_FILES` list and add `"backup-lane.md"` as a new entry, preserving the list's existing formatting and order style. Touch nothing else in the file.

 succeeded in 640ms:
   1: # 0.13.0 design — Kimi backup reviewer lane (codification)
   2: 
   3: Date: 2026-07-25. Status: APPROVED (user), dual advisory findings folded
   4: (section 14). Builds on the lane's live field run: it conducted the real
   5: 0.12.0 plan debate (2 rounds, session resume, read-only agent-file
   6: containment held, throwaway clone stayed clean) before it had a design
   7: cycle of its own.
   8: 
   9: ## 1. Purpose and scope
  10: 
  11: Codify the Kimi K3 backup reviewer lane into the plugin: when the
  12: primary reviewer transport (codex) is down, the debate substitutes a
  13: SECOND cross-vendor reviewer instead of falling to the single-vendor
  14: DEGRADED skeptic. Backup reviewer lane ONLY this cycle — multi-reviewer
  15: panels, hub-and-spoke mediation machinery, and the seat reshuffle are
  16: 0.14.0 scope (user ruling).
  17: 
  18: ## 2. User decisions (not under debate)
  19: 
  20: - Backup model: `kimi-code/k3-256k`, thinking on, effort high (pinned
  21:   via `~/.kimi/config.toml` `.overrides`, applied 2026-07-25).
  22: - Triggers: `quota-exhausted` and transport-broken classes AUTO-QUALIFY
  23:   the backup option at the consent gate; manual invocation anytime; the
  24:   governing consent rule is never bypassed — the user picks the lane.
  25: - Approach B: dedicated `references/backup-lane.md` owns lane mechanics.
  26: - Hub-and-spoke: reviewers never talk directly; the session mediates and
  27:   verifies before relaying (0.14.0 builds the panel form; the invariant
  28:   binds now).
  29: - Standing process rulings apply: Sol plan check-off before
  30:   implementation, mode-diff debate gates merge, attestation on final tip.
  31: 
  32: ## 3. Lane semantics and recording
  33: 
  34: Inside a debate the backup lane is a full peer: same debate protocol
  35: (strike rule, verdict grammar, 4-round cap, session adjudication), same
  36: XML-tag brief conventions, same anti-sycophancy rules.
  37: 
  38: Recording (codifies the 0.12.0 precedent, resolving the format gap both
  39: advisors examined): a backup-lane debate is **Verification status:
  40: FULL** — cross-vendor independence is preserved (Moonshot is not
  41: Anthropic). `frozen-plan-format.md` gains a pinned **lane-substitution
  42: combination**: `Verification status: FULL` MAY carry `Degradation:
  43: <primary-lane failure class>` + `Authorized by: user at round N` when a
  44: backup cross-vendor lane substituted for the primary; the Participants
  45: line names the actual backup participant with its transport and session
  46: id (a kimi-shaped example joins the codex-shaped template). The
  47: Degraded-mode note stays bound to DEGRADED status; lane substitution is
  48: NOT degradation. The single-vendor skeptic remains the only true
  49: DEGRADED mode. This retroactively legitimizes the 0.12.0 record's
  50: field-state combination.
  51: 
  52: Route-note grammar stays normalized and lane-agnostic (advisory
  53: finding): the attestation verifier requires the exact string `effective
  54: route confirmed` (tools/verify-attestation.ps1) and the behavioral
  55: contract expects that wording. "Confirmed" for a given lane means every
  56: round's route evidence matched THAT lane's canonical declarations per
  57: its reference; backup-lane.md defines what confirmation means for kimi
  58: (client-side, attributed — section 6). The evidence class is recorded in
  59: the debate record prose, never by mutating the attestation grammar.
  60: 
  61: ## 4. Transport contract and single-source discipline
  62: 
  63: Probed facts (2026-07-25, kimi-cli 1.49.0; re-probed where section 12
  64: requires): headless dispatch `kimi --quiet --thinking -m <backup-id>
  65: --agent-file <yaml> -w <clone> -p <brief pointer>`; resume
  66: `kimi -r <uuid>` (session id printed every run); route line `Using LLM
  67: model: provider='managed:kimi-code' model='k3-256k'` appended to
  68: `~/.kimi/logs/kimi.log`; bad model fails loud ("LLM not set"); print
  69: mode auto-approves ALL tools and `--plan` does not block writes (the
  70: reason containment is structural); effort has NO CLI flag — pinned via
  71: config `.overrides`, evidence class = config validation (the log carries
  72: no effort field); inline `--config` replaces the whole config and is
  73: unusable for dispatch-time pinning.
  74: 
  75: Single-source (both advisors, convergent): the backup model id literal
  76: gets EXACTLY ONE authority — a new, distinctly-labeled declaration block
  77: in `model-prompting-notes.md` (the file already declared THE reviewer
  78: transport single source). Everything else uses placeholders:
  79: backup-lane.md's pinned command carries `<canonical-backup-model-id>`
  80: resolved by reading the notes file at dispatch (mirroring SKILL.md's
  81: `<canonical-model-id>` pattern), the doctor row and drift script carry
  82: no literal (agy precedent), and the consistency sweep's allowed homes
  83: are the notes declaration, the eval test file that pins it, and nothing
  84: else.
  85: 
  86: Label no-collision constraint (both advisors): the primary parse
  87: surfaces are `re.search(r"Canonical model id: \x60([^\x60]+)\x60")` in
  88: run_behavioral_evals.py (case-sensitive) and a case-INSENSITIVE
  89: PowerShell `-match` in check-drift.ps1. The backup labels (`Canonical
  90: backup reviewer model id:` / `Canonical backup thinking flag:`) must
  91: never contain the substring `Canonical model id:` under case-insensitive
  92: comparison, and the primary declarations must PRECEDE the backup block
  93: in the file. A regression test pins: both runtime surfaces still resolve
  94: the PRIMARY id from the amended file; the backup label matches neither
  95: primary regex case-insensitively; ordering holds.
  96: 
  97: ## 5. Containment
  98: 
  99: Committed artifacts (inventory, complete): 
 100: - `references/kimi-reviewer-agent.yaml` — tools allowlist exactly
 101:   SetTodoList, ReadFile, ReadMediaFile, Glob, Grep (no write, shell, or
 102:   web tool entries).
 103: - `references/kimi-reviewer-system.md` — SELF-AUTHORED system prompt
 104:   (no copy of kimi-cli's internal file; no drift coupling). Plan-time
 105:   probe 12.1 confirms the self-authored variant behaves; failure branch:
 106:   fall back to the probed copy-at-invocation form, and if that also
 107:   fails the lane is BROKEN and this design returns for revision.
 108: 
 109: Write-probe (falsifiable oracle, advisory finding): before round 1 of
 110: every backup-lane debate, in a FRESH disposable session with the exact
 111: debate configuration (same agent-file, model, flags, workspace), ask the
 112: contained agent to create a named marker file. PASS requires ALL of:
 113: explicit refusal in the reply, marker absent on disk, clone status delta
 114: empty. Anything else = lane BROKEN (integrity class, section 7) — never
 115: dispatch a review over it.
 116: 
 117: Clone isolation: reviews run in a throwaway clone of the repo (never the
 118: real tree), brief delivered as the untracked `KIMI-REVIEW-BRIEF.md`.
 119: Post-round check (fixing the internal contradiction the advisory
 120: caught): `git status --porcelain` must list EXACTLY the brief file and
 121: nothing else; any other delta quarantines that round's reply (integrity
 122: class). The brief is retained as evidence per the raw-rounds convention.
 123: 
 124: ## 6. Route evidence and per-round attribution
 125: 
 126: `~/.kimi/logs/kimi.log` is a shared, user-global append stream — a bare
 127: "the line appears in the log" check cannot attribute the line to a
 128: specific dispatch (both advisors, convergent). The lane's rule:
 129: 
 130: - Before EVERY dispatch (fresh or resumed): capture the log's current
 131:   length (byte offset).
 132: - After the call: exactly ONE new `Using LLM model:` line carrying the
 133:   canonical backup id must appear past the captured offset. Zero
 134:   matching new lines, more than one, or a wrong id = route-attribution
 135:   failure — the reply is DISCARDED unread (same rule as codex
 136:   route-mismatch), fail closed.
 137: - This check runs on EVERY round, resumes included.
 138: 
 139: Resume (PROBED 2026-07-25 — the codex sandbox lesson reproduced on this
 140: transport): a bare `kimi -r <uuid>` DROPS the agent file and loads the
 141: DEFAULT agent with the full tool set (Shell, WriteFile, StrReplaceFile,
 142: web tools all live) while the route line still reads clean — the probe's
 143: refusal came from conversation priming alone, with write tools restored
 144: underneath. The codified resume form therefore re-pins `--agent-file`,
 145: `-m`, and `--thinking` on every resumed call (probed working: the
 146: re-pinned resume loaded the committed yaml and exactly the five-tool
 147: allowlist); model/thinking inheritance comes from config defaults, not
 148: the session, so the re-pin is load-bearing whenever the backup id ever
 149: diverges from the config default.
 150: 
 151: Mechanical containment evidence (discovered by the same probe): kimi.log
 152: also appends per-invocation `Loading agent: <path>` and
 153: `Loaded tools: [<list>]` lines. The per-round check asserts, past the
 154: captured offset, all THREE: exactly one route line with the canonical
 155: backup id, a `Loading agent:` line naming the committed yaml, and a
 156: `Loaded tools:` line equal to the allowlist exactly (no write, shell, or
 157: web tool entries). This closes the config-vs-priming ambiguity
 158: mechanically on every round, fresh and resumed alike; the write-probe
 159: (section 5) remains as the behavioral belt.
 160: 
 161: ## 7. Failure classes and the consent gate
 162: 
 163: fallbacks.md remains the failure-class namespace (single home —
 164: advisory finding); backup-lane.md points at it and defines nothing
 165: class-shaped of its own. New content in fallbacks.md:
 166: 
 167: - The consent-gate banner gains the backup-lane option: `[run backup
 168:   lane (cross-vendor preserved)]` — offered automatically when the
 169:   failing class is `quota-exhausted` or any transport-broken class;
 170:   available on request otherwise. When offered, the banner's `What it
 171:   would NOT verify:` line names the backup lane's known evidence gap:
 172:   reviewer reasoning effort (config-validation only — no per-call
 173:   evidence).
 174: - `transport-broken` mapping (advisory finding — operational, not
 175:   vibes): the classes that auto-qualify the backup option are
 176:   codex-missing, model-rejected, auth-expired, and a route-mismatch or
 177:   missing-rollout that survives its gate. `quota-exhausted` qualifies by
 178:   the user's standing ruling.
 179: - Backup-lane failure table (detection → retry → disposition):
 180:   kimi-missing (`kimi --version` fails; no retry; gate), kimi-bad-model

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'docs\\superpowers\\specs\\2026-07-25-kimi-backup-lane-design.md'; "'$i=0; Get-Content -LiteralPath $p | ForEach-Object { $i++; if ($i -ge 181 -and $i -le 328) { '"'{0,4}: {1}' -f "'$i,$_ } }' in C:\Users\Brandon\Documents\parallax
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'docs\\superpowers\\plans\\2026-07-25-kimi-backup-lane.md'; "'$i=0; Get-Content -LiteralPath $p | ForEach-Object { $i++; if ($i -ge 361 -and $i -le 540) { '"'{0,4}: {1}' -f "'$i,$_ } }' in C:\Users\Brandon\Documents\parallax
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'docs\\superpowers\\plans\\2026-07-25-kimi-backup-lane.md'; "'$i=0; Get-Content -LiteralPath $p | ForEach-Object { $i++; if ($i -ge 541 -and $i -le 726) { '"'{0,4}: {1}' -f "'$i,$_ } }' in C:\Users\Brandon\Documents\parallax
 succeeded in 640ms:
 361: 
 362: ---
 363: 
 364: ### Task 4: Declarations, fallbacks, and record format
 365: 
 366: **Files:**
 367: - Modify: `skills/multi-model-verify/references/model-prompting-notes.md` (append new section at end of file)
 368: - Modify: `skills/multi-model-verify/references/fallbacks.md` (banner options line; new section before "## Degraded-mode output requirements")
 369: - Modify: `skills/multi-model-verify/references/frozen-plan-format.md` (after the Degraded-mode note explanation)
 370: - Test: `evals/multi-model-verify/test_backup_lane.py` (notes declarations, fallbacks wiring, plan-format pin) + full suite
 371: 
 372: **Interfaces:**
 373: - Consumes: backup-lane.md (Task 3) as the pointer target.
 374: - Produces: the declarations both runtime parsers and the doctor row read; the consent-gate wiring SKILL.md references.
 375: 
 376: - [ ] **Step 1: Append the backup declarations to model-prompting-notes.md**
 377: 
 378: Append at the END of the file (after the "## Reusable recipes" section) EXACTLY:
 379: 
 380: ```markdown
 381: 
 382: ## The backup reviewer lane (currently Kimi K3 via kimi-cli)
 383: 
 384: THE single source for the BACKUP reviewer's identity — the same
 385: swap-by-one-edit rule as the primary declarations above, and the same
 386: consistency-test enforcement (the backup literal is forbidden
 387: everywhere else; command surfaces carry `<canonical-backup-model-id>`).
 388: The primary declarations above MUST stay ahead of this block: both
 389: runtime parsers match the first `Canonical model id:` occurrence, and
 390: the drift script's PowerShell match is case-insensitive.
 391: 
 392: Canonical backup reviewer model id: `kimi-code/k3-256k`
 393: 
 394: Canonical backup thinking flag: `--thinking`
 395: 
 396: Everything else about the lane — transport, containment, per-round
 397: evidence, clone isolation, failure routing — lives in
 398: references/backup-lane.md. The lane enters only through the
 399: fallbacks.md consent gate.
 400: ```
 401: 
 402: - [ ] **Step 2: Add the banner option in fallbacks.md**
 403: 
 404: In the consent-gate banner fenced block, replace the line:
 405: 
 406: ```text
 407: Options: [fix codex] [run degraded] [abort]
 408: ```
 409: 
 410: with:
 411: 
 412: ```text
 413: Options: [fix codex] [run backup lane (cross-vendor preserved)] [run degraded] [abort]
 414: ```
 415: 
 416: - [ ] **Step 3: Insert the backup-lane section in fallbacks.md**
 417: 
 418: Insert a new section immediately BEFORE the `## Degraded-mode output requirements (after consent)` heading, EXACTLY:
 419: 
 420: ```markdown
 421: ## Backup reviewer lane (cross-vendor substitution)
 422: 
 423: A SECOND cross-vendor reviewer (transport, containment, and per-round
 424: evidence: references/backup-lane.md; identity declared in
 425: model-prompting-notes.md) can substitute for the primary WITHOUT
 426: reducing vendor diversity — so a substituted debate stays
 427: `Verification status: FULL`, recorded per frozen-plan-format.md's lane
 428: substitution note. Substitution is still a consented transition: the
 429: gate offers it, the user picks it, and `Authorized by:` records the
 430: choice.
 431: 
 432: - Auto-qualified (the gate OFFERS the backup option) for:
 433:   `quota-exhausted` (standing user ruling), codex-missing,
 434:   model-rejected, auth-expired, and a route-mismatch or
 435:   missing-rollout that survives its own gate. Available on user
 436:   request for anything else.
 437: - When the backup option is offered, the banner's
 438:   `What it would NOT verify:` line names the backup lane's known
 439:   evidence gap: reviewer reasoning effort (config-validation only —
 440:   no per-call evidence).
 441: - Backup-lane failures (detection → retry → disposition):
 442:   - kimi-missing: `kimi --version` fails → no retry, consent gate.
 443:   - kimi-bad-model: `LLM not set` (exit 1, loud) → no retry, consent
 444:     gate.
 445:   - route-attribution failure (offset rule in backup-lane.md): nothing
 446:     transient → no retry, reply DISCARDED unread, consent gate.
 447:   - resume failure: one same-parameters retry, then the consent gate
 448:     with the fresh-per-round option (full brief re-sent each round).
 449:   - integrity failure (write-probe fail, or a clone delta beyond the
 450:     brief): no retry, reply quarantined, consent gate.
 451:   - catch-all: one same-parameters retry, then the consent gate —
 452:     mirrors the primary catch-all.
 453: - BOTH lanes down: the honest choices are wait for a reset, the
 454:   single-vendor DEGRADED skeptic, or abort — never a silent third
 455:   vendor.
 456: ```
 457: 
 458: - [ ] **Step 4: Pin lane substitution in frozen-plan-format.md**
 459: 
 460: Immediately AFTER the fenced template block's closing fence and its explanatory paragraph ("The three status fields are structured on purpose..."), append EXACTLY this paragraph as its own block:
 461: 
 462: ```markdown
 463: Lane substitution (backup reviewer): `Verification status: FULL` MAY
 464: carry a `Degradation:` class plus `Authorized by: user at round N` when
 465: a backup cross-vendor lane substituted for the primary — lane substitution is NOT degradation; the Degradation field then names the
 466: PRIMARY-lane failure that triggered substitution, and the Participants
 467: line names the actual backup participant, e.g.
 468: `Kimi K3 (kimi-cli, session <id>)`. This codifies the combination first
 469: used by the 0.12.0 record. The Degraded-mode note stays bound to
 470: DEGRADED status; a debate in which a backup cross-vendor lane substituted for the primary is recorded with this combination, never as DEGRADED.
 471: ```
 472: 
 473: - [ ] **Step 5: Run the tests**
 474: 
 475: Run: `python -m pytest evals/multi-model-verify/test_backup_lane.py evals/multi-model-verify/test_multi_model_verify.py -q`
 476: Expected: notes/fallbacks/plan-format pins PASS; the pre-existing consent-banner and catch-all pins in test_multi_model_verify.py stay green (the banner test regex is an alternation; verified against the amended line during design review). SKILL/README pin still FAILS (Task 5).
 477: 
 478: - [ ] **Step 6: Commit**
 479: 
 480: ```bash
 481: git add skills/multi-model-verify/references/model-prompting-notes.md skills/multi-model-verify/references/fallbacks.md skills/multi-model-verify/references/frozen-plan-format.md
 482: git commit -m "0.13.0: wire backup lane into declarations, fallbacks, record format"
 483: ```
 484: 
 485: ---
 486: 
 487: ### Task 5: SKILL.md and README routing
 488: 
 489: **Files:**
 490: - Modify: `skills/multi-model-verify/SKILL.md` (Overview sentence; Preflight item 1; finish-line note)
 491: - Modify: `README.md` (consent-gate flowchart; box list)
 492: - Test: `evals/multi-model-verify/test_backup_lane.py::test_skill_and_readme_route_the_lane` + full suite + skill lint
 493: 
 494: **Interfaces:**
 495: - Consumes: backup-lane.md and the fallbacks wiring (Tasks 3-4).
 496: - Produces: the routed lane a session actually follows.
 497: 
 498: - [ ] **Step 1: SKILL.md Overview**
 499: 
 500: In the Overview paragraph, replace the sentence fragment:
 501: 
 502: ```text
 503: Two equal-weight advisors — this session and a cross-vendor reviewer driven
 504: through the codex CLI (canonical reviewer model:
 505: references/model-prompting-notes.md) — verify and refute each other's
 506: claims before the cheap implementer touches code.
 507: ```
 508: 
 509: with:
 510: 
 511: ```text
 512: Two equal-weight advisors — this session and a cross-vendor reviewer driven
 513: through the codex CLI (canonical reviewer model:
 514: references/model-prompting-notes.md) — verify and refute each other's
 515: claims before the cheap implementer touches code. A second cross-vendor
 516: BACKUP reviewer lane (references/backup-lane.md — REQUIRED READING
 517: before any backup round) substitutes through the fallbacks.md consent
 518: gate when the primary transport is down; same protocol, different
 519: transport, `Verification status: FULL` preserved.
 520: ```
 521: 
 522: - [ ] **Step 2: SKILL.md Preflight item 1**
 523: 
 524: Replace:
 525: 
 526: ```text
 527:    On failure follow references/fallbacks.md
 528:    (degraded mode, visibly flagged; never silently skip cross-vendor review).
 529: ```
 530: 
 531: with:
 532: 
 533: ```text
 534:    On failure follow references/fallbacks.md
 535:    (the consent gate may offer the cross-vendor backup lane —
 536:    references/backup-lane.md — before any single-vendor degraded mode;
 537:    never silently skip cross-vendor review).
 538: ```
 539: 
 540: - [ ] **Step 3: SKILL.md finish line note**

 succeeded in 699ms:
 541: 
 542: In the Finish line section, after the sentence ending "the route note: `effective route confirmed` when every round's header matched the canonical declarations, else the transport-failure class that fallbacks.md handled.", append to the same paragraph:
 543: 
 544: ```text
 545: The route-note grammar is lane-agnostic: for a backup-lane debate,
 546: `effective route confirmed` means every round satisfied
 547: references/backup-lane.md's per-round evidence rules against the backup
 548: declarations; the evidence class is recorded in the debate record prose.
 549: ```
 550: 
 551: - [ ] **Step 4: README flowchart and box list**
 552: 
 553: In the mermaid consent-gate flowchart, after the line `G -->|fix codex| OK`, insert:
 554: 
 555: ```text
 556:     G -->|run backup lane| BK["cross-vendor backup reviewer<br/>(FULL, lane substitution recorded)"] --> OK
 557: ```
 558: 
 559: In the "What's in the box" list, after the row/line describing `references/fallbacks.md`, add a line matching the list's existing style:
 560: 
 561: ```text
 562: `references/backup-lane.md` — the cross-vendor backup reviewer lane (consent-gated substitution when codex is down; the gate's "run backup lane" option)
 563: ```
 564: 
 565: - [ ] **Step 5: Run tests and lint**
 566: 
 567: Run: `python -m pytest evals -q` — Expected: ALL tests pass now (the full new-pin set green, no regressions).
 568: Run: `python evals/tools/skill_lint.py skills/multi-model-verify --strict` — Expected: PASS 0/0 (backup-lane.md referenced and existing).
 569: Run: `python evals/tools/skill_scanner.py skills` — Expected: clean.
 570: Run: `python evals/tools/run_trigger_evals.py` — Expected: all clear.
 571: 
 572: - [ ] **Step 6: Commit**
 573: 
 574: ```bash
 575: git add skills/multi-model-verify/SKILL.md README.md
 576: git commit -m "0.13.0: route backup lane in skill overview, preflight, finish line, readme"
 577: ```
 578: 
 579: ---
 580: 
 581: ### Task 6: Doctor row and drift coverage
 582: 
 583: **Files:**
 584: - Modify: `commands/doctor.md` (new check 8 after check 7)
 585: - Modify: `tools/check-drift.ps1` (kimi version probe + snapshot field, flag-surface probe, vocabulary probe — carry-forward style, agy precedent)
 586: - Modify: `evals/tools/drift_statemachine_tests.ps1` stub CLIs/assertions as needed for the new probes
 587: - Test: full pytest suite + the offline state-machine suite (CONTROLLER-run)
 588: 
 589: **Interfaces:**
 590: - Consumes: the notes declarations (Task 4) — both surfaces PARSE, never hardcode, the backup id.
 591: - Produces: operational watch for the kimi transport.
 592: 
 593: - [ ] **Step 1: Doctor check 8**
 594: 
 595: Append after check 7 in `commands/doctor.md`, matching the numbered-check format of the file, a check with EXACTLY this content:
 596: 
 597: ```markdown
 598: 8. **Backup reviewer transport (kimi).** Run `kimi --version`. Parse the
 599:    backup id from the line `Canonical backup reviewer model id:` in
 600:    skills/multi-model-verify/references/model-prompting-notes.md (this
 601:    check carries no model literal). Verify both containment artifacts
 602:    exist: references/kimi-reviewer-agent.yaml and
 603:    references/kimi-reviewer-system.md. Report the version, the parsed
 604:    id, and the artifact presence. Any failure is reported as
 605:    BACKUP-LANE UNAVAILABLE (the primary lane is unaffected) — the fix
 606:    pointer is references/backup-lane.md.
 607: ```
 608: 
 609: - [ ] **Step 2: Drift script — version snapshot (carry-forward)**
 610: 
 611: Mirror the agy field exactly: add a `$kimiVersion` probe beside the agy probe (`kimi --version`, tolerate absence), a `$kimiVersionToSave` carry-forward inside the existing `if ($snapshot)` block, and `kimi = $kimiVersionToSave` in `$newSnapshot` — same pattern, same failure tolerance as the agy field. No inline `if` in the hashtable (PS 5.1 hazard).
 612: 
 613: - [ ] **Step 3: Drift script — flag-surface and vocabulary probes**
 614: 
 615: Beside the codex flag probe, add a kimi probe block: capture `kimi --help` once; require each of `--quiet`, `--thinking`, `-m`, `--agent-file`, `-w`, `-p`, `-r` to appear in the help text; then run `python -c "import kimi_cli.tools.file, kimi_cli.tools.todo"` and require exit 0. Each miss appends a drift finding naming the missing flag or module (finding style copied from the codex flag probe's findings). Both probes are skipped with a single carried-forward note when `kimi --version` failed (no cascade).
 616: 
 617: - [ ] **Step 4: State-machine suite stubs**
 618: 
 619: Extend the stub CLIs in `evals/tools/drift_statemachine_tests.ps1` so the existing scenarios still drive the script end-to-end offline: a stub `kimi` answering `--version` and `--help` (help text containing all seven flags) and a stub `python` import success where the harness fakes binaries. Add one scenario assertion: a help text MISSING `--agent-file` produces a drift finding naming it.
 620: 
 621: - [ ] **Step 5: Run the suites (CONTROLLER-run)**
 622: 
 623: Run: `python -m pytest evals -q` — Expected: all green.
 624: Controller runs: `evals/tools/drift_statemachine_tests.ps1` — Expected: ALL SCENARIOS PASS including the new assertion. This suite is run by the CONTROLLER, never inside the implementer subagent (0.10.0 lesson).
 625: 
 626: - [ ] **Step 6: Commit**
 627: 
 628: ```bash
 629: git add commands/doctor.md tools/check-drift.ps1 evals/tools/drift_statemachine_tests.ps1
 630: git commit -m "0.13.0: doctor kimi row and drift flag, vocabulary, version coverage"
 631: ```
 632: 
 633: ---
 634: 
 635: ### Task 7: Behavioral eval case
 636: 
 637: **Files:**
 638: - Modify: `evals/multi-model-verify/evals.json` (one new case)
 639: - Test: `python evals/tools/run_behavioral_evals.py --list` (case parses; manual cases are runner-skipped)
 640: 
 641: **Interfaces:**
 642: - Consumes: the shipped fallbacks/backup-lane text (Tasks 3-4).
 643: - Produces: the versioned behavioral expectation for backup routing.
 644: 
 645: - [ ] **Step 1: Add the case**
 646: 
 647: Append to the `evals` array in `evals/multi-model-verify/evals.json` (comma-correct, preserving formatting) EXACTLY:
 648: 
 649: ```json
 650: {
 651:   "id": "backup-lane-consented-substitution",
 652:   "setup": { "manual": true, "no_codex": true, "with_reference": true },
 653:   "surface": ["skills/multi-model-verify/SKILL.md", "skills/multi-model-verify/references/fallbacks.md", "skills/multi-model-verify/references/backup-lane.md"],
 654:   "prompt": "Plan the port of References/DemoWidget into a module for this project. Use multi-model verification before freezing the plan. (Environment: codex CLI is not installed; kimi-cli is installed and authenticated; the user has already answered the consent gate: 'run backup lane'.)",
 655:   "expected_output": "The debate runs over the BACKUP lane per references/backup-lane.md: contained dispatch with the committed agent yaml, per-round offset-attributed route AND containment evidence from kimi.log, and a debate record showing Verification status FULL with the lane substitution and the user authorization recorded.",
 656:   "expectations": [
 657:     "The codex preflight failure is reported and the recorded consent ('run backup lane') is quoted - substitution is never inferred",
 658:     "Every kimi dispatch uses the committed kimi-reviewer-agent.yaml via --agent-file, and resumed calls re-pin --agent-file, -m, and --thinking (a bare kimi -r is a violation)",
 659:     "Per-round evidence follows backup-lane.md: pre-dispatch log offset captured; exactly one new Using LLM model: line with the canonical backup id, a Loading agent: line naming the committed yaml, and a Loaded tools: line equal to the allowlist - all past the offset",
 660:     "The write-probe precedes round 1 (refusal + absent marker + clean delta) and the review runs in a throwaway clone whose post-round git status lists exactly KIMI-REVIEW-BRIEF.md",
 661:     "The debate record shows Verification status FULL with Degradation naming the primary-lane failure class and Authorized by recording the user - never DEGRADED"
 662:   ]
 663: }
 664: ```
 665: 
 666: - [ ] **Step 2: Verify the runner parses it**
 667: 
 668: Run: `python evals/tools/run_behavioral_evals.py --list`
 669: Expected: the new case listed (marked manual/skipped); no parse errors.
 670: 
 671: - [ ] **Step 3: Run the full offline suite**
 672: 
 673: Run: `python -m pytest evals -q` — Expected: all green (the evals.json sweep pins tolerate the new case; the backup literal does not appear in it — the expectations say "canonical backup id" by name).
 674: 
 675: - [ ] **Step 4: Commit**
 676: 
 677: ```bash
 678: git add evals/multi-model-verify/evals.json
 679: git commit -m "0.13.0: behavioral case for consented backup substitution"
 680: ```
 681: 
 682: ---
 683: 
 684: ### Task 8: Version bump, dev loop, live verification (ATTENDED; runs only after the plan debate converges)
 685: 
 686: **Files:**
 687: - Modify: `.claude-plugin/plugin.json` (version 0.13.0)
 688: - None else in-repo (scratch clone + probe records; SDD ledger notes)
 689: 
 690: **Interfaces:**
 691: - Consumes: the installed 0.13.0 cache (dev loop: bump, `claude plugin update parallax@parallax`, restart — skills/ changed, so the restart is REQUIRED before any live step tests anything).
 692: - Produces: the live evidence the diff debate's brief cites.
 693: 
 694: - [ ] **Step 1: Bump and commit BEFORE the dev loop**
 695: 
 696: Set `.claude-plugin/plugin.json` version to `0.13.0`, commit (`0.13.0: bump plugin version`), verify `git status --porcelain` is empty.
 697: 
 698: - [ ] **Step 2 (ATTENDED - user): Dev loop**
 699: 
 700: User runs `claude plugin update parallax@parallax` and restarts the session. Without the restart the installed skill text is the cached pre-0.13.0 set — a live run before restart tests nothing (0.12.0 lesson, twice-proven: verify by evidence content, not report status).
 701: 
 702: - [ ] **Step 3: Behavioral evals for the touched surfaces (CONTROLLER-run, billable — budget note)**
 703: 
 704: Run: `python evals/tools/run_behavioral_evals.py --changed`
 705: Expected selection: the cases whose surfaces intersect this branch (SKILL.md, model-prompting-notes.md, fallbacks.md changed → at least plan-mode-debate-runs, diff-mode-spec-fidelity, degraded-consent-gate, no-manufactured-objections; the new manual case prints as SKIPPED). These are REAL headless runs graded over the primary reviewer lane — confirm codex quota headroom first (doctor check 4b); if quota blocks, record the deferral in the ledger and surface it in the diff-debate brief rather than skipping silently.
 706: 
 707: - [ ] **Step 4: Live backup-lane verification through the CODIFIED machinery**
 708: 
 709: All commands built by reading the INSTALLED skill text (cache), resolving `<canonical-backup-model-id>` from the installed notes file:
 710: 1. Fresh throwaway clone of the repo in the session scratchpad; write a scratch review brief as `KIMI-REVIEW-BRIEF.md` (any small real review target, e.g. "review agents/flash-implementer.md's report format section for internal consistency; cite lines").
 711: 2. WRITE-PROBE per backup-lane.md (fresh disposable session, exact debate configuration, marker request): expect refusal + absent marker + clean delta.
 712: 3. Round 1 dispatch per the pinned command; per-round evidence checks (offset, route line, Loading agent, Loaded tools) — all three past the offset.
 713: 4. One resumed exchange with the re-pinned resume form; the same per-round checks repeat and pass.
 714: 5. Post-round clone check: exactly `KIMI-REVIEW-BRIEF.md`, nothing else.
 715: 6. Negative probe: run the round-1 command with `-m kimi-code/nonexistent-model` in the clone — expect `LLM not set`, exit 1, no reply file consumed.
 716: 7. Record all outcomes with log offsets and session ids in the SDD ledger.
 717: 
 718: - [ ] **Step 5: Record**
 719: 
 720: Write the dev-loop, behavioral-run, and live-verification outcomes into the SDD ledger (`.superpowers/sdd/2026-07-25-kimi-backup-lane/`) — the diff-debate brief cites them.
 721: 
 722: ---
 723: 
 724: ## Debate record
 725: 
 726: (to be completed at freeze: dual plan debate — Sol primary check-off + Kimi second lane, both user-directed for this cycle — per frozen-plan-format.md)

 succeeded in 708ms:
 181:   ("LLM not set", loud; no retry; gate), route-attribution failure
 182:   (section 6; no retry — nothing transient; reply discarded; gate),
 183:   resume failure (one retry, then gate with the fresh-per-round option),
 184:   integrity failure (write-probe fail or clone delta beyond the brief;
 185:   no retry; reply quarantined; gate), catch-all (one same-parameters
 186:   retry, then gate — mirrors the primary catch-all). Both lanes down =
 187:   the honest choices: wait for reset, single-vendor DEGRADED skeptic, or
 188:   abort — never a silent third vendor.
 189: 
 190: ## 8. SKILL.md and README wiring
 191: 
 192: SKILL.md (advisory finding — the lane must be ROUTED, not just
 193: documented): the Overview names the backup lane's existence; a
 194: lane-selection paragraph states the primary is default, the backup
 195: enters only through the fallbacks.md consent gate (auto-qualified or
 196: manual), and backup-lane.md is REQUIRED READING before a backup round;
 197: mode plan/diff dispatch steps gain a one-line pointer ("backup lane:
 198: transport per references/backup-lane.md, same protocol"); the finish
 199: line's route-note grammar is explicitly lane-agnostic (section 3).
 200: README: the consent-gate flowchart gains the backup-lane option beside
 201: the three existing exits, plus a one-sentence lane description in the
 202: What's-in-the-box list.
 203: 
 204: ## 9. Doctor and drift
 205: 
 206: Doctor: new check row for the backup transport — `kimi --version`, the
 207: notes-file backup declaration parse, and presence of the two committed
 208: containment artifacts. Non-billable only; exact probe shape from probed
 209: commands (no speculative flags).
 210: 
 211: Drift (advisory finding — a version snapshot alone is toothless for a
 212: reviewer transport): kimi version snapshot field in carry-forward style
 213: (agy precedent) PLUS a non-billable flag-surface probe — `kimi --help`
 214: grepped for the six load-bearing flags (`--quiet`, `--thinking`, `-m`,
 215: `--agent-file`, `-w`, `-p`) and `-r` — run alongside the codex flag
 216: probe. The containment-vocabulary probe is FEASIBLE (probe 12.3:
 217: kimi-cli is plain-pip installed; the yaml's tool module paths import
 218: non-billably) and joins the drift probe set.
 219: 
 220: ## 10. Testing
 221: 
 222: Offline pins (CI, no CLI calls): agent.yaml allowlist exact-match plus
 223: absence of any write/shell/web tool entry; backup-lane.md exact-sentence
 224: pins (route-attribution rule, exactly-the-brief clone rule, write-probe
 225: oracle, resume form); fallbacks banner option + transport-broken mapping
 226: pins; frozen-plan-format lane-substitution pin; the notes parser
 227: regression test (section 4); kimi-literal single-source sweep; the new
 228: reference files join the structural pins (REQUIRED_REFERENCE_FILES,
 229: no-backslash rule — pinned paths are forward-slash).
 230: 
 231: Behavioral (local, opt-in): one new evals.json case declaring surface
 232: `fallbacks.md` + `backup-lane.md` — consented backup selection routes to
 233: the kimi transport contract (graded like existing cases). Drift-script
 234: changes carry the standing requirement: the offline state-machine suite
 235: runs with kimi-probe scenarios added to its stub CLIs.
 236: 
 237: ## 11. Live verification (attended task; 0.12.0's lesson applied)
 238: 
 239: Through the CODIFIED machinery at head — the installed skill text, the
 240: committed yaml + system prompt, the placeholder-resolved command — not
 241: from memory: fresh throwaway clone; write-probe with the section-5
 242: oracle; one real review round on a scratch brief with offset-attributed
 243: route evidence; one resumed (or fresh-per-round, per probe 12.2)
 244: continuation exchange; post-round exactly-the-brief check. Evidence in
 245: the SDD ledger; the diff-debate brief cites it.
 246: 
 247: ## 12. Plan-time probes — ALL RESOLVED (2026-07-25, kimi-cli 1.49.0)
 248: 
 249: 1. Self-authored system prompt under --agent-file: PASS — plain markdown
 250:    file, no template args needed; reviewer behaved, write-probe oracle
 251:    validated in the same dispatch (explicit refusal + marker absent +
 252:    clean delta).
 253: 2. Resume flag inheritance: FAIL-AS-FEARED, then contained — bare
 254:    `kimi -r` loaded the DEFAULT agent (full tools incl. WriteFile and
 255:    Shell; log evidence `Loading agent: ...agents/default/agent.yaml` +
 256:    `Loaded tools: [...]`) while the behavioral refusal masked it. The
 257:    re-pinned resume (`kimi --quiet -r <uuid> --agent-file <yaml>`)
 258:    loaded the committed yaml and exactly the five-tool allowlist.
 259:    Outcome: re-pinned resume form codified (section 6) + the mechanical
 260:    Loading-agent/Loaded-tools per-round assertions adopted.
 261: 3. Containment-vocabulary probe: FEASIBLE — kimi-cli is plain-pip
 262:    installed; `python -c "import kimi_cli.tools.file, kimi_cli.tools.todo"`
 263:    succeeds non-billably (drift can carry it).
 264: 4. Notes parser regression: PASS — amended notes text (backup block
 265:    after the primary declarations) leaves both runtime parsers resolving
 266:    the primary `gpt-5.6-sol`; backup label matches neither primary regex
 267:    case-insensitively; ordering holds.
 268: 5. Bad-model signature: PASS — `kimi --quiet -m kimi-code/nonexistent-model`
 269:    prints `LLM not set`, exit 1 (loud; same signature class as the
 270:    probed missing-managed-config case).
 271: 
 272: Probe artifacts: session scratchpad probe-0130/ (probe121-out.txt,
 273: probe122a-out.txt, probe122b-out.txt, probe125-out.txt, amended-notes.md,
 274: agent/agent-self.yaml, agent/system-self.md); probe sessions
 275: f8889815-b957-4456-99d2-95d7304aef3d (12.1/12.2 chain) and
 276: 53ba9c17-74a5-4fc5-81b0-04b3180e7651 (12.5); route/agent/tool log lines
 277: in ~/.kimi/logs/kimi.log, offset-attributed per dispatch.
 278: 
 279: ## 13. Trimmed and out of scope
 280: 
 281: - `kimi export`: NOT part of any workflow step (advisory scope
 282:   challenge). backup-lane.md keeps one caution sentence — export writes
 283:   a session zip into CWD, so never run it inside a repo — because the
 284:   operator-error class is real; nothing depends on export.
 285: - Panels, Opus driver switch, fable-reviewer, escalation implementer:
 286:   0.14.0.
 287: - No new implementer machinery: this cycle touches the REVIEWER side
 288:   only.
 289: 
 290: ## 14. Review provenance
 291: 
 292: Dual cross-vendor advisory pass on the design draft (2026-07-25,
 293: pre-spec; hub-and-spoke, blind — neither advisor saw the other's
 294: review):
 295: - Kimi K3 (`kimi-code/k3-256k`, kimi-cli 1.49.0, contained read-only
 296:   agent against a throwaway clone at main/498d72f, session
 297:   849d24f7-e171-43b6-bda4-5027488ddd2e, route line verified): FIX, 10
 298:   findings — incl. the resume flag-inheritance probe, the
 299:   Degradation-on-FULL format gap, label no-collision precision, and a
 300:   firsthand corroboration of the containment allowlist from inside the
 301:   contained session.
 302: - GPT-5.6 Sol (codex exec read-only, session
 303:   019f9c5f-a234-72c0-a1fe-cd29e6d9065d, effective route confirmed):
 304:   FIX, 11 findings — incl. SKILL.md routing, the attestation
 305:   route-note grammar constraint, the clone-clean/brief contradiction,
 306:   the write-probe oracle, and the export scope trim.
 307: - Convergent findings (single-source, log attribution, SKILL wiring,
 308:   failure-class home, drift depth) adopted in their strongest form; the
 309:   one direct disagreement (format gap: flag vs precedent) dissolved —
 310:   both true, resolved by pinning the combination (section 3). All
 311:   session-verified against the repo before folding; raw replies retained
 312:   under the plans rounds dir at plan freeze.
 313: - NOT part of the cross-vendor gate: the Sol plan check-off and the
 314:   mode-diff debate remain ahead per standing rulings.
 315: 
 316: ## 15. Risks and residuals
 317: 
 318: - Server-side model substitution is undetectable for kimi exactly as for
 319:   codex and agy — the route evidence is client-side; language discipline
 320:   applies (named residual, accepted).
 321: - Reviewer effort has NO per-call evidence (config-validation only) —
 322:   named in the consent banner (section 7).
 323: - kimi-cli is pip-installed and updates under its own schedule; drift
 324:   coverage (section 9) is the watch, and the plan re-probes before
 325:   shipping (section 12).
 326: - The backup lane shares the user's Kimi account quota; no headroom
 327:   probe exists this cycle (0.12.0-style quota probing for kimi is
 328:   future work if the lane sees regular use).

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$paths=@('"'skills\\multi-model-verify\\references\\model-prompting-notes.md','skills\\multi-model-verify\\references\\fallbacks.md','skills\\multi-model-verify\\references\\frozen-plan-format.md','skills\\multi-model-verify\\SKILL.md','README.md','commands\\doctor.md','tools\\check-drift.ps1','evals\\tools\\drift_statemachine_tests.ps1','evals\\multi-model-verify\\test_multi_model_verify.py','evals\\multi-model-verify\\evals.json','docs\\superpowers\\plans\\2026-07-25-flash-implementer.md','docs\\superpowers\\specs\\2026-07-25-flash-implementer-design.md','.claude-plugin\\plugin.json'); foreach("'$p in $paths){$l=Get-Content -LiteralPath $p; '"'{0}"'`t{1}`t{2}'"' -f "'$p,$l.Count,(Get-Item -LiteralPath $p).Length}' in C:\Users\Brandon\Documents\parallax
 succeeded in 519ms:
skills\multi-model-verify\references\model-prompting-notes.md`t206`t13149
skills\multi-model-verify\references\fallbacks.md`t160`t8449
skills\multi-model-verify\references\frozen-plan-format.md`t90`t4381
skills\multi-model-verify\SKILL.md`t200`t11697
README.md`t344`t20030
commands\doctor.md`t144`t8041
tools\check-drift.ps1`t705`t40635
evals\tools\drift_statemachine_tests.ps1`t584`t28715
evals\multi-model-verify\test_multi_model_verify.py`t1826`t91486
evals\multi-model-verify\evals.json`t95`t13192
docs\superpowers\plans\2026-07-25-flash-implementer.md`t824`t44576
docs\superpowers\specs\2026-07-25-flash-implementer-design.md`t384`t23331
.claude-plugin\plugin.json`t20`t624

codex
The first traceability pass found two areas that need close contract checking: the plan’s routing edits may not reach both mode dispatch sections, and the “exact allowlist” test may only prove inclusion. I’m now checking those against the live 0.12.0 text and verifying every replacement target byte-for-byte so I don’t turn wording differences into false findings.
2026-07-26T04:14:38.653676Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_reasoning_summaries` at line 86 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$files = @('"'skills/multi-model-verify/SKILL.md','skills/multi-model-verify/references/fallbacks.md','skills/multi-model-verify/references/frozen-plan-format.md','skills/multi-model-verify/references/model-prompting-notes.md','README.md','commands/doctor.md'); "'$patterns = @('"'Two equal-weight advisors','On failure follow','Finish line','effective route confirmed','Options:','Degraded-mode output','three status fields','Canonical model id:','Reusable recipes','G -->\\|fix codex\\| OK','What.s in the box','fallbacks.md','"'^7'"\\.','"'^8'"\\.'); foreach("'$f in $files){ Write-Output ('"'### ' + "'$f); foreach($pat in $patterns){ Select-String -Path $f -Pattern $pat -CaseSensitive | ForEach-Object { '"'{0,4}: {1}' -f "'$_.LineNumber,$_.Line } } }' in C:\Users\Brandon\Documents\parallax
 succeeded in 844ms:
### skills/multi-model-verify/SKILL.md
  10: Two equal-weight advisors — this session and a cross-vendor reviewer driven
  38:    which rides different billing. On failure follow references/fallbacks.md
 156: ## Finish line
 166: powershell -NoProfile -File <plugin-root>/tools/write-attestation.ps1 -RepoRoot <reviewed-repo> -BaseSha <base> -HeadSha <head> -Verdict <PASS|FIX|ESCALATE> -VerificationStatus <FULL|DEGRADED> -RouteNote "<effective route confirmed | the transport-failure class>" -Rounds <n> -Participants "<session-model> (session) / <reviewer-model> (reviewer)" [-CheckpointFile <application-checkpoint-artifact>]
 187: the route note: `effective route confirmed` when every round's header
  38:    which rides different billing. On failure follow references/fallbacks.md
  98:    (fallbacks.md), never a review result — and capture the `session id:`
 105:    transport failure (fallbacks.md), never a review result.
 186: `DEGRADED (<class>, authorized by user at round N)` per fallbacks.md — and
 189: fallbacks.md handled. The session emits this line itself; never delegate it
### skills/multi-model-verify/references/fallbacks.md
  18: Options: [fix codex] [run degraded] [abort]
 118: ## Degraded-mode output requirements (after consent)
### skills/multi-model-verify/references/frozen-plan-format.md
  70: The three status fields are structured on purpose: mode `diff` parses
### skills/multi-model-verify/references/model-prompting-notes.md
  31: Canonical model id: `gpt-5.6-sol`
 199: ## Reusable recipes
  17:   this loop — and the reason degraded mode (fallbacks.md) must run the
  96:   mismatch is a TRANSPORT failure (fallbacks.md consent gate), never a
 149:   missing-rollout in fallbacks.md skips the retry (adopted 0.10.0, debate
 185:   feeds the consent gate in fallbacks.md; the ids live HERE because
 186:   fallbacks.md never names models.
### README.md
  90:     G -->|fix codex| OK
  60: ## What's in the box
  79: (`skills/multi-model-verify/references/fallbacks.md`): **no transition that reduces
### commands/doctor.md
  84: report it as `effective route confirmed`, never "used and confirmed".

2026-07-26T04:14:45.362966Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_reasoning_summaries` at line 86 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'skills\\multi-model-verify\\references\\fallbacks.md'; "'$i=0; Get-Content -LiteralPath $p | ForEach-Object { $i++; if ($i -ge 1 -and $i -le 160) { '"'{0,4}: {1}' -f "'$i,$_ } }' in C:\Users\Brandon\Documents\parallax
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'skills\\multi-model-verify\\references\\frozen-plan-format.md'; "'$i=0; Get-Content -LiteralPath $p | ForEach-Object { $i++; if ($i -ge 1 -and $i -le 90) { '"'{0,4}: {1}' -f "'$i,$_ } }' in C:\Users\Brandon\Documents\parallax
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'skills\\multi-model-verify\\SKILL.md'; "'$i=0; Get-Content -LiteralPath $p | ForEach-Object { $i++; if ($i -ge 1 -and $i -le 200) { '"'{0,4}: {1}' -f "'$i,$_ } }' in C:\Users\Brandon\Documents\parallax
 succeeded in 600ms:
   1: ---
   2: name: multi-model-verify
   3: description: Use when planning a reference port, an API-sensitive module, or any change risky enough to need cross-model verification before implementation, or when an implemented diff must be checked against its plan and reference before merge. Fires automatically alongside superpowers requesting-code-review via the review-companion hook.
   4: ---
   5: 
   6: # Multi-Model Verify
   7: 
   8: ## Overview
   9: 
  10: Two equal-weight advisors — this session and a cross-vendor reviewer driven
  11: through the codex CLI (canonical reviewer model:
  12: references/model-prompting-notes.md) — verify and refute each other's
  13: claims before the cheap implementer touches code. The reviewer lane's
  14: documented fabrication risk (METR; see model-prompting-notes.md) is
  15: mitigated by the debate structure — evidence grounding plus mutual
  16: refutation — not by down-weighting either side.
  17: 
  18: **REQUIRED READING before the first round:** references/debate-protocol.md.
  19: 
  20: Companion to superpowers, not a replacement: it fills the cross-model review
  21: gap superpowers rules out of scope. `/codex:adversarial-review` remains the
  22: human-invoked pre-merge gate; if the codex plugin's stop-review-gate is
  23: toggled on, its stop-time review overlaps mode `diff` — expected, not a bug.
  24: 
  25: ## When to use
  26: 
  27: - **Mode `plan`** — after superpowers brainstorming proposes approaches for a
  28:   port or API-sensitive module, before the implementation plan is written.
  29: - **Mode `diff`** — after implementation, alongside superpowers
  30:   requesting-code-review (the review-companion hook injects this
  31:   automatically with the matching base/head SHAs).
  32: - NOT for plain bugfixes, docs or release chores, GUI tweaks, or lint sweeps.
  33: 
  34: ## Preflight (both modes)
  35: 
  36: 1. `codex --version` must succeed, and `codex login status` must report
  37:    `Logged in using ChatGPT` — exit 0 alone also passes an API-key login,
  38:    which rides different billing. On failure follow references/fallbacks.md
  39:    (degraded mode, visibly flagged; never silently skip cross-vendor review).
  40: 2. For port work: the reference source must exist under `References/`
  41:    (quote paths — some contain spaces, e.g. `References/M+ Timer`). Missing
  42:    → **HARD STOP before any debate round runs**: ask the user for the path
  43:    and go no further — do not run a codex exchange about the absence, do
  44:    not debate from memory, do not start a degraded mode. If the folder
  45:    holds version subdirectories (e.g. a `v1.4/` next to a `v1.1_old/`), use
  46:    the newest unless the user names one, and cite the versioned path:
  47:    `References/<name>/<version>/<file>:<line>`.
  48:    For non-port work there is no reference folder — claims ground in the
  49:    project's own source, specs, or upstream docs instead, same strike rule.
  50: 3. The reviewed repo must carry no AGENTS.md and no `.agents/` entries:
  51:    codex auto-ingests AGENTS.md as instructions, and it advertises
  52:    repo-level `.agents/skills/*/SKILL.md` to the model, which read a
  53:    planted one as its FIRST action (both probed 2026-07-24: the planted
  54:    AGENTS.md controlled the reviewer's reply; the planted skill entered
  55:    its context; see model-prompting-notes.md) — back-channels into the
  56:    auditor that break independence. Enumerate the whole tree in one
  57:    listing — `git ls-files --cached --others '*AGENTS.md' '.agents/*'` —
  58:    which covers tracked, untracked, AND ignored files at any depth
  59:    (`.git` itself is never listed); a root-only or tracked-only check
  60:    misses a nested drop.
  61:    If present: STOP and surface it to the user — never dispatch a review
  62:    over an instruction back-channel. Files above the repo's git root are
  63:    NOT ingested (same probe), and `~/.codex/AGENTS.md` is the user's own
  64:    global instruction file — note it in the debate record if it exists,
  65:    but it is not a stop. Skills from the user's own codex plugin cache
  66:    load the same way — record them in the debate record as a
  67:    non-blocking environment note with the cache path cited, like the
  68:    global AGENTS.md; not a stop and never a finding.
  69: 
  70: ## Mode plan
  71: 
  72: 1. Draft the session position: chosen approach, port-fidelity claims, and the
  73:    API/behavior risk register. Every reference claim cites a
  74:    `References/<name>/<file>:<line>` actually read this session — anchor
  75:    EVERY file with its full path the first time you cite it, manifests and
  76:    secondary files included (shorthand only once that file is anchored);
  77:    every API claim cites authoritative local docs (WoW projects:
  78:    `.wow-api-reference/` or a dated in-game probe result) — never memory.
  79:    The position has no separate artifact — it IS the claims section of the
  80:    step-2 brief.
  81: 2. Compose the reviewer's debate brief per references/model-prompting-notes.md, write
  82:    it to a scratchpad file, then run round 1:
  83: 
  84:    ```powershell
  85:    Get-Content -Raw <brief-file> | codex exec --sandbox read-only -m <canonical-model-id> -c model_reasoning_effort=<canonical-effort> --output-last-message <reply-file> - > <transcript-file> 2>&1
  86:    ```
  87: 
  88:    `<canonical-model-id>` and `<canonical-effort>` are the two declarations
  89:    in references/model-prompting-notes.md — read the literal values from
  90:    there, never from memory (the reviewer swaps by editing that one file,
  91:    and a remembered id silently defeats the swap). Apply that file's env
  92:    hygiene to the invocation.
  93: 
  94:    From `<transcript-file>`: verify the effective route — the header's
  95:    `model:`, `provider:`, and `reasoning effort:` lines against the
  96:    canonical declarations, and the `sandbox:` line reads `read-only`, per
  97:    model-prompting-notes.md; a mismatch is a transport failure
  98:    (fallbacks.md), never a review result — and capture the `session id:`
  99:    line. Read the reviewer's reply from `<reply-file>` — the transcript
 100:    logs every file the reviewer reads and can run tens of KB, with the
 101:    reply buried at the bottom. Every round writes FRESH round-numbered
 102:    `<reply-file>`/`<transcript-file>` paths: after a failed call, a reused
 103:    path serves the previous round's reply and reads exactly like success —
 104:    a reply not freshly written by this round's call is a stale reply, a
 105:    transport failure (fallbacks.md), never a review result.
 106: 3. Later rounds keep the reviewer's state by resuming that session — flags MUST
 107:    precede the resume subcommand (flags after it are a usage error):
 108: 
 109:    ```powershell
 110:    codex exec --sandbox read-only -m <canonical-model-id> -c model_reasoning_effort=<canonical-effort> --output-last-message <reply-file> resume <SESSION_ID> "<rebuttal-brief>" > <transcript-file> 2>&1
 111:    ```
 112: 
 113:    Each resume's header must echo the resumed `session id:` and the same
 114:    effective route — the round-1 check repeated, `sandbox:` included (a
 115:    resume that silently started a fresh session has dropped the reviewer's
 116:    debate state). Sandbox mode has NO continuity across resumes: a resume
 117:    missing `--sandbox read-only` resolves to the config default — on a
 118:    `workspace-write` default the read-only auditor becomes a writing agent
 119:    mid-review (probed 2026-07-24, the write landed; see
 120:    model-prompting-notes.md). The header's `sandbox:` line is where that
 121:    surfaces.
 122: 
 123: 4. Iterate per debate-protocol.md until convergence or the round cap, then
 124:    escalate any unresolved points to the user with both positions stated.
 125: 5. Freeze the converged plan per references/frozen-plan-format.md under the
 126:    project's superpowers plans dir (KitnEssentials:
 127:    `dev/docs/superpowers/plans/`; other projects: the superpowers default
 128:    `docs/superpowers/plans/`).
 129: 
 130: ## Mode diff
 131: 
 132: Same transport and protocol. First read the frozen plan's debate record and
 133: its **Verification status** field. Then the brief carries the frozen plan
 134: path, the base/head SHAs superpowers code review used, and the
 135: `git diff base..head` output. Both sides check **spec fidelity** (drift from
 136: the frozen plan — the implementer makes zero judgment calls, so any drift is
 137: a finding) and, for port work, **port fidelity** (drift from the reference
 138: source), ending PASS / FIX / ESCALATE.
 139: 
 140: A FIX this session applies itself goes through the **application
 141: checkpoint** (references/application-checkpoint.md) before the first edit.
 142: The fixed range is then re-reviewed, and only the post-re-review terminal
 143: PASS is attested — with the emitter's `-CheckpointFile` binding the
 144: checkpoint; a FIX verdict whose fixes are still unapplied is never the
 145: attested verdict.
 146: 
 147: **Degraded-plan poisoning rule:** a plan whose Verification status is
 148: DEGRADED cannot produce an ordinary diff PASS. Mode diff must first reopen
 149: and cross-verify the plan's claims and approved deviations (the
 150: retrospective pass the plan never got), and only then verify the
 151: implementation against it — confirming a diff matches an unsound plan
 152: verifies nothing. If cross-vendor verification is STILL unavailable, the
 153: only terminal state is `ESCALATE — CROSS-VENDOR GATE UNSATISFIED`: merging
 154: then requires the user's explicitly recorded risk acceptance, never a PASS.
 155: 
 156: ## Finish line
 157: 
 158: Before it: the session's final-adjudication step (debate-protocol.md) —
 159: verify the last round's findings against the repo and issue the terminal
 160: verdict; a reviewer's PASS/FIX is never terminal by itself.
 161: 
 162: **Mode diff only — record the verdict mechanically.** After the terminal
 163: verdict, run the attestation emitter from this plugin's checkout:
 164: 
 165: ```powershell
 166: powershell -NoProfile -File <plugin-root>/tools/write-attestation.ps1 -RepoRoot <reviewed-repo> -BaseSha <base> -HeadSha <head> -Verdict <PASS|FIX|ESCALATE> -VerificationStatus <FULL|DEGRADED> -RouteNote "<effective route confirmed | the transport-failure class>" -Rounds <n> -Participants "<session-model> (session) / <reviewer-model> (reviewer)" [-CheckpointFile <application-checkpoint-artifact>]
 167: ```
 168: 
 169: When an application checkpoint governed fix application, pass it via
 170: `-CheckpointFile`: the record then also binds the checkpoint's hash and
 171: the emitter-computed changed-path set, `<head>` is the POST-fix,
 172: re-reviewed head — never the head the FIX verdict was issued on — and
 173: the artifact must already carry its appended verification results (the
 174: recorded hash covers the final artifact).
 175: 
 176: It writes `.git/parallax/attestations/<head-sha>.json` inside the reviewed
 177: repo — untracked by design, so recording the verdict cannot move HEAD out
 178: from under its own SHA. The pre-push lane (`tools/verify-attestation.ps1`)
 179: later warns when a `main` push has no matching attestation for the pushed
 180: head (fast-forward: pushed sha == attested head; merge commit: parent1 ==
 181: attested base, parent2 == attested head — a squash changes the sha and
 182: correctly forces re-review).
 183: 
 184: End with one status line: participating models, rounds used, converged vs
 185: escalated points, the verification status — `FULL`, or
 186: `DEGRADED (<class>, authorized by user at round N)` per fallbacks.md — and
 187: the route note: `effective route confirmed` when every round's header
 188: matched the canonical declarations, else the transport-failure class that
 189: fallbacks.md handled. The session emits this line itself; never delegate it
 190: to a subagent.
 191: 
 192: ## Common mistakes
 193: 
 194: - Accepting the reviewer's claims about reference code without the cited lines —
 195:   strike the claim per protocol; do not argue against it.
 196: - Re-sending the full debate context each round instead of resuming the
 197:   codex session.
 198: - Running mode `diff` against different SHAs than the code review used.
 199: - Treating convergence as failure — a sound plan converging in one round is
 200:   the system working, not a skipped debate.

 succeeded in 603ms:
   1: # Frozen plan format
   2: 
   3: The debate's output is a superpowers-compatible implementation plan. The
   4: implementer (the pinned lane in `agents/`, or the session model, via superpowers
   5: subagent-driven-development or executing-plans) follows it with **zero
   6: judgment calls** — anything the plan leaves open is a plan defect, found in
   7: mode `diff` as drift.
   8: 
   9: ## Base format
  10: 
  11: Follow the superpowers writing-plans template exactly (header with Goal /
  12: Architecture / Tech Stack / Global Constraints; bite-sized checkbox tasks
  13: with exact Files / Interfaces / complete code / exact commands / expected
  14: output; no placeholders). Every task's verification command must be able
  15: to FAIL: the debate checks each one for oracle adequacy — a proof that
  16: would pass while the feature is broken (a compile check standing in for a
  17: behavior check, a test that never exercises the changed path) is a plan
  18: defect, found in the debate, not in production. Save location: the
  19: superpowers default
  20: `docs/superpowers/plans/YYYY-MM-DD-<feature-name>.md`, unless the project
  21: overrides it (example: KitnEssentials uses `dev/docs/superpowers/plans/`).
  22: 
  23: Port-specific Global Constraints to copy in verbatim when the work is a
  24: port (KitnDev-family example — adapt the specifics per project):
  25: 
  26: - Project naming/style conventions the port must follow (KE example:
  27:   KE namespace, flat DB keys, `KE:ApplyFontToText` for SOFTOUTLINE-capable
  28:   text, string literals only — see the module-port skill).
  29: - Reference source and version the port tracks:
  30:   `References/<name>/` at the fidelity the debate agreed on (faithful core
  31:   logic; project-specific deviations listed one per line WITH the debate
  32:   round that approved each).
  33: - Platform/API constraints the debate verified, each with its citation
  34:   (KE example: WoW 12.0 secret-value rules).
  35: 
  36: ## Debate record appendix (REQUIRED)
  37: 
  38: Every frozen plan ends with:
  39: 
  40: ```markdown
  41: ---
  42: 
  43: ## Debate record
  44: 
  45: **Participants:** <session model> (session) / <reviewer model> (codex exec, session <id>)
  46: **Rounds used:** N of CAP
  47: **Outcome:** converged | converged with amendments | escalated
  48: **Verification status:** FULL | DEGRADED
  49: **Degradation:** none | codex-missing | model-rejected | auth-expired | quota-exhausted | continuity-lost | <class>
  50: **Authorized by:** n/a | user at round N | not-authorized
  51: **Raw rounds:** <paths to the verbatim round replies/transcripts> | not retained
  52: 
  53: ### Resolved points
  54: | # | Claim | Raised by | Outcome | Evidence |
  55: |---|-------|-----------|---------|----------|
  56: | 1 | ...   | reviewer  | accepted into Task 3 | References/<addon>/<file>:<line> |
  57: | 2 | ...   | session   | refuted  | .wow-api-reference/<file> |
  58: | 3 | ...   | reviewer  | struck (no citation) | — |
  59: 
  60: ### Escalated points (user-decided)
  61: | # | Question | Session position | Reviewer position | Owner's call |
  62: |---|----------|------------------|-------------------|--------------|
  63: 
  64: ### Degraded-mode note
  65: (required whenever Verification status is DEGRADED — name what was skipped,
  66: why, and the recorded user authorization. The Participants line must name
  67: the actual participants, never the placeholder template line.)
  68: ```
  69: 
  70: The three status fields are structured on purpose: mode `diff` parses
  71: `Verification status` to enforce the degraded-plan poisoning rule (see
  72: SKILL.md) — prose notes cannot be enforced. `Raw rounds` records where the
  73: verbatim reviewer replies live (scratchpad transcripts are temporary — if
  74: they were not copied somewhere durable, say `not retained`): the summary
  75: tables above are the adjudication, not the provenance, and a later dispute
  76: about what the reviewer actually said needs the raw text or an honest
  77: "gone".
  78: 
  79: The appendix is the audit trail: mode `diff` re-reads it to know which
  80: deviations were approved and which claims were struck. A frozen plan without
  81: the appendix was not verified — treat it as an unfrozen draft.
  82: 
  83: ## Freezing
  84: 
  85: A plan is frozen when: both sides verdict PASS — or the final round's FIXes
  86: are accepted on the record (converged with amendments, per
  87: debate-protocol.md) — with any escalations carrying the user's recorded
  88: call, the writing-plans self-review checklist has run, and the file is
  89: saved. After freezing, changes require reopening the debate
  90: (a new round appended to the record) — the implementer never edits the plan.

 succeeded in 646ms:
   1: # Fallbacks and degraded modes
   2: 
   3: **Governing rule: no transition that reduces vendor diversity, evidence
   4: quality, or conversation continuity happens without explicit user consent.**
   5: The skill never enters degraded mode automatically. Degradation that is not
   6: consented to is a stop, not a mode.
   7: 
   8: ## The consent gate
   9: 
  10: When a failure survives bounded recovery (below), STOP and present a banner
  11: to the user — before any degraded work happens:
  12: 
  13: ```text
  14: == CROSS-VENDOR VERIFICATION UNAVAILABLE ==
  15: What failed:      <exact command + error>
  16: What degraded mode would verify:     <e.g. claims via a fresh same-vendor skeptic>
  17: What it would NOT verify:            <cross-vendor independence>
  18: Options: [fix codex] [run degraded] [abort]
  19: ```
  20: 
  21: - The user's choice is recorded in the debate record (`Authorized by:` field
  22:   — see frozen-plan-format.md).
  23: - **Unattended runs fail closed**: if no user can answer, terminate as
  24:   `BLOCKED/DEGRADED-NOT-AUTHORIZED` with a non-success status. Never wait
  25:   indefinitely, never infer consent.
  26: 
  27: ## Bounded recovery (automatic, consent-free)
  28: 
  29: One retry with the SAME model, sandbox, effort, and session parameters is
  30: the DEFAULT recovery for any failure that has no named immediate-gate rule
  31: below (codex-missing, model-rejected, auth-expired, route-mismatch,
  32: quota-exhausted, and a missing-rollout resume go straight to the gate —
  33: retrying those changes nothing). The retry reduces nothing, so it needs no
  34: consent. A failed retry goes to the consent gate. This covers timeouts and
  35: transient transport errors.
  36: 
  37: Cost framing (why exactly ONE): a pre-dispatch failure (spawn error, auth
  38: preflight, missing CLI) never reached the provider, so its retry is free; a
  39: post-dispatch failure (timeout, empty reply, malformed output) may have
  40: already incurred quota cost, so the single automatic retry is the bounded
  41: re-spend — anything beyond it is the user's call at the consent gate, never
  42: a silent loop.
  43: 
  44: **Catch-all: any codex failure not named in this file — nonzero exit, empty
  45: reply file, a stale reply file (a reused path serving an earlier round's
  46: reply after a failed call; round paths must be fresh, and stale content is
  47: DISCARDED unread), malformed output, network loss, resume failure —
  48: gets the same treatment: one same-parameters retry (for a stale reply:
  49: with a fresh path), then the consent gate.**
  50: No failure class is ever an implicit license to degrade.
  51: 
  52: ## Failure classes
  53: 
  54: ### codex CLI missing or broken
  55: `codex --version` fails → consent gate immediately (there is nothing to
  56: retry). If the user chooses degraded mode: replace each reviewer round with
  57: a FRESH-context skeptic subagent (the session model's vendor, read-only
  58: tools) — fresh context is
  59: mandatory; same-context self-critique rubber-stamps (see
  60: model-prompting-notes.md). Brief the skeptic with the same XML-style brief
  61: the reviewer would have received, including the strike rule and the
  62: anti-manufactured-objection rule.
  63: 
  64: ### Reviewer model rejected (400 "not supported when using Codex with a ChatGPT account")
  65: The account tier lost access to the canonical reviewer model (the id is
  66: declared ONLY in model-prompting-notes.md). Do NOT silently substitute
  67: another model — the reviewer model is a user directive. Consent gate: fix
  68: subscription / run degraded / abort. Tier-gating diagnostics for the
  69: current lane (which sibling model confirms gating vs a CLI problem) live
  70: in model-prompting-notes.md's Lane diagnostics bullet.
  71: 
  72: ### Usage limit reached (session or weekly quota) — class `quota-exhausted`
  73: The ChatGPT account's usage quota is exhausted; codex's error names which
  74: limit was hit (the ~5-hour session window vs the weekly cap) and when it
  75: resets. **Skip the retry** — nothing about a quota window is transient, and
  76: a retry only burns another attempt against it. Go straight to the consent
  77: gate with codex's reset time quoted verbatim in the `What failed:` line;
  78: here the "fix codex" option means *wait for the reset* or *upgrade the
  79: tier* — record which. If a debate is mid-flight, also note the codex
  80: session id in the debate record so the debate resumes after the reset
  81: instead of restarting from round 1.
  82: 
  83: ### codex auth expired or wrong auth mode
  84: `codex login status` fails, or succeeds without reporting `Logged in using
  85: ChatGPT` (an API-key login also exits 0 but rides different billing) → the
  86: fix needs the user anyway (`codex login` is an interactive browser
  87: sign-in): consent gate.
  88: 
  89: ### Effective route mismatch — class `route-mismatch`
  90: The codex startup header (client-resolved `model:` / `provider:` /
  91: `reasoning effort:` / `sandbox:` — see model-prompting-notes.md) disagrees
  92: with the canonical declarations, the `sandbox:` line is not `read-only`,
  93: or a resume's `session id:` is not the one
  94: requested. Nothing here is transient — a config.toml override, a profile,
  95: or a dropped session does not fix itself: **skip the retry**, consent gate.
  96: The reviewer reply from the mismatched call is DISCARDED unread — it came
  97: over an unverified route and never enters the debate record.
  98: 
  99: ### Session id lost or resume fails
 100: Losing the resumed session degrades conversation continuity — an advertised
 101: protocol property — so it is NOT automatic: one retry, then the consent
 102: gate with the specific option "continue with fresh per-round reviewer calls
 103: (full brief re-sent each round; costs tokens, loses the reviewer's debate
 104: memory)".
 105: 
 106: One resume failure is deterministic and skips the retry: output matching
 107: `no rollout found for thread id ... (code -32600)` — class
 108: `missing-rollout` (probed 2026-07-24; the reply file is not written).
 109: The rollout is gone; skip the retry and go straight to this
 110: class's consent gate with the same fresh-per-round option.
 111: 
 112: ### Stale API evidence
 113: If the project's API-reference drift check reports the build changed under
 114: a claim, that claim is **struck until re-verified** — the strike rule does
 115: not weaken to a flag. Re-verify against the updated reference (or an
 116: in-game/runtime probe) before the claim re-enters the debate.
 117: 
 118: ## Degraded-mode output requirements (after consent)
 119: 
 120: - The SUPERVISING SESSION emits the banner — never delegate the banner to
 121:   the skeptic subagent's output, or a formatting miss becomes a quiet path.
 122: - Every round after the transition starts with
 123:   `== DEGRADED (single-vendor) ==`.
 124: - The finish line and the frozen plan carry the structured fields
 125:   (`Verification status: DEGRADED`, `Degradation: <class>`,
 126:   `Authorized by: user at round N`) per frozen-plan-format.md.
 127: - The debate record's Participants line must name the actual participants
 128:   (e.g. `<session model> (session) / <session model> skeptic subagent
 129:   (DEGRADED - codex-missing)`), never the cross-vendor template line.
 130: - A DEGRADED-frozen plan poisons downstream PASSes — see SKILL.md mode
 131:   `diff` for the enforcement rule.
 132: 
 133: ## Reference failures (not degraded modes — hard stops)
 134: 
 135: ### Reference source missing for a port
 136: Ask the user for the path. NEVER proceed with an ungrounded debate — a
 137: debate about remembered reference behavior is two models fabricating at
 138: each other. This is a hard stop, not a degraded mode.
 139: 
 140: ## Known limits (documented, not silent)
 141: 
 142: - The review-companion hook is ADVISORY: it injects the diff-mode reminder;
 143:   it cannot block a merge. The enforcement backstop is the finish rule and
 144:   the user. It is also registered for failed review dispatches
 145:   (PostToolUseFailure), so a crashed code review still surfaces the gate.
 146: - Hook fingerprint rot is detected by the eval suite (pinned template
 147:   fixture in CI + installed-template canary locally), not at runtime.
 148: 
 149: ## Alternative transport (documented, rejected for v1)
 150: 
 151: CLIProxyAPI (the Theo/Tibo "claudex" setup) runs a SEPARATE Claude Code
 152: process pointed at a local proxy that translates to the Codex backend
 153: (same ChatGPT OAuth), with env vars like `CLAUDE_CODE_SUBAGENT_MODEL`
 154: selecting the reviewer model — it does not touch the existing session's
 155: traffic.
 156: Revisit only if codex exec proves limiting (e.g. a cross-vendor implementer
 157: lane needs Claude Code's own tool harness). It does NOT bypass the
 158: subscription tier gate — same auth, same 400 on free accounts. Costs: a
 159: local proxy service to run and keep patched, a second configured process,
 160: and untested cross-vendor hook and permission semantics.

2026-07-26T04:14:51.607361Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_reasoning_summaries` at line 86 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'commands\\doctor.md';"'$i=0;Get-Content -LiteralPath $p|%{$i++;if($i-ge1-and$i-le144){'"'{0,4}: {1}'-f"'$i,$_}}' in C:\Users\Brandon\Documents\parallax
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'skills\\multi-model-verify\\references\\model-prompting-notes.md';"'$i=0;Get-Content -LiteralPath $p|%{$i++;if($i-ge1-and$i-le206){'"'{0,4}: {1}'-f"'$i,$_}}' in C:\Users\Brandon\Documents\parallax
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'README.md';"'$i=0;Get-Content -LiteralPath $p|%{$i++;if($i-ge55-and$i-le105){'"'{0,4}: {1}'-f"'$i,$_}}' in C:\Users\Brandon\Documents\parallax
 succeeded in 622ms:
  55:   verifies the reviewer's final round against the repo and emits the
  56:   terminal verdict itself. A reviewer PASS/FIX is input, never the
  57:   decision; genuine deadlocks escalate to the user. The rule attaches to
  58:   the *role*, not the model — it holds whoever fills the session seat.
  59: 
  60: ## What's in the box
  61: 
  62: | Piece | What it does |
  63: |---|---|
  64: | `skills/multi-model-verify/` | The debate skill: both modes, debate protocol, frozen-plan format, model prompting notes, fallbacks/consent gate |
  65: | `hooks/` | PostToolUse + PostToolUseFailure hook (matcher `Task\|Agent`): fingerprints the superpowers code-reviewer dispatch, injects the mode-`diff` reminder with matching SHAs; inert everywhere else |
  66: | `agents/implementer.md` | Zero-judgment direct-typing executor for frozen-plan tasks (model pinned in the file's frontmatter) |
  67: | `agents/flash-implementer.md` | Zero-judgment Flash lane: haiku wrapper drives Gemini Flash through the Antigravity CLI headlessly; route + authorship evidence checked every run (model literal pinned in the file) |
  68: | `commands/drift-triage.md` | `/parallax:drift-triage` — reads the newest drift report, verifies each finding against the live contract surfaces, repairs on a branch |
  69: | `commands/doctor.md` | `/parallax:doctor` — operational health check: checkout-vs-installed version, hook registration, superpowers fingerprint, codex transport round-trip, drift task + pending entries. Reports, never fixes |
  70: | `commands/intake.md` | `/parallax:intake` — external-reference intake: clone read-only as untrusted subject data, ground every claimed delta on both sides, probe-gate behavior claims, rank dispositions for the user's scope pick, then hand into the multi-model-verify debate |
  71: | `evals/` | Four gate tiers for the skill itself — see [Verify](#verify) |
  72: | `tools/check-drift.ps1` | Weekly drift watch over the three upstreams the contract depends on — see [Drift protection](#drift-protection) |
  73: | `tools/write-attestation.ps1` · `tools/verify-attestation.ps1` | SHA-bound review attestations — see [Attestation lane](#attestation-lane) |
  74: | `.githooks/pre-push` | Non-blocking attestation check on `main` pushes (`git config core.hooksPath .githooks` to enable) |
  75: 
  76: ## Fails loud, never silent
  77: 
  78: The governing rule
  79: (`skills/multi-model-verify/references/fallbacks.md`): **no transition that reduces
  80: vendor diversity, evidence quality, or conversation continuity happens
  81: without explicit user consent.**
  82: 
  83: ```mermaid
  84: flowchart TD
  85:     X[codex failure] --> R{one same-parameters retry}
  86:     R -->|recovered| OK[debate continues]
  87:     R -->|still failing| G["CONSENT GATE banner:<br/>what failed · what degraded mode<br/>would and would NOT verify"]
  88:     X -. "quota-exhausted:<br/>skip the retry" .-> G
  89:     X -. "missing-rollout resume:<br/>skip the retry" .-> G
  90:     G -->|fix codex| OK
  91:     G -->|run degraded| D["single-vendor, visibly flagged;<br/>DEGRADED plan poisons any diff PASS"]
  92:     G -->|abort or unattended| B2[BLOCKED / DEGRADED-NOT-AUTHORIZED]
  93: ```
  94: 
  95: - One same-parameters retry is the **only** automatic recovery; every other
  96:   transition stops at the consent gate. Unattended runs fail closed.
  97:   (Pre-dispatch failures retry free; a post-dispatch retry is the bounded
  98:   re-spend — never a loop.)
  99: - Session/weekly usage limits get a dedicated `quota-exhausted` class: no
 100:   retry (quota windows don't clear in seconds), and the banner quotes
 101:   codex's reset time verbatim. A resume failing with the missing-rollout
 102:   signature ("no rollout found ... code -32600", probed 2026-07-24) also
 103:   skips the retry — the rollout is gone; straight to the session-loss
 104:   consent gate.
 105: - **Effective-route check (0.6.0; `sandbox:` line added in 0.8.0)**: every

 succeeded in 622ms:
   1: # Model prompting notes
   2: 
   3: ## Fable 5 (the session side)
   4: 
   5: From Anthropic's Fable 5 prompting guide (distilled 2026-07; re-check the
   6: guide when it updates):
   7: 
   8: - **Ground progress claims**: state what was actually run and observed, not
   9:   what should have happened — the guide's grounding pattern nearly eliminated
  10:   fabricated status reports in Anthropic's evals. In debate terms: your
  11:   PASS/FIX verdicts cite the evidence you actually read this session.
  12: - **State the boundaries**: tell the model what is out of scope explicitly.
  13:   The debate brief states what is NOT under debate (already-decided user
  14:   directives, e.g. equal-weight advisors, the chosen reference addon).
  15: - **Fresh-context verification beats self-critique**: Fable reviewing its own
  16:   plan in the same context rubber-stamps. That is the reason Sol exists in
  17:   this loop — and the reason degraded mode (fallbacks.md) must run the
  18:   skeptic pass in a FRESH subagent, not inline.
  19: 
  20: ## The reviewer lane (currently GPT-5.6 Sol via the codex CLI)
  21: 
  22: THE single source for the reviewer transport. Swapping the reviewer model
  23: is a one-line edit HERE and nowhere else: the executable surfaces (the
  24: behavioral runner's grader, the drift watch's cross-review) PARSE these
  25: two declarations at runtime and fail loud when they are missing, and the
  26: instruction surfaces (SKILL.md transport commands, /parallax:doctor's
  27: probe, /parallax:drift-triage's example) direct the agent to read the
  28: values from this file rather than type a remembered id. The consistency
  29: test forbids a hardcoded `-m` model literal anywhere else in the repo.
  30: 
  31: Canonical model id: `gpt-5.6-sol`
  32: 
  33: Canonical reasoning effort: `high`
  34: 
  35: - **Outcome-oriented briefs**: tell Sol the outcome to verify, not the steps
  36:   to take. Its codex harness plans its own file reads. OpenAI's GPT-5.6
  37:   guidance (developers.openai.com/api/docs/guides/prompt-guidance,
  38:   fetched 2026-07-16; the review example also appears on
  39:   /api/docs/guides/latest-model): "state the goal, relevant context, constraints, required
  40:   evidence, success criteria, and output format." Its own review-task
  41:   example maps directly onto our debate briefs: "Review this database
  42:   migration plan for failure modes... For each finding, cite the relevant
  43:   step, estimate impact and likelihood, and recommend a specific
  44:   mitigation."
  45: - **Six-element shape — goal, context, constraints, required evidence,
  46:   success criteria, output format** (the 5.6 framing; use only the parts
  47:   that help). The XML-style tags below map onto it: task=goal,
  48:   claims=context+evidence, rules=success criteria+output format,
  49:   boundaries=constraints. The tags themselves are OUR convention, not
  50:   OpenAI's — 5.6 prescribes no tags for review tasks — kept because the
  51:   strike rule needs addressable sections to strike against.
  52: - **Lean briefs, rules stated ONCE** (5.6 guidance; leaner prompts scored
  53:   10-15% better in OpenAI's own coding-agent evals): state the evidence
  54:   rules and verdict grammar in full in round 1; later rounds REFERENCE
  55:   them ("evidence rules and verdict grammar as before"), never restate.
  56:   Avoid repeated negations ("do not mutate" three ways) — 5.6 reads
  57:   repetition as noise and it can trigger needless approval requests.
  58:   Prefer decision rules over ALWAYS/NEVER except for true invariants
  59:   (read-only sandbox, the strike rule, the verdict grammar).
  60: - **Final check** (OUR convention, not OpenAI's — the guide's review
  61:   example asks for impact/likelihood/mitigation per finding, not an
  62:   unverified-information pass): every brief ends by asking Sol to flag
  63:   information it could NOT verify — those flags feed the strike rule
  64:   instead of masquerading as findings.
  65: - **Structure the brief with XML-style tags**:
  66: 
  67:   ```text
  68:   <role>Adversarial reviewer, equal weight, in a two-model debate.</role>
  69:   <task>Refute or confirm each numbered claim about the port plan below.</task>
  70:   <rules>Cite References/<addon>/<file>:<line> for every claim you make or
  71:   contest; uncited claims will be struck. Do not manufacture objections:
  72:   if a claim stands, say PASS and move on. End with PASS, FIX (with the
  73:   specific fix), or ESCALATE per claim.</rules>
  74:   <claims>...numbered claims with Fable's citations...</claims>
  75:   <boundaries>...what is already decided and not under debate...</boundaries>
  76:   <final-check>List any claim you could not verify against files you read,
  77:   as UNVERIFIED — do not fold unverified material into your verdict.</final-check>
  78:   ```
  79: 
  80: - **Effort**: pin `-c model_reasoning_effort=<canonical effort above>` per
  81:   call. Do not raise it to ultra/xhigh for debate rounds — Sol propagates
  82:   its effort to every subagent it spawns, which burns tokens without
  83:   changing verdicts. The same value is set in `~/.codex/config.toml`, but
  84:   pin it per call anyway so the debate is config-independent. (The doctor's
  85:   transport probe deliberately uses `low` — it is a reachability check, not
  86:   a review.) 5.6 migration advice: "preserve your current reasoning effort
  87:   as the baseline, then compare one level lower" — `medium` is a tuning
  88:   candidate, but only via a full behavioral-suite pass at both levels;
  89:   never silently downgrade the review lane.
  90: - **Effective route confirmation (every call, fresh or resume)**: codex
  91:   echoes the RESOLVED config in its startup header — capture stdout and
  92:   check the first `model: `, `provider: `, and `reasoning effort: ` lines
  93:   against the canonical declarations above (provider must be `openai`),
  94:   the first `sandbox: ` line reads `read-only`, and on a resume that
  95:   `session id: ` equals the id you resumed. A
  96:   mismatch is a TRANSPORT failure (fallbacks.md consent gate), never a
  97:   review result — a config.toml override or profile can silently swap the
  98:   reviewer, and the header is where that surfaces. Vocabulary discipline:
  99:   the header is CLIENT-RESOLVED metadata, so report "effective route
 100:   confirmed", never "used and confirmed" (codex exposes no server-attested
 101:   runtime identity); the reviewer's prose claiming a model name is never
 102:   identity evidence. Probed 2026-07-19 (codex v0.144.1): fresh and resumed
 103:   calls both emit the full header block. Probed 2026-07-24 (v0.144.1):
 104:   sandbox mode has NO continuity across resumes — a resume WITHOUT
 105:   `--sandbox read-only` resolved to the config default (`workspace-write`)
 106:   on the SAME session id and a test write LANDED; with the flag before the
 107:   `resume` subcommand the write was blocked and the header read
 108:   `sandbox: read-only`. One omitted flag silently turns the read-only
 109:   auditor into a writing agent; the `sandbox:` header line is the tripwire.
 110: - **Env hygiene for the call — one sequence, one environment**: clear
 111:   `CODEX_API_KEY`, `OPENAI_API_KEY`, `OPENAI_BASE_URL`, and `CODEX_HOME`
 112:   FIRST, then run the `codex login status` preflight, then dispatch —
 113:   all in that same sanitized environment (a preflight in ambient env can
 114:   pass or fail on overrides the dispatch never sees). The first two vars
 115:   can flip auth to API-key billing, the base URL can reroute even
 116:   ChatGPT-authenticated traffic. The preflight must report `Logged in
 117:   using ChatGPT` — exit 0 alone also passes an API-key login. `CODEX_HOME`
 118:   redirects auth.json and config.toml wholesale (see its probe bullet
 119:   below); clearing it reverts codex to the default home, so a legitimately
 120:   relocated home fails the auth preflight LOUDLY instead of silently
 121:   rerouting the lane.
 122: - **`CODEX_HOME` is reroute-capable**: probed 2026-07-24 (codex-cli
 123:   0.144.1, Windows): with ambient `CODEX_HOME` pointed at an empty scratch
 124:   directory, `codex login status` reported `Not logged in` (exit 1) while
 125:   the real home reported `Logged in using ChatGPT` — the variable
 126:   redirects auth.json and config.toml wholesale, so an ambient override
 127:   can route a debate to a DIFFERENT ChatGPT account while every header
 128:   line still reads clean (the header names model/provider, not account).
 129:   Claim source: jinn (pinned 6c46f57) strips `CODEX_*` and re-pins
 130:   `CODEX_HOME` per engine child (packages/jinn/src/shared/child-env.ts:31).
 131:   Settles: `CODEX_HOME` is in the env denylist (adopted 0.10.0, debate
 132:   2026-07-24).
 133: - **Session resume, not context re-send**: capture the `session id:` from
 134:   round 1 and resume it (flags before the subcommand). OpenAI documents
 135:   5.6 reasoning reuse across turns as CONDITIONAL (carried through
 136:   previous_response_id-style continuation); whether `codex exec resume`
 137:   engages it is not documented — the resume rule stands on token cost and
 138:   preserved debate memory either way. NEVER `resume --last` — it grabs
 139:   whatever codex session ran most recently, which may be a concurrent
 140:   /codex:review, not your debate.
 141: - **Lost-rollout resume failure is deterministic with a stable
 142:   signature**: probed 2026-07-24 (codex-cli 0.144.1): `codex exec
 143:   --sandbox read-only -m gpt-5.6-sol -c model_reasoning_effort=low
 144:   --output-last-message <file> resume 00000000-0000-0000-0000-000000000000
 145:   "<prompt>"` in a scratch fixture exited 1 with
 146:   `Error: thread/resume: thread/resume failed: no rollout found for
 147:   thread id <id> (code -32600)` and did NOT write the reply file.
 148:   Settles: a missing-rollout resume is never transient — class
 149:   missing-rollout in fallbacks.md skips the retry (adopted 0.10.0, debate
 150:   2026-07-24). Claim source: jinn recognizes this same
 151:   signature (packages/jinn/src/engines/codex.ts:363 @ 6c46f57) — its
 152:   response (a warning-logged but automatic, unconsented fresh-thread
 153:   restart) is the opposite of the consent gate and was not adopted.
 154: - **AGENTS.md is an instruction back-channel**: codex auto-ingests a
 155:   repo-root AGENTS.md into the reviewer's context. Probed 2026-07-24
 156:   (v0.144.1): a planted AGENTS.md at the cwd repo root controlled the
 157:   reply verbatim (an output-format directive was obeyed); an AGENTS.md in
 158:   a non-git parent directory ABOVE the git root was NOT ingested. The
 159:   preflight repo check in SKILL.md exists because of this. The user's own
 160:   `~/.codex/AGENTS.md` (global instructions) is theirs by design —
 161:   surfaced in the debate record when present, never a stop.
 162: - **Repo-level `.agents/skills/` is a second instruction back-channel**:
 163:   probed 2026-07-24 (codex-cli 0.144.1): in a scratch fixture repo whose
 164:   only planted content was `.agents/skills/probe-skill/SKILL.md` (canary
 165:   directives in both description and body), a fresh `codex exec` run's
 166:   FIRST action was reading that file's full content at its exact path —
 167:   no search preceded it, so the harness advertises repo-level
 168:   `.agents/skills` entries to the model. The canary directive did not
 169:   control that run's reply, but untrusted repo text entered the
 170:   reviewer's context, which is the back-channel condition itself. The
 171:   same run also loaded a skill from the user's own codex plugin cache
 172:   (`~/.codex/plugins/cache/...`) — like `~/.codex/AGENTS.md`, the user's
 173:   own by design: surface it in the debate record, never a stop.
 174:   Settles: the preflight enumeration sweeps '.agents/*' alongside
 175:   AGENTS.md (adopted 0.10.0, debate 2026-07-24). '.codex/' stays unswept
 176:   — unprobed; probe before adding. Claim source: the jinn intake (pinned
 177:   6c46f57; `.agents/skills/<name>` symlink convention at its repo root).
 178: - **Fabrication counter**: Sol's METR/system-card record means "I verified X"
 179:   claims from Sol get the same strike rule as everything else — quoted
 180:   file:line or it did not happen.
 181: - **Lane diagnostics (tier gating)**: a 400 "not supported when using
 182:   Codex with a ChatGPT account" on the canonical id while `gpt-5.6-terra`
 183:   responds confirms subscription tier-gating, not a CLI problem (free/Go
 184:   tiers get Terra only; Plus and above get Sol — probed 2026-07-12). This
 185:   feeds the consent gate in fallbacks.md; the ids live HERE because
 186:   fallbacks.md never names models.
 187: - **Structured quota state is locally readable (experimental)**: probed
 188:   2026-07-24 (codex-cli 0.144.1): `codex app-server --stdio` with an
 189:   NDJSON `initialize` (`capabilities.experimentalApi: true`, stdin held
 190:   OPEN — the server exits when stdin closes) then
 191:   `account/rateLimits/read` (id 2) answered in under 10s with per-limit
 192:   windows (`usedPercent`, `windowDurationMins`, `resetsAt` epoch),
 193:   `planType`, and credits. Handshake mirrored from jinn
 194:   (packages/jinn/src/shared/engine-limits.ts:370-453 @ 6c46f57).
 195:   Experimental capability — expect drift. The doctor's quota-headroom row
 196:   (check 4b) reads this surface (adopted 0.10.0, debate 2026-07-24);
 197:   complements, never replaces, the reactive quota-exhausted class.
 198: 
 199: ## Reusable recipes
 200: 
 201: The installed codex plugin ships prompt recipes and antipatterns under its
 202: `gpt-5-4-prompting` skill (plugin cache, `skills/gpt-5-4-prompting/`). They
 203: were written for GPT-5.4-era codex: the structural advice (tight task
 204: framing, no chain-of-thought micromanagement) still applies to Sol, but
 205: verify any model-specific flag or behavior claim against current OpenAI docs
 206: before relying on it.

 succeeded in 667ms:
   1: ---
   2: description: Operational health check for the parallax plugin - versions, hook, fingerprint, transport, drift state
   3: ---
   4: 
   5: Run the parallax operational checks below and present ONE table:
   6: check | state | verdict (OK / STALE / BROKEN / N/A) | fix. N/A marks a
   7: check that cannot apply here or an experimental surface that did not
   8: answer; an N/A verdict never contributes to overall failure. End with a
   9: one-line overall summary. Report only - fix nothing without being asked.
  10: 
  11: ## 1. Checkout vs installed version
  12: 
  13: Two different files, and mixing them up is the whole point of this check:
  14: 
  15: - CHECKOUT: `~/.claude/plugins/known_marketplaces.json` -> the `parallax`
  16:   marketplace's source. A LOCAL DIRECTORY source has a `source.path`; read
  17:   `.claude-plugin/plugin.json` `version` there. A GITHUB source (the
  18:   README's stable install, `Bmwascher/parallax`) has NO local checkout —
  19:   that is not breakage: report this check as `N/A (GitHub install — the
  20:   installed version is authoritative)` and skip the comparison.
  21: - INSTALLED: `~/.claude/plugins/installed_plugins.json` -> the
  22:   `parallax@parallax` entry's `version` (its `installPath` is the
  23:   VERSIONED CACHE COPY, never the checkout).
  24: 
  25: Mismatch (directory source only) = STALE, and everything running right now
  26: is the cached version: the dev loop is bump ->
  27: `claude plugin update parallax@parallax` -> restart the session.
  28: 
  29: ## 2. Hook registration and matcher
  30: 
  31: Read the INSTALLED copy's `hooks/hooks.json`. Verify both `PostToolUse`
  32: and `PostToolUseFailure` are registered and their matchers match the tool
  33: name `Agent` (regex-match the literal string). A bare `Task` matcher is
  34: BROKEN (tool renamed in Claude Code 2.1.63).
  35: 
  36: ## 3. Superpowers fingerprint
  37: 
  38: From `installed_plugins.json`, find superpowers' installPath and read
  39: `skills/requesting-code-review/code-reviewer.md`. Both literals "Senior
  40: Code Reviewer" and "Git Range to Review" present = OK; one = BROKEN (hook
  41: warns but cannot extract); none = BROKEN (diff gate inert). Also confirm
  42: `**Base:**`/`**Head:**` placeholders still appear.
  43: 
  44: ## 4. codex transport
  45: 
  46: First sanitize the probe shell — clear `CODEX_API_KEY`, `OPENAI_API_KEY`,
  47: `OPENAI_BASE_URL`, and `CODEX_HOME` (`Remove-Item Env:<name>`) so the
  48: preflight and the probe below see the SAME environment the review lane
  49: uses. Then
  50: `codex --version`, then `codex login status`. The login check must report
  51: the first-party auth STATE — output matching `Logged in using ChatGPT` —
  52: not merely exit 0 (an API-key login also exits 0 but rides different
  53: billing; report that as BROKEN with the actual output). If both pass, run
  54: the cheapest possible round-trip probe. The probe writes to a FRESH unique
  55: output file (the `Get-Random` name below), so a stale `TRANSPORT-OK` from
  56: an earlier run can never read as a pass over a failed probe.
  57: 
  58: Read the canonical model id from the INSTALLED copy's
  59: `skills/multi-model-verify/references/model-prompting-notes.md` — resolve
  60: it under the `installPath` from check 1, exactly like checks 2 and 3 (a
  61: bare relative path only works when the current directory happens to be the
  62: plugin checkout). The `Canonical model id` declaration is the ONE place
  63: the reviewer model is defined; use it as `<id>` below. A missing
  64: declaration is itself a BROKEN finding. Effort `low` is deliberate — this
  65: is a reachability check, not a review.
  66: 
  67: ```powershell
  68: $probe = "$env:TEMP\parallax-doctor-$(Get-Random).txt"
  69: $hdr = "$env:TEMP\parallax-doctor-hdr-$(Get-Random).txt"
  70: "Reply with exactly: TRANSPORT-OK" | codex exec --sandbox read-only -m <id> -c model_reasoning_effort=low --output-last-message $probe - > $hdr 2>&1
  71: # OK requires ALL of: exit 0; $probe contains exactly TRANSPORT-OK; and the
  72: # captured header ($hdr) echoes the EFFECTIVE ROUTE - first `model: ` line
  73: # equals <id>, first `provider: ` line `openai`, first `reasoning effort: `
  74: # line `low` (the probe pins low), first `sandbox: ` line `read-only` (the
  75: # probe pins it; sandbox mode has no cross-resume continuity, so a default
  76: # bleeding through here would also bleed into review resumes). codex
  77: # prints the RESOLVED config there, so a config.toml override or profile
  78: # silently swapping the reviewer surfaces as a mismatch = BROKEN (report
  79: # header value vs canonical).
  80: ```
  81: 
  82: OK requires the zero exit, fresh `TRANSPORT-OK` content, AND the header
  83: match; anything else is BROKEN. The header is client-resolved metadata —
  84: report it as `effective route confirmed`, never "used and confirmed".
  85: 
  86: ## 4b. codex quota headroom (best effort, experimental)
  87: 
  88: Same sanitized shell as check 4. `codex app-server --stdio` answers the
  89: JSON-RPC method `account/rateLimits/read` locally (probed 2026-07-24;
  90: experimental capability — drift is expected and is exactly what N/A is
  91: for). Hold stdin OPEN — the server exits when it closes.
  92: 
  93: ```powershell
  94: python -c "import json,shutil,subprocess,threading;bin=shutil.which('codex');p=subprocess.Popen([bin,'app-server','--stdio'],stdin=subprocess.PIPE,stdout=subprocess.PIPE,stderr=subprocess.PIPE,text=True);r={};t=threading.Thread(target=lambda:[r.update(m=l) for l in p.stdout if 'rateLimits' in l],daemon=True);t.start();p.stdin.write(json.dumps({'id':1,'method':'initialize','params':{'clientInfo':{'name':'parallax-doctor','version':'0'},'capabilities':{'experimentalApi':True}}})+'\n'+json.dumps({'id':2,'method':'account/rateLimits/read','params':None})+'\n');p.stdin.flush();t.join(timeout=10);p.kill();print(r.get('m','NO-ANSWER'))"
  95: ```
  96: 
  97: Answer received: report `usedPercent`, `windowDurationMins` (as days/hours),
  98: `resetsAt` (as local time), and `planType` in the state column — verdict
  99: OK. `NO-ANSWER`, a spawn error, or malformed JSON: verdict
 100: `N/A (experimental surface unavailable)` — NEVER BROKEN from this row
 101: alone, and never retry in a loop. This row reads account state only; it
 102: sends no review traffic and must not replace check 4's transport probe.
 103: 
 104: ## 5. Drift watch
 105: 
 106: `schtasks /Query /TN "parallax drift watch" /V /FO LIST` - task exists,
 107: next run time sane, and the "Task To Run" path points at an existing
 108: `check-drift.ps1`. A legacy `crosscheck drift watch` task still present
 109: (pre-rename installs) is STALE - the fix is re-running
 110: `tools/check-drift.ps1 -Register`, which migrates it. Then check
 111: `tools\drift-pending.json` next to it:
 112: entries present = list each (status, stamp) and point at
 113: /parallax:drift-triage.
 114: 
 115: Also read `tools\drift-snapshot.json` next to that `check-drift.ps1`: if
 116: its `codex` field differs from the live `codex --version` from check 4,
 117: note it (STALE, informational) — the codex CLI changed since the last
 118: weekly run, so the transport flag surface was last probed against the OLD
 119: version; the next drift run re-probes it.
 120: 
 121: ## 6. Behavioral eval target
 122: 
 123: Note (informational): `evals/tools/run_behavioral_evals.py` tests the
 124: INSTALLED plugin by default; `--head` tests the checkout via `--plugin-dir`.
 125: If check 1 found a version mismatch, say so here: any behavioral run made
 126: WITHOUT `--head` tested the stale cache, not the checkout. A `--head` run is
 127: unaffected by the mismatch.
 128: 
 129: ## 7. agy transport (Flash implementer lane)
 130: 
 131: Resolve the INSTALLED copy's `agents/flash-implementer.md` under the
 132: `installPath` from check 1 and parse the canonical model literal from its
 133: Lane note (the pinned model-ID token declared there) — the agent file is the ONE
 134: place the implementer model is defined; carry no literal here. A missing
 135: declaration is itself BROKEN. Then:
 136: 
 137: - `& "$env:LOCALAPPDATA\agy\bin\agy.exe" --version` — missing binary =
 138:   BROKEN (the Flash lane cannot dispatch), report the install one-liner.
 139: - `agy models` — output must contain the parsed literal. Sign-out or a
 140:   missing model = BROKEN with the actual output. No generation probe —
 141:   this is a reachability check, and agy free-tier quota is opaque.
 142: 
 143: Report the route language as declared in the agent file: evidence is
 144: client-side, requested and propagated only.

2026-07-26T04:15:16.036517Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_reasoning_summaries` at line 86 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Select-String -Path 'docs\\superpowers\\specs\\2026-07-25-flash-implementer-design.md' -Pattern 'per-task|per task|SDD|subagent-driven|controller|flash-implementer|ledger' -CaseSensitive:"'$false -Context 2,3 | ForEach-Object { '"'{0,4}: {1}' -f "'$_.LineNumber,$_.Line }' in C:\Users\Brandon\Documents\parallax
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Select-String -Path 'docs\\superpowers\\plans\\2026-07-25-flash-implementer.md' -Pattern 'per-task|per task|SDD|subagent-driven|controller|flash-implementer|ledger' -CaseSensitive:"'$false -Context 2,3 | ForEach-Object { '"'{0,4}: {1}' -f "'$_.LineNumber,$_.Line }' in C:\Users\Brandon\Documents\parallax
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Select-String -Path 'evals\\multi-model-verify\\test_multi_model_verify.py' -Pattern 'REQUIRED_REFERENCE_FILES|fallbacks|Options:|Canonical model id|no-backslash|backslash' -CaseSensitive:"'$false -Context 2,4 | Select-Object -First 80 | ForEach-Object { '"'{0,4}: {1}' -f "'$_.LineNumber,$_.Line }' in C:\Users\Brandon\Documents\parallax
 succeeded in 574ms:
   3: > **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
   5: **Goal:** Add `agents/flash-implementer.md`, a haiku-wrapper SDD implementer that delegates all code-writing to Gemini 3.6 Flash via the Antigravity CLI (agy) headlessly, with mechanically enforced route and authorship checks.
   7: **Architecture:** Architecture A from the spec (`docs/superpowers/specs/2026-07-25-flash-implementer-design.md`): a new wrapper agent beside the untouched `implementer.md`. The wrapper never types repo code (tools allowlist without Edit/Write + transcript/tree corroboration); every failure is loud (`STATUS: blocked`); reroute is consent-gated. Model literal single-sourced to the two agent files.
  11: **STATUS: FROZEN — CONVERGED.** Frozen 2026-07-25 by the backup-lane plan debate (Kimi K3-256k, 2 rounds); reopened same day by the primary reviewer's check-off (Sol, codex window reset early) and re-frozen at round 4: r1 FIX (5 blocking), r2 FIX (3 blocking + 2 minor), r3 FIX (4 blocking), r4 terminal PASS — primary check-off GRANTED with one trivial accepted amendment (applied). Every finding session-verified and accepted; amendments folded into spec, this plan, and the artifacts via SDD fix waves. The diff debate gates the merge as always. Tasks 5-6 contain ATTENDED steps (user-interactive); Task 6 additionally requires the plugin dev-loop (bump, update, restart).
  15: - The canonical implementer-lane literal is the RESOLVED ID `gemini-3.6-flash-medium`; after this cycle it appears ONLY in `agents/implementer.md` and `agents/flash-implementer.md` (spec section 5).
  43: """Contract pins for the Flash implementer lane (agents/flash-implementer.md).
  55: FLASH = REPO / "agents" / "flash-implementer.md"
 180:     assert "flash-implementer" in body
 241: - Create: `agents/flash-implementer.md`
 248: - [ ] **Step 1: Write `agents/flash-implementer.md` exactly as follows**
 252: name: flash-implementer
 286: ## Inputs (from the dispatching controller)
 292: - A log-file path OUTSIDE the workspace (the controller owns it; you never
 410: the Flash lane instead — see `flash-implementer.md`, the supervisor-pattern
 422: git add agents/flash-implementer.md agents/implementer.md
 423: git commit -m "0.12.0: add flash-implementer agent, mark shared contract"
 446: "| `agents/flash-implementer.md` | Zero-judgment Flash lane: haiku wrapper drives Gemini Flash through the Antigravity CLI headlessly; route + authorship evidence checked every run (model literal pinned in the file) |"
 458: "a vendor swap uses the supervisor pattern `agents/flash-implementer.md` implements (documented in `agents/implementer.md`'s Lane note):"
 491: - Consumes: `agents/flash-implementer.md`'s Lane note as the parse source.
 499: Resolve the INSTALLED copy's `agents/flash-implementer.md` under the
 601: absent = restore `true` and record in the SDD ledger:
 645: - None in-repo (scratch repo + probe records; SDD ledger notes)
 666: `parallax:flash-implementer` (fresh subagent) with a minimal task:
 669: scratchpad. The controller supplies ONLY these frozen inputs — the
 671: never a controller addition (Sol diff-debate F1). Expected report:
 675: `python hello.py` output; DEVIATIONS none. Controller re-runs
 677: controller-owned verification). Then COMMIT the green result in the
 683: Controller runs, directly in the scratch repo (split from a single
 702: `parallax:flash-implementer` with a trivial task in the scratch repo.
 715: SDD ledger: file created = the flag works in print mode (both bypass
 723: Write the dry-run + red-probe + skip-probe outcomes into the SDD ledger
 724: (`.superpowers/sdd/2026-07-25-flash-implementer/`), with log paths — the
 737: **Raw rounds:** docs/superpowers/plans/rounds/2026-07-25-flash-implementer/ (plan-round1-brief.md, plan-round{1,2}-transcript.txt — Kimi; sol-checkoff-round{1,2,3,4}-{brief,reply,header} — Sol; diff-round{n}-{brief,reply,header} — Sol mode-diff)
 777: | 25 | Task 6 green run patched the frozen dispatch input at runtime — the no-commands line was controller-added, not contract text, and lived only in the gitignored ledger | Sol diff-r1 | accepted; exact brief-borne closing line added to Dispatch step 1, test-pinned, plugin bumped 0.12.1, Task 6 Step 3 + 4b rerun with frozen inputs only | flash-dryrun.log:126 soft-deny vs flash-dryrun2.log clean; agent Dispatch step 1; test_flash_implementer.py dispatch pins |
 805: controller-added no-commands line (retained logs: flash-dryrun.log line
 823: sentinel rerun blocked at preflight 3) is recorded in the SDD ledger and

 succeeded in 612ms:
  27: REQUIRED_REFERENCE_FILES = [
  31:     "fallbacks.md",
  84:         for name in REQUIRED_REFERENCE_FILES:
  87:     def test_no_backslash_paths_anywhere(self):
  88:         for path in [SKILL_MD, *(REFERENCES / n for n in REQUIRED_REFERENCE_FILES)]:
  91:                 f"{path.name} contains a backslash - use forward slashes and"
 116:         assert re.search(r"Canonical model id: `[^`\n]+`", notes), (
 213:         # (rocket-fuel review finding, confirmed against fallbacks.md
 220:         fallbacks = read(REFERENCES / "fallbacks.md")
 221:         assert re.search(r"stale reply", fallbacks, re.IGNORECASE), (
 235:         declared = re.search(r"Canonical model id: `([^`\n]+)`", notes)
 264:             assert "Canonical model id" in src and \
 334: class TestFallbacks:
 339:     def fallbacks(self):
 340:         return read(REFERENCES / "fallbacks.md")
 343:         assert "codex --version" in self.fallbacks()
 346:         text = self.fallbacks()
 355:         text = self.fallbacks()
 361:         text = self.fallbacks()
 366:         text = self.fallbacks()
 371:         text = self.fallbacks()
 378:         text = self.fallbacks()
 388:         text = self.fallbacks()
 401:         text = self.fallbacks()
 414:         text = self.fallbacks()
 421:         text = self.fallbacks()
 431:         joined = self.fallbacks() + read(SKILL_MD)
 768:         # fallbacks.md; both fix cases grade application-checkpoint.md;
 776:         assert ("skills/multi-model-verify/references/fallbacks.md"
 803:              "surface": ["skills/multi-model-verify/references/fallbacks.md"]},
 816:     def test_select_cases_backslash_paths_normalized(self):
 817:         # git on Windows can hand back backslash separators; the mapping is
1614:         assert "Canonical model id" in body and \

 succeeded in 620ms:
   8: The per-task SDD implementer lane is pinned to Sonnet (`agents/implementer.md`,
 100: - Architecture **A**: new `agents/flash-implementer.md`; `implementer.md`
 105:   C (controller shells the CLI directly) rejected: no isolation, no report
 129:   per-task effort choice by any seat, no effort slot in the plan format —
 137: ### 1. Agent: `agents/flash-implementer.md`
 187:   --add-dir <workdir> --log-file <fresh per-task log>`. Fresh log file per
 190:   SDD execution record (the `.superpowers/sdd/<date>-<topic>/` workspace
 191:   the subagent-driven-development skill maintains) records per-task lane
 211:   never a controller-supplied constraint.
 273: `gemini-3.6-flash-medium`, living in `agents/flash-implementer.md` — the
 285:   `flash-implementer.md`; its "Nothing else in the plugin references the
 324: - Preflight cost: `agy models` is a network round-trip per task dispatch.
 325:   Deliberate — per-task freshness over quota thrift; the wrapper is fresh
 326:   per task, so there is no cheaper session-scoped cache to use.
 344: `agents/flash-implementer.md` under the installed copy's `installPath`,

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'docs\\superpowers\\specs\\2026-07-25-flash-implementer-design.md';"'$i=0;Get-Content -LiteralPath $p|%{$i++;if(($i-ge180-and$i-le220)-or($i-ge310-and$i-le350)){'"'{0,4}: {1}'-f"'$i,$_}}' in C:\Users\Brandon\Documents\parallax
 succeeded in 493ms:
 180:      (trust entries and allow rules already disagree on drive-letter
 181:      spelling on this machine), so the lane bans the rule CLASS rather
 182:      than attempting workdir path-matching;
 183:   4. `git status --porcelain` is EMPTY before dispatch (Sol check-off F2:
 184:      without a pre-dispatch baseline, pre-existing changes are
 185:      unattributable — a dirty tree is blocked, not absorbed).
 186: - Dispatch: `agy -p <brief> --model gemini-3.6-flash-medium
 187:   --add-dir <workdir> --log-file <fresh per-task log>`. Fresh log file per
 188:   attempt, written under the session scratchpad (never the repo tree — no
 189:   .gitignore dependency); each task's report carries its log path, and the
 190:   SDD execution record (the `.superpowers/sdd/<date>-<topic>/` workspace
 191:   the subagent-driven-development skill maintains) records per-task lane
 192:   provenance, which the diff-debate brief then carries. Adding an
 193:   implementer line to the frozen-plan debate record's Participants block is
 194:   declared scope for the plan debate to settle.
 195: - Brief-file lifecycle (Sol check-off F2): the brief is the SOLE transient
 196:   file the wrapper may create in the repo tree — a declared exception to
 197:   its never-write rule. Unique reserved name (`AGY-TASK-BRIEF-<unique>.md`,
 198:   unique suffix from the dispatch's log-file basename); collision
 199:   preflight (any existing `AGY-TASK-BRIEF-*` file in the workspace =
 200:   stale state = blocked); deletion guaranteed after agy exits — success,
 201:   failure, or interruption path alike — and always BEFORE any evidence
 202:   check, so the brief never appears in the attribution set.
 203: - Brief closing line (Sol diff-debate F1, live-verified 2026-07-25): the
 204:   brief ends with an exact test-pinned line telling Flash to run no
 205:   commands and attempt no verification — the wrapper verifies after agy
 206:   exits. Print mode auto-denies command execution, so a task whose
 207:   verbatim text carries a Verification command otherwise soft-denies and
 208:   blocks the green path (the first live dry-run reproduced exactly this;
 209:   the wrapper's blocked report was the designed loud failure). The line
 210:   is brief-borne — written by the wrapper itself per its Dispatch recipe,
 211:   never a controller-supplied constraint.
 212: - Route check, every run, on the log file:
 213:   1. `Print mode: starting` line present with `model="gemini-3.6-flash-medium"`
 214:      (the RESOLVED ID is the comparand — same string §5 pins as canonical);
 215:   2. `Propagating selected model override` line present (presence-only; its
 216:      display label is a second namespace and is NOT matched);
 217:   3. transcript/tree corroboration (evidence source corrected by Sol
 218:      check-off F1 — the `--log-file` log carries NO file actions, probed):
 219:      parse `conversationID="<uuid>"` from the log's `Print mode: starting`
 220:      line, read the brain transcript at
 310:   (trusted for the occasion), plus TWO failure probes (split by Sol
 311:   check-off F5 — the earlier single disjunctive probe was vacuous: a
 312:   wrapper refusing the contradictory brief would "pass" without ever
 313:   exercising the failure path): (a) a RAW agy probe, no agent, proving
 314:   the CLI's loud invalid-model rejection with no writes; (b) a
 315:   reachable-failure probe through the REAL agent — save the original
 316:   settings content, plant a NONMATCHING sentinel `write_file(` rule (the
 317:   preflight bans the class, so a sentinel triggers it without granting
 318:   any functional permission), dispatch a trivial task, require preflight
 319:   check 3 to block quoting the sentinel, then restore the saved settings
 320:   content (including on aborted runs) and confirm the restored file
 321:   matches the saved copy. Dev-loop ordering applies and is part of the task:
 322:   the agent is not live until `plugin.json` bumps,
 323:   `claude plugin update parallax@parallax` runs, and the session restarts.
 324: - Preflight cost: `agy models` is a network round-trip per task dispatch.
 325:   Deliberate — per-task freshness over quota thrift; the wrapper is fresh
 326:   per task, so there is no cheaper session-scoped cache to use.
 327: - Plan debate + diff debate (Sol): after the ~Jul 29 reset. The branch does
 328:   not merge without the diff debate's terminal PASS.
 329: - Behavioral battery: deliberately not run. Honest framing: this cycle
 330:   edits `references/frozen-plan-format.md`, a skill reference file that NO
 331:   behavioral case declares in its surface (and `agents/*` appears in no
 332:   surface either) — so `--changed` selects nothing by design, and the gap
 333:   itself is declared here for the debate to contest, alongside whether an
 334:   `agents/`-surfaced behavioral case (Flash-fails → wrapper reports
 335:   blocked, never substitutes) is wanted this cycle or deferred.
 336: - State-machine suite: `tools/check-drift.ps1` untouched → skipped
 337:   (CLAUDE.md opt-in rule).
 338: 
 339: ### 7. Scope edges
 340: 
 341: Version 0.12.0. Doctor gains a minimal agy row — binary version + pinned
 342: model present in `agy models`, no generation probe — and follows doctor's
 343: existing single-source discipline: the row PARSES the canonical ID out of
 344: `agents/flash-implementer.md` under the installed copy's `installPath`,
 345: carrying no literal of its own (the check-4 pattern). Drift-watch: the
 346: drift snapshot gains the `agy --version` string as an informational row
 347: (the snapshot already tracks the codex version — declining drift coverage
 348: for the youngest dependency while naming it the top risk would be
 349: inconsistent); full agy flag-surface drift coverage remains a non-goal this
 350: cycle. Other non-goals: reviewer lane untouched; no CI changes;

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$skill=Get-Content -Raw '"'skills\\multi-model-verify\\SKILL.md'; "'$overview=@'"'
Two equal-weight advisors — this session and a cross-vendor reviewer driven
through the codex CLI (canonical reviewer model:
references/model-prompting-notes.md) — verify and refute each other's
claims before the cheap implementer touches code.
'@; "'$pre=@'"'
   On failure follow references/fallbacks.md
   (degraded mode, visibly flagged; never silently skip cross-vendor review).
'@; "'$fb=Get-Content -Raw '"'skills\\multi-model-verify\\references\\fallbacks.md'; "'$opt='"'Options: [fix codex] [run degraded] [abort]'; [pscustomobject]@{OverviewTargetExact="'$skill.Contains($overview.TrimStart("`r","`n"));PreflightTargetExact=$skill.Contains($pre.TrimStart("`r","`n"));FallbackTargetExact=$fb.Contains($opt)} | Format-List' in C:\Users\Brandon\Documents\parallax
 succeeded in 496ms:

OverviewTargetExact  : False
PreflightTargetExact : False
FallbackTargetExact  : True


exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$skill=(Get-Content -Raw '"'skills\\multi-model-verify\\SKILL.md') -replace \""'`r`n","`n"; $overview="Two equal-weight advisors — this session and a cross-vendor reviewer driven`nthrough the codex CLI (canonical reviewer model:`nreferences/model-prompting-notes.md) — verify and refute each other'"'s"'`nclaims before the cheap implementer touches code.`n"; $pre="   On failure follow references/fallbacks.md`n   (degraded mode, visibly flagged; never silently skip cross-vendor review).`n"; [pscustomobject]@{OverviewLFExact=$skill.Contains($overview);PreflightLFExact=$skill.Contains($pre)} | Format-List' in C:\Users\Brandon\Documents\parallax
 succeeded in 536ms:

OverviewLFExact  : False
PreflightLFExact : False


exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Select-String -Path 'tools\\check-drift.ps1' -Pattern '\\"'$agyVersion|agy'"\\s*=|codex --help|codex.*flag|snapshot|newSnapshot|VersionToSave|app-server|--sandbox|flagsToCheck' -CaseSensitive:"'$false -Context 3,5 | ForEach-Object { '"'{0,4}: {1}' -f "'$_.LineNumber,$_.Line }' in C:\Users\Brandon\Documents\parallax
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Select-String -Path 'evals\\tools\\drift_statemachine_tests.ps1' -Pattern 'agy|codex|stub|PATH|python|snapshot|scenario|--help' -CaseSensitive:"'$false -Context 2,4 | ForEach-Object { '"'{0,4}: {1}' -f "'$_.LineNumber,$_.Line } | Select-Object -First 240' in C:\Users\Brandon\Documents\parallax
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'evals\\multi-model-verify\\test_multi_model_verify.py';"'$i=0;Get-Content -LiteralPath $p|%{$i++;if(($i-ge20-and$i-le130)-or($i-ge330-and$i-le445)){'"'{0,4}: {1}'-f"'$i,$_}}' in C:\Users\Brandon\Documents\parallax
 succeeded in 587ms:
  10: #   3. codex CLI    - the exec transport flags the skill's commands depend on
  11: #                     (--sandbox, --output-last-message, -m/-c, exec resume).
  14: # fixture (normalized line endings) and probe the codex exec flag surface.
  37: $SnapshotFile = Join-Path $PSScriptRoot "drift-snapshot.json"
 127: $agyVersion = ""
 130:     $agyVersion = (& $agyExe --version 2>$null | Select-Object -First 1)
 181:     foreach ($flag in @("--sandbox", "--output-last-message", "--model", "--config")) {
 183:             $findings += "[CRITICAL] codex exec --help ($codexVersion) no longer lists $flag - the skill's transport commands are broken; update SKILL.md + model-prompting-notes.md"
 194: $snapshot = $null
 195: if (Test-Path $SnapshotFile) {
 196:     $snapshot = Get-Content $SnapshotFile -Raw | ConvertFrom-Json
 200: # an empty snapshot field would also disable next week's change detection,
 204: $claudeVersionToSave = $claudeVersion
 205: $codexVersionToSave = $codexVersion
 206: $agyVersionToSave = $agyVersion
 207: $spVersionToSave = $spVersion
 208: if ($snapshot) {
 209:     if (-not $claudeVersionToSave -and $snapshot.claude) { $claudeVersionToSave = $snapshot.claude }
 210:     if (-not $codexVersionToSave -and $snapshot.codex) { $codexVersionToSave = $snapshot.codex }
 211:     if (-not $agyVersionToSave -and $snapshot.agy) { $agyVersionToSave = $snapshot.agy }
 212:     if (-not $spVersionToSave -and $snapshot.superpowers) { $spVersionToSave = $snapshot.superpowers }
 215: if ($snapshot -and $claudeVersion -and $snapshot.claude -and ($snapshot.claude -ne $claudeVersion)) {
 220:         $findings += "[WARN] Claude Code changed $($snapshot.claude) -> $claudeVersion but the changelog fetch failed (offline?) - retrying next run"
 221:         $claudeVersionToSave = $snapshot.claude  # do not advance; retry next run
 229:             elseif ($startIdx -ge 0 -and $lines[$i] -match ("^##\s+" + [regex]::Escape([string]$snapshot.claude) + "\b")) { $endIdx = $i; break }
 232:             $findings += "[WARN] Claude Code changed $($snapshot.claude) -> $claudeVersion but the changelog has no $claudeVersion entry yet - retrying next run"
 233:             $claudeVersionToSave = $snapshot.claude  # do not advance; retry next run
 241:                 $findings += "[WARN] Claude Code $($snapshot.claude) -> $claudeVersion changelog mentions parallax-relevant surfaces:"
 245:                 $notes += "Claude Code $($snapshot.claude) -> $claudeVersion - no parallax-relevant keywords in the changelog slice"
 251: if ($snapshot -and $codexVersion -and $snapshot.codex -and ($snapshot.codex -ne $codexVersion)) {
 252:     $notes += "codex CLI $($snapshot.codex) -> $codexVersion (flag surface re-probed above)"
 254: if ($snapshot -and $spVersion -and $snapshot.superpowers -and ($snapshot.superpowers -ne $spVersion)) {
 255:     $notes += "superpowers $($snapshot.superpowers) -> $spVersion (template canary re-hashed above)"
 258: # --- report, toast, snapshot ---------------------------------------------------
 282: $newSnapshot = @{
 283:     claude      = $claudeVersionToSave
 284:     codex       = $codexVersionToSave
 285:     agy         = $agyVersionToSave
 286:     superpowers = $spVersionToSave
 289: $newSnapshot | ConvertTo-Json | Set-Content -Path $SnapshotFile
 293: # snapshot advances regardless of triage outcome - without this record, a
 592:                                 Get-Content -Raw $briefPath | codex exec --sandbox read-only -m $model -c model_reasoning_effort=$effort --output-last-message $outPath - > $hdrPath 2>&1
 620:                                         # call pins --sandbox read-only, so

 succeeded in 649ms:
  20: SKILL_DIR = REPO_ROOT / "skills" / "multi-model-verify"
  21: HOOKS_JSON = REPO_ROOT / "hooks" / "hooks.json"
  22: HOOK_SCRIPT = REPO_ROOT / "hooks" / "superpowers-review-companion.ps1"
  23: SKILL_MD = SKILL_DIR / "SKILL.md"
  24: REFERENCES = SKILL_DIR / "references"
  25: EVALS_DIR = Path(__file__).resolve().parent
  26: 
  27: REQUIRED_REFERENCE_FILES = [
  28:     "debate-protocol.md",
  29:     "frozen-plan-format.md",
  30:     "model-prompting-notes.md",
  31:     "fallbacks.md",
  32:     "application-checkpoint.md",
  33: ]
  34: 
  35: 
  36: def read(path):
  37:     assert path.is_file(), f"missing file: {path}"
  38:     return path.read_text(encoding="utf-8")
  39: 
  40: 
  41: def load_runner_module():
  42:     """Import the behavioral runner as a module (its main() is guarded, its
  43:     module level is constants only) so pure functions are unit-testable."""
  44:     import importlib.util
  45:     path = REPO_ROOT / "evals" / "tools" / "run_behavioral_evals.py"
  46:     spec = importlib.util.spec_from_file_location("run_behavioral_evals", path)
  47:     mod = importlib.util.module_from_spec(spec)
  48:     spec.loader.exec_module(mod)
  49:     return mod
  50: 
  51: 
  52: def frontmatter(text):
  53:     match = re.match(r"^---\n(.*?)\n---\n", text, re.DOTALL)
  54:     assert match, "SKILL.md must start with YAML frontmatter"
  55:     return match.group(1)
  56: 
  57: 
  58: class TestSkillStructure:
  59:     def test_skill_md_exists(self):
  60:         assert SKILL_MD.is_file()
  61: 
  62:     def test_frontmatter_name_matches_directory(self):
  63:         fm = frontmatter(read(SKILL_MD))
  64:         name = re.search(r"^name:\s*(\S+)\s*$", fm, re.MULTILINE)
  65:         assert name, "frontmatter needs a name field"
  66:         assert name.group(1) == "multi-model-verify"
  67:         assert re.fullmatch(r"[a-z0-9]+(-[a-z0-9]+)*", name.group(1))
  68: 
  69:     def test_description_is_trigger_only(self):
  70:         fm = frontmatter(read(SKILL_MD))
  71:         desc = re.search(r"^description:\s*(.+)$", fm, re.MULTILINE)
  72:         assert desc, "frontmatter needs a description field"
  73:         text = desc.group(1).strip()
  74:         assert text.startswith("Use when"), "description must start 'Use when'"
  75:         assert len(text) <= 1024
  76:         # Workflow summaries in descriptions shortcut the skill body
  77:         # (superpowers writing-skills SDO finding) - keep them out.
  78:         for banned in ("round", "codex exec", "session id", "freeze"):
  79:             assert banned not in text.lower(), (
  80:                 f"description leaks workflow detail: {banned!r}"
  81:             )
  82: 
  83:     def test_reference_files_exist(self):
  84:         for name in REQUIRED_REFERENCE_FILES:
  85:             assert (REFERENCES / name).is_file(), f"missing references/{name}"
  86: 
  87:     def test_no_backslash_paths_anywhere(self):
  88:         for path in [SKILL_MD, *(REFERENCES / n for n in REQUIRED_REFERENCE_FILES)]:
  89:             text = read(path)
  90:             assert "\\" not in text, (
  91:                 f"{path.name} contains a backslash - use forward slashes and"
  92:                 " relative paths only"
  93:             )
  94: 
  95: 
  96: class TestTransportContract:
  97:     """The codex invocation shapes were live-verified 2026-07-12 on 0.144.1.
  98: 
  99:     These strings are load-bearing: get them wrong and debates silently run
 100:     on the wrong model, with write access, or lose cross-round state.
 101:     """
 102: 
 103:     def test_model_pinned_via_canonical_source(self):
 104:         # The reviewer model is a ONE-LINE swap: SKILL.md carries the
 105:         # canonical placeholder, and the declaration lives solely in
 106:         # model-prompting-notes.md (Sol holistic C2, built 0.5.0).
 107:         # These regexes mirror the executables' parse EXACTLY and assert
 108:         # parseability only - constraining the id to a vendor prefix or the
 109:         # effort to a fixed vocabulary here would make this test a second
 110:         # authority over the declaration, the very defect the canonical
 111:         # source exists to kill (Sol review round 1, 0.5.0).
 112:         text = read(SKILL_MD)
 113:         assert "-m <canonical-model-id>" in text
 114:         assert "model-prompting-notes.md" in text
 115:         notes = read(REFERENCES / "model-prompting-notes.md")
 116:         assert re.search(r"Canonical model id: `[^`\n]+`", notes), (
 117:             "the canonical model declaration must exist and be parseable"
 118:         )
 119:         assert re.search(r"Canonical reasoning effort: `[^`\n]+`",
 120:                          notes), (
 121:             "the canonical effort declaration must exist and be parseable"
 122:         )
 123: 
 124:     def test_sandbox_read_only(self):
 125:         text = read(SKILL_MD)
 126:         assert "--sandbox read-only" in text
 127: 
 128:     def test_effort_pinned(self):
 129:         text = read(SKILL_MD)
 130:         assert "model_reasoning_effort=<canonical-effort>" in text
 330:             "the finish line must route through the adjudication step"
 331:         )
 332: 
 333: 
 334: class TestFallbacks:
 335:     """Loud-degradation contract (Sol round-2 audit, 2026-07-12): degraded
 336:     mode is consent-gated, structured, and poisons downstream PASSes. These
 337:     pin the specific rules, not word presence."""
 338: 
 339:     def fallbacks(self):
 340:         return read(REFERENCES / "fallbacks.md")
 341: 
 342:     def test_preflight_documented(self):
 343:         assert "codex --version" in self.fallbacks()
 344: 
 345:     def test_consent_gate_principle(self):
 346:         text = self.fallbacks()
 347:         assert re.search(
 348:             r"no transition that reduces vendor diversity,\s+evidence"
 349:             r"\s+quality,\s+or\s+(conversation\s+)?continuity\s+.*?without"
 350:             r"\s+explicit\s+.*?consent",
 351:             text, re.IGNORECASE | re.DOTALL,
 352:         ), "the governing consent-gate principle is missing"
 353: 
 354:     def test_no_automatic_degraded_entry(self):
 355:         text = self.fallbacks()
 356:         assert re.search(r"never enters? degraded mode (automatically|on its own)",
 357:                          text, re.IGNORECASE)
 358:         assert re.search(r"fix codex|run degraded|abort", text, re.IGNORECASE)
 359: 
 360:     def test_bounded_recovery_before_gate(self):
 361:         text = self.fallbacks()
 362:         assert re.search(r"one (automatic )?retry.*same (model|parameters)",
 363:                          text, re.IGNORECASE)
 364: 
 365:     def test_unattended_fails_closed(self):
 366:         text = self.fallbacks()
 367:         assert "BLOCKED/DEGRADED-NOT-AUTHORIZED" in text
 368:         assert re.search(r"never infer consent", text, re.IGNORECASE)
 369: 
 370:     def test_failure_class_catch_all(self):
 371:         text = self.fallbacks()
 372:         assert re.search(
 373:             r"any (codex|transport) failure not (listed|named).*consent gate",
 374:             text, re.IGNORECASE | re.DOTALL,
 375:         ), "unlisted codex failure classes need the catch-all rule"
 376: 
 377:     def test_session_loss_is_gated(self):
 378:         text = self.fallbacks()
 379:         assert re.search(
 380:             r"(session|continuity).{0,120}(consent gate|not automatic)",
 381:             text, re.IGNORECASE | re.DOTALL,
 382:         ), "losing session continuity must route through the consent gate"
 383: 
 384:     def test_quota_limit_is_named_class(self):
 385:         # Session/weekly usage limits are not transport blips: no retry
 386:         # (the window will not clear in seconds), straight to the consent
 387:         # gate with codex's reset time surfaced.
 388:         text = self.fallbacks()
 389:         assert "quota-exhausted" in text
 390:         assert re.search(r"(session|weekly).{0,60}(limit|quota|cap)",
 391:                          text, re.IGNORECASE)
 392:         assert re.search(r"skip the retry", text, re.IGNORECASE)
 393:         assert re.search(r"reset time", text, re.IGNORECASE)
 394: 
 395:     def test_missing_rollout_is_named_class(self):
 396:         # Probed 2026-07-24 (codex-cli 0.144.1): resuming a nonexistent
 397:         # session id fails deterministically with "no rollout found for
 398:         # thread id <id> (code -32600)" and writes NO reply file. Never
 399:         # transient: skip the retry, straight to the session-loss
 400:         # consent gate (jinn intake, pinned 6c46f57).
 401:         text = self.fallbacks()
 402:         assert "no rollout found" in text
 403:         assert "-32600" in text
 404:         assert "missing-rollout" in text, (
 405:             "the class NAME must be pinned - README and the notes"
 406:             " cross-reference it"
 407:         )
 408:         assert re.search(r"rollout.{0,240}skip the retry", text,
 409:                          re.IGNORECASE | re.DOTALL), (
 410:             "the missing-rollout signature must skip the retry"
 411:         )
 412: 
 413:     def test_stale_evidence_is_struck(self):
 414:         text = self.fallbacks()
 415:         assert re.search(r"struck until re-verified", text, re.IGNORECASE)
 416: 
 417:     def test_route_mismatch_is_named_class(self):
 418:         # The effective-route check (0.6.0): a header/canonical mismatch is
 419:         # not transient - skip the retry, straight to the gate - and the
 420:         # reply from the mismatched call never enters the debate.
 421:         text = self.fallbacks()
 422:         assert "route-mismatch" in text
 423:         assert "DISCARDED" in text, (
 424:             "the mismatched call's reply must be discarded unread"
 425:         )
 426:         assert re.search(r"Logged in using\s+ChatGPT", text), (
 427:             "the auth class must pin the first-party STATE, not exit-0"
 428:         )
 429: 
 430:     def test_missing_reference_refusal(self):
 431:         joined = self.fallbacks() + read(SKILL_MD)
 432:         assert re.search(r"References/", joined)
 433:         assert re.search(r"hard stop", joined, re.IGNORECASE)
 434: 
 435: 
 436: class TestDegradedStatusFields:
 437:     """Structured degraded status (Sol round-2 fix B): parseable fields, not
 438:     prose, so mode diff can enforce the poisoning rule."""
 439: 
 440:     def test_frozen_plan_has_verification_status_field(self):
 441:         text = read(REFERENCES / "frozen-plan-format.md")
 442:         assert "**Verification status:** FULL | DEGRADED" in text
 443:         assert "**Degradation:**" in text
 444:         assert "**Authorized by:**" in text
 445: 

 succeeded in 679ms:
   6: # lifecycle - with every external dependency stubbed:
  11: #   - stub claude.cmd / codex.ps1 on a prepended PATH, behavior selected per
  12: #     scenario via CLAUDE_STUB_MODE / CODEX_STUB_MODE
  26: # Scenarios (triage-timeout LAST - its killed stub can orphan a child that
  28: #   carry-forward      failed version probe keeps the snapshot value
  42: # Runtime: several minutes - four scenarios re-run the full pytest suite
  55: $RepoRoot = Split-Path (Split-Path $HarnessRoot)  # repo root
  56: $Root = Join-Path $env:TEMP ("parallax-sm-" + (Get-Date -Format "HHmmss") + "-" + (Get-Random -Maximum 9999))
  57: New-Item -ItemType Directory -Force -Path $Root | Out-Null
  61: foreach ($name in @("PATH", "USERPROFILE", "HOME", "TEMP", "TMP",
  62:                     "CLAUDE_STUB_MODE", "CODEX_STUB_MODE",
  86: # --- workspace: clone, stubs, fake profile -----------------------------------
  88: $Clone = Join-Path $Root "repo"
  91: Copy-Item (Join-Path $RepoRoot "tools\check-drift.ps1") (Join-Path $Clone "tools\check-drift.ps1") -Force
  92: $DriftScript = Join-Path $Clone "tools\check-drift.ps1"
  93: if (-not (Test-Path $DriftScript)) {
  97: $ToolsDir = Join-Path $Clone "tools"
  98: $ReportsDir = Join-Path $ToolsDir "drift-reports"
  99: $SnapshotFile = Join-Path $ToolsDir "drift-snapshot.json"
 100: $PendingFile = Join-Path $ToolsDir "drift-pending.json"
 102: $StubDir = Join-Path $Root "stubs"
 103: New-Item -ItemType Directory -Force -Path $StubDir | Out-Null
 105: # claude.cmd: version probe + the triage agent, mode via CLAUDE_STUB_MODE.
 109: # The hang mode never exits on its own so the kill path always fires.
 113: if "%CLAUDE_STUB_MODE%"=="version-fail" exit /b 1
 114: if "%CLAUDE_STUB_MODE%"=="hang" goto hang
 115: if "%CLAUDE_STUB_MODE%"=="blocked" goto blocked
 116: if "%CLAUDE_STUB_MODE%"=="noaction" goto noaction
 117: if "%CLAUDE_STUB_MODE%"=="fixes" goto fixes
 118: if "%CLAUDE_STUB_MODE%"=="badfix" goto badfix
 119: echo stub agent ran and produced no verdict line
 123: if "%CLAUDE_STUB_MODE%"=="version-fail" (
 124: echo claude: unexpected stub failure
 137: echo VERDICT: BLOCKED stub cannot resolve this class of drift
 141: echo Findings are stub-environment noise; nothing to change.
 146: echo stub fix marker> STUB-FIX.txt
 147: echo Applied stub fix.
 148: echo VERDICT: FIXES-APPLIED stub state-machine fix
 152: echo def test_stub_planted_failure():> evals\multi-model-verify\test_zz_stub_planted.py
 153: echo     assert False, "planted by the state-machine harness">> evals\multi-model-verify\test_zz_stub_planted.py
 155: echo VERDICT: FIXES-APPLIED stub fix that breaks the gate
 157: '@ | Set-Content -Path (Join-Path $StubDir "claude.cmd") -Encoding ASCII
 159: # codex.ps1: healthy transport by default.
 160: #   drop-config  - exec --help omits --config, so the flag probe raises
 168: if ($argList -contains "--version") { Write-Output "codex-cli 7.7.7"; exit 0 }
 170:     if ($env:CODEX_STUB_MODE -eq "auth-fail") { Write-Output "Not logged in"; exit 1 }
 174: if ($argList.Count -ge 1 -and $argList[0] -eq "exec" -and ($argList -contains "--help")) {
 176:     if ($env:CODEX_STUB_MODE -eq "drop-config") {
 179:     Write-Output "usage: codex exec [flags] $flags"
 186:     # healthy stub reflects the requested route back; wrong-model simulates
 198:     if ($env:CODEX_STUB_MODE -eq "wrong-model") { $reqModel = "stub-swapped-model" }
 204:     if ($env:CODEX_STUB_MODE -eq "bad-review") {
 207:     Set-Content -Path $argList[$idx + 1] -Value $verdict
 210: '@ | Set-Content -Path (Join-Path $StubDir "codex.ps1") -Encoding ASCII
 212: $env:PATH = "$StubDir;" + $env:PATH
 214: # Harness-owned TEMP: the script derives its worktree path from $env:TEMP,
 218: $FakeTemp = Join-Path $Root "temp"
 219: New-Item -ItemType Directory -Force -Path $FakeTemp | Out-Null
 226: $FakeProfile = Join-Path $Root "profile"
 227: $SpTemplate = Join-Path $FakeProfile ".claude\plugins\cache\claude-plugins-official\superpowers\6.1.1\skills\requesting-code-review\code-reviewer.md"
 228: $FakeSp = Join-Path $FakeProfile ".claude\plugins\cache\claude-plugins-official\superpowers\6.1.1"
 229: New-Item -ItemType Directory -Force -Path (Split-Path $SpTemplate) | Out-Null
 230: $PinnedFixture = Join-Path $Clone "evals\multi-model-verify\fixtures\superpowers-code-reviewer-6.1.1.md"
 233: # resolves its job persistence path through USERPROFILE-expanded folders,
 235: # with "The Persistence Path does not exist" (probed 2026-07-13 - the miss
 238:     New-Item -ItemType Directory -Force -Path (Join-Path $FakeProfile $dir) | Out-Null
 243:             @{ version = "6.1.1"; installPath = $FakeSp }
 247: ConvertTo-Json -InputObject $registry -Depth 5 | Set-Content -Path (Join-Path $FakeProfile ".claude\plugins\installed_plugins.json")
 248: [IO.File]::WriteAllText((Join-Path $FakeProfile ".gitconfig"),
 259: function Set-Snapshot($claude, $codex, $sp) {
 260:     $snap = @{ claude = $claude; codex = $codex; superpowers = $sp; updated = "2026-01-01T00:00:00" }
 261:     ConvertTo-Json -InputObject $snap | Set-Content -Path $SnapshotFile
 265:     # Defaults match the stub versions exactly: no version-change notes, no
 266:     # changelog fetch - every scenario runs fully offline. The superpowers
 267:     # template is restored to the pinned fixture so only the scenario that
 269:     Set-Snapshot "1.2.3" "7.7.7" "6.1.1"
 271:     if (Test-Path $PendingFile) { Remove-Item $PendingFile -Force }
 272:     if (Test-Path $ReportsDir) { Remove-Item -Recurse -Force $ReportsDir }
 275: function Invoke-Drift($scenario, $claudeMode, $codexMode, $timeoutMs) {
 277:     Write-Output "SCENARIO $scenario"
 278:     $toastLog = Join-Path $Root "$scenario-toasts.txt"
 279:     if (Test-Path $toastLog) { Remove-Item $toastLog -Force }
 282:     $env:CLAUDE_STUB_MODE = $claudeMode
 283:     $env:CODEX_STUB_MODE = $codexMode
 288:     if (Test-Path $ReportsDir) {
 297:     if (Test-Path $script:LastToastLog) { return (Get-Content $script:LastToastLog -Raw) }
 302:     if (-not (Test-Path $PendingFile)) { return @() }
 316: function Complete-Scenario($failsBefore) {
 327: # --- scenario: carry-forward ---------------------------------------------------
 329: # snapshot's last known-good value (an empty field would disable next week's
 330: # change detection). The failing stub also fails the triage call -> manual.
 334: Set-Snapshot "9.9.9" "7.7.7" "6.1.1"
 338: $snap = Get-Content $SnapshotFile -Raw | ConvertFrom-Json
 343: Complete-Scenario $b
 345: # --- scenario: blocked-verdict ---------------------------------------------------
 355: ConvertTo-Json -InputObject $old -Depth 3 | Set-Content -Path $PendingFile
 357: Assert-True ($script:LastReport -match "Auto-triage not trusted \(exit 0; verdict 'BLOCKED") "BLOCKED falls to the manual path"
 361: Complete-Scenario $b
 363: # --- scenario: no-verdict --------------------------------------------------------
 373: Complete-Scenario $b
 375: # --- scenario: critical-dismissal ------------------------------------------------
 387: Complete-Scenario $b
 389: # --- scenario: warn-only-silence --------------------------------------------------
 397: Add-Content -Path $SpTemplate -Value "`nAn upstream edit that keeps both fingerprint literals.`n"
 403: Assert-True (-not (Test-Path $PendingFile)) "WARN-only dismissal leaves no pending entry"
 404: Complete-Scenario $b
 406: # --- scenario: pending-auto-clear -------------------------------------------------
 416: ConvertTo-Json -InputObject $ghost -Depth 3 | Set-Content -Path $PendingFile
 422: Complete-Scenario $b
 424: # --- scenario: fixes-applied ------------------------------------------------------
 425: # The full happy path: agent edits the worktree, script re-runs the pytest
 426: # gate itself, commits, verifies the commit landed, gets the stub
 443: Complete-Scenario $b
 445: # --- scenario: gate-failure --------------------------------------------------------
 448: # cannot verify. The stub plants a genuinely failing test, so only a real
 461: Complete-Scenario $b
 463: # --- scenario: malformed-review -----------------------------------------------------
 470: # 'bad-review' keeps codex's flag surface HEALTHY, so it raises no finding
 473: Add-Content -Path $SpTemplate -Value "`nAn upstream edit that keeps both fingerprint literals.`n"
 481: Complete-Scenario $b
 483: # --- scenario: commit-failure -----------------------------------------------------
 489: $hookPath = Join-Path $Clone ".git\hooks\pre-commit"
 490: [IO.File]::WriteAllText($hookPath, "#!/bin/sh`nexit 1`n")
 492: Remove-Item $hookPath -Force
 498: Complete-Scenario $b
 500: # --- scenario: route-mismatch ------------------------------------------------------
 508: # wrong-model keeps codex's flag surface healthy (no finding of its own) -
 510: Add-Content -Path $SpTemplate -Value "`nAn upstream edit that keeps both fingerprint literals.`n"
 518: Complete-Scenario $b
 520: # --- scenario: auth-preflight-fail -------------------------------------------------
 521: # A failed `codex login status` preflight skips the billable review call
 526: Add-Content -Path $SpTemplate -Value "`nAn upstream edit that keeps both fingerprint literals.`n"
 529: Assert-True ($script:LastReport -match 'cross-review UNAVAILABLE - codex auth not ready') "failed preflight records the auth reason"
 533: Complete-Scenario $b
 535: # --- scenario: triage-timeout (LAST - see header) ----------------------------------
 545: Complete-Scenario $b
 551:     Write-Output "ALL SCENARIOS PASS"
 559:     # a stub-laden PATH behind would be worse than any test it runs.
 564:         # Everything - clone, stubs, fake profile, and every worktree the
 566:         # timeout stub can hold a handle for a few seconds; retry briefly

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Select-String -Path 'evals\\tools\\run_behavioral_evals.py' -Pattern 'add_argument|--list|manual|skip' -CaseSensitive:"'$false -Context 2,4 | ForEach-Object { '"'{0,4}: {1}' -f "'$_.LineNumber,$_.Line }; Select-String -Path '"'evals\\multi-model-verify\\evals.json' -Pattern 'manual|surface|setup' -CaseSensitive:"'$false -Context 0,2 | Select-Object -First 50 | ForEach-Object { '"'{0,4}: {1}' -f "'$_.LineNumber,$_.Line }' in C:\Users\Brandon\Documents\parallax
 succeeded in 511ms:
   4: neither, so CI only runs `--list` as a self-test.
  13:     python run_behavioral_evals.py --list                 # CI self-test
  18: Cases with setup.manual are reported SKIPPED(manual) - they need state a
 144:     """Pure --changed selection. Returns (selected, skipped): selected is
 146:     entry-diff note; skipped is [case]. base_entries is {id: entry} parsed
 149:     toward skipping)."""
 151:     selected, skipped = [], []
 164:             skipped.append(case)
 167:     return selected, skipped
 307:     agent that skips the diff and reads the file cannot find the drift, so
 432:     if setup.get("manual"):
 433:         return "SKIPPED(manual)", setup["manual"], []
 684:     ap.add_argument("--case", action="append", help="run only these case ids")
 685:     ap.add_argument("--changed", nargs="?", const="", default=None,
 690:                          " every skip by name")
 691:     ap.add_argument("--list", action="store_true", help="list cases and exit (CI self-test)")
 692:     ap.add_argument("--model", default="sonnet",
 694:     ap.add_argument("--timeout", type=int, default=900,
 696:     ap.add_argument("--artifacts", type=Path,
 699:     ap.add_argument("--head", action="store_true",
 702:                          " skips the bump+update dance while iterating."
 727:         selected, skipped = select_cases(
 729:         for c in skipped:
 730:             print(f"SKIPPED(unchanged surface) {c['id']}")
   4:   "note": "Tier 3: executed by evals/tools/run_behavioral_evals.py (LOCAL-ONLY - needs authenticated claude + codex CLIs; CI only runs --list). Cases with setup.manual need state a fixture cannot fake and are skipped by the runner.",
   8:       "setup": { "with_reference": true },
   9:       "surface": ["skills/multi-model-verify/SKILL.md", "skills/multi-model-verify/references/model-prompting-notes.md", "skills/multi-model-verify/references/debate-protocol.md"],
  21:       "setup": { "with_reference": true, "diff_state": true },
  22:       "surface": ["skills/multi-model-verify/SKILL.md", "skills/multi-model-verify/references/model-prompting-notes.md", "skills/multi-model-verify/references/debate-protocol.md"],
  34:       "setup": { "no_codex": true, "with_reference": true },
  35:       "surface": ["skills/multi-model-verify/SKILL.md", "skills/multi-model-verify/references/fallbacks.md"],
  47:       "setup": { "with_reference": false },
  48:       "surface": ["skills/multi-model-verify/SKILL.md"],
  59:       "setup": { "with_reference": true, "mutation_tools": true },
  60:       "surface": ["skills/multi-model-verify/SKILL.md", "skills/multi-model-verify/references/application-checkpoint.md"],
  72:       "setup": { "with_reference": true, "mutation_tools": true },
  73:       "surface": ["skills/multi-model-verify/SKILL.md", "skills/multi-model-verify/references/application-checkpoint.md"],
  84:       "setup": { "with_reference": true },
  85:       "surface": ["skills/multi-model-verify/SKILL.md", "skills/multi-model-verify/references/model-prompting-notes.md", "skills/multi-model-verify/references/debate-protocol.md"],

2026-07-26T04:17:11.130805Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_reasoning_summaries` at line 86 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "git grep -n '0\\.12\\.0\\|\"version\"' -- '.claude-plugin/*' 'README.md' 'marketplace.json' '.claude-plugin/**' | Select-Object -First 100" in C:\Users\Brandon\Documents\parallax
 succeeded in 552ms:
.claude-plugin/plugin.json:3:  "version": "0.12.1",

2026-07-26T04:17:18.033398Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_reasoning_summaries` at line 86 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'evals\\multi-model-verify\\test_multi_model_verify.py';"'$i=0;Get-Content -LiteralPath $p|%{$i++;if($i-ge225-and$i-le275){'"'{0,4}: {1}'-f"'$i,$_}}' in C:\Users\Brandon\Documents\parallax
 succeeded in 519ms:
 225:     def test_reviewer_id_has_single_source(self):
 226:         # A hardcoded reviewer model literal anywhere but the canonical
 227:         # declaration file re-opens the partial-migration hole: that surface
 228:         # keeps calling the OLD reviewer after a swap. The executables parse
 229:         # the declarations at runtime instead.
 230:         # Two markers: the contiguous flag form, and the canonical id
 231:         # literal itself (parsed from the notes) - the literal catches
 232:         # argument-list syntax like '"-m", "<id>"' that the flag form
 233:         # misses (Sol review round 1, 0.5.0).
 234:         notes = read(REFERENCES / "model-prompting-notes.md")
 235:         declared = re.search(r"Canonical model id: `([^`\n]+)`", notes)
 236:         assert declared, "canonical declaration missing - cannot sweep"
 237:         markers = ("-m gpt" + "-", declared.group(1))
 238:         # ...and a syntax-aware shape check: ANY literal that looks like a
 239:         # model id (vendor-dash-digit) following -m, in flag, quoted, or
 240:         # argument-list form, is forbidden regardless of the CURRENT
 241:         # declaration - after a swap, a stale OLD id matches neither
 242:         # marker above (Sol review round 2, 0.5.0). Placeholders
 243:         # (-m <canonical-model-id>) and variables (-m $model / "-m", model)
 244:         # do not match the shape.
 245:         id_after_m = re.compile(r'-m[\s",]+["\x27]?[A-Za-z][A-Za-z0-9.]*-\d')
 246:         notes_name = "model-prompting-notes.md"
 247:         offenders = []
 248:         for pattern in ("skills/**/*.md", "commands/*.md", "tools/*.ps1",
 249:                         "evals/**/*.py", "evals/**/*.json", "evals/**/*.ps1",
 250:                         "README.md", "CLAUDE.md", "hooks/*"):
 251:             for f in REPO_ROOT.glob(pattern):
 252:                 if f.is_file() and f.name != notes_name:
 253:                     text = read(f)
 254:                     if any(mk in text for mk in markers) or id_after_m.search(text):
 255:                         offenders.append(str(f.relative_to(REPO_ROOT)))
 256:         assert not offenders, (
 257:             f"hardcoded reviewer model literal outside {notes_name}:"
 258:             f" {offenders}"
 259:         )
 260:         # ...and both executables actually parse the canonical source.
 261:         runner = read(REPO_ROOT / "evals" / "tools" / "run_behavioral_evals.py")
 262:         drift = read(REPO_ROOT / "tools" / "check-drift.ps1")
 263:         for src in (runner, drift):
 264:             assert "Canonical model id" in src and \
 265:                 "Canonical reasoning effort" in src, (
 266:                     "executable surfaces must parse the canonical"
 267:                     " declarations, not hardcode them"
 268:                 )
 269: 
 270:     def test_session_id_capture_documented(self):
 271:         text = read(SKILL_MD)
 272:         assert "session id" in text.lower()
 273: 
 274:     def test_reply_captured_to_file(self):
 275:         # codex exec prints a full multi-KB transcript; without this flag the

2026-07-26T04:17:38.189255Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_reasoning_summaries` at line 86 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Select-String -Path 'tools\\check-drift.ps1' -Pattern '\\bpython\\b|python3|pytest' -CaseSensitive:"'$false -Context 2,3 | ForEach-Object { '"'{0,4}: {1}' -f "'$_.LineNumber,$_.Line }; Select-String -Path '"'evals\\tools\\drift_statemachine_tests.ps1' -Pattern 'pytest|python' -CaseSensitive:"'$false -Context 2,3 | ForEach-Object { '"'{0,4}: {1}' -f "'$_.LineNumber,$_.Line }' in C:\Users\Brandon\Documents\parallax
 succeeded in 489ms:
 345: #   - The agent gets NO git, NO codex, NO shell beyond python: it can only
 347: #   - The script independently re-runs pytest and inspects the diff before
 396: owns git and runs the pytest gate itself on whatever you change).
 515:                 python -m pytest evals -q > $null 2>&1
 577:                                 "made the fix below in the parallax plugin repo; the pytest gate",
   5: # trust matrix, pytest gate, commit, cross-review, toast matrix, pending
  34: #   fixes-applied      diff -> pytest gate -> commit -> cross-review -> toast
  42: # Runtime: several minutes - four scenarios re-run the full pytest suite
  44: # or via pytest with PARALLAX_STATEMACHINE=1 set
 108: # script's own pytest gate is what has to catch it.
 253: # AND guards the pytest wrapper for this harness against recursing when the
 425: # The full happy path: agent edits the worktree, script re-runs the pytest
 428: # pytest run inside the worktree).
 449: # pytest run can catch it. SLOW (real pytest run).
 485: # success, discard the changes, keep no branch. SLOW (real pytest run).
 504: # lands; the human merge decision is the real gate. SLOW (real pytest run).
 522: # entirely and records the crisp auth reason. SLOW (real pytest run).

2026-07-26T04:17:45.618837Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_reasoning_summaries` at line 86 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'docs\\superpowers\\plans\\2026-07-25-flash-implementer.md';"'$i=0;Get-Content -LiteralPath $p|%{$i++;if($i-ge724-and$i-le755){'"'{0,4}: {1}'-f"'$i,$_}}' in C:\Users\Brandon\Documents\parallax
 succeeded in 525ms:
 724: (`.superpowers/sdd/2026-07-25-flash-implementer/`), with log paths — the
 725: diff-debate brief cites them.
 726: 
 727: ---
 728: 
 729: ## Debate record
 730: 
 731: **Participants:** Fable 5 (session) / Kimi K3 `kimi-code/k3-256k` (kimi-cli 1.49.0, session 4ba130bb-1615-4c95-9afc-d1e3049bb857) / GPT-5.6 Sol (codex exec, sessions 019f9ad5-e318-7831-8a3f-0c02c94caa8e primary check-off, 019f9c24-8351-7942-aeea-06f87774d26b mode-diff)
 732: **Rounds used:** 2 of 4 (Kimi, plan mode) + 4 of 4 (Sol, primary check-off: r1 FIX, r2 FIX, r3 FIX, r4 terminal) + 3 of 4 (Sol, mode-diff at merge: r1 FIX, r2 FIX, r3 terminal PASS — resolved rows 25-26)
 733: **Outcome:** converged with amendments
 734: **Verification status:** FULL
 735: **Degradation:** quota-exhausted (primary lane at the plan gate; backup lane substituted for rounds 1-2, primary check-off ran on the reset window)
 736: **Authorized by:** user at round 1 (backup-lane substitution; the user's standing ruling retained the primary check-off before branch close)
 737: **Raw rounds:** docs/superpowers/plans/rounds/2026-07-25-flash-implementer/ (plan-round1-brief.md, plan-round{1,2}-transcript.txt — Kimi; sol-checkoff-round{1,2,3,4}-{brief,reply,header} — Sol; diff-round{n}-{brief,reply,header} — Sol mode-diff)
 738: 
 739: Backup-lane note: Kimi ran read-only via a custom agent-file
 740: (ReadFile/ReadMediaFile/Glob/Grep/SetTodoList; pre-review write-probe
 741: refused) against a throwaway clone — the real tree was never exposed.
 742: Route evidence per round: `Using LLM model: provider='managed:kimi-code'
 743: model='k3-256k'` in ~/.kimi/logs/kimi.log (client-side class). Clone
 744: `git status` after round 2: sole untracked file was the session-authored
 745: brief (retained evidence: rounds/.../clone-status-after-round2.txt).
 746: Sol round 1 route: header block model gpt-5.6-sol / provider openai /
 747: sandbox read-only / effort high.
 748: 
 749: ### Resolved points
 750: 
 751: | # | Claim | Raised by | Outcome | Evidence |
 752: |---|-------|-----------|---------|----------|
 753: | 1 | Task 4 doctor text trips the plan's own literal sweep | Kimi r1 | accepted; doctor text reworded (5391455) | plan Task 4 vs test sweep globs |
 754: | 2 | Task 1 expected parity failure signature wrong (FileNotFoundError first) | Kimi r1 | accepted; expectation corrected | test_flash_implementer.py `_read` order |
 755: | 3 | README drop-ins absence pin vacuous (line-wrapped source) | Kimi r1 | accepted; presence pins added | README.md:132-133 line break |

2026-07-26T04:17:55.619161Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_reasoning_summaries` at line 86 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git diff --stat main..HEAD; git diff --name-only main..HEAD; git show --stat --oneline e432087' in C:\Users\Brandon\Documents\parallax
 succeeded in 455ms:
 .../plans/2026-07-25-kimi-backup-lane.md           | 726 +++++++++++++++++++++
 .../specs/2026-07-25-kimi-backup-lane-design.md    | 328 ++++++++++
 2 files changed, 1054 insertions(+)
docs/superpowers/plans/2026-07-25-kimi-backup-lane.md
docs/superpowers/specs/2026-07-25-kimi-backup-lane-design.md
e432087 0.13.0: kimi backup lane plan candidate
 .../plans/2026-07-25-kimi-backup-lane.md           | 726 +++++++++++++++++++++
 1 file changed, 726 insertions(+)

2026-07-26T04:18:00.105437Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_reasoning_summaries` at line 86 column 5
Repository/input inventory and full candidate/spec reads are complete. The traceability map is now being validated against live 0.12.0 contracts and executable test behavior.
  ✓ Inventory repository instructions and pin branch/head/review inputs
  ✓ Map every approved spec requirement to candidate plan tasks and embedded pins
  → Check candidate against existing 0.12.0 contracts and repository tests/docs
  • Adversarially verify findings and issue cited PASS/FIX/ESCALATE with UNVERIFIED list
2026-07-26T04:18:11.600397Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_reasoning_summaries` at line 86 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$env:PYTHONDONTWRITEBYTECODE='"'1'; python -m pytest evals -q -p no:cacheprovider" in C:\Users\Brandon\Documents\parallax
2026-07-26T04:18:15.212545Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 1.2 seconds
Output:
python: 
Line |
   2 |  $env:PYTHONDONTWRITEBYTECODE='1'; python -m pytest evals -q -p no:cac …
     |                                    ~~~~~~
     | The term 'python' is not recognized as a name of a cmdlet, function, script file, or executable program.
Check the spelling of the name, or if a path was included, verify that the path is correct and try again.

 exited 1 in 1201ms:
python: 
Line |
   2 |  $env:PYTHONDONTWRITEBYTECODE='1'; python -m pytest evals -q -p no:cac …
     |                                    ~~~~~~
     | The term 'python' is not recognized as a name of a cmdlet, function, script file, or executable program.
Check the spelling of the name, or if a path was included, verify that the path is correct and try again.

2026-07-26T04:18:15.236274Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_reasoning_summaries` at line 86 column 5
codex
The contract comparison confirms the replacement-pin concern: both SKILL.md “replace” targets are absent even after normalizing line endings, while the fallback option target exists. The baseline pytest command is currently UNVERIFIED because this shell has no `python` executable; I’ll still inspect its static contracts and check whether the Windows launcher is available without treating tool absence as a plan defect.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Command py,python3,pwsh,powershell -ErrorAction SilentlyContinue | Select-Object Name,Source,Version; where.exe py 2>$null; where.exe python3 2>$null' in C:\Users\Brandon\Documents\parallax
2026-07-26T04:18:22.135386Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 1.2 seconds
Output:

Name           Source                                                    Version
----           ------                                                    -------
pwsh.exe       C:\Program Files\PowerShell\7\pwsh.exe                    7.6.3.0
powershell.exe C:\WINDOWS\System32\WindowsPowerShell\v1.0\powershell.exe 10.0.26100.8875


 exited 1 in 1188ms:

Name           Source                                                    Version
----           ------                                                    -------
pwsh.exe       C:\Program Files\PowerShell\7\pwsh.exe                    7.6.3.0
powershell.exe C:\WINDOWS\System32\WindowsPowerShell\v1.0\powershell.exe 10.0.26100.8875


2026-07-26T04:18:22.246813Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_reasoning_summaries` at line 86 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$i=0;Get-Content -LiteralPath '"'CLAUDE.md'|%{"'$i++;'"'{0,4}: {1}'-f"'$i,$_}' in C:\Users\Brandon\Documents\parallax
 succeeded in 566ms:
   1: # parallax — Claude Code plugin (NOT a WoW addon)
   2: 
   3: This repo is developer tooling: a Claude Code plugin providing cross-model
   4: verification (session ⇄ cross-vendor reviewer debates; the reviewer lane is
   5: declared in the skill's model-prompting-notes.md) plus its eval harness. It lives
   6: under KitnDev for convenience, but the WoW addon family conventions in
   7: `../AGENTS.md` (dev loop, /reload, luacheck/busted, 12.0 API rules,
   8: References/) do NOT apply here — only the git basics do (feature branches
   9: for real work, lowercase imperative commits, no AI attribution).
  10: 
  11: ## Verification
  12: - `python evals/tools/skill_lint.py skills/multi-model-verify --strict`
  13: - `python evals/tools/skill_scanner.py skills`
  14: - `python evals/tools/run_trigger_evals.py`
  15: - `python -m pytest evals -q`
  16: CI runs all four on every push (.github/workflows/skill-evals.yml).
  17: 
  18: Two suites are local-only and opt-in, so run them by hand when you touch
  19: what they cover:
  20: - skill/prompt changes -> `python evals/tools/run_behavioral_evals.py`
  21:   (real headless runs, graded by the cross-vendor reviewer; `--head` tests
  22:   the checkout instead of the installed cache; for small contract edits
  23:   `--changed` runs only cases whose declared surface intersects the diff
  24:   vs main, printing every skip by name).
  25: - `tools/check-drift.ps1` changes -> run
  26:   `evals/tools/drift_statemachine_tests.ps1` (or, to go through pytest:
  27:   `$env:PARALLAX_STATEMACHINE = "1"; python -m pytest evals -q` — it is
  28:   PowerShell, so no `VAR=1 cmd` prefix). Drives the real script through its
  29:   whole state machine offline against stub CLIs. Slow: four scenarios
  30:   re-run the full pytest suite inside a disposable worktree.
  31: 
  32: ## Dev loop
  33: The plugin is installed user-scope from a LOCAL marketplace pointing at
  34: this working copy, but installs are VERSIONED CACHE COPIES — checkout
  35: edits are NOT live until you: bump `.claude-plugin/plugin.json`, run
  36: `claude plugin update parallax@parallax` (qualified name required),
  37: and restart the session when hooks/ or skills/ changed. A restart alone
  38: only reloads the cached version. GitHub remote (Bmwascher/parallax,
  39: private) serves stable installs on other machines.
  40: 
  41: ## Skill editing rules
  42: The multi-model-verify skill's transport commands (codex exec flags, resume
  43: syntax) are LIVE-VERIFIED contracts locked by evals/multi-model-verify/
  44: test_multi_model_verify.py — change the tests first (they encode review
  45: findings), then the skill.

codex
The plan is not gate-ready. The defects are now repo-verified rather than speculative: one consent-flow contradiction, missing mode-level routing, several false-negative pins (notably resumed `-m/--thinking` and exact tools), three line-join/insertion failures, and a Task 6 that leaves substantive PowerShell/test design to the implementer. I’m doing the closing citation pass and separating runtime items that remain unverified.
All candidate findings were rechecked against the pinned branch, approved spec, and current 0.12.0 contract text. Runtime suites remain separately unverified because no Python executable exists in this shell.
  ✓ Inventory repository instructions and pin branch/head/review inputs
  ✓ Map every approved spec requirement to candidate plan tasks and embedded pins
  ✓ Check candidate against existing 0.12.0 contracts and repository tests/docs
  ✓ Adversarially verify findings and issue cited PASS/FIX/ESCALATE with UNVERIFIED list
2026-07-26T04:20:37.144971Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_reasoning_summaries` at line 86 column 5
codex
The candidate is not implementation-ready.

1. **Consent-gate behavior contradicts the trigger rule.** The plan permanently adds the backup option to the shared banner ([plan:402](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:402)), while both the spec and the inserted prose say it is offered automatically only for named classes and otherwise only on request ([spec:22](/C:/Users/Brandon/Documents/parallax/docs/superpowers/specs/2026-07-25-kimi-backup-lane-design.md:22), [plan:432](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:432)). The existing banner also describes only one mode’s verification gap ([fallbacks.md:13](/C:/Users/Brandon/Documents/parallax/skills/multi-model-verify/references/fallbacks.md:13)); merely changing `Options:` cannot express the backup lane’s separate effort-evidence gap. Provide conditional banner forms or explicit per-option consequences.

2. **The required mode-plan/mode-diff routing is absent.** The spec requires pointers in both dispatch sections ([spec:190](/C:/Users/Brandon/Documents/parallax/docs/superpowers/specs/2026-07-25-kimi-backup-lane-design.md:190)). Task 5 edits only Overview, Preflight, and Finish line ([plan:498](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:498), [plan:522](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:522), [plan:540](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:540)). Its test merely requires `backup-lane.md` somewhere in SKILL.md ([plan:151](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:151)), so both dispatch pointers could remain missing while green.

3. **Load-bearing containment tests have false negatives.** The resume pin asserts only through `--agent-file`; it does not require resumed calls to re-pin `-m` and `--thinking` ([plan:109](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:109)), despite those flags being explicitly load-bearing ([spec:139](/C:/Users/Brandon/Documents/parallax/docs/superpowers/specs/2026-07-25-kimi-backup-lane-design.md:139)). The “exact allowlist” test proves only that five entries are present and selected markers absent, allowing additional tools ([plan:100](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:100)); the spec requires exact equality ([spec:220](/C:/Users/Brandon/Documents/parallax/docs/superpowers/specs/2026-07-25-kimi-backup-lane-design.md:220)). Parse the YAML tools list and compare it exactly, assert the complete resumed command, and add all new reference artifacts to the structural/no-backslash coverage required by the spec ([spec:227](/C:/Users/Brandon/Documents/parallax/docs/superpowers/specs/2026-07-25-kimi-backup-lane-design.md:227)).

4. **Task 6 violates the frozen-plan zero-judgment contract.** The repository requires complete code and falsifiable commands, with no open implementation decisions ([frozen-plan-format.md:11](/C:/Users/Brandon/Documents/parallax/skills/multi-model-verify/references/frozen-plan-format.md:11)). Task 6 instead says “mirror” existing code and modify stubs/assertions “as needed” ([plan:609](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:609), [plan:617](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:617)). It specifies only a missing-flag negative scenario—none for vocabulary-import failure or Kimi version carry-forward. The Python shim also needs exact forwarding behavior because the drift script later invokes `python -m pytest` ([check-drift.ps1:515](/C:/Users/Brandon/Documents/parallax/tools/check-drift.ps1:515)). Supply exact script/test edits and decisive scenarios for all three drift behaviors.

5. **Three embedded edit anchors reproduce the line-join defect class.** The Overview target ends after “touches code” ([plan:500](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:500)), but the current physical line continues into the next sentence ([SKILL.md:13](/C:/Users/Brandon/Documents/parallax/skills/multi-model-verify/SKILL.md:13)). The Preflight target begins with `On failure` ([plan:524](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:524)), while that text currently begins mid-line after another sentence ([SKILL.md:38](/C:/Users/Brandon/Documents/parallax/skills/multi-model-verify/SKILL.md:38)). The README insertion targets a nonexistent fallbacks row and supplies non-table text ([plan:559](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:559)); the actual section is a table with no such row ([README.md:62](/C:/Users/Brandon/Documents/parallax/README.md:62)). Re-pin full physical source lines and provide a valid table row.

6. **The doctor row repeats a known path-resolution bug.** The existing doctor contract explicitly says artifacts must resolve beneath the installed plugin’s `installPath` and warns that bare relative paths depend on the current directory ([doctor.md:58](/C:/Users/Brandon/Documents/parallax/commands/doctor.md:58)). The proposed row checks `references/kimi-reviewer-*.{yaml,md}` directly ([plan:598](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:598)). Pin resolution through `installPath/skills/multi-model-verify/references/`.

7. **One RED expectation names a test that the command does not run.** Step 3 invokes only `test_backup_lane.py` but says the modified `test_multi_model_verify.py` pin also fails ([plan:182](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:182)). Run both files or remove that expected observation.

UNVERIFIED:

- The claimed pytest baseline/count, lint results, and PowerShell state-machine outcomes could not be executed because this review shell has no Python executable; these runtime claims were not used in the verdict ([plan:187](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:187), [plan:565](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:565), [plan:621](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:621)).
- Attended dev-loop and live backup-lane behavior remain runtime-unverified ([plan:698](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:698)). Spec §12 Kimi probe facts were treated as GIVEN, as directed.

FIX
tokens used
123,469
The candidate is not implementation-ready.

1. **Consent-gate behavior contradicts the trigger rule.** The plan permanently adds the backup option to the shared banner ([plan:402](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:402)), while both the spec and the inserted prose say it is offered automatically only for named classes and otherwise only on request ([spec:22](/C:/Users/Brandon/Documents/parallax/docs/superpowers/specs/2026-07-25-kimi-backup-lane-design.md:22), [plan:432](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:432)). The existing banner also describes only one mode’s verification gap ([fallbacks.md:13](/C:/Users/Brandon/Documents/parallax/skills/multi-model-verify/references/fallbacks.md:13)); merely changing `Options:` cannot express the backup lane’s separate effort-evidence gap. Provide conditional banner forms or explicit per-option consequences.

2. **The required mode-plan/mode-diff routing is absent.** The spec requires pointers in both dispatch sections ([spec:190](/C:/Users/Brandon/Documents/parallax/docs/superpowers/specs/2026-07-25-kimi-backup-lane-design.md:190)). Task 5 edits only Overview, Preflight, and Finish line ([plan:498](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:498), [plan:522](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:522), [plan:540](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:540)). Its test merely requires `backup-lane.md` somewhere in SKILL.md ([plan:151](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:151)), so both dispatch pointers could remain missing while green.

3. **Load-bearing containment tests have false negatives.** The resume pin asserts only through `--agent-file`; it does not require resumed calls to re-pin `-m` and `--thinking` ([plan:109](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:109)), despite those flags being explicitly load-bearing ([spec:139](/C:/Users/Brandon/Documents/parallax/docs/superpowers/specs/2026-07-25-kimi-backup-lane-design.md:139)). The “exact allowlist” test proves only that five entries are present and selected markers absent, allowing additional tools ([plan:100](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:100)); the spec requires exact equality ([spec:220](/C:/Users/Brandon/Documents/parallax/docs/superpowers/specs/2026-07-25-kimi-backup-lane-design.md:220)). Parse the YAML tools list and compare it exactly, assert the complete resumed command, and add all new reference artifacts to the structural/no-backslash coverage required by the spec ([spec:227](/C:/Users/Brandon/Documents/parallax/docs/superpowers/specs/2026-07-25-kimi-backup-lane-design.md:227)).

4. **Task 6 violates the frozen-plan zero-judgment contract.** The repository requires complete code and falsifiable commands, with no open implementation decisions ([frozen-plan-format.md:11](/C:/Users/Brandon/Documents/parallax/skills/multi-model-verify/references/frozen-plan-format.md:11)). Task 6 instead says “mirror” existing code and modify stubs/assertions “as needed” ([plan:609](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:609), [plan:617](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:617)). It specifies only a missing-flag negative scenario—none for vocabulary-import failure or Kimi version carry-forward. The Python shim also needs exact forwarding behavior because the drift script later invokes `python -m pytest` ([check-drift.ps1:515](/C:/Users/Brandon/Documents/parallax/tools/check-drift.ps1:515)). Supply exact script/test edits and decisive scenarios for all three drift behaviors.

5. **Three embedded edit anchors reproduce the line-join defect class.** The Overview target ends after “touches code” ([plan:500](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:500)), but the current physical line continues into the next sentence ([SKILL.md:13](/C:/Users/Brandon/Documents/parallax/skills/multi-model-verify/SKILL.md:13)). The Preflight target begins with `On failure` ([plan:524](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:524)), while that text currently begins mid-line after another sentence ([SKILL.md:38](/C:/Users/Brandon/Documents/parallax/skills/multi-model-verify/SKILL.md:38)). The README insertion targets a nonexistent fallbacks row and supplies non-table text ([plan:559](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:559)); the actual section is a table with no such row ([README.md:62](/C:/Users/Brandon/Documents/parallax/README.md:62)). Re-pin full physical source lines and provide a valid table row.

6. **The doctor row repeats a known path-resolution bug.** The existing doctor contract explicitly says artifacts must resolve beneath the installed plugin’s `installPath` and warns that bare relative paths depend on the current directory ([doctor.md:58](/C:/Users/Brandon/Documents/parallax/commands/doctor.md:58)). The proposed row checks `references/kimi-reviewer-*.{yaml,md}` directly ([plan:598](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:598)). Pin resolution through `installPath/skills/multi-model-verify/references/`.

7. **One RED expectation names a test that the command does not run.** Step 3 invokes only `test_backup_lane.py` but says the modified `test_multi_model_verify.py` pin also fails ([plan:182](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:182)). Run both files or remove that expected observation.

UNVERIFIED:

- The claimed pytest baseline/count, lint results, and PowerShell state-machine outcomes could not be executed because this review shell has no Python executable; these runtime claims were not used in the verdict ([plan:187](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:187), [plan:565](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:565), [plan:621](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:621)).
- Attended dev-loop and live backup-lane behavior remain runtime-unverified ([plan:698](/C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-kimi-backup-lane.md:698)). Spec §12 Kimi probe facts were treated as GIVEN, as directed.

FIX
