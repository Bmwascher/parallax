Round 18. The packet boundary is now the exact list you specified. Plan header
reads revision 17. Same evidence rules and verdict grammar.

Both failures you named were real and I have recorded them rather than quietly
replacing the sentence, because the bad wording is the TIDIER one and a later
editor will be tempted by it.

## The fix

`docs/superpowers/plans/2026-08-01-lane-credential-and-lock.md:76` is now your
exact inclusion list, verbatim: (1) the `For agentic workers` instruction;
(2) Goal, Architecture and Tech Stack; (3) Global Constraints; (4) Measured
facts the plan is built on; (5) Fixed names and values; (6) this entire
`The implementer's task packet` section; (7) its ONE assigned task. Excluded, by
name: Status text, Revision history, design and debate session pointers, other
tasks, Debate record, raw rounds, debate conversation.

`:78` records why the list is exhaustive and names both ways the relative
boundary failed — the revision history is above the line while the next sentence
excluded it, and the section's own body is not above the line even though the
packet must carry it.

I took your list without changes. The one thing I want you to confirm is item 1:
the `For agentic workers` instruction at `:3` names the required sub-skill, so
including it means every implementer is told to use subagent-driven development
or executing-plans. For a single-task implementer that reads as either harmless
or slightly wrong. You put it in the list, so I kept it, but say if you meant
something narrower.

## The revision record

`:15` is the r17 entry. It names the relative-boundary hazard rather than just
the fix, since this document keeps growing and any future relative reference has
the same defect waiting in it.

## Where this stands

All ten tasks passed on your round 17. The only open item was this sentence.

If this is a PASS: I will change the header from DRAFT to FROZEN, and building
starts with Task 1, dispatched to a zero-judgment implementer carrying exactly
the seven blocks above. Confirm the header change is the last edit you expect,
so a frozen plan is not immediately reopened by an eighteenth amendment.
