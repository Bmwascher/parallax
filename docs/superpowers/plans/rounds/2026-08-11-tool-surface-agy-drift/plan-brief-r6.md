<role>Same adversarial reviewer, same debate, round 6. Equal weight.</role>

<task>Your one round-5 fix is applied at head `dcf2c17`. Verify it. Then
say whether the round is DRY.</task>

<rules>
Same three invariants, same citation rule, same PASS/FIX/ESCALATE.

THIS IS THE LAST FIX-VERIFY UNIT. The user authorized it specifically to
give this debate a chance to terminate cleanly. If this round is
contested, the budget is exhausted and the cycle stops for authorization
again before anything else happens.

That is not a reason to withhold a real defect. It IS a reason to hold a
finding to the standard the protocol already sets: raise a new risk only
with evidence attached, and do not manufacture an objection.

SCOPE. This round is the round-5 fix and anything it broke. Rounds 1
through 5 have covered the rest, twice over in places. Re-opening settled
ground now would be a new debate wearing this one's clothes.

If it is dry, say DRY first and plainly.
</rules>

<applied>

**Your round-5 FIX - "never PROMOTED" was wider than the evidence.**

You cited `references/debate-protocol.md:67-95`, which carries the
round-cap and fix-verify-budget rules promoted out of the measured 0.21.1
cycle, and `:100-106`, which carries the pre-existing-defect scope rule
and states its own reason for living there: "the rule is written down here
rather than improvised per debate, because an attestation that means
something different run to run means nothing." Both confirmed by reading
them.

Two edits in `docs/superpowers/plans/2026-08-11-tool-surface-agy-drift.md`:

1. The Task 3 heading is now "**The documented flow has no REQUIRED
   promotion step for adjudication rules.**" It states that the phrase
   "never promoted" was wider than the evidence, that promotion HAS
   happened, cites both protocol regions with what each was promoted from,
   and ends: "What is missing is not the act; it is any step that REQUIRES
   it."

2. Task 8's closing paragraph now reads: "The gap is that NO DOCUMENTED
   STEP REQUIRES PROMOTION. It happens when somebody thinks of it, which
   is why two rules made it into `debate-protocol.md` and this one did
   not... The narrower claim about THIS rule stands on its own evidence:
   it was written in 0.17.0, it was never promoted, and 0.24.0 paid for it
   again."

The debate README gained a round-5 section recording the finding, the
narrowing, that rounds 3, 4 and 5 each narrowed this same sentence and the
third pass was still too wide, and the per-round finding counts: 5, 6, 5,
2, 1.

</applied>

<questions>

1. Is the narrowing correct and complete, and does the retained narrower
   claim about this one rule stand on its own evidence?
2. Does the phrase survive anywhere else in the changed files?
3. DRY or not?

</questions>

<meters>
Entering this round: 5 contested exchanges against a declared cap of 4,
one authorized by the user; 5/6 total fix-verify units. This exchange
spends unit 6, the last.
</meters>
