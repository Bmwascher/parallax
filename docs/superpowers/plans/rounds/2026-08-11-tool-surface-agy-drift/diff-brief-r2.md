<role>Adversarial reviewer, equal weight, in a two-model DIFF debate. Round 2. Neither side's claim outranks the other's; only evidence does.</role>

<task>Round 1 returned six FIX, one PASS, one ESCALATE. Every finding was verified against the code before anything changed, and every one was ACCEPTED. Adjudicate the fixes. A FIX is new code and gets no discount: attack it as new work, not as a correction you already approved.

Range is now `ef428c3..ac7dc43` on branch `0.24.0-tool-surface-agy-drift`, repo C:/Users/Brandon/Documents/parallax, read-only. Your round-1 reply is retained verbatim at `docs/superpowers/plans/rounds/2026-08-11-tool-surface-agy-drift/diff-reply-r1.txt`. The session's adjudications are amendment 9 of `.git/parallax/application-checkpoints/2026-08-11-1630-7945ac1-0240-build.md`, and section 7 of that file is no longer empty.</task>

<rules>
Cite repo-relative file:line for every claim. End each numbered item PASS, FIX (with the specific fix) or ESCALATE. Do not manufacture objections: if a fix stands, say PASS.

The three invariants still bind: a claim may never be wider than its evidence; an unmade, failed or unreadable measurement is never a clean one; a test is not evidence until it has been watched to FAIL for the reason it claims.

ONE THING CHANGED IN YOUR OWN CONFIGURATION, and you should know it. Round 1 dispatched without `--disable memories`. This round carries it, because your round-1 claim 8 was right and the measurement it forced is below. Your session is the same one, resumed; the store is what changed.

YOU HAVE SEEN THIS BRIEF ONCE ALREADY, AND YOUR ANSWER TO IT WAS NEVER READ. Do not refer back to it. The first send crossed a day boundary, so this client put a refreshed `<environment_context>` in front of the brief; the round-evidence binding refused a slice carrying text this side did not write, and under that contract the reply is DISCARDED UNREAD. It was. Nothing from it has been read, quoted, or acted on. That is filed as backlog item 42 and the guard is not being relaxed to get past it. Answer in FULL, as if for the first time - a reply that points at the discarded one is a reply this side cannot use.
</rules>

<claims>

1. **Claim 8 was correct and the drift is repaired at the source.** The probe now sends `experimentalFeature/list` beside `mcpServerStatus/list` on every poll, on separate id ranges, and every failure direction of the feature surface blocks on its own terms: silent, RPC error, and result-without-data each have a case (`tools/codex-tool-surface-probe.ps1`, `evals/multi-model-verify/test_codex_tool_surface_probe.py`). The frozen plan was NOT rewritten; it carries an appended Amendment 1 recording that Task 1 was built incomplete and no deviation was logged (`docs/superpowers/plans/2026-08-11-tool-surface-agy-drift.md`, tail). Attack the repair, and attack whether an appended amendment is the right way to record a build that did not match a frozen task.

2. **The memories half of item 7 is now measured AND controlled, and this is the cycle's most consequential change.** Measured live 2026-08-12, before the flag: the review configuration reported `memories=True` while `plugins` and `apps` were correctly false. After: `memories=False` in the dispatch configuration against `memories=True` in the baseline, same probe, same run. Confirmed again on the FINAL tree, 2026-08-14, on both PowerShell hosts, alongside `plugins` and `apps` making the same True-to-False transition. `codex exec` was separately measured to ACCEPT the flag - exit 0, clean route - rather than trusting a measurement taken against `app-server`. `--disable memories` now rides the fresh dispatch and every resume, pinned in both places, with the contract region and its pin updated together (`skills/multi-model-verify/SKILL.md`; `skills/multi-model-verify/references/model-prompting-notes.md`; `evals/multi-model-verify/test_multi_model_verify.py`). Continuity within one review comes from resuming that review's own session.

   **A NARROWING I found while writing this claim, so it is not overstated.** The PROMPT axis was already covered: `evals/multi-model-verify/test_codex_context_probe.py` shows the context probe treats a `<memories_instructions>` block as an unknown surface and BLOCKS on it, on either pass. So a memories block that reached the rendered prompt would already have stopped a round. What was uncontrolled is the FEATURE itself - the store being enabled at all, which the prompt probe cannot see because it reads only the prompt. Two different surfaces, and only one of them was covered.

   Attack it: is disabling the store on every resume actually what the user's decision implies, is the control sound, and does anything in the repo still assume the store is available?

3. **Both false-cleans from claim 1 are closed and neither fix opened a new one.** A non-blank line that is not a JSON-RPC frame now marks the stream unreadable instead of being skipped, and the "wrote nothing at all" reason is now only used when the server wrote nothing. A result with no `data` member blocks rather than becoming an empty surface. Both have fail-first cases.

   **This claim was CAUGHT BEING TOO WIDE by the session, before you saw it, and repaired by running the measurement rather than by softening the sentence.** The draft said the real client had been re-run after the strictness change. It had not: the only live run on record predated those fixes. It has now been run on the final tree, 2026-08-14, on BOTH hosts: `status` clean, exit 0, and the two JSON reports byte-identical by sha256 `2509110c5408...` rather than by inspection. Recorded in checkpoint section 7 with the correction stated as a correction. Noted separately in the same run: the baseline tool count has moved 133 to 137 since 2026-08-12 with no action taken on this machine, the third distinct reading of it in this build, which is why it is reported and never asserted stable.

   Attack the strictness anyway: is there a legitimate line the real server can emit that this now rejects, that two clean runs would not have provoked?

4. **The third host assumption is closed, and quoting alone was measured NOT to be enough.** The batch branch now uses `/s /c "<whole command line>"`. Attack it: is `/s` the right lever, and is there a path shape that still breaks it?

5. **Claim 4's twelfth disagreement was inside my own previous fix, and presence is now separate from value.** A key holding `null` or `""` is PRESENT, is saved as a measured value, and is never reported removed; a parsed file with the key gone is a measured absence and drops it; an unparseable or missing file carries the last known value forward. Scenario `agy-allow-null` was watched to fail first. Attack the state table for a path where a measured value is lost or an unmeasured one is promoted.

6. **Claim 7's three sentences are corrected at the source**, and claim 2's propagation is complete: all six surfaces you named now carry the proxy qualification (`tools/codex-context-probe.ps1`; `docs/superpowers/plans/rounds/2026-07-28-reviewer-isolation/README.md`; `docs/superpowers/specs/2026-07-28-reviewer-isolation-design.md`, both sites; `docs/superpowers/plans/rounds/2026-08-11-tool-surface-agy-drift/README.md`; the frozen plan by appended amendment). Attack it: find a seventh surface, or an instance whose wording says something the others do not.

7. **The ESCALATE is answered with records, not assurances.** Checkpoint section 7 now quotes every observed failure: five assertions for the version and key checks, two for the settings-parse check, two for the null-key check, three for the line-ending guard, and the byte-order-mark case including the earlier guard that PASSED with the defect restored. Your ESCALATE asked a wider question than any single red-green record answers, so the whole agy block was DELETED from the watcher and the state machine re-run against the mutilated file: **28 assertions red, every one inside an agy scenario, none outside it.** Fifteen of the sixteen agy scenarios went red, including the positive control. The sixteenth, `agy-allow-null`, did NOT, and the checkpoint says so in the same breath rather than rounding it up - both its assertions are survival assertions that the surviving carry-forward path satisfies, so the sweep is silent about it in either direction; it has its own red-green record from run 3 instead. The limit was written into the checkpoint BEFORE the numbers came back, and the sweep's scenario-level granularity is stated too: several red scenarios still contain individually vacuous green assertions. Log retained in-repo at `agy-deletion-sweep.log`. Section 7 also states what is NOT covered: the preamble-read guard is verified by reading only, `codex exec`'s own surface is unprobed, and the behavioural suite was not run. Attack the record: is any entry in it wider than the run it cites?

8. **Item 40 stays FILED rather than fixed, and you agreed the disposition was correct.** Nothing changed there. Say so if you have changed your mind, and say so if any OTHER pre-existing defect was quietly fixed inside this branch under cover of the scope rule.

9. **NEW WORK, found by the session rather than by you, and it changes CI.** The `powershell-hosts` job lists PowerShell-facing test modules by name, and `test_codex_tool_surface_probe.py` - this branch's own deliverable - was in neither list. Its module skips when no host is found, so the ubuntu job ran it under that runner's pwsh and windows-latest ran it under nothing. Windows PowerShell 5.1 was therefore uncovered, and 5.1 is the host the probe was BROKEN ON, by the byte-order-mark defect of amendment 6. The module is now in both lists (`.github/workflows/skill-evals.yml`). **Adding it there was NOT sufficient, and that was checked rather than assumed:** `evals/tools/check_workflow_paths.py` enumerates `REQUIRED_DUAL_HOST_MODULES` and asserts each entry appears in EACH host step, so a module named in both steps but absent from that list is unlocked and could be deleted from one step later with the guard still green. It was added to the list in the same act and the pin was WATCHED TO FAIL, with the module deleted from the pwsh step alone: `required dual-host module missing from host step pwsh.exe: evals/multi-model-verify/test_codex_tool_surface_probe.py`, and clean against the real workflow in the same run.

   A same-class gap found in the same look was FILED not fixed: `evals/tools/drift_statemachine_tests.ps1:533` hardcodes `powershell.exe`, so all 37 scenarios drive the drift watcher on 5.1 only - backlog item 41, OPEN, and it states plainly that whether the watcher passes under PowerShell 7 is UNMEASURED. Attack all three: is the CI addition correct and sufficient, is the pin real, and is item 41 genuinely outside the surface or is that the convenient answer rather than the right one?

</claims>

<boundaries>
Unchanged from round 1, plus backlog item 41. Out of scope: `tools/check-drift.ps1:700` and `commands/doctor.md:70` (backlog item 31), `evals/tools/drift_statemachine_tests.ps1:533` (backlog item 41), and backlog items 12, 15, 26-remainder, 29, 32, 33, 34, 35. The version bump is Task 9 and is deliberately LAST; the manifest still reading 0.23.0 is the ordering rule, not drift, and it lands before merge.
</boundaries>

<final-check>List any claim you could not verify against files you actually read, as UNVERIFIED. Do not fold unverified material into your verdict.</final-check>
