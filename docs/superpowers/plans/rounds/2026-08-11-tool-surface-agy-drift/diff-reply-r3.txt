Verdict: claims 1, 4, and 6 pass; claims 2, 3, 5, and 7 need fixes.

1. The derived windows are disjoint: status ends at `100 + PollCount`, while feature responses begin at `102 + PollCount`; the same calculated bounds are returned to the reader (`tools/codex-tool-surface-probe.ps1:335-382`). The 101-poll regression case verifies both surfaces remain distinguishable, and the retained fail-first result shows the former cross-attribution (`evals/multi-model-verify/test_codex_tool_surface_probe.py:689-712`, `.git/parallax/application-checkpoints/2026-08-11-1630-7945ac1-0240-build.md:764-771`). At zero or a negative count the loop emits no polls, leaving empty ranges; the absence of a matching response reaches the explicit “never answered” block (`tools/codex-tool-surface-probe.ps1:337-353`, `tools/codex-tool-surface-probe.ps1:541-557`). **PASS**

2. `data:null` now blocks, and its feature-surface false-clean was watched to fail before repair (`tools/codex-tool-surface-probe.ps1:441-456`, `.git/parallax/application-checkpoints/2026-08-11-1630-7945ac1-0240-build.md:764-768`). Two defects remain in the accepted fix:

   - Present-but-null and missing `data` are collapsed into one flag, after which both blocked reasons falsely say “no data member”; the null-specific test even requires that inaccurate sentence (`tools/codex-tool-surface-probe.ps1:442-464`, `tools/codex-tool-surface-probe.ps1:541-567`, `evals/multi-model-verify/test_codex_tool_surface_probe.py:660-687`).
   - Round 2 expressly required readable feature-entry schema, but `Get-Features` accepts any non-null element, casts any name to string, and accepts any enablement value (`docs/superpowers/plans/rounds/2026-08-11-tool-surface-agy-drift/diff-reply-r2.txt:3-5`, `tools/codex-tool-surface-probe.ps1:589-598`). Only names derived from `--disable` are subsequently validated, so malformed unpoliced entries still reach the clean report (`tools/codex-tool-surface-probe.ps1:677-707`, `tools/codex-tool-surface-probe.ps1:749-755`).

   **FIX — distinguish null data from a missing member, or use the accurate generic reason “no readable data value”; validate that `data` is the expected collection and every feature has a nonempty string name plus an unambiguous boolean enablement field; add watched-fail null-reason and malformed-unpoliced-entry cases.**

3. The five named policy directions are implemented and covered: enabled, absent, non-boolean, missing enablement, and duplicated disabled features (`tools/codex-tool-surface-probe.ps1:677-707`, `evals/multi-model-verify/test_codex_tool_surface_probe.py:555-657`). However, feature names are matched using case-insensitive `-eq` (`tools/codex-tool-surface-probe.ps1:682`). Consequently, `Memories=False` satisfies the requirement derived from `--disable memories`, although the declared limit says a future name mismatch must block as “never reported”; the measured fixture records the actual names in lowercase (`evals/multi-model-verify/test_codex_tool_surface_probe.py:95-104`). **FIX — match names with `-ceq` and add a watched-fail mixed-case feature case proving a nonidentical name is treated as missing.**

4. The percent check is performed before `Process.Start`, and its blocked reason accurately states that the resolved batch command cannot be passed literally through `cmd.exe` (`tools/codex-tool-surface-probe.ps1:197-228`). The regression uses an actual `%PATH%` directory component and requires BLOCKED plus a percent-specific reason (`evals/multi-model-verify/test_codex_tool_surface_probe.py:428-459`). Refusing the unaimable batch instrument is the honest outcome. **PASS**

5. The new token function correctly separates the tested scalar cases, and the null-preservation and null-to-empty assertions were watched red before repair (`tools/check-drift.ps1:145-166`, `evals/tools/drift_statemachine_tests.ps1:1082-1110`, `.git/parallax/application-checkpoints/2026-08-11-1630-7945ac1-0240-build.md:781-784`). But the watcher accepts the setting’s value without a type restriction, while both token generation and final snapshot serialization call `ConvertTo-Json` without an explicit depth (`tools/check-drift.ps1:281-295`, `tools/check-drift.ps1:154-166`, `tools/check-drift.ps1:585-594`). Thus the broad claim that the value’s JSON type is kept is unsupported for nested object or array values; the new scenarios exercise only `null` and `""` (`evals/tools/drift_statemachine_tests.ps1:1082-1110`). **FIX — serialize tokens and snapshots with an explicit, checked depth that preserves every admitted JSON value, block/report serialization truncation, and add fail-first nested-object and nested-array round-trip/change cases.**

6. The workflow now accurately records when coverage was added (`.github/workflows/skill-evals.yml:83-92`). Backlog item 7 preserves the earlier overclaim as history and explains that the former probe only read the feature surface before the enforcement cases were added (`docs/superpowers/plans/2026-07-27-0150-backlog.md:619-629`). I found no third standing source asserting that the pre-enforcement control was already real; retained briefs and replies describe historical debate positions rather than current contracts. **PASS**

7. Filing item 42 instead of repairing the binder is consistent with the predeclared verification surface, which excludes the binder, and with the rule to record defects outside that surface (`.git/parallax/application-checkpoints/2026-08-11-1630-7945ac1-0240-build.md:40-60`, `skills/multi-model-verify/references/debate-protocol.md:100-131`). The record itself is inconsistent:

   - Item 42 says the same-day post-refresh resume was “NOT measured,” while both the checkpoint and debate record say the retry measured exactly that behavior and bound clean (`docs/superpowers/plans/2026-07-27-0150-backlog.md:2743-2747`, `.git/parallax/application-checkpoints/2026-08-11-1630-7945ac1-0240-build.md:530-533`, `docs/superpowers/plans/rounds/2026-08-11-tool-surface-agy-drift/diff-debate-record.md:119-131`).
   - “Every debate that spans midnight” and “any debate that spans midnight” are wider than the single observed changed-preamble case (`docs/superpowers/plans/2026-07-27-0150-backlog.md:2707-2723`, `docs/superpowers/plans/2026-07-27-0150-backlog.md:2740-2741`, `.git/parallax/application-checkpoints/2026-08-11-1630-7945ac1-0240-build.md:523-530`). The binder only applies its identity failure when a resumed slice contains two user records, so midnight alone is not sufficient (`tools/read-codex-round-evidence.ps1:639-685`).

   **FIX — keep item 42 open, record the same-day retry as measured, and narrow all universal midnight statements to the observed condition: a resumed slice carrying a refreshed, nonidentical preamble ahead of the brief.**

UNVERIFIED: None.