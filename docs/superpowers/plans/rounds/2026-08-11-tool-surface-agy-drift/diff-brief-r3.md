<role>Adversarial reviewer, equal weight, in a two-model DIFF debate. Round 3. Neither side's claim outranks the other's; only evidence does.</role>

<task>Round 2 returned five FIX and four PASS. Every finding was verified against the code before anything changed, and every one was ACCEPTED. Adjudicate the fixes. A FIX is new code and gets no discount: attack it as new work, not as a correction you already approved.

Range is now `ef428c3..8b46296` on branch `0.24.0-tool-surface-agy-drift`, repo C:/Users/Brandon/Documents/parallax, read-only. Your round-2 reply is retained verbatim at `docs/superpowers/plans/rounds/2026-08-11-tool-surface-agy-drift/diff-reply-r2.txt`. The session's adjudications are amendment 11 of `.git/parallax/application-checkpoints/2026-08-11-1630-7945ac1-0240-build.md`.</task>

<rules>
Cite repo-relative file:line for every claim. End each numbered item PASS, FIX (with the specific fix) or ESCALATE. Do not manufacture objections: if a fix stands, say PASS.

The three invariants still bind: a claim may never be wider than its evidence; an unmade, failed or unreadable measurement is never a clean one; a test is not evidence until it has been watched to FAIL for the reason it claims.
</rules>

<claims>

1. **The two id windows can no longer collide at any poll count.** The bases are derived - `$statusBase = 100`, `$featureBase = $statusBase + $pollCount + 1` - and the windows are handed to the reader on the transport result rather than restated as constants, so reader and writer cannot drift apart (`tools/codex-tool-surface-probe.ps1`). The case runs 101 polls, the first count that collided, at a 1ms interval so it measures the arithmetic and not the clock. **Watched to fail:** against the pre-fix code that same case returned `{'status': 'clean', ... 'baseline_features': ['node_repl=']}` - a status answer read through the feature window, your finding demonstrated rather than asserted. Attack the derivation at its edges: `-PollCount 0` and a negative value both produce empty windows and therefore BLOCK, which is the safe direction, but say so if you find a value where it does not.

2. **`data: null` blocks, and it was the third reading of that rule.** Member presence alone accepted it, and the reducers then walked `@($null)` - the one-element array holding `$null` that this file already documents for `tools: {}` - skipped the single element, and produced an empty surface. **Watched to fail:** the feature-surface variant returned `{'status': 'clean', ... 'baseline_features': []}` before the fix; the status variant blocked only via pass 1's calibration, which is why the feature case is the diagnostic one. Attack the strictness: is there a legitimate response where `data` is null and the surface is genuinely empty rather than unread?

3. **The disabled-feature policy is now ENFORCED, and this was the round's most serious finding.** You were right on both halves: only the reading half had been built, and the module's own fixture fed `memories=True` into pass 2 while asserting a CLEAN verdict, so a test certified the state that should stop a round. The probe now blocks when any feature the dispatch disabled comes back missing, duplicated, non-boolean, or enabled. Five fail-first cases cover those directions. The stub's default feature payload was `[]` - a server unlike the real one in exactly the direction the new policy reads - and now defaults to the shape of the live 2026-08-14 reading.

   **A LIMIT I am declaring rather than waiting for you to find.** The policed list is derived by reading the `--disable <name>` pairs out of the dispatch arguments and matching `<name>` against the FEATURE name. That the two are the same string is MEASURED for all three flags this dispatch sends - plugins, apps and memories all appear under those exact names in the live reading - and it is not guaranteed for a flag added later. The failure direction if they ever diverge is a BLOCK reading "the feature surface never reported it", not a false clean. Attack that: is failing safe good enough here, or does the mismatch need naming at the source?

4. **A batch path carrying `%` is refused rather than launched.** Percent is expanded by cmd while parsing, inside double quotes as well, so the quoting that fixed `a & b (x)` cannot reach it and `%%` escapes only inside a batch FILE. The comment claiming `/s` runs the line "verbatim" was corrected in the same edit. **Watched to fail:** under the pre-fix code that path blocked for the WRONG reason, `pass 1 (baseline): the app server exited 1`, which is what launching an unaimed instrument looks like from outside. Attack the direction: this is the one case in its class whose correct outcome is BLOCKED rather than clean, so say if you think refusing is too strong and what the alternative would measure.

5. **The watcher keeps the value's JSON type.** One token function serves both the comparison and the note text, because `[string]` collapsed `null` and `""` into the same saved text and made a real change between them silent. **Watched to fail:** exactly two assertions red against the pre-fix watcher with nothing else moving - `a null value is saved as NULL, not flattened to an empty string` and `a null to empty-string change is REPORTED, not absorbed as equal`. One consequence is written into the code rather than left to be discovered: the first run after this change can emit ONE cosmetic note against a snapshot written by the old form. Attack the state table again for a path where a measured value is lost or an unmeasured one is promoted.

6. **The false sentence is corrected at the source, and so is its sibling.** The workflow comment no longer says the module was added "as soon as it existed"; it says it was in neither list until the final gate run went looking. You did not raise the second one, and I am putting it in front of you rather than leaving it: backlog item 7 said the memories control "is a real one" while nothing enforced it, which was the same overclaim in a different file. It now records that the sentence was wider than the code until this round. Attack it: is there a third instance of that claim still standing anywhere?

7. **A ROUND WAS THROWN AWAY BEFORE THIS ONE, and you should know why.** Round 2 was dispatched twice. The first send crossed a day boundary, the client put a refreshed `<environment_context>` in front of the brief, and the round-evidence binding refused a slice carrying text this side did not write. The reply was DISCARDED UNREAD - nothing from it was read, quoted, or acted on - and the quota was spent for nothing. The guard was not relaxed. Its rule was once arithmetic, the field falsified it, identity replaced it, and the field has now falsified identity: a bound earned from rounds that all ran inside one day. Filed as backlog item 42, priority HIGH. The retry the user authorised bound clean and incidentally measured what the item records as unknown. Attack the disposition: is filing right, or should this branch not merge until the binder is fixed?

</claims>

<boundaries>
Unchanged, plus item 42. Out of scope: `tools/check-drift.ps1:700` and `commands/doctor.md:70` (item 31), `evals/tools/drift_statemachine_tests.ps1:533` (item 41), the round-evidence binder (item 42), and backlog items 12, 15, 26-remainder, 29, 32, 33, 34, 35. The version bump is Task 9 and is deliberately LAST; the manifest still reading 0.23.0 is the ordering rule, not drift.
</boundaries>

<final-check>List any claim you could not verify against files you actually read, as UNVERIFIED. Do not fold unverified material into your verdict.</final-check>
