1. The appended amendment is the correct record form: the frozen body remains intact, the plan requires changes to be recorded through a reopened debate, and Amendment 1 explicitly identifies the incomplete build (`docs/superpowers/plans/2026-08-11-tool-surface-agy-drift.md:3-8`, `docs/superpowers/plans/2026-08-11-tool-surface-agy-drift.md:526-555`).

   The repair still has two false-clean paths. `PollCount` is unconstrained, while status IDs use `100+n`, feature IDs use `200+n`, and the readers assume fixed ranges 100–199 and 200–299. At 100 or more polls those ranges overlap and responses can be attributed to the wrong surface (`tools/codex-tool-surface-probe.ps1:54-60`, `tools/codex-tool-surface-probe.ps1:305-321`, `tools/codex-tool-surface-probe.ps1:474-483`). Also, `"result":{"data":null}` passes the member-presence check and reduces to an empty feature list, producing a clean report without a readable feature surface (`tools/codex-tool-surface-probe.ps1:405-412`, `tools/codex-tool-surface-probe.ps1:487-545`, `tools/codex-tool-surface-probe.ps1:637-655`).

   **FIX — constrain `PollCount` to a genuinely disjoint range or allocate dynamic IDs, validate `data` and feature entries as readable schema, and add watched-fail overlap and `data:null` cases.**

2. The dispatch mechanics are correct: `--disable memories` rides both fresh and resumed calls, while continuity comes from resuming the same session (`skills/multi-model-verify/SKILL.md:174-188`, `skills/multi-model-verify/SKILL.md:236-250`). The prompt-axis narrowing also stands: a memories block appearing on pass 2 blocks, and the parameterized inline case exercises both passes (`evals/multi-model-verify/test_codex_context_probe.py:655-661`, `evals/multi-model-verify/test_codex_context_probe.py:1065-1086`).

   The feature control is not enforced. The probe explicitly says no feature can block and always returns clean after reporting enablement (`tools/codex-tool-surface-probe.ps1:627-644`). Its test supplies `memories=True` for the dispatch surface and requires both a clean verdict and `memories=True` in the report (`evals/multi-model-verify/test_codex_tool_surface_probe.py:84-91`, `evals/multi-model-verify/test_codex_tool_surface_probe.py:484-500`). That contradicts item 7’s claim that the control is real because the feature reports resolved enablement (`docs/superpowers/plans/2026-07-27-0150-backlog.md:608-617`) and omits the accepted requirement for an enabled-feature policy and unexpectedly-enabled fail-first case (`docs/superpowers/plans/rounds/2026-08-11-tool-surface-agy-drift/diff-reply-r1.txt:68-74`).

   **FIX — require `memories` to be present with a boolean false value in pass 2; block if it is missing, malformed, or enabled; and replace the clean-with-`memories=True` test with watched-fail cases.**

3. Both identified parser false-cleans are closed. Every nonblank non-frame marks the stream unreadable, including when a later valid response exists, and any observed result lacking `data` blocks even if another result is usable (`tools/codex-tool-surface-probe.ps1:377-420`, `tools/codex-tool-surface-probe.ps1:487-522`). The focused cases cover both directions, and the checkpoint records them failing before repair (`evals/multi-model-verify/test_codex_tool_surface_probe.py:311-350`, `.git/parallax/application-checkpoints/2026-08-11-1630-7945ac1-0240-build.md:533-557`). The live final-tree rerun on both hosts is recorded separately from the moving baseline count (`.git/parallax/application-checkpoints/2026-08-11-1630-7945ac1-0240-build.md:531-548`). PASS

4. `/s /c "<whole line>"` repairs the tested ampersand/parentheses path, but it does not make the interpolated command text literal. The implementation claims its regex covers any cmd metacharacter, yet `%` is absent from that regex and the batch path is inserted directly into text that cmd reparses (`tools/codex-tool-surface-probe.ps1:182-207`). The only new case exercises `a & b (x)`, not a path containing an environment-expansion sequence such as `%PATH%` (`evals/multi-model-verify/test_codex_tool_surface_probe.py:390-413`).

   **FIX — preserve percent signs literally across cmd parsing, correct the “executes verbatim” claim, and add a watched-fail batch path containing a `%NAME%` sequence.**

5. Presence is now tracked separately, but value identity is still lost. The frozen task requires the setting’s value to be recorded so a change becomes drift (`docs/superpowers/plans/2026-08-11-tool-surface-agy-drift.md:341-348`). The implementation converts both the newly measured value and prior snapshot value to strings, collapsing JSON `null` and `""` into the same saved value and making a `null → ""` transition invisible (`tools/check-drift.ps1:270-272`, `tools/check-drift.ps1:493-496`, `tools/check-drift.ps1:553-560`). The new scenario asserts only that the property survives and no removal note appears; it never asserts that `null` remains `null` or that changing it to `""` is reported (`evals/tools/drift_statemachine_tests.ps1:1075-1089`).

   **FIX — preserve and compare the typed JSON value, then add watched-fail assertions that saved `null` remains null and that `null → ""` emits drift.**

6. All six previously omitted sites now carry the proxy qualification: the context-probe header, the old isolation README, both design-spec sites, the current rounds README, and the appended frozen-plan amendment (`tools/codex-context-probe.ps1:8-17`, `docs/superpowers/plans/rounds/2026-07-28-reviewer-isolation/README.md:23-42`, `docs/superpowers/specs/2026-07-28-reviewer-isolation-design.md:198-205`, `docs/superpowers/specs/2026-07-28-reviewer-isolation-design.md:409-423`, `docs/superpowers/plans/rounds/2026-08-11-tool-surface-agy-drift/README.md:39-45`, `docs/superpowers/plans/2026-08-11-tool-surface-agy-drift.md:526-540`). The three source sentences are narrowed to “unreported,” “known operational checks,” and the actual failed-measurement consequence (`docs/superpowers/plans/2026-08-11-tool-surface-agy-drift.md:498-505`, `tools/check-drift.ps1:127-142`, `docs/superpowers/plans/2026-07-27-0150-backlog.md:2651-2656`). PASS

7. The record supports its stated width. Section 7 separately records the focused fail-first assertions, fixture-integrity failures, and BOM failure (`.git/parallax/application-checkpoints/2026-08-11-1630-7945ac1-0240-build.md:549-577`). It describes the deletion instrument before its result, including the surviving downstream carry-forward paths and the fact that only measurement assertions are diagnostic (`.git/parallax/application-checkpoints/2026-08-11-1630-7945ac1-0240-build.md:579-607`).

   The retained result contains 28 red assertions, all within 15 of the 16 agy scenarios; `agy-allow-null` is expressly recorded as silent and supported instead by its independent red-green record (`.git/parallax/application-checkpoints/2026-08-11-1630-7945ac1-0240-build.md:609-657`, `docs/superpowers/plans/rounds/2026-08-11-tool-surface-agy-drift/agy-deletion-sweep.log:362-364`, `docs/superpowers/plans/rounds/2026-08-11-tool-surface-agy-drift/agy-deletion-sweep.log:462-492`). Scenario-level rather than assertion-level coverage and the three untested limits are explicitly stated (`.git/parallax/application-checkpoints/2026-08-11-1630-7945ac1-0240-build.md:659-673`, `.git/parallax/application-checkpoints/2026-08-11-1630-7945ac1-0240-build.md:706-715`). PASS

8. Item 40 remains correctly filed. It names the two untouched probes, identifies the failed-measurement defect, and specifies the future state-machine cases without changing those pre-existing sites (`docs/superpowers/plans/2026-07-27-0150-backlog.md:2619-2649`). That follows the standing rule to record same-class pre-existing defects outside the predeclared verification surface (`skills/multi-model-verify/references/debate-protocol.md:100-131`). Amendment 10 identifies item 41 as the only other adjacent pre-existing gap found during this work and records it rather than quietly repairing it (`.git/parallax/application-checkpoints/2026-08-11-1630-7945ac1-0240-build.md:476-519`). PASS

9. The operational CI addition and pin are real. The module is present in both host steps, is included in `REQUIRED_DUAL_HOST_MODULES`, and the guard verifies every required module against each uniquely discovered host step (`.github/workflows/skill-evals.yml:92-124`, `evals/tools/check_workflow_paths.py:61-85`, `evals/tools/check_workflow_paths.py:141-185`). The exact one-host deletion failure is retained in the checkpoint (`.git/parallax/application-checkpoints/2026-08-11-1630-7945ac1-0240-build.md:675-681`). Item 41 accurately records the hardcoded host and leaves PowerShell 7 behavior unmeasured (`evals/tools/drift_statemachine_tests.ps1:533`, `docs/superpowers/plans/2026-07-27-0150-backlog.md:2658-2689`); its filing matches the predeclared surface, which names `evals/multi-model-verify/` but not this harness (`.git/parallax/application-checkpoints/2026-08-11-1630-7945ac1-0240-build.md:40-60`).

   The new workflow comment is false: it says the module was added to both lists “as soon as it existed,” while Amendment 10 states it was in neither list until this final-gate review (`.github/workflows/skill-evals.yml:83-91`, `.git/parallax/application-checkpoints/2026-08-11-1630-7945ac1-0240-build.md:476-493`).

   **FIX — replace “as soon as it existed” with wording that records it was added after the final-gate review found the omission.**

UNVERIFIED: None.