1. The reported measurements are internally consistent with the implementation: status uses the stated flags, and directory subjects enter recursive enumeration before hashing (`tools/new-review-mirror.ps1:429`, `tools/new-review-mirror.ps1:477`). The specification distinguishes the nested-checkout junction’s single status entry from a plain folder’s individual entries (`docs/superpowers/specs/2026-09-05-mirror-link-relink-design.md:33`). **PASS.**

2. The hashing and verification call chain is correct: directory subjects expand, file bytes are hashed, and verification recomputes the mirror digest (`tools/new-review-mirror.ps1:483`, `tools/new-review-mirror.ps1:514`, `tools/new-review-mirror.ps1:579`, `tools/new-review-mirror.ps1:739`).

   The explanation that “all four” requested identity components are bound overclaims target identity. The digest contains status fields and relative file names with content hashes; it contains no resolved target pathname. Redirecting a junction to an identical directory therefore need not change it (`tools/new-review-mirror.ps1:490`, `tools/new-review-mirror.ps1:521`, `tools/new-review-mirror.ps1:545`). **FIX — correct the target-binding assertion at `docs/superpowers/specs/2026-09-05-mirror-link-relink-design.md:76` to describe content binding. The two-host `Test-Path` result remains UNVERIFIED below.**

3. The proposed robocopy command changes only the addition of `/XJD` (`docs/superpowers/plans/2026-09-05-item90-mirror-link-relink.md:513`, `docs/superpowers/plans/2026-09-05-item90-mirror-link-relink.md:523`). Local `robocopy /?` reports `/XJD` as “eXclude symbolic links for Directories and Junction points” and `/SL` as copying symbolic links “instead of as the link targets.” With neither `/XJF` nor `/SL`, the stated file-link behavior follows. **PASS.**

4. The named remediation operations precede the insertion point: deletion and pruning, hooks-directory creation, staging and committing, and extra-input copying all occur before the final sweep (`tools/new-review-mirror.ps1:1194`, `tools/new-review-mirror.ps1:1203`, `tools/new-review-mirror.ps1:1222`, `tools/new-review-mirror.ps1:1249`, `tools/new-review-mirror.ps1:1255`, `tools/new-review-mirror.ps1:1316`). The proposed link sweep contains enumeration and refusal without deletion (`docs/superpowers/plans/2026-09-05-item90-mirror-link-relink.md:600`).

   The stronger “last writer” and shared-target safety claims fail. The subsequent baseline calls ordinary `git status`, which can write refreshed index information (`tools/new-review-mirror.ps1:1350`, `tools/new-review-mirror.ps1:429`; [Git’s documented index refresh](https://git-scm.com/docs/git-status#_background_refresh)). The subsequent probe also writes `OverrideOut` (`tools/new-review-mirror.ps1:1391`, `tools/codex-context-probe.ps1:1059`).

   Concrete shared-target instances exist. Given `R\linked -> T`, `-OverrideOut T\override.txt` passes guards that protect only the source and mirror roots, then modifies content exposed through the junction after the baseline. More severely, `-MirrorPath T -Force` passes the source/mirror overlap comparison and recursively deletes the reference target itself (`tools/new-review-mirror.ps1:814`, `tools/new-review-mirror.ps1:843`, `tools/new-review-mirror.ps1:1071`).

   **FIX — before any mutation, check mirror and override destinations against recorded link targets, including resolution through existing ancestor links. Disable optional index writes in status captures. Add regressions for the target-overlap cases.**

5. The relative-path calculation is identical to the existing `$rel` expression, and deepest-path accounting still runs before the conditional push (`docs/superpowers/plans/2026-09-05-item90-mirror-link-relink.md:498`, `tools/new-review-mirror.ps1:1044`). The flag resets inside each entry iteration (`docs/superpowers/plans/2026-09-05-item90-mirror-link-relink.md:486`).

   Retaining the refusal statements does not retain their previous coverage. For `source\linked -> outside` and `outside\self -> outside`, the current walk reaches the inner link and rejects the repeated target. The planned walk stops at the outer link, so the inner cycle never reaches either guard (`tools/new-review-mirror.ps1:1033`, `tools/new-review-mirror.ps1:1049`, `docs/superpowers/plans/2026-09-05-item90-mirror-link-relink.md:505`). The existing cycle and overlap tests cover links encountered directly in the source tree (`evals/multi-model-verify/test_review_mirror.py:1383`, `evals/multi-model-verify/test_review_mirror.py:1402`).

   **FIX — preserve reachable-link validation separately from destination-budget accounting, and add a cycle-behind-a-link regression.**

6. The retained literal is immediately adjacent to the replaced literals, and the assertion uses `_norm` (`evals/multi-model-verify/test_backup_lane.py:1913`, `evals/multi-model-verify/test_backup_lane.py:1931`). The proposed Markdown matches the new pin word for word, including its retained context (`docs/superpowers/plans/2026-09-05-item90-mirror-link-relink.md:208`, `docs/superpowers/plans/2026-09-05-item90-mirror-link-relink.md:239`). The region remains `mirror-path-budget`, already declared by the coverage checker (`skills/multi-model-verify/references/backup-lane.md:770`, `evals/multi-model-verify/test_contract_coverage.py:724`). **PASS.**

7. The six cases contain assertions capable of failing. The reparse-bit check fixes the existing junction-blind `is_symlink()` assertion; the back-channel case checks both the refusal and preserved content; the vanished-target case excludes the successful-build skip message (`evals/multi-model-verify/test_review_mirror.py:1373`, `docs/superpowers/plans/2026-09-05-item90-mirror-link-relink.md:316`, `docs/superpowers/plans/2026-09-05-item90-mirror-link-relink.md:419`, `docs/superpowers/plans/2026-09-05-item90-mirror-link-relink.md:448`).

   Their combined coverage misses the central nested-checkout case. Every proposed target is a plain directory, so the successful cases can hash individual `linked/x.txt` subjects without exercising directory expansion at all (`docs/superpowers/plans/2026-09-05-item90-mirror-link-relink.md:329`, `docs/superpowers/plans/2026-09-05-item90-mirror-link-relink.md:358`, `docs/superpowers/plans/2026-09-05-item90-mirror-link-relink.md:375`). All six can therefore pass while the single-directory-subject branch is broken. The drift case also passes against the existing copying implementation because the source comparison detects the edit first (`tools/new-review-mirror.ps1:720`).

   No proposed case contains a host-specific exclusion between Windows PowerShell 5.1 and PowerShell 7; junction setup can skip on either, and host selection already exists (`docs/superpowers/plans/2026-09-05-item90-mirror-link-relink.md:298`, `evals/multi-model-verify/test_review_mirror.py:42`).

   **FIX — add nested-checkout junction cases covering initial successful verification, content drift, and mirror-only redirection. Assert the single status subject and directory-manifest expansion on both hosts.**

8. The receipt has thirteen required string fields plus `schema`, and rejects additional fields (`tools/dispatch-round.ps1:196`, `tools/dispatch-round.ps1:208`). Both wrapper verifications reuse the same identity arguments (`tools/dispatch-round.ps1:274`, `tools/dispatch-round.ps1:286`, `tools/dispatch-round.ps1:310`). The block reader stops at `links:`, and both scalar readers select their requested prefix (`evals/multi-model-verify/test_review_mirror.py:150`, `evals/multi-model-verify/test_review_mirror.py:1508`, `evals/multi-model-verify/test_dispatch_round.py:156`). **PASS.**

9. The session measurement addresses the operation actually used: recursively removing a directory containing a junction (`docs/superpowers/specs/2026-09-05-mirror-link-relink-design.md:55`, `tools/new-review-mirror.ps1:1071`). The planned regression checks that the first mirror contains a reparse point, performs a forced rebuild, and verifies preserved target content (`docs/superpowers/plans/2026-09-05-item90-mirror-link-relink.md:401`). That is an adequate basis for retaining this deletion operation; one-machine provenance alone does not justify another deletion mechanism. **PASS.**

10. The timing task can write the addon’s Git index through the builder’s source status captures (`tools/new-review-mirror.ps1:1092`, `tools/new-review-mirror.ps1:1169`, `tools/new-review-mirror.ps1:429`; [Git’s documented index refresh](https://git-scm.com/docs/git-status#_background_refresh)). Setting `GIT_OPTIONAL_LOCKS=0` for both timing invocations avoids that optional write without changing the extracted historical tool.

    The file-count oracle is insufficient. It enumerates from the mirror root, whereas the session measurement enumerates with the link itself as the starting path (`docs/superpowers/plans/2026-09-05-item90-mirror-link-relink.md:753`, `docs/superpowers/specs/2026-09-05-mirror-link-relink-design.md:41`). PowerShell documents that directory links encountered during recursion require `-FollowSymlink` for traversal; the measurement does not establish the claimed equal totals ([Get-ChildItem documentation](https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.management/get-childitem?view=powershell-7.5#-followsymlink)). Even equal totals cannot establish equal file names or bytes.

    The output filter also retains the `links:` header while discarding its indented entries, although the report asks for the link evidence (`docs/superpowers/plans/2026-09-05-item90-mirror-link-relink.md:755`, `docs/superpowers/plans/2026-09-05-item90-mirror-link-relink.md:777`).

    **FIX — disable optional Git writes, compare relative file names and hashes beneath each mirror link against its target, retain the complete links block, and require the specific successful-build skip message before recording a successful timing row.**

11. The proposed headers, rankings, symmetric pairing, and digest replacement follow the lint contract (`BACKLOG.md:3`, `evals/tools/backlog_lint.py:55`, `evals/tools/backlog_lint.py:470`, `evals/tools/backlog_lint.py:489`, `docs/superpowers/plans/2026-09-05-item90-mirror-link-relink.md:151`).

    Item 91’s counts contradict its own explanation and the code. Construction performs three manifest hashing passes: source before, source after, and mirror baseline. The old copy adds a fourth content read, not a fourth hashing pass (`tools/new-review-mirror.ps1:1092`, `tools/new-review-mirror.ps1:1100`, `tools/new-review-mirror.ps1:1169`, `tools/new-review-mirror.ps1:1370`). Each verification hashes both sides, so the wrapper’s two verifications perform four passes; `-Prepare` additionally verifies identity (`tools/new-review-mirror.ps1:720`, `tools/new-review-mirror.ps1:739`, `tools/dispatch-round.ps1:286`, `tools/dispatch-round.ps1:310`, `tools/dispatch-round.ps1:459`).

    **FIX — correct item 91’s title and Cost at `docs/superpowers/plans/2026-09-05-item90-mirror-link-relink.md:105`, and the matching four-hashes assertion at `docs/superpowers/specs/2026-09-05-mirror-link-relink-design.md:130`.**

12. The plan specifies both hosts, `main..HEAD`, and the version commit after the debate (`docs/superpowers/plans/2026-09-05-item90-mirror-link-relink.md:831`, `docs/superpowers/plans/2026-09-05-item90-mirror-link-relink.md:835`, `docs/superpowers/plans/2026-09-05-item90-mirror-link-relink.md:842`, `docs/superpowers/plans/2026-09-05-item90-mirror-link-relink.md:844`).

    It omits the behavioral gate applicable to the changed skill prose, background execution and naming for full gates, and restarting the session after installing skill changes (`CLAUDE.md:35`, `CLAUDE.md:57`, `CLAUDE.md:133`, `CLAUDE.md:166`). **FIX — add `python evals/tools/run_behavioral_evals.py --head --changed`, run full gates as named background tasks, and include the required session restart.**

Additional finding: Task 1’s missing-scratchpad fallback creates `README.md`, but its commit command always stages `brief.md` and `reply.md`. The documented fallback therefore reaches a command naming nonexistent files (`docs/superpowers/plans/2026-09-05-item90-mirror-link-relink.md:46`, `docs/superpowers/plans/2026-09-05-item90-mirror-link-relink.md:162`). **FIX — provide the corresponding explicit-path staging command for the README fallback.**

**UNVERIFIED:** Claim 2’s actual `Test-Path $full -PathType Container` result for a junction on both hosts remains unresolved. The branch is at `tools/new-review-mirror.ps1:477`; the recorded measurement describes enumeration, not that predicate (`docs/superpowers/specs/2026-09-05-mirror-link-relink-design.md:41`). Claims 1 and 9 were assessed for internal consistency using the supplied session measurements; their external observations were not independently reproduced.

The instruction text affecting claim 12 is in `CLAUDE.md`: “run them by hand when you touch what they cover” (`CLAUDE.md:35`), “DISPATCH DEBATE ROUNDS AND FULL GATES IN THE BACKGROUND” (`CLAUDE.md:57`), and “restart the session when hooks/ or skills/ changed” (`CLAUDE.md:166`). Those are explicit requirements in the reviewed artifact. My interpretation is that this plan’s contract-prose edit triggers the behavioral and restart requirements; I treated those passages as review evidence.