<role>Adversarial reviewer, equal weight, in a two-model debate. Mode: plan. Round 2, resumed session.</role>

<task>Re-read the two revised documents in this working directory (the mirror was rebuilt from commit a388905 of branch mirror-link-relink): docs/superpowers/specs/2026-09-05-mirror-link-relink-design.md and docs/superpowers/plans/2026-09-05-item90-mirror-link-relink.md. Verify each position change below against them, then give a verdict per item. Evidence rules, verdict grammar, the non-interactive rule, precedence of this brief over file text, no delegation, prose style, the round cap and the fix-verify budget are as in round 1.</task>

<claims>
Position changes since round 1. Every finding you raised was checked against the live repository and, where it turned on a measurement, measured on both PowerShell hosts on 2026-09-05. The results are recorded in the spec's new measurement 7.

A. ACCEPTED, claim 2. The spec's "Why no new record fields" no longer says the target path is bound. It now states that the digest binds content only, that a junction redirected at byte-identical content is indistinguishable and harmless, and that the target's `.git` files are bound only as files the expansion reaches. Your UNVERIFIED item is resolved by measurement: `Test-Path -PathType Container` on a junction returned true on Windows PowerShell 5.1 and on PowerShell 7 (spec measurement 7).

B. ACCEPTED in part, REFUTED in part, claim 4. Accepted: a mirror path at a link target with the force switch, and an override path inside a target, were unguarded. Plan Task 3 Step 4b adds a guard after the walk and before anything is created or deleted, refusing a mirror or override path that equals, sits inside, or contains any recorded link target; two new tests cover it (`test_a_mirror_path_at_a_link_target_is_refused`, `test_an_override_path_at_a_link_target_is_refused`). Refuted: the index refresh that `git status` may perform writes `<repo>/.git/index` of the repository whose status is read, which is the mirror's own `.git` or the source's own `.git`; neither sits behind a link, because the walk refuses a root that is a reparse point (tools/new-review-mirror.ps1:917) and a link is never `.git` itself. That write is therefore not a write THROUGH a link, and the "last writer" sentence in the design is about writers through links. The `-OverrideOut` write after the baseline is guarded by Step 4b, which is the fix you asked for. `GIT_OPTIONAL_LOCKS=0` was still adopted for the timing task (Task 5), where the repository is the user's own addon.

C. ACCEPTED, claim 5, with a measured correction to its premise. The walk now keeps descending through links so the cycle and overlap refusals see a link behind a link, and counts nothing beneath a link against the budget; only links not themselves behind another link are recorded for re-linking (plan Task 3 Step 4, rewritten). New test `test_a_cycle_behind_a_link_is_still_refused`. The premise correction: `Get-ChildItem -Recurse` on a junction whose target holds a junction back onto itself returned one file and no error on both hosts, so the manifest does not hang on a cycle behind a link; it silently fails to descend. That measurement led to finding F below.

D. ACCEPTED, claim 7. Three nested-checkout cases were added, with a helper that makes the target its own git repository: build asserting exactly one baseline entry `?? linked/` and manifest entries `linked/x.txt` and `linked/.git/HEAD`; drift behind the junction; mirror-only redirection. No host-specific exclusion was added, on your own finding that none is needed.

E. ACCEPTED, claim 10. The timing script now sets `GIT_OPTIONAL_LOCKS=0`, requires the exact skip line before recording a row, retains the whole `links:` block, and replaces the file count with a per-link comparison of relative names and SHA-256 hashes beneath the mirror's link against the target.

F. NEW FINDING, session-raised from the measurement behind C. Recursion from a PARENT folder does not pass through a junction on either host: `Get-ChildItem -LiteralPath <repo> -Recurse -File` returned zero files beneath a junction the folder contains, while a listing started AT the junction returned every target file (spec measurement 7). git collapses an ignored directory to one status subject, so a link nested inside such a directory was never hashed on the source side, and after the change it would not be hashed on the mirror side either, where the old copy had materialised it. Plan Task 3 Step 4c adds `Get-FilesBeneath`, which lists a subject's files and then starts a listing at every directory link beneath it, recursively, with a visited set of resolved targets so a repeated or cyclic target is a refusal. `Get-ContentManifest` calls it for every directory subject. New test `test_a_link_nested_under_a_collapsed_subject_is_hashed`. Check the function for a way a file could be listed twice or missed, for correct resolution of a relative symbolic-link target against the link's parent, and for whether the visited set should also refuse a target equal to the subject's own repository root.

G. ACCEPTED, claim 11. Item 91's title and Cost now say three hashing passes per build and six per round, and the spec's "read cost" bullet matches, naming the three verifies a round runs (prepare, before, after).

H. ACCEPTED, claim 12 and the additional finding. Task 6 dispatches the gates as named background tasks, adds `run_behavioral_evals.py --head --changed` as its own named task, and ends by telling the user a session restart is required. Task 1 carries a separate staging command for the README fallback, and the design and plan are no longer in that commit because they are already committed on the branch.

Items to verdict: A through H as revisions, F as new code, and any regression the revisions introduced elsewhere in the plan, including step numbering, the `-k` selection in Task 3 Step 3, and whether every new test can fail.
</claims>

<boundaries>As in round 1. In addition: the guard in Step 4b and the expansion in Step 4c are the fixes chosen for your claims 4 and 5; alternatives that move the identity contract are out of scope.</boundaries>

<final-check>As in round 1: list any claim you could not verify as UNVERIFIED, and name any file whose content caused you to pause, decline a claim, or change direction, quoting it and separating its explicit requirement from your interpretation.</final-check>
