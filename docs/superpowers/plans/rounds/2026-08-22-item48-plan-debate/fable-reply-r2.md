Round 2, Fable lane. Continuity: nonce **FABLE-I48-7QX2**; the "strongest task" was **Task 7**, because it pre-named both failure directions and the Windows parent-PATH resolution trap with honest stop-and-record rules. Both from memory of round 1.

Subject verdicted: `docs/superpowers/plans/2026-08-22-item48-pwsh7-feasibility.md` at commit `4efc7b9` (I read the working tree on `item51-inline-brief-transport`; it carries every amendment the brief describes, and I have no way to hash it against blob `e2a87c41` with read-only tools — if the tree has moved past `4efc7b9`, this verdict is of the tree).

Position changes on relayed items: 1-12 all remain ACCEPTED as implemented, with the exceptions below where the implementation of an accepted fix carries a new instance. Nothing struck.

## B first, since it is the ask that matters: the amendments reproduce the class, five times

1. **Amendment 2 (docs/ prefix exception) fixes Task-3-time instances and leaves the same mechanism open for every later task.** The exception (plan lines 564-576) orders explicit rows "for every match inside the record directory" once, during Task 3. Tasks 4 and 7 then CREATE new tracked files under `&lt;REC&gt;` full of family matches — `reexec/parent.ps1` alone carries `-File` (lines 700, 702) and `ProcessStartInfo` (line 704), `run.py` carries `powershell.exe` and `pwsh.exe` paths (739-740) and `subprocess.run` — and no later task re-applies the exception. `covered_by_prefix` (392-397) needs no explicit row to cover a new file, so at Task 9 Step 1 the re-run (1364-1369) comes back green with those executed scripts attested `record — never executed` by the blanket row. That is verbatim the hazard the amendment's own rationale states at lines 573-575, one task later. FIX: extend the exception into a rule — any task that adds a family match under `&lt;REC&gt;` adds its explicit rows before its commit — or have Task 9 Step 1 run `--emit` filtered to the record directory and require it empty of prefix-only coverage.

2. **Amendment 12 (named-parameter arm) is internally inconsistent as specified, and drops its own control.** (a) `run_named` returns keys `returncode/stderr/child_ran/bound/bound_exact` (895-901), but `main()`'s `broken` check reads `v["stage_a_parent_exact"]` and `v["stage_b_child_count"]` (817-819) and the summary line reads `v["stage_b_child_exact"]` (825-826) — on named-arm dicts those are KeyErrors. The plan says only "extend main()'s loop... and extend the broken check" (904-905), so the reconciliation is left to implementer improvisation inside the one arm that can produce a NO. (b) parent.ps1 writes `PROBE_PARENT_OUT` in named runs too, but `run_named` unlinks it (872-874) and never reads it — the named arms have no stage A at all, the control the driver's own docstring calls "what makes it meaningful" (720-723). FIX: specify the extended `main()` verbatim with uniform keys, and record stage A for named arms.

3. **Amendment 9 (hook's own shape) introduces a hang in its success path.** The real hook's first act is `[Console]::In.ReadToEnd()` (hooks/superpowers-review-companion.ps1:13). probe.py invokes it via `subprocess.run` with inherited stdin and NO timeout (plan 1232-1234) — contrast run.py's `timeout=300` (786). In exactly the outcome Step 3 pre-names as a finding (parent-PATH resolution succeeds, 1257-1266), the real hook launches and blocks on an unclosed inherited stdin, and the probe hangs with no oracle firing. The replaced `-Command Write-Output ok` could not do this. FIX: `stdin=subprocess.DEVNULL` and a `timeout=` in probe.py.

4. **Step 2b (asserted facts replaced with captured ones) captures a value that does not measure the fact.** The skeleton line asserts "cut from `main` at `6842547`" (line 82); Step 2b's second command is `git rev-parse --short HEAD` (220). HEAD equals the cut point only while the branch has no commits; the plan file itself typically lands on the branch first in this repo, and then the captured HEAD written into a line that says "cut from main at" is a false claim manufactured by the fix. FIX: capture `git merge-base main HEAD` for that field.

5. **Amendment 5 (verdict rules) forward-references a later step, and the placeholder replacement was lost in the rewrite.** Rule 4 gates the verdict on "any residual limit from Step 4" (1431-1434), but Step 4 — collecting the residuals — runs AFTER Step 3 writes the verdict line (1440). And where round 1's Task 9 said "Replace `NOT YET WRITTEN.` under `## Verdict`", the amended Steps 2-3 say only "write... as subsections" and "Add a subsection" (1386-1387, 1401), so literal execution leaves the skeleton's `NOT YET WRITTEN. Filled by the final task...` (line 91) standing in the shipped record — and the Step 1 grep gate (1373-1377) tolerates exactly that one hit and never re-runs. FIX: swap Steps 3 and 4 (or point rule 4 at the sections directly), and say "replace the placeholder" explicitly.

## A. The rest of the amended plan, same class

- survey.py's narrowing comment: "the extra 207 were prose references that no migration would edit" (298-300). A universal characterization of 207 lines, and it has at least one counterexample in the dropped set (see C): README.md:392 is a prose reference a migration plausibly must edit — an instruction to run a shipped .ps1 from an unspecified host. State it as "sampled" or as "prose references, not re-read individually".
- Task 2 Step 3 rationale: "its own source contains every host literal, launch verb and bare-invocation token" (459-460). True as token containment, but only the host (`pwsh`) and launch (`subprocess.run`, `Start-Process`, `-File`) families actually self-MATCH; `powershell.exe` appears only as `powershell\.exe`, and no bare alternative matches its own source line. The load-bearing conclusion (commit first, N reproducible) survives; the sentence is one notch wider than the mechanism.
- Nit: the survey.py comment cites `.github/workflows/skill-evals.yml:70` (286-287) — line 70 is the step name; the `run:` match is at :71.
- Cosmetic: Task 4 has two steps numbered 6 (925, 950).
- Otherwise the amended Task 1 check (whole-bullet, heading order — 161-205) is sound against backlog 3568-3580, both headings unique; Task 5's revision binding, Task 6's runs/reads/mentions, Task 8's step-timing and gross-vs-net text all state claims at the width of their evidence. The counts 3977/3243/734/120/241/373 are UNVERIFIED by me — I cannot execute — and the plan correctly labels them observations, not targets (480-481, 500-502).

## C. What the narrowing drops

Real instances found: **README.md:392** (`.\evals\tools\drift_statemachine_tests.ps1`, an instruction to run a shipped script with no arguments — alt 2's `(?=\s+-)` lookahead fails, no call operator, no host name, no launch verb) and **CLAUDE.md:41** (same script in backticks, followed by `` ` `` not a flag). Both are `doc-instruction` entry points by the plan's own vocabulary (554), both tracked, both outside `docs/`, and both are item 48's entry-point 6 — "a human running any of the shipped scripts by hand" (backlog 3510-3511). The cited SKILL.md:94 instance, by contrast, IS caught (`-RepoRoot` satisfies the lookahead — verified). The narrowing is real and recorded as such, which is right; but the flagless-invocation shape should either join the family (`\.ps1` at end-of-token in instruction context) or be named in `### What this method cannot see`.

## D. The split's seams

One real seam: `load_rows()` silently last-wins on duplicate keys — `rows[(rel, int(line), fam)] = ...` (388) with no duplicate check, so a row double-written by two subagents (or one subagent appending twice) with CONFLICTING classifications is invisible to every oracle including the `FAMILY` line, which counts hits, not rows. FIX: `sys.exit` on a duplicate key in `load_rows()`. Lost rows are the safe direction — an unclassified match reappears red. Minor friction, not a seam: the host and launch subtasks end with an uncommitted TSV (only `bare` runs Steps 3-5, line 499), which sits in tension with the "commit after every task" constraint (54) and loses work to a crash between subagents, though visibly.

## E. Anything else

Nothing beyond the Task 9 ordering already named in B5. Task order across tasks remains sound; Task 8 consumes Task 5's run ids, which precede it.

The plan's architecture survived amendment intact, the five findings above are all bounded, specified fixes inside amendments, and three of them (B1, B2, B3) sit in the two arms the verdict most depends on — the inventory's completeness gate and the NO-arm.

FIX
