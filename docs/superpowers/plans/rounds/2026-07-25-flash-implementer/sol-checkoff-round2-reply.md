1. **BLOCKING — [plan:616](C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-flash-implementer.md:616), [plan:622](C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-flash-implementer.md:622), [agent:58](C:/Users/Brandon/Documents/parallax/agents/flash-implementer.md:58): Task 6’s green run cannot satisfy the new clean-tree preflight.** Scratch setup creates `hello.py` but never stages and commits it, so `git status --porcelain` is non-empty and the real agent blocks before dispatch. After the green run, `hello.py` remains modified, contaminating the later no-write oracle and blocked-run `FILES CHANGED` report. **Fix:** commit the initial scratch baseline in Step 2, then commit or reset the green result and verify an empty porcelain status before each subsequent probe.

2. **BLOCKING — [plan:590](C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-flash-implementer.md:590), [plan:279](C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-flash-implementer.md:279): Task 5 still dry-checks the obsolete three-preflight contract.** It checks only models, trust, and a “repo-scoped” rule, while the amended agent requires five checks and bans every `write_file(` rule regardless of path. Thus Task 5 can claim its produced settings state is ready while the real agent immediately blocks. **Fix:** make Step 6 execute all five checks verbatim: model, trust, absence of any `write_file(` entry, clean porcelain status, and absence of stale brief files. If an unrelated write rule remains, block for user disposition rather than silently deleting shared configuration.

3. **BLOCKING — [test:39](C:/Users/Brandon/Documents/parallax/evals/multi-model-verify/test_flash_implementer.py:39), [test:68](C:/Users/Brandon/Documents/parallax/evals/multi-model-verify/test_flash_implementer.py:68): the safety-critical F2/F4 amendments are not actually pinned.** The current assertions would still pass if the agent regressed to path-scoped allow-rule matching, removed the stale-brief preflight, or stopped guaranteeing cleanup: they merely require `write_file(`, “allow rule,” the brief prefix, and “sole transient exception” somewhere in the body. **Fix:** assert the exact broad-ban sentence, stale-brief prohibition, log-basename uniqueness rule, success/failure cleanup requirement, and ROUTE’s transcript path. This makes the claimed offline enforcement real.

4. **MINOR — [plan:646](C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-flash-implementer.md:646): Step 4b deliberately installs the design’s durable bypass class without interruption-safe restoration.** “Then REMOVE” is insufficient if the attended run aborts between mutation and cleanup. **Fix:** preserve the original settings content and restore it in `finally`; preferably use a nonmatching sentinel `write_file(` entry because preflight only needs to detect the class, not grant a functional scratch permission.

5. **MINOR — [spec:110](C:/Users/Brandon/Documents/parallax/docs/superpowers/specs/2026-07-25-flash-implementer-design.md:110), [spec:245](C:/Users/Brandon/Documents/parallax/docs/superpowers/specs/2026-07-25-flash-implementer-design.md:245), [plan:7](C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-flash-implementer.md:7), [plan:705](C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-flash-implementer.md:705): several authoritative cross-references remain stale.** They still say log/tree, workdir-scoped allow rule, or log-only ROUTE; resolved point 7 says it was superseded by point 13, but the replacement red-probe resolution is point 14. **Fix:** synchronize these references to transcript/tree, the global rule-class ban, log-plus-transcript provenance, and `#14`.

**Verdict: FIX.** The original five findings are substantively resolved in the current agent and debate record: transcript evidence is correctly selected, the backup route and containment evidence are retained, the appendix now follows the required schema, the broad rule-class ban is implemented, and the red probes are separated. The disclosed agent/plan line-wrap is word-for-word identical and is not a finding. Fresh static checks confirmed a clean tree, wordwise plan/agent equality, and only the allowed model-literal homes; the reported `10 passed` and `143 passed, 1 skipped` remain **UNVERIFIED** here because this sandbox still has no Python interpreter. Check-off remains withheld because Tasks 5–6 are not executable under the amended five-preflight contract and the tests do not yet enforce two load-bearing amendments. The later mode-diff debate remains separate.