2026-07-25T20:29:58.438631Z ERROR codex_models_manager::cache: failed to load models cache: missing field `supports_reasoning_summaries` at line 86 column 5
OpenAI Codex v0.144.1
--------
workdir: C:\Users\Brandon\Documents\parallax
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: read-only
reasoning effort: high
reasoning summaries: none
session id: 019f9ad5-e318-7831-8a3f-0c02c94caa8e
--------
user
# Round 3 — round-2 findings resolved

All five round-2 findings accepted and applied; session verification per
finding:

1. (Task 6 clean-tree) Step 2 now COMMITS the scratch baseline before
   trust; Step 3 verifies porcelain-empty before dispatch and commits the
   green result afterward with a second porcelain-empty check, so 4a/4b
   start clean.
2. (Task 5 dry-check) Step 6 now runs all FIVE preflight checks verbatim
   (models, trust, no write_file( entry at all, porcelain-empty, no stale
   AGY-TASK-BRIEF-*), and an unrelated write rule STOPS for user
   disposition — never silent deletion of shared configuration.
3. (pins) test_flash_dispatch_contract pins "the dispatch log file's
   basename" and "on success, failure, and interruption alike" exactly;
   test_flash_preflight_pins pins the full "any `write_file(` entry,
   whatever path it names" fragment and "No file matching
   `AGY-TASK-BRIEF-*`"; new test_flash_route_report_carries_transcript
   pins "AND the brain transcript's path". Test file = plan block
   verbatim; 11/11 and full suite 144 passed 1 skipped (implementer +
   re-reviewer evidence; your sandbox has no interpreter).
4. (Step 4b restore) The probe now saves the ORIGINAL settings content
   first, plants a NONMATCHING sentinel `write_file(/parallax-sentinel-
   never-matches/)` (class detection without functional grant), and
   restores the saved content afterward including on
   aborted/interrupted runs (restore-first on resume), with a re-read
   confirming the restored file matches the saved copy.
5. (stale refs) All synchronized: transcript/tree corroboration wording
   in plan header + spec Decisions + spec section 6; workdir-scoped
   language replaced by the class ban everywhere; resolved point 7 now
   points at #14; resolved rows 15-19 added for your round-2 findings.

Commits since round 2: 96a8224 (spec+plan amendments), 7cae001 (test-pin
sync; one more disclosed line-join in the agent file so the
interruption-cleanup pin matches contiguously — word-for-word identical,
scoped re-review PASS, same class as the previously disclosed join).

Deliver your round-3 verdict on the check-off: findings if any remain,
then PASS / FIX / ESCALATE.


codex
I’ll run the same closing protocol against only `3b5074f..HEAD`, then re-open the amended Task 5/6 and test/agent contracts in full where needed. I’ll treat the reported Python counts as UNVERIFIED unless this sandbox has gained an interpreter.
  → Inspect round-3 commits and amended evidence
  • Re-verify all five round-2 findings in current files
  • Check for amendment regressions and issue verdict
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "git status --short --branch
git log --oneline --decorate -6
git show --stat --oneline 96a8224
git show --stat --oneline 7cae001
git diff --check 3b5074f..HEAD -- ':"'!docs/superpowers/plans/rounds/2026-07-25-flash-implementer/sol-checkoff-round1-header.txt'"'
Get-ChildItem -LiteralPath 'docs\\superpowers\\plans\\rounds\\2026-07-25-flash-implementer' -File | Sort-Object Name | Select-Object Name,Length" in C:\Users\Brandon\Documents\parallax
 succeeded in 579ms:
## feat/0120-flash-implementer
7cae001 (HEAD -> feat/0120-flash-implementer) 0.12.0: strengthen contract pins per sol round-2
96a8224 0.12.0: fold sol check-off round-2 amendments into spec and plan
3b5074f 0.12.0: sync tests and agent to sol round-1 amendments
6348a47 0.12.0: fold sol check-off round-1 amendments into spec and plan
bce91b8 0.12.0: single-source implementer model literals
f8b51e4 0.12.0: add flash-implementer agent, mark shared contract
96a8224 0.12.0: fold sol check-off round-2 amendments into spec and plan
 .../plans/2026-07-25-flash-implementer.md          | 77 ++++++++++++++++------
 .../specs/2026-07-25-flash-implementer-design.md   | 14 ++--
 2 files changed, 63 insertions(+), 28 deletions(-)
7cae001 0.12.0: strengthen contract pins per sol round-2
 agents/flash-implementer.md                        |  5 +----
 evals/multi-model-verify/test_flash_implementer.py | 19 ++++++++++++++++---
 2 files changed, 17 insertions(+), 7 deletions(-)

Name                           Length
----                           ------
clone-status-after-round2.txt      25
plan-round1-brief.md             2953
plan-round1-transcript.txt      11336
plan-round2-transcript.txt       4893
sol-checkoff-round1-brief.md     2645
sol-checkoff-round1-header.txt 389791
sol-checkoff-round1-reply.md     6826


context compacted
codex
The branch is clean and the amendment range is exactly the two disclosed commits. I’m now checking the amended text against the shipped agent/tests and, separately, whether the debate record’s claimed raw artifacts actually exist.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git diff --unified=80 3b5074f..HEAD -- docs/superpowers/specs/2026-07-25-flash-implementer-design.md docs/superpowers/plans/2026-07-25-flash-implementer.md agents/flash-implementer.md tests/test_flash_implementer.py' in C:\Users\Brandon\Documents\parallax
 succeeded in 335ms:
diff --git a/agents/flash-implementer.md b/agents/flash-implementer.md
index b448f48..242b9d1 100644
--- a/agents/flash-implementer.md
+++ b/agents/flash-implementer.md
@@ -1,127 +1,124 @@
 ---
 name: flash-implementer
 description: Zero-judgment Flash implementer for frozen-plan tasks. Use when executing build tasks from a debate-frozen implementation plan - give it ONE task's verbatim text plus the plan's Global Constraints and a log-file path. It delegates ALL code-writing to Gemini 3.6 Flash via the Antigravity CLI headlessly, verifies route and authorship evidence, runs the task's verification itself, and reports. It never types repo code and never makes design decisions.
 model: haiku
 tools: Read, Grep, Glob, Bash
 ---
 
 # Flash implementer (agy wrapper)
 
 You supervise ONE task from a frozen implementation plan. Gemini 3.6 Flash
 does ALL the typing through the Antigravity CLI (`agy`); you do preflight,
 dispatch, evidence checks, verification, and honest reporting. You never
 edit or create repo files yourself — your tool grant has no Edit or Write,
 and using Bash to write repo content is equally forbidden: a changed file
 the brain transcript cannot account for fails the task. The one declared
 carve-out is the brief file below — the sole transient exception to the
 never-write rule, and it never survives to the evidence checks.
 
 <!-- shared-contract:start -->
 ## The contract
 
 - Build exactly what the task says: the files it lists, the code it shows,
   the commands it specifies. Nothing else.
 - No improvements, no drive-by refactors, no added error handling, no scope
   adjustments. A deviation is a defect even when it looks better — the diff
   gets checked against the plan afterward, and unexplained drift fails it.
 - **INPUT GAP rule:** if the task references a file, interface, value, or
   convention that is not in your brief and not discoverable at the exact
   path the task names, STOP and report the gap. Never invent or guess the
   missing piece.
 - Run the task's verification commands yourself and read the output. Never
   claim completion without re-running verification — "should work" means
   the task is not done.
 <!-- shared-contract:end -->
 
 ## Inputs (from the dispatching controller)
 
 - The task's verbatim text and the plan's Global Constraints.
 - The workspace directory (this cycle: the main checkout only).
 - A log-file path OUTSIDE the workspace (the controller owns it; you never
   place logs in the repo tree).
 
 ## Preflight (all five must pass BEFORE dispatch)
 
 1. `agy models` (binary at `$LOCALAPPDATA/agy/bin/agy.exe`) — output must
    contain `gemini-3.6-flash-medium`. Anything else (missing binary,
    sign-out, missing model) is blocked.
 2. `~/.gemini/antigravity-cli/settings.json` — `trustedWorkspaces` must
    contain the workspace directory. If not: blocked, and the report quotes
    the fix ("run one interactive `agy` session in the workspace and approve
    trust").
 3. The same settings file must carry NO file-writing per-tool allow rule
    at all — any `write_file(` entry, whatever path it names, is blocking.
    A persisted settings allow rule is the durable, call-site-invisible
    bypass class — its absence is the load-bearing permission control; path
    spellings vary, so the rule CLASS is banned rather than path-matched.
    If present: blocked, quoting the rule.
 4. `git status --porcelain` in the workspace must be EMPTY. A dirty tree
    makes authorship attribution impossible — blocked, quoting the paths.
 5. No file matching `AGY-TASK-BRIEF-*` exists in the workspace — a stale
    brief means an earlier dispatch died mid-cleanup: blocked.
 
 ## Dispatch
 
 1. Write the brief to `<workspace>/AGY-TASK-BRIEF-<unique>.md` with a Bash
    heredoc — `<unique>` is the dispatch log file's basename, so briefs
    never collide. Content: the task's verbatim text, the Global
    Constraints, and the exact files list. (stdin does not reach the model
    in print mode — probed 2026-07-25; the workspace brief file is the
    delivery mechanism.) This file is the sole transient exception to your
    never-write rule.
 2. Run (single line):
    `agy -p "Read the file AGY-TASK-BRIEF-<unique>.md in the workspace and execute it exactly." --model gemini-3.6-flash-medium --add-dir <workspace> --log-file <log-path>`
-3. Delete the brief file immediately after agy exits — on success,
-   failure, and interruption alike — and always BEFORE any evidence
-   check, so it never appears in `git status`. If your run is resumed
-   after an interruption, delete any leftover brief FIRST.
+3. Delete the brief file immediately after agy exits — on success, failure, and interruption alike — and always BEFORE any evidence check, so it never appears in `git status`. If your run is resumed after an interruption, delete any leftover brief FIRST.
 
 ## Route and authorship checks (every run)
 
 - On the log file: `Print mode: starting` line present containing
   `model="gemini-3.6-flash-medium"`.
 - On the log file: `Propagating selected model override` line present
   (presence only — its display label is not matched).
 - Transcript/tree corroboration: parse `conversationID="<uuid>"` from the
   log's `Print mode: starting` line, then read the brain transcript at
   `~/.gemini/antigravity-cli/brain/<conversationID>/.system_generated/logs/transcript_full.jsonl`
   (the `--log-file` log itself carries NO file actions — probed). Every path git status reports changed must appear in the brain transcript as a successful file-changing action. A changed file the transcript never
   mentions means someone other than Flash typed it — blocked, no matter
   what the tests say. A missing transcript is blocked.
 - This evidence is client-side: report the route as **requested and
   propagated**, never "used and confirmed". Server-side substitution is
   not detectable from this evidence class.
 
 ## Failure handling — loud, never silent
 
 Blocked (quote the exact output) on: any preflight failure, the print-mode
 soft-deny line ("auto-denied"), nonzero exit, a missing or mismatched
 route line, a corroboration mismatch, or writes diverted to agy's internal
 scratch (expected files absent from the tree). Never retry with
 `--dangerously-skip-permissions` — that flag is forbidden in this lane, as
 is ANY approval-bypass flag or persisted per-tool allow rule added to agy
 settings. Never complete the work yourself: rerouting a blocked task to a
 Claude tier is the user's decision, recorded in the plan's Escalated
 points — not yours.
 
 ## Report format (your final message)
 
 - **STATUS:** done | blocked | INPUT GAP: <exactly what is missing>
 - **ROUTE:** the resolved model ID as requested and propagated, plus the
   retained log file's path AND the brain transcript's path
 - **FILES CHANGED:** actual paths from `git status` — on blocked, STILL
   list every path Flash already touched so the session can revert a
   partial write
 - **VERIFICATION:** each command you ran yourself, with its real output
   (condensed)
 - **DEVIATIONS:** must be "none" — anything else means you stopped and are
   explaining why the task could not be built as written
 
 ## Lane note
 
 This agent pins the Flash implementation lane. Canonical model literal:
 `gemini-3.6-flash-medium` (Gemini 3.6 Flash, medium reasoning effort,
 Antigravity CLI resolved ID). The literal lives ONLY here;
 `implementer.md` pins its own lane's model in its frontmatter and Lane
 note — every other surface points at the agent files. Trust is per-directory and interactive-only, so this lane runs in
 the main checkout this cycle — a worktree trust story is future work.
diff --git a/docs/superpowers/plans/2026-07-25-flash-implementer.md b/docs/superpowers/plans/2026-07-25-flash-implementer.md
index e678fed..da5c89a 100644
--- a/docs/superpowers/plans/2026-07-25-flash-implementer.md
+++ b/docs/superpowers/plans/2026-07-25-flash-implementer.md
@@ -1,195 +1,208 @@
 # Flash Implementer Lane Implementation Plan
 
 > **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
 
 **Goal:** Add `agents/flash-implementer.md`, a haiku-wrapper SDD implementer that delegates all code-writing to Gemini 3.6 Flash via the Antigravity CLI (agy) headlessly, with mechanically enforced route and authorship checks.
 
-**Architecture:** Architecture A from the spec (`docs/superpowers/specs/2026-07-25-flash-implementer-design.md`): a new wrapper agent beside the untouched `implementer.md`. The wrapper never types repo code (tools allowlist without Edit/Write + log/tree corroboration); every failure is loud (`STATUS: blocked`); reroute is consent-gated. Model literal single-sourced to the two agent files.
+**Architecture:** Architecture A from the spec (`docs/superpowers/specs/2026-07-25-flash-implementer-design.md`): a new wrapper agent beside the untouched `implementer.md`. The wrapper never types repo code (tools allowlist without Edit/Write + transcript/tree corroboration); every failure is loud (`STATUS: blocked`); reroute is consent-gated. Model literal single-sourced to the two agent files.
 
 **Tech Stack:** Claude Code agent frontmatter, agy 1.1.7 print mode, pytest (offline contract pins), PowerShell (drift snapshot).
 
 **STATUS: FROZEN** (re-frozen pending Sol round 2). Frozen 2026-07-25 by the backup-lane plan debate (Kimi K3-256k); REOPENED same day by the primary reviewer's check-off (Sol round 1, FIX — codex window reset early): five blocking findings, all session-verified and accepted; amendments folded into spec, this plan, and the Tasks 1-2 artifacts via an SDD fix wave. The diff debate gates the merge as always. Tasks 5-6 contain ATTENDED steps (user-interactive); Task 6 additionally requires the plugin dev-loop (bump, update, restart).
 
 ## Global Constraints
 
 - The canonical implementer-lane literal is the RESOLVED ID `gemini-3.6-flash-medium`; after this cycle it appears ONLY in `agents/implementer.md` and `agents/flash-implementer.md` (spec section 5).
 - The wrapper's tools allowlist is exactly Read, Grep, Glob, Bash — no Edit, no Write, no NotebookEdit (spec section 1).
 - Route language is "requested and propagated", never "used and confirmed" (spec section 2; doctor check-4 discipline).
 - Loud failure only: any preflight, dispatch, route-check, or corroboration failure is `STATUS: blocked` with quoted evidence; reroute to a Claude tier is a user decision recorded under the frozen plan's Escalated points (spec section 4).
 - The Flash lane runs in the MAIN CHECKOUT ONLY this cycle (spec Decisions).
 - Probed facts this plan treats as fixed (2026-07-25, agy 1.1.7): stdin does NOT reach the model in print mode; a workspace file named in a short `-p` pointer is read reliably (probed to 7,152 bytes); default-verbosity logs carry NO response-side model attribution (client-side `Print mode: starting` / `Propagating selected model override` lines only).
 - All new test code and evals content is pure ASCII.
 - Commits: lowercase imperative, prefixed `0.12.0:`, no AI-attribution trailers.
 - This cycle's tasks are built by the CURRENT implementer lanes (sonnet/haiku) — the Flash lane cannot build itself.
 - Frozen after debate: changes require reopening the debate.
 
 ---
 
 ### Task 1: Contract tests (all failing first)
 
 **Files:**
 - Create: `evals/multi-model-verify/test_flash_implementer.py`
 - Test: same file (this task is the test suite)
 
 **Interfaces:**
 - Consumes: `agents/implementer.md` (shared-block markers arrive in Task 2 — the parity test FAILS until then; that is expected order).
 - Produces: the pinned strings Tasks 2-3 must satisfy verbatim. Later tasks copy pinned text FROM these tests, never the reverse.
 
 - [ ] **Step 1: Write the failing test file**
 
 ```python
 """Contract pins for the Flash implementer lane (agents/flash-implementer.md).
 
 Amended by design spec 2026-07-25 (advisory review B1-B8): these tests pin
 the agent file's contract text so drift in the dispatch recipe, route
 check, forbidden-bypass class, or report format fails offline with zero
 CLI calls. The two agent files are the only allowed homes for the
 implementer model literal.
 """
 import re
 from pathlib import Path
 
 REPO = Path(__file__).resolve().parents[2]
 FLASH = REPO / "agents" / "flash-implementer.md"
 CLASSIC = REPO / "agents" / "implementer.md"
 CANONICAL_ID = "gemini-3.6-flash-medium"
 SHARED_START = "<!-- shared-contract:start -->"
 SHARED_END = "<!-- shared-contract:end -->"
 
 
 def _read(p):
     return p.read_text(encoding="utf-8")
 
 
 def _frontmatter(text):
     m = re.match(r"^---\n(.*?)\n---\n", text, re.DOTALL)
     assert m, "missing frontmatter"
     return m.group(1)
 
 
 def test_flash_frontmatter_pins_model_and_tools():
     fm = _frontmatter(_read(FLASH))
     assert re.search(r"^model: haiku$", fm, re.MULTILINE)
     m = re.search(r"^tools: (.+)$", fm, re.MULTILINE)
     assert m, "tools allowlist missing"
     tools = [t.strip() for t in m.group(1).split(",")]
     assert sorted(tools) == ["Bash", "Glob", "Grep", "Read"]
 
 
 def test_flash_dispatch_contract():
     body = _read(FLASH)
     assert "--model " + CANONICAL_ID in body
     assert "--add-dir" in body
     assert "--log-file" in body
-    # unique-suffix brief name + lifecycle (Sol check-off F2)
+    # unique-suffix brief name + full lifecycle, pinned by exact sentence
+    # fragments so a regression cannot pass on loose keywords
+    # (Sol check-off round 2, finding 3)
     assert "AGY-TASK-BRIEF-" in body
     assert "sole transient exception" in body.lower()
+    assert "the dispatch log file's basename" in body
+    assert "on success, failure, and interruption alike" in body
     # stdin is probed-dead in print mode; the body must not suggest it
     assert "stdin" not in body.lower() or "does not reach" in body.lower()
 
 
 def test_flash_route_check_strings():
     body = _read(FLASH)
     assert 'Print mode: starting' in body
     assert 'model="' + CANONICAL_ID + '"' in body
     assert "Propagating selected model override" in body
     assert "requested and propagated" in body
     assert "used and confirmed" not in body.replace(
         'never "used and confirmed"', "")
     # transcript/tree corroboration (Sol check-off F1: the log carries no
     # file actions; evidence lives in the brain transcript)
     assert "transcript_full.jsonl" in body
     assert "conversationID" in body
     assert ("every path git status reports changed must appear in the "
             "brain transcript as a successful file-changing action"
             ) in body.lower()
 
 
 def test_flash_preflight_pins():
     body = _read(FLASH)
     assert "agy models" in body
     assert "trustedWorkspaces" in body
-    # conservative rule-class ban (Sol check-off F4) + clean baseline (F2)
-    assert "write_file(" in body
+    # conservative rule-class ban + clean baseline + stale-brief check,
+    # pinned by exact sentence fragments (Sol check-off round 2,
+    # finding 3): loose keyword pins would still pass a regression to
+    # path-scoped matching or a dropped preflight
+    assert "any `write_file(` entry, whatever path it names" in body
     assert "allow rule" in body
     assert "git status --porcelain" in body
+    assert "No file matching `AGY-TASK-BRIEF-*`" in body
+
+
+def test_flash_route_report_carries_transcript():
+    body = _read(FLASH)
+    assert "AND the brain transcript's path" in body
 
 
 def test_flash_forbidden_bypass_class():
     body = _read(FLASH)
     assert "--dangerously-skip-permissions" in body
     idx = body.find("--dangerously-skip-permissions")
     window = body[max(0, idx - 200):idx + 200].lower()
     assert "never" in window or "forbidden" in window
     assert "persisted" in body and "settings" in body
 
 
 def test_flash_report_headings():
     body = _read(FLASH)
     for heading in ("**STATUS:**", "**ROUTE:**", "**FILES CHANGED:**",
                     "**VERIFICATION:**", "**DEVIATIONS:**"):
         assert heading in body, heading
     # the four shared headings are pinned in BOTH files so a unilateral
     # rename in implementer.md cannot pass the suite (ROUTE is
     # lane-specific to the flash file)
     classic = _read(CLASSIC)
     for heading in ("**STATUS:**", "**FILES CHANGED:**",
                     "**VERIFICATION:**", "**DEVIATIONS:**"):
         assert heading in classic, heading
 
 
 def _shared_block(text, path):
     assert SHARED_START in text and SHARED_END in text, (
         "missing shared-contract markers in " + str(path))
     return text.split(SHARED_START)[1].split(SHARED_END)[0]
 
 
 def test_shared_contract_parity():
     # byte-identical shared block; ROUTE is lane-specific and lives
     # outside the block (spec section 1)
     assert _shared_block(_read(FLASH), FLASH) == _shared_block(
         _read(CLASSIC), CLASSIC)
 
 
 def test_classic_lane_note_retired_stale_claim():
     body = _read(CLASSIC)
     assert "Nothing else in the plugin references" not in body
     assert "flash-implementer" in body
 
 
 SWEEP_GLOBS = [
     "skills/**/*.md", "commands/*.md", "tools/*.ps1", "hooks/*",
     "evals/**/*.py", "evals/**/*.json", "evals/**/*.ps1",
     "README.md", "CLAUDE.md", "agents/*.md",
 ]
 # The two agent files are the contract homes; this test file necessarily
 # carries the literal as its enforcement pin.
 ALLOWED = {FLASH.resolve(), CLASSIC.resolve(),
            Path(__file__).resolve()}
 
 
 def test_flash_literal_single_source():
     offenders = []
     for pattern in SWEEP_GLOBS:
         for p in REPO.glob(pattern):
             if p.resolve() in ALLOWED:
                 continue
             if "gemini-3.6-flash" in p.read_text(encoding="utf-8",
                                                  errors="replace"):
                 offenders.append(str(p))
     assert offenders == []
 
 
 def test_sonnet_implementer_literals_removed():
     readme = _read(REPO / "README.md")
     assert "currently `model: sonnet`" not in readme
     # absence pins alone are vacuous against line-wrapped source; the
     # presence pins below are the real oracles for Task 3's rewrites
     assert "`haiku`/`opus` are drop-ins" not in readme
     assert "any Claude tier is a drop-in" in readme
     fpf = _read(REPO / "skills" / "multi-model-verify" / "references"
                 / "frozen-plan-format.md")
     assert "Sonnet 5" not in fpf
     assert "the pinned lane in `agents/`" in fpf
 ```
 
@@ -512,222 +525,244 @@ if (-not $agyVersionToSave -and $snapshot.agy) { $agyVersionToSave = $snapshot.a
 
 3. In the `$newSnapshot` object (~lines 274-279, beside `codex = ...`):
 
 ```powershell
 agy = $agyVersionToSave
 ```
 
 Informational only: no comparison logic, no failure path — mirrors how the
 codex version note works (STALE note, never BROKEN).
 
 - [ ] **Step 3: State-machine suite (mandatory — check-drift.ps1 changed)**
 
 Run: `evals/tools/drift_statemachine_tests.ps1`
 Expected: ALL PASS. This is the CLAUDE.md opt-in rule; it is slow (four scenarios re-run pytest in a disposable worktree) — budget ~15+ minutes.
 
 - [ ] **Step 4: Full pytest again (attestation tests read doctor.md)**
 
 Run: `python -m pytest evals -q`
 Expected: green.
 
 - [ ] **Step 5: Commit**
 
 ```bash
 git add commands/doctor.md tools/check-drift.ps1
 git commit -m "0.12.0: doctor agy row and drift snapshot version field"
 ```
 
 ---
 
 ### Task 5: Settings hygiene, repo trust, version bump (ATTENDED)
 
 **Files:**
 - Modify: `~/.gemini/antigravity-cli/settings.json` (user-global, outside repo)
 - Modify: `.claude-plugin/plugin.json`
 
 **Interfaces:**
 - Consumes: spec section 3's declared fallback.
 - Produces: a settings state the agent's preflight (Task 2 text) passes against; plugin version 0.12.0.
 
 - [ ] **Step 1: Remove the probe-dir allow rule (keep its trust entry for now)**
 
 Edit `~/.gemini/antigravity-cli/settings.json`: delete the `write_file`
 entry in `permissions.allow` whose path targets the agy-probe scratchpad
 directory, whatever its exact path spelling in the file (leave the array
 empty if nothing else remains), then re-read the file to confirm the
 deletion. Do NOT remove the probe dir's `trustedWorkspaces` entry yet —
 Step 2's probe needs a trusted directory.
 
 - [ ] **Step 2: allowNonWorkspaceAccess narrowing attempt (bounded)**
 
 Try exactly one narrowing: set `allowNonWorkspaceAccess` to `false`, then
 run this raw probe against the still-trusted probe dir (PowerShell;
 `$probe` = the agy-probe scratchpad dir from the spec's probe record):
 
 ```powershell
 & "$env:LOCALAPPDATA\agy\bin\agy.exe" -p "Create a file named narrow-probe.txt in the workspace directory $probe containing exactly the line: NARROW-OK" --model gemini-3.6-flash-low --add-dir $probe --log-file "$probe\narrow.log"
 ```
 
 `narrow-probe.txt` lands with NARROW-OK = keep `false`. Soft-denied or
 absent = restore `true` and record in the SDD ledger:
 "allowNonWorkspaceAccess=true required for print-mode writes as of agy
 1.1.7 — documented fallback per spec section 3." Do not iterate further.
 
 - [ ] **Step 3: Drop the probe-dir trust entry**
 
 Remove the probe dir from `trustedWorkspaces` (session-temporary dir; the
 repo is the lane's only workspace this cycle).
 
 - [ ] **Step 4 (ATTENDED - user): Trust the repo**
 
 User runs one interactive `agy` session in
 `C:\Users\Brandon\Documents\parallax`, approves trust, exits. Verify:
 `trustedWorkspaces` in the settings file now contains the repo path.
 
 - [ ] **Step 5: Version bump**
 
 Edit `.claude-plugin/plugin.json`: `"version": "0.12.0"`.
 
 - [ ] **Step 6: Preflight dry-check + commit**
 
-Run the agent's three preflight checks by hand against the real
-environment (models list, trust entry, no repo-scoped allow rule) — all
-three must pass. Then:
+Run the agent's FIVE preflight checks by hand against the real
+environment, verbatim from the agent body: (1) `agy models` contains the
+pinned ID; (2) `trustedWorkspaces` contains the repo path; (3) the
+settings file carries NO `write_file(` entry at all — if an unrelated
+write rule remains, STOP and surface it for user disposition (never
+silently delete shared configuration); (4) `git status --porcelain` in
+the repo is empty; (5) no `AGY-TASK-BRIEF-*` file exists in the repo.
+All five must pass. Then:
 
 ```bash
 git add .claude-plugin/plugin.json
 git commit -m "0.12.0: bump plugin version"
 ```
 
 ---
 
 ### Task 6: Live verification (ATTENDED; runs only after the plan debate converges)
 
 **Files:**
 - None in-repo (scratch repo + probe records; SDD ledger notes)
 
 **Interfaces:**
 - Consumes: the installed 0.12.0 cache (dev loop: `claude plugin update parallax@parallax`, restart — the agent is NOT live before that).
 - Produces: the live evidence the diff debate's brief cites.
 
 - [ ] **Step 1 (ATTENDED - user): Dev loop**
 
 Bump is committed (Task 5); user runs `claude plugin update parallax@parallax` and restarts the session. Without the restart the dispatched agent is the cached pre-0.12.0 set — a dry-run before restart tests nothing.
 
 - [ ] **Step 2: Scratch repo setup**
 
 Create a scratch git repo in the session scratchpad with one file
-(`hello.py`, `print("hello")`), plus an interactive trust approval
-(ATTENDED - user, same mechanism as Task 5 Step 3).
+(`hello.py`, `print("hello")`), and COMMIT it — the agent's clean-tree
+preflight (check 4) requires an empty `git status --porcelain` before
+dispatch (Sol check-off round 2, finding 1). Then an interactive trust
+approval (ATTENDED - user, same mechanism as Task 5 Step 4).
 
 - [ ] **Step 3: Green dry-run through the REAL agent**
 
-Dispatch `parallax:flash-implementer` (fresh subagent) with a minimal task:
+Verify `git status --porcelain` in the scratch repo is empty. Dispatch
+`parallax:flash-implementer` (fresh subagent) with a minimal task:
 "Modify hello.py so it prints exactly 'hello flash'. Verification:
 `python hello.py` prints `hello flash`." plus a log path in the
 scratchpad. Expected report: STATUS done; ROUTE carries
-`gemini-3.6-flash-medium` + log path; FILES CHANGED lists `hello.py`;
-VERIFICATION shows the wrapper's own `python hello.py` output;
-DEVIATIONS none. Controller re-runs `python hello.py` itself (never trust
-the report alone — 0.10.0 lesson: controller-owned verification).
+`gemini-3.6-flash-medium` + log path + brain transcript path; FILES
+CHANGED lists `hello.py`; VERIFICATION shows the wrapper's own
+`python hello.py` output; DEVIATIONS none. Controller re-runs
+`python hello.py` itself (never trust the report alone — 0.10.0 lesson:
+controller-owned verification). Then COMMIT the green result in the
+scratch repo and verify `git status --porcelain` is empty again, so the
+later probes start from a clean baseline.
 
 - [ ] **Step 4a: Raw red probe — the CLI's invalid-model path (no agent)**
 
 Controller runs, directly in the scratch repo (split from a single
 disjunctive probe by Sol check-off F5 — a wrapper refusing a
 contradictory brief must not count as exercising this path):
 
 ```powershell
 & "$env:LOCALAPPDATA\agy\bin\agy.exe" -p "Reply with exactly: X" --model gemini-9.9-fake --add-dir <scratch> --log-file <scratchpad>\red-raw.log
 ```
 
 Expected: loud "invalid model selection" rejection, nonzero exit, no
 file changes in the scratch tree.
 
 - [ ] **Step 4b: Reachable-failure probe — the REAL agent's blocked path**
 
-Plant a scratch-scoped rule in `~/.gemini/antigravity-cli/settings.json`
-`permissions.allow`: `write_file(<scratch, in the file's own path
-spelling>)`. Dispatch `parallax:flash-implementer` with a trivial task in
-the scratch repo. Expected: STATUS blocked at preflight check 3, quoting
-the planted rule; no dispatch reaches agy; no file changes. Then REMOVE
-the planted rule and re-read the settings file to confirm.
+First save the ORIGINAL settings content
+(`~/.gemini/antigravity-cli/settings.json`) to the session scratchpad.
+Then plant a NONMATCHING SENTINEL rule in `permissions.allow`:
+`write_file(/parallax-sentinel-never-matches/)` — preflight check 3 bans
+the rule CLASS, so the sentinel triggers it without ever granting a
+functional permission (Sol check-off round 2, finding 4). Dispatch
+`parallax:flash-implementer` with a trivial task in the scratch repo.
+Expected: STATUS blocked at preflight check 3, quoting the sentinel; no
+dispatch reaches agy; `git status --porcelain` still empty. Restore the
+saved ORIGINAL settings content afterward — including on an aborted or
+interrupted run, restore-before-anything-else on resume — and re-read
+the file to confirm it matches the saved copy.
 
 - [ ] **Step 5 (ATTENDED - user): skip-permissions print-mode probe**
 
 User runs, in the scratch repo (isolated), the one command the spec's
 probe task left open:
 `agy -p "Create a file named skip-probe.txt containing SKIP-OK" --model gemini-3.6-flash-low --add-dir <scratch> --dangerously-skip-permissions --log-file <scratchpad>\probe-skip.log`
 after REMOVING the scratch repo's trust entry. Record the outcome in the
 SDD ledger: file created = the flag works in print mode (both bypass
 surfaces live); soft-denied = the flag is inert in print mode (the
 settings assertion is the whole live defense). Either way the lane's
 contract is unchanged — the flag stays forbidden; this only settles which
 defense is load-bearing, per spec section 2.
 
 - [ ] **Step 6: Record**
 
 Write the dry-run + red-probe + skip-probe outcomes into the SDD ledger
 (`.superpowers/sdd/2026-07-25-flash-implementer/`), with log paths — the
 diff-debate brief cites them.
 
 ---
 
 ## Debate record
 
 **Participants:** Fable 5 (session) / Kimi K3 `kimi-code/k3-256k` (kimi-cli 1.49.0, session 4ba130bb-1615-4c95-9afc-d1e3049bb857) / GPT-5.6 Sol (codex exec, session 019f9ad5-e318-7831-8a3f-0c02c94caa8e)
 **Rounds used:** 2 of 4 (Kimi, plan mode) + 2 of 4 (Sol, primary check-off)
 **Outcome:** converged with amendments
 **Verification status:** FULL
 **Degradation:** quota-exhausted (primary lane at the plan gate; backup lane substituted for rounds 1-2, primary check-off ran on the reset window)
 **Authorized by:** user at round 1 (backup-lane substitution; the user's standing ruling retained the primary check-off before branch close)
 **Raw rounds:** docs/superpowers/plans/rounds/2026-07-25-flash-implementer/ (plan-round1-brief.md, plan-round{1,2}-transcript.txt — Kimi; sol-checkoff-round1-{brief,reply,header}.md/txt — Sol; round-2 files added at convergence)
 
 Backup-lane note: Kimi ran read-only via a custom agent-file
 (ReadFile/ReadMediaFile/Glob/Grep/SetTodoList; pre-review write-probe
 refused) against a throwaway clone — the real tree was never exposed.
 Route evidence per round: `Using LLM model: provider='managed:kimi-code'
 model='k3-256k'` in ~/.kimi/logs/kimi.log (client-side class). Clone
 `git status` after round 2: sole untracked file was the session-authored
 brief (retained evidence: rounds/.../clone-status-after-round2.txt).
 Sol round 1 route: header block model gpt-5.6-sol / provider openai /
 sandbox read-only / effort high.
 
 ### Resolved points
 
 | # | Claim | Raised by | Outcome | Evidence |
 |---|-------|-----------|---------|----------|
 | 1 | Task 4 doctor text trips the plan's own literal sweep | Kimi r1 | accepted; doctor text reworded (5391455) | plan Task 4 vs test sweep globs |
 | 2 | Task 1 expected parity failure signature wrong (FileNotFoundError first) | Kimi r1 | accepted; expectation corrected | test_flash_implementer.py `_read` order |
 | 3 | README drop-ins absence pin vacuous (line-wrapped source) | Kimi r1 | accepted; presence pins added | README.md:132-133 line break |
 | 4 | Sweep omits evals/**/*.ps1 vs the reviewer sweep it mirrors | Kimi r1 | accepted; glob added | test_multi_model_verify.py:249 |
 | 5 | Flash Lane note misstates where the literal lives | Kimi r1 | accepted; reworded | implementer.md carries no gemini literal |
 | 6 | Drift-snapshot snippet violates carry-forward invariant; PS 5.1 parse risk | Kimi r1 | accepted; Task 4 Step 2 rewritten to carry-forward | check-drift.ps1:193-205 |
-| 7 | Red-probe expectation contradicts pinned dispatch contract | Kimi r1 | accepted; made disjunctive (superseded by #13) | plan Task 6 vs agent body pin |
+| 7 | Red-probe expectation contradicts pinned dispatch contract | Kimi r1 | accepted; made disjunctive (superseded by #14) | plan Task 6 vs agent body pin |
 | 8 | Settings-deletion instruction pins unverifiable path spelling | Kimi r1 | accepted; made spelling-agnostic | settings file outside workspace |
 | 9 | Parity narrower than spec claim (headings unpinned in classic) | Kimi r1 | accepted; headings pinned in both files | spec section 1 vs test scope |
 | 10 | Corroboration evidence source wrong: --log-file log carries NO file actions; brain transcript does | Sol r1 | accepted; evidence source corrected to transcript_full.jsonl | probe9.log: 0 hits for probe-write4; brain/<cid>/.../transcript_full.jsonl: 5 hits |
 | 11 | Brief-file lifecycle unsafe (collision, interruption, unbaselined git status) | Sol r1 | accepted; unique name + collision preflight + guaranteed cleanup + clean-baseline preflight | agent body Dispatch/Preflight sections |
 | 12 | Debate record lacked required schema fields and session adjudication | Sol r1 | accepted; this record | frozen-plan-format.md:36-68, debate-protocol.md:63 |
 | 13 | Settings check had no path-matching algorithm | Sol r1 | accepted; conservative rule-class ban (any write_file( entry) | trust entry vs allow rule spelling mismatch on this machine |
 | 14 | Red probe vacuous (wrapper refusal passes without exercising failure path) | Sol r1 | accepted; split into raw-CLI probe + reachable-failure probe | plan Task 6 Steps 4a/4b |
+| 15 | Task 6 green run cannot satisfy the new clean-tree preflight | Sol r2 | accepted; scratch baseline committed, post-run commit + clean check | plan Task 6 Steps 2-3 |
+| 16 | Task 5 dry-check still tested the obsolete three-preflight contract | Sol r2 | accepted; Step 6 runs all five checks verbatim, unrelated rules escalate to user | plan Task 5 Step 6 |
+| 17 | F2/F4 amendments not actually pinned (loose keyword asserts) | Sol r2 | accepted; exact-sentence pins + ROUTE transcript pin added | test_flash_implementer.py dispatch/preflight tests |
+| 18 | Step 4b installs a functional bypass without interruption-safe restore | Sol r2 | accepted; nonmatching sentinel rule + save/restore-in-finally | plan Task 6 Step 4b |
+| 19 | Stale cross-references (log/tree, workdir-scoped, log-only ROUTE, #13 for #14) | Sol r2 | accepted; synchronized | plan header, spec Decisions + section 6, resolved point 7 |
 
 ### Escalated points (user-decided)
 
 | # | Question | Session position | Reviewer position | Owner's call |
 |---|----------|------------------|-------------------|--------------|
 
 (none this debate; the consent-gated reroute mechanism records future
 blocked-task reroutes here)
 
 ### Session adjudication (debate-protocol closing step)
 
 Each Sol round-1 finding was verified against live evidence before
 acceptance: #10 by grepping the retained WRITE4 probe log (zero
 file-path hits) and the brain transcript (five hits); #11/#14 against
 the agent body and plan text as cited; #12 against
 frozen-plan-format.md:36-68; #13 against the live settings file's mixed
 path spellings. Kimi's rounds 1-2 were adjudicated at the time (all nine
 findings verified, fixes committed 5391455, round-2 re-verification by
 the reviewer). No finding was refuted; no point escalated. Convergence =
 Sol round-2 confirmation of these amendments (transcript retained under
 Raw rounds).
diff --git a/docs/superpowers/specs/2026-07-25-flash-implementer-design.md b/docs/superpowers/specs/2026-07-25-flash-implementer-design.md
index b0fd643..f80f20c 100644
--- a/docs/superpowers/specs/2026-07-25-flash-implementer-design.md
+++ b/docs/superpowers/specs/2026-07-25-flash-implementer-design.md
@@ -32,338 +32,338 @@ The obvious transport died during probing:
   "Transitioning Gemini CLI to Antigravity CLI"), installed and probed
   end-to-end the same night. gemini-cli plays no part in this design.
 
 ## Probe record (agy 1.1.7, Windows, 2026-07-25)
 
 All probes ran headless in a scratch directory; log evidence retained in the
 session scratchpad (`agy-probe/probe*.log`).
 
 - Auth: Google Sign-In (system keyring), one-time interactive. `agy models`
   requires sign-in and lists `gemini-3.6-flash-{high,medium,low}` (plus 3.5
   variants, `gemini-3.1-pro-*`, `claude-sonnet-4-6`, `claude-opus-4-6-thinking`,
   `gpt-oss-120b-medium` — the CLI is multi-vendor).
 - Pinned headless run: `agy -p "..." --model gemini-3.6-flash-low --log-file F`
   → correct reply, exit 0; log carries route-request evidence:
   `Print mode: starting (... model="gemini-3.6-flash-low" ...)` and
   `Propagating selected model override to backend: label="Gemini 3.6 Flash (Low)"`.
   Both lines are CLIENT-side (what the CLI requested and propagated), not
   server-side attribution of what was served.
 - Unknown model: rejected loudly at the CLI layer before any API call
   ("invalid model selection ... not recognized"), available models listed.
 - Write path: print mode cannot prompt, so unapproved tools soft-deny loudly
   ("jetski: ... auto-denied", tool confirmation "Edit"/CodeAction).
   `--mode accept-edits` does NOT apply in print mode. Without `--add-dir`,
   writes divert to the CLI's internal scratch workspace
   (`~/.gemini/antigravity-cli/scratch/`) — files never reach the real tree.
 - Working combination (WRITE4-OK): one-time interactive trust of the target
   directory (persists to `trustedWorkspaces` in
   `~/.gemini/antigravity-cli/settings.json`; the interactive session also set
   `allowNonWorkspaceAccess: true`) + headless `--add-dir <dir>` binding →
   file created in the bound tree, no prompting.
 - File-action evidence does NOT live in the `--log-file` log (probed
   2026-07-25, Sol primary check-off F1: the WRITE4 run's log contains zero
   mentions of the file it wrote). It lives in agy's internal brain
   transcript: parse `conversationID="<uuid>"` from the log's
   `Print mode: starting` line, then read
   `~/.gemini/antigravity-cli/brain/<conversationID>/.system_generated/logs/transcript_full.jsonl`
   (the WRITE4 file appears there in successful file-changing actions).
 - `agy --help` (v1.1.7) lists `--dangerously-skip-permissions` ("Auto-approve
   all tool permission requests without prompting"). Whether it takes effect
   in print mode is UNPROBED (accept-edits provably does not).
 - Environment note: an "Orca" app has pre-planted hooks in BOTH
   `~/.gemini/settings.json` (gemini-cli hook events) and agy's stop hooks
   (`jsonhook__orca-status_Stop_0_0` failed non-fatally in probe logs). Inert
   outside Orca sessions; watch in drift triage if it starts failing loudly.
 
 ## Review provenance
 
 Same-vendor advisory review (Opus 5, dispatched 2026-07-25, two rounds):
 initial verdict FIX with 8 blocking findings; after one factual correction
 (the skip-permissions flag IS an agy flag, per `agy --help`) and the
 amendments now folded into this text, revised verdict PASS conditional on
 the amendments living in the spec, which this revision satisfies. This pass
 is NOT cross-vendor verification and forms no part of the Sol gate: the
 plan debate (before implementation) and diff debate (before merge) remain
 required and are quota-gated to the ~Jul 29 codex reset.
 
 Amended again 2026-07-25 after the PRIMARY reviewer's check-off (GPT-5.6
 Sol, codex window reset early; round 1 FIX, five blocking findings, all
 session-verified and accepted): F1 corroboration evidence source corrected
 to the brain transcript (the log carries no file actions — probed); F2
 brief-file lifecycle + clean-baseline preflight; F4 settings check made
 conservative (ban the write_file rule class, no path matching); F5 red
 probe split into raw-CLI and reachable-failure probes; F3 (debate-record
 schema) lands in the plan's Debate record, not this spec. Full round
 record in the plan's debate appendix.
 
 ## Decisions (user, 2026-07-25)
 
 - Architecture **A**: new `agents/flash-implementer.md`; `implementer.md`
   kept as the direct-typing lane. Chosen over rewriting `implementer.md`
   wholesale (B) after weighing the fable-advisor reference (which added its
   vendor wrapper BESIDE the Claude lane), the untouched haiku transcription
   lane, and the instant sonnet fallback against B's single-file tidiness.
   C (controller shells the CLI directly) rejected: no isolation, no report
   anchor. Known cost, accepted with mitigation: the two agent files share
   contract sections that can drift; agent files cannot include each other,
   so a byte-identity parity test is the only available enforcement and is
   REQUIRED, not optional.
 - True-Flash-only, fails loudly: the wrapper never types repo code itself
   and never completes work Flash failed. Enforced mechanically where
-  possible (tools allowlist, log/tree corroboration — see §1, §2), by
+  possible (tools allowlist, transcript/tree corroboration — see §1, §2), by
   contract text where not.
 - Consent-gated reroute: a blocked task surfaces to the session AND the user;
   rerouting to a Claude tier is an explicit user decision recorded in the
   frozen plan's **Escalated points (user-decided)** section — the existing
   artifact, not a new table. No automatic degradation — same ethos as the
   debate lane's transport consent gate.
 - Scope: the Flash lane runs in the MAIN CHECKOUT ONLY this cycle. Trust is
   per-directory and grantable only interactively, so fresh worktrees are
   untrusted by construction; a worktree trust story is future-cycle work.
   Coupling made explicit: with no disposable worktree, the
   FILES-CHANGED-on-blocked rule (§4) is the recovery affordance for a
   partially written tree.
 - Effort: ONE canonical variant this cycle, `gemini-3.6-flash-medium`. No
   per-task effort choice by any seat, no effort slot in the plan format —
   a zero-judgment lane gets zero dispatch-time judgment calls.
 - **Sol check-off required before this branch closes**: plan debate before
   implementation, diff debate before merge, both after the ~Jul 29 reset;
   spec and plan drafting proceed now.
 
 ## Design
 
 ### 1. Agent: `agents/flash-implementer.md`
 
 `model: haiku` frontmatter (wrapper/supervisor) plus an explicit `tools:`
 allowlist — Read, Grep, Glob, Bash; **no Edit, no Write, no NotebookEdit** —
 so the ergonomic path for the wrapper to type code itself does not exist.
-(Bash remains a write primitive in principle; the log/tree corroboration in
+(Bash remains a write primitive in principle; the transcript/tree corroboration in
 §2 is the control that catches that path.) If the long-brief probe (§2)
 forces brief-via-file, the brief is written with a Bash heredoc — Write
 stays out of the allowlist.
 
 Same input contract as `implementer.md` (ONE task's verbatim text + the
 plan's Global Constraints), same INPUT GAP rule. Report format gains one
 lane-specific line:
 
 - **STATUS:** done | blocked | INPUT GAP: <what is missing>
 - **ROUTE:** resolved model ID as requested-and-propagated, from the log,
   plus the retained log file's path
 - **FILES CHANGED:** actual paths from `git status` — on `blocked` this
   MUST still list every path Flash already touched, so the session can
   revert a partial write
 - **VERIFICATION:** each command run by the wrapper itself, with real output
 - **DEVIATIONS:** must be "none" or an explanation of why the task could
   not be built as written
 
 The parity test (§6) pins the shared sections (INPUT GAP rule, report
 headings other than ROUTE) byte-identical across both agent files; ROUTE is
 declared lane-specific and excluded from parity.
 
 ### 2. Dispatch recipe
 
 - Preflight, all blocking, before any dispatch:
   1. `agy models` output contains `gemini-3.6-flash-medium` (also proves
      sign-in and binary presence);
   2. `~/.gemini/antigravity-cli/settings.json` `trustedWorkspaces` contains
      the workdir — if not, block with the exact re-trust instruction (one
      interactive `agy` session in the workdir);
   3. the same settings file carries NO file-writing per-tool allow rule
      AT ALL (any `write_file(` entry, whatever path it names) — the
      durable, call-site-invisible bypass class; this settings assertion is
      the LOAD-BEARING permission control (a flag is at least visible
      per-dispatch; a settings rule survives invisibly forever — tonight's
      probing itself left one behind). Conservative by amendment (Sol
      check-off F4): agy's path normalization is not offline-verifiable
      (trust entries and allow rules already disagree on drive-letter
      spelling on this machine), so the lane bans the rule CLASS rather
      than attempting workdir path-matching;
   4. `git status --porcelain` is EMPTY before dispatch (Sol check-off F2:
      without a pre-dispatch baseline, pre-existing changes are
      unattributable — a dirty tree is blocked, not absorbed).
 - Dispatch: `agy -p <brief> --model gemini-3.6-flash-medium
   --add-dir <workdir> --log-file <fresh per-task log>`. Fresh log file per
   attempt, written under the session scratchpad (never the repo tree — no
   .gitignore dependency); each task's report carries its log path, and the
   SDD execution record (the `.superpowers/sdd/<date>-<topic>/` workspace
   the subagent-driven-development skill maintains) records per-task lane
   provenance, which the diff-debate brief then carries. Adding an
   implementer line to the frozen-plan debate record's Participants block is
   declared scope for the plan debate to settle.
 - Brief-file lifecycle (Sol check-off F2): the brief is the SOLE transient
   file the wrapper may create in the repo tree — a declared exception to
   its never-write rule. Unique reserved name (`AGY-TASK-BRIEF-<unique>.md`,
   unique suffix from the dispatch's log-file basename); collision
   preflight (any existing `AGY-TASK-BRIEF-*` file in the workspace =
   stale state = blocked); deletion guaranteed after agy exits — success,
   failure, or interruption path alike — and always BEFORE any evidence
   check, so the brief never appears in the attribution set.
 - Route check, every run, on the log file:
   1. `Print mode: starting` line present with `model="gemini-3.6-flash-medium"`
      (the RESOLVED ID is the comparand — same string §5 pins as canonical);
   2. `Propagating selected model override` line present (presence-only; its
      display label is a second namespace and is NOT matched);
   3. transcript/tree corroboration (evidence source corrected by Sol
      check-off F1 — the `--log-file` log carries NO file actions, probed):
      parse `conversationID="<uuid>"` from the log's `Print mode: starting`
      line, read the brain transcript at
      `~/.gemini/antigravity-cli/brain/<conversationID>/.system_generated/logs/transcript_full.jsonl`,
      and require every path `git status` reports changed to appear in that
      transcript as a successful file-changing action. A changed file the
      transcript never mentions means the wrapper (or something else) typed
      it — blocked regardless of test results. A missing transcript is
      blocked. The ROUTE report line carries the transcript path beside the
      log path.
   This evidence class is CLIENT-side: the check verifies the route was
   **requested and propagated**, never "used and confirmed" (the doctor
   check-4 language discipline, `commands/doctor.md`). Server-side
   substitution is undetectable with today's evidence — named in Risks.
 - Probe task (pre-plan, throwaway harness, no agent file needed), pinned
   before the agent body freezes:
   1. long-brief mechanics — argument vs stdin vs file, quoting, size limits;
   2. does agy emit ANY response-side model attribution (log tail, response
      metadata; agy has no `--output-format` flag) — if yes, that artifact
      becomes the route check and the client-side lines demote to
      preconditions; the probe's answer is written into this section as the
      final route-check contract text;
   3. does `--dangerously-skip-permissions` take effect in print mode at all
      (accept-edits provably does not) — if inert, the settings assertion is
      the whole live defense, and §4's flag ban is belt-and-suspenders.
 
 ### 3. One-time setup (documented in the agent file)
 
 agy installed + signed in (done 2026-07-25). The repo root is trusted via
 one interactive agy session (same mechanism as the probe dir). An
 implementation task minimizes tonight's probe leftovers in agy's settings:
 remove the probe-dir `write_file` allow rule (required — §2's preflight
-asserts its absence for workdirs), and attempt the narrowest working
+bans the write_file( rule class outright), and attempt the narrowest working
 `allowNonWorkspaceAccess` config with a DECLARED fallback: if narrowing
 does not converge quickly, leave the value as-is and document it and why —
 the task must not stall, and the setting is shared with the Orca app's
 environment. Trust is per-directory; the main-checkout-only scope decision
 above is the worktree story for this cycle.
 
 ### 4. Failure handling — loud, never silent
 
 `STATUS: blocked` with the exact output quoted, on: any preflight failure
-(sign-out, missing model, untrusted workdir, workdir allow rule present),
+(sign-out, missing model, untrusted workdir, any write_file( allow rule present),
 the print-mode soft-deny line, nonzero exit, route-check failure (missing
-route lines, wrong resolved ID, log/tree corroboration mismatch), files
+route lines, wrong resolved ID, transcript/tree corroboration mismatch), files
 diverted to internal scratch. Forbidden by class in the agent body: the
 `--dangerously-skip-permissions` flag by name, any `--mode`/approval bypass,
-AND persisting any per-tool allow rule for the workdir in agy settings —
+AND persisting any file-writing per-tool allow rule in agy settings —
 the durable settings-rule form is the load-bearing prohibition. On blocked,
 FILES CHANGED lists Flash's partial writes (per §1) as the recovery
 affordance. Reroute is consent-gated per Decisions.
 
 ### 5. Model literal placement
 
 The canonical implementer-lane literal is the RESOLVED ID
 `gemini-3.6-flash-medium`, living in `agents/flash-implementer.md` — the
 same string the preflight and route check compare against, so no
 requested/resolved ambiguity survives. After this cycle, no implementer
 model literal exists outside the two agent files. That claim is made true
 by enumerated edits, not assumption:
 
 - `README.md` repo-map row (~line 66): "currently `model: sonnet`" literal
   replaced by a pointer to the agent files; new row for the Flash lane.
 - `README.md` role-plug section (~lines 131-134): "`sonnet` today;
   `haiku`/`opus` are drop-ins" reworded to point at the agent files without
   naming tiers.
 - `agents/implementer.md` Lane note: gains the pointer to
   `flash-implementer.md`; its "Nothing else in the plugin references the
   implementer's model" sentence is RETIRED (already false today — README:66,
   frozen-plan-format.md:4). Its own frontmatter/Lane-note literals stay:
   agent files are the allowed homes.
 - `skills/multi-model-verify/references/frozen-plan-format.md` line 4:
   "Sonnet 5" literal becomes a generic reference to the implementer agents.
 
 Enforced by a new sweep test mirroring
 `test_reviewer_id_has_single_source`: scans the reviewer sweep's file set
 PLUS `agents/*.md`, with exactly the two agent files as the allowed homes —
 so a future third agent file is swept by default, not silently missed.
 
 ### 6. Verification for this cycle
 
 - Offline pytest (zero quota, both vendors): contract pins on the new agent
   file — dispatch flags, resolved-ID comparand, route-check strings
-  (including the log/tree corroboration rule), forbidden-class phrasing,
+  (including the transcript/tree corroboration rule), forbidden-class phrasing,
   report headings including ROUTE, `model: haiku` frontmatter, and the
   `tools:` allowlist (no Edit/Write/NotebookEdit) — plus the §1 parity test
   and the §5 implementer-literal sweep.
 - Pre-plan probes (throwaway harness, no agent file): the §2 probe task.
   These are probes, not implementation — the plan-debate gate is not
   crossed.
 - Live verification through the REAL agent happens during implementation,
   AFTER the plan debate: one end-to-end dry-run task in a scratch repo
   (trusted for the occasion), plus TWO failure probes (split by Sol
   check-off F5 — the earlier single disjunctive probe was vacuous: a
   wrapper refusing the contradictory brief would "pass" without ever
   exercising the failure path): (a) a RAW agy probe, no agent, proving
   the CLI's loud invalid-model rejection with no writes; (b) a
   reachable-failure probe through the REAL agent — plant a scratch-scoped
   `write_file` allow rule in agy settings, dispatch a trivial task,
   require preflight check 3 to block quoting the rule, then remove the
   planted rule. Dev-loop ordering applies and is part of the task:
   the agent is not live until `plugin.json` bumps,
   `claude plugin update parallax@parallax` runs, and the session restarts.
 - Preflight cost: `agy models` is a network round-trip per task dispatch.
   Deliberate — per-task freshness over quota thrift; the wrapper is fresh
   per task, so there is no cheaper session-scoped cache to use.
 - Plan debate + diff debate (Sol): after the ~Jul 29 reset. The branch does
   not merge without the diff debate's terminal PASS.
 - Behavioral battery: deliberately not run. Honest framing: this cycle
   edits `references/frozen-plan-format.md`, a skill reference file that NO
   behavioral case declares in its surface (and `agents/*` appears in no
   surface either) — so `--changed` selects nothing by design, and the gap
   itself is declared here for the debate to contest, alongside whether an
   `agents/`-surfaced behavioral case (Flash-fails → wrapper reports
   blocked, never substitutes) is wanted this cycle or deferred.
 - State-machine suite: `tools/check-drift.ps1` untouched → skipped
   (CLAUDE.md opt-in rule).
 
 ### 7. Scope edges
 
 Version 0.12.0. Doctor gains a minimal agy row — binary version + pinned
 model present in `agy models`, no generation probe — and follows doctor's
 existing single-source discipline: the row PARSES the canonical ID out of
 `agents/flash-implementer.md` under the installed copy's `installPath`,
 carrying no literal of its own (the check-4 pattern). Drift-watch: the
 drift snapshot gains the `agy --version` string as an informational row
 (the snapshot already tracks the codex version — declining drift coverage
 for the youngest dependency while naming it the top risk would be
 inconsistent); full agy flag-surface drift coverage remains a non-goal this
 cycle. Other non-goals: reviewer lane untouched; no CI changes;
 transcription tasks stay on `implementer.md`/haiku; agy's Claude/gpt-oss
 models unused; gemini-cli unused (uninstall + `GEMINI_API_KEY` removal are
 optional user cleanup, not cycle scope).
 
 ## Risks
 
 - **Server-side model substitution is undetectable today.** The route check
   proves requested-and-propagated, not served — the same epistemic gap the
   codex lane's header check has, handled with the same language discipline.
   If the §2 probe finds response-side attribution, the gap closes; if not,
   it is a NAMED residual risk. Mitigating context: the diff debate reads
   the code itself regardless of who wrote it — substitution degrades
   provenance, not the correctness gate.
 - **agy is young and Google moves fast**: v1.1.7, endpoint literally named
   `daily-cloudcode-pa.googleapis.com`, predecessor killed with a month's
   notice. Mitigations: loud preflight, pinned-contract tests that fail on
   flag drift, the drift-snapshot version row, sonnet one dispatch away
   behind the consent gate, and architecture A keeping the old lane intact.
 - **Free-tier quota opacity**: agy AI-credit limits are undocumented. A
   mid-plan exhaustion surfaces as a loud dispatch failure → consent gate;
   the partial-write listing (§1/§4) bounds the recovery cost. No headroom
   probe exists; observability gap, not solved this cycle.
 - **Print-mode permission model is under-documented**: today's working
   recipe is probe-verified, not doc-verified. The pinned-contract tests and
   the §2 probe task are the defense; doc drift lands in drift triage.
 
 ## Non-goals
 
 - No reviewer-lane or debate-protocol changes; no eval-case changes (the
   possible `agents/`-surfaced case is a declared debate question, not
   committed scope).
 - No attempt to use agy's non-Gemini models anywhere.
 - No plugin runtime behavior change beyond the new agent (version bumps to
   0.12.0 per release convention).

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'rg -n "STATUS: FROZEN|Task 5:|Task 6:|Step 2:|Step 3:|Step 4a|Step 4b|Preflight|write_file|AGY-TASK-BRIEF|Debate record|Participants|Rounds used|Outcome:|Verification status|Degradation|Authorized by|Raw rounds|Resolved points|Escalated points|round 2|round 3|#14|#13|clone-status" docs/superpowers/plans/2026-07-25-flash-implementer.md' in C:\Users\Brandon\Documents\parallax
2026-07-25T20:31:05.244391Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 1 seconds
Output:
rg: 
Line |
   2 |  rg -n "STATUS: FROZEN|Task 5:|Task 6:|Step 2:|Step 3:|Step 4a|Step 4b …
     |  ~~
     | The term 'rg' is not recognized as a name of a cmdlet, function, script file, or executable program.
Check the spelling of the name, or if a path was included, verify that the path is correct and try again.

 exited 1 in 987ms:
rg: 
Line |
   2 |  rg -n "STATUS: FROZEN|Task 5:|Task 6:|Step 2:|Step 3:|Step 4a|Step 4b …
     |  ~~
     | The term 'rg' is not recognized as a name of a cmdlet, function, script file, or executable program.
Check the spelling of the name, or if a path was included, verify that the path is correct and try again.

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'docs/superpowers/plans/2026-07-25-flash-implementer.md'; Select-String -Path "'$p -Pattern '"'STATUS: FROZEN','Task 5:','Task 6:','Step 2:','Step 3:','Step 4a','Step 4b','Preflight','write_file','AGY-TASK-BRIEF','Debate record','Participants','Rounds used','Outcome:','Verification status','Degradation','Authorized by','Raw rounds','Resolved points','Escalated points','#14','#13','clone-status' | ForEach-Object { '{0}:{1}' -f "'$_.LineNumber,$_.Line }' in C:\Users\Brandon\Documents\parallax
 succeeded in 436ms:
11:**STATUS: FROZEN** (re-frozen pending Sol round 2). Frozen 2026-07-25 by the backup-lane plan debate (Kimi K3-256k); REOPENED same day by the primary reviewer's check-off (Sol round 1, FIX — codex window reset early): five blocking findings, all session-verified and accepted; amendments folded into spec, this plan, and the Tasks 1-2 artifacts via an SDD fix wave. The diff debate gates the merge as always. Tasks 5-6 contain ATTENDED steps (user-interactive); Task 6 additionally requires the plugin dev-loop (bump, update, restart).
18:- Loud failure only: any preflight, dispatch, route-check, or corroboration failure is `STATUS: blocked` with quoted evidence; reroute to a Claude tier is a user decision recorded under the frozen plan's Escalated points (spec section 4).
87:    assert "AGY-TASK-BRIEF-" in body
112:def test_flash_preflight_pins():
119:    # path-scoped matching or a dropped preflight
120:    assert "any `write_file(` entry, whatever path it names" in body
123:    assert "No file matching `AGY-TASK-BRIEF-*`" in body
209:- [ ] **Step 2: Run the new file to verify everything fails for the right reasons**
214:- [ ] **Step 3: Confirm the rest of the suite is untouched**
251:does ALL the typing through the Antigravity CLI (`agy`); you do preflight,
283:## Preflight (all five must pass BEFORE dispatch)
293:   at all — any `write_file(` entry, whatever path it names, is blocking.
300:5. No file matching `AGY-TASK-BRIEF-*` exists in the workspace — a stale
305:1. Write the brief to `<workspace>/AGY-TASK-BRIEF-<unique>.md` with a Bash
313:   `agy -p "Read the file AGY-TASK-BRIEF-<unique>.md in the workspace and execute it exactly." --model gemini-3.6-flash-medium --add-dir <workspace> --log-file <log-path>`
339:Blocked (quote the exact output) on: any preflight failure, the print-mode
372:- [ ] **Step 2: Edit `agents/implementer.md`**
397:- [ ] **Step 3: Run the contract tests**
431:- [ ] **Step 2: README role-plug section**
443:- [ ] **Step 3: frozen-plan-format.md line 4**
498:- [ ] **Step 2: Snapshot field in `tools/check-drift.ps1`**
535:- [ ] **Step 3: State-machine suite (mandatory — check-drift.ps1 changed)**
554:### Task 5: Settings hygiene, repo trust, version bump (ATTENDED)
562:- Produces: a settings state the agent's preflight (Task 2 text) passes against; plugin version 0.12.0.
566:Edit `~/.gemini/antigravity-cli/settings.json`: delete the `write_file`
573:- [ ] **Step 2: allowNonWorkspaceAccess narrowing attempt (bounded)**
588:- [ ] **Step 3: Drop the probe-dir trust entry**
603:- [ ] **Step 6: Preflight dry-check + commit**
605:Run the agent's FIVE preflight checks by hand against the real
608:settings file carries NO `write_file(` entry at all — if an unrelated
611:the repo is empty; (5) no `AGY-TASK-BRIEF-*` file exists in the repo.
621:### Task 6: Live verification (ATTENDED; runs only after the plan debate converges)
634:- [ ] **Step 2: Scratch repo setup**
638:preflight (check 4) requires an empty `git status --porcelain` before
642:- [ ] **Step 3: Green dry-run through the REAL agent**
657:- [ ] **Step 4a: Raw red probe — the CLI's invalid-model path (no agent)**
670:- [ ] **Step 4b: Reachable-failure probe — the REAL agent's blocked path**
675:`write_file(/parallax-sentinel-never-matches/)` — preflight check 3 bans
679:Expected: STATUS blocked at preflight check 3, quoting the sentinel; no
705:## Debate record
707:**Participants:** Fable 5 (session) / Kimi K3 `kimi-code/k3-256k` (kimi-cli 1.49.0, session 4ba130bb-1615-4c95-9afc-d1e3049bb857) / GPT-5.6 Sol (codex exec, session 019f9ad5-e318-7831-8a3f-0c02c94caa8e)
708:**Rounds used:** 2 of 4 (Kimi, plan mode) + 2 of 4 (Sol, primary check-off)
709:**Outcome:** converged with amendments
710:**Verification status:** FULL
711:**Degradation:** quota-exhausted (primary lane at the plan gate; backup lane substituted for rounds 1-2, primary check-off ran on the reset window)
712:**Authorized by:** user at round 1 (backup-lane substitution; the user's standing ruling retained the primary check-off before branch close)
713:**Raw rounds:** docs/superpowers/plans/rounds/2026-07-25-flash-implementer/ (plan-round1-brief.md, plan-round{1,2}-transcript.txt — Kimi; sol-checkoff-round1-{brief,reply,header}.md/txt — Sol; round-2 files added at convergence)
721:brief (retained evidence: rounds/.../clone-status-after-round2.txt).
725:### Resolved points
735:| 7 | Red-probe expectation contradicts pinned dispatch contract | Kimi r1 | accepted; made disjunctive (superseded by #14) | plan Task 6 vs agent body pin |
739:| 11 | Brief-file lifecycle unsafe (collision, interruption, unbaselined git status) | Sol r1 | accepted; unique name + collision preflight + guaranteed cleanup + clean-baseline preflight | agent body Dispatch/Preflight sections |
740:| 12 | Debate record lacked required schema fields and session adjudication | Sol r1 | accepted; this record | frozen-plan-format.md:36-68, debate-protocol.md:63 |
741:| 13 | Settings check had no path-matching algorithm | Sol r1 | accepted; conservative rule-class ban (any write_file( entry) | trust entry vs allow rule spelling mismatch on this machine |
743:| 15 | Task 6 green run cannot satisfy the new clean-tree preflight | Sol r2 | accepted; scratch baseline committed, post-run commit + clean check | plan Task 6 Steps 2-3 |
744:| 16 | Task 5 dry-check still tested the obsolete three-preflight contract | Sol r2 | accepted; Step 6 runs all five checks verbatim, unrelated rules escalate to user | plan Task 5 Step 6 |
745:| 17 | F2/F4 amendments not actually pinned (loose keyword asserts) | Sol r2 | accepted; exact-sentence pins + ROUTE transcript pin added | test_flash_implementer.py dispatch/preflight tests |
746:| 18 | Step 4b installs a functional bypass without interruption-safe restore | Sol r2 | accepted; nonmatching sentinel rule + save/restore-in-finally | plan Task 6 Step 4b |
747:| 19 | Stale cross-references (log/tree, workdir-scoped, log-only ROUTE, #13 for #14) | Sol r2 | accepted; synchronized | plan header, spec Decisions + section 6, resolved point 7 |
749:### Escalated points (user-decided)
761:file-path hits) and the brain transcript (five hits); #11/#14 against
763:frozen-plan-format.md:36-68; #13 against the live settings file's mixed
768:Raw rounds).

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'docs/superpowers/plans/2026-07-25-flash-implementer.md'; "'$lines=Get-Content -LiteralPath $p; foreach($range in @(@(550,703),@(705,775))){ for($i=$range[0];$i -le $range[1];$i++){ '"'{0,4}: {1}' -f "'$i,$lines[$i-1] } }' in C:\Users\Brandon\Documents\parallax
 succeeded in 447ms:
 550: ```
 551: 
 552: ---
 553: 
 554: ### Task 5: Settings hygiene, repo trust, version bump (ATTENDED)
 555: 
 556: **Files:**
 557: - Modify: `~/.gemini/antigravity-cli/settings.json` (user-global, outside repo)
 558: - Modify: `.claude-plugin/plugin.json`
 559: 
 560: **Interfaces:**
 561: - Consumes: spec section 3's declared fallback.
 562: - Produces: a settings state the agent's preflight (Task 2 text) passes against; plugin version 0.12.0.
 563: 
 564: - [ ] **Step 1: Remove the probe-dir allow rule (keep its trust entry for now)**
 565: 
 566: Edit `~/.gemini/antigravity-cli/settings.json`: delete the `write_file`
 567: entry in `permissions.allow` whose path targets the agy-probe scratchpad
 568: directory, whatever its exact path spelling in the file (leave the array
 569: empty if nothing else remains), then re-read the file to confirm the
 570: deletion. Do NOT remove the probe dir's `trustedWorkspaces` entry yet —
 571: Step 2's probe needs a trusted directory.
 572: 
 573: - [ ] **Step 2: allowNonWorkspaceAccess narrowing attempt (bounded)**
 574: 
 575: Try exactly one narrowing: set `allowNonWorkspaceAccess` to `false`, then
 576: run this raw probe against the still-trusted probe dir (PowerShell;
 577: `$probe` = the agy-probe scratchpad dir from the spec's probe record):
 578: 
 579: ```powershell
 580: & "$env:LOCALAPPDATA\agy\bin\agy.exe" -p "Create a file named narrow-probe.txt in the workspace directory $probe containing exactly the line: NARROW-OK" --model gemini-3.6-flash-low --add-dir $probe --log-file "$probe\narrow.log"
 581: ```
 582: 
 583: `narrow-probe.txt` lands with NARROW-OK = keep `false`. Soft-denied or
 584: absent = restore `true` and record in the SDD ledger:
 585: "allowNonWorkspaceAccess=true required for print-mode writes as of agy
 586: 1.1.7 — documented fallback per spec section 3." Do not iterate further.
 587: 
 588: - [ ] **Step 3: Drop the probe-dir trust entry**
 589: 
 590: Remove the probe dir from `trustedWorkspaces` (session-temporary dir; the
 591: repo is the lane's only workspace this cycle).
 592: 
 593: - [ ] **Step 4 (ATTENDED - user): Trust the repo**
 594: 
 595: User runs one interactive `agy` session in
 596: `C:\Users\Brandon\Documents\parallax`, approves trust, exits. Verify:
 597: `trustedWorkspaces` in the settings file now contains the repo path.
 598: 
 599: - [ ] **Step 5: Version bump**
 600: 
 601: Edit `.claude-plugin/plugin.json`: `"version": "0.12.0"`.
 602: 
 603: - [ ] **Step 6: Preflight dry-check + commit**
 604: 
 605: Run the agent's FIVE preflight checks by hand against the real
 606: environment, verbatim from the agent body: (1) `agy models` contains the
 607: pinned ID; (2) `trustedWorkspaces` contains the repo path; (3) the
 608: settings file carries NO `write_file(` entry at all — if an unrelated
 609: write rule remains, STOP and surface it for user disposition (never
 610: silently delete shared configuration); (4) `git status --porcelain` in
 611: the repo is empty; (5) no `AGY-TASK-BRIEF-*` file exists in the repo.
 612: All five must pass. Then:
 613: 
 614: ```bash
 615: git add .claude-plugin/plugin.json
 616: git commit -m "0.12.0: bump plugin version"
 617: ```
 618: 
 619: ---
 620: 
 621: ### Task 6: Live verification (ATTENDED; runs only after the plan debate converges)
 622: 
 623: **Files:**
 624: - None in-repo (scratch repo + probe records; SDD ledger notes)
 625: 
 626: **Interfaces:**
 627: - Consumes: the installed 0.12.0 cache (dev loop: `claude plugin update parallax@parallax`, restart — the agent is NOT live before that).
 628: - Produces: the live evidence the diff debate's brief cites.
 629: 
 630: - [ ] **Step 1 (ATTENDED - user): Dev loop**
 631: 
 632: Bump is committed (Task 5); user runs `claude plugin update parallax@parallax` and restarts the session. Without the restart the dispatched agent is the cached pre-0.12.0 set — a dry-run before restart tests nothing.
 633: 
 634: - [ ] **Step 2: Scratch repo setup**
 635: 
 636: Create a scratch git repo in the session scratchpad with one file
 637: (`hello.py`, `print("hello")`), and COMMIT it — the agent's clean-tree
 638: preflight (check 4) requires an empty `git status --porcelain` before
 639: dispatch (Sol check-off round 2, finding 1). Then an interactive trust
 640: approval (ATTENDED - user, same mechanism as Task 5 Step 4).
 641: 
 642: - [ ] **Step 3: Green dry-run through the REAL agent**
 643: 
 644: Verify `git status --porcelain` in the scratch repo is empty. Dispatch
 645: `parallax:flash-implementer` (fresh subagent) with a minimal task:
 646: "Modify hello.py so it prints exactly 'hello flash'. Verification:
 647: `python hello.py` prints `hello flash`." plus a log path in the
 648: scratchpad. Expected report: STATUS done; ROUTE carries
 649: `gemini-3.6-flash-medium` + log path + brain transcript path; FILES
 650: CHANGED lists `hello.py`; VERIFICATION shows the wrapper's own
 651: `python hello.py` output; DEVIATIONS none. Controller re-runs
 652: `python hello.py` itself (never trust the report alone — 0.10.0 lesson:
 653: controller-owned verification). Then COMMIT the green result in the
 654: scratch repo and verify `git status --porcelain` is empty again, so the
 655: later probes start from a clean baseline.
 656: 
 657: - [ ] **Step 4a: Raw red probe — the CLI's invalid-model path (no agent)**
 658: 
 659: Controller runs, directly in the scratch repo (split from a single
 660: disjunctive probe by Sol check-off F5 — a wrapper refusing a
 661: contradictory brief must not count as exercising this path):
 662: 
 663: ```powershell
 664: & "$env:LOCALAPPDATA\agy\bin\agy.exe" -p "Reply with exactly: X" --model gemini-9.9-fake --add-dir <scratch> --log-file <scratchpad>\red-raw.log
 665: ```
 666: 
 667: Expected: loud "invalid model selection" rejection, nonzero exit, no
 668: file changes in the scratch tree.
 669: 
 670: - [ ] **Step 4b: Reachable-failure probe — the REAL agent's blocked path**
 671: 
 672: First save the ORIGINAL settings content
 673: (`~/.gemini/antigravity-cli/settings.json`) to the session scratchpad.
 674: Then plant a NONMATCHING SENTINEL rule in `permissions.allow`:
 675: `write_file(/parallax-sentinel-never-matches/)` — preflight check 3 bans
 676: the rule CLASS, so the sentinel triggers it without ever granting a
 677: functional permission (Sol check-off round 2, finding 4). Dispatch
 678: `parallax:flash-implementer` with a trivial task in the scratch repo.
 679: Expected: STATUS blocked at preflight check 3, quoting the sentinel; no
 680: dispatch reaches agy; `git status --porcelain` still empty. Restore the
 681: saved ORIGINAL settings content afterward — including on an aborted or
 682: interrupted run, restore-before-anything-else on resume — and re-read
 683: the file to confirm it matches the saved copy.
 684: 
 685: - [ ] **Step 5 (ATTENDED - user): skip-permissions print-mode probe**
 686: 
 687: User runs, in the scratch repo (isolated), the one command the spec's
 688: probe task left open:
 689: `agy -p "Create a file named skip-probe.txt containing SKIP-OK" --model gemini-3.6-flash-low --add-dir <scratch> --dangerously-skip-permissions --log-file <scratchpad>\probe-skip.log`
 690: after REMOVING the scratch repo's trust entry. Record the outcome in the
 691: SDD ledger: file created = the flag works in print mode (both bypass
 692: surfaces live); soft-denied = the flag is inert in print mode (the
 693: settings assertion is the whole live defense). Either way the lane's
 694: contract is unchanged — the flag stays forbidden; this only settles which
 695: defense is load-bearing, per spec section 2.
 696: 
 697: - [ ] **Step 6: Record**
 698: 
 699: Write the dry-run + red-probe + skip-probe outcomes into the SDD ledger
 700: (`.superpowers/sdd/2026-07-25-flash-implementer/`), with log paths — the
 701: diff-debate brief cites them.
 702: 
 703: ---
 705: ## Debate record
 706: 
 707: **Participants:** Fable 5 (session) / Kimi K3 `kimi-code/k3-256k` (kimi-cli 1.49.0, session 4ba130bb-1615-4c95-9afc-d1e3049bb857) / GPT-5.6 Sol (codex exec, session 019f9ad5-e318-7831-8a3f-0c02c94caa8e)
 708: **Rounds used:** 2 of 4 (Kimi, plan mode) + 2 of 4 (Sol, primary check-off)
 709: **Outcome:** converged with amendments
 710: **Verification status:** FULL
 711: **Degradation:** quota-exhausted (primary lane at the plan gate; backup lane substituted for rounds 1-2, primary check-off ran on the reset window)
 712: **Authorized by:** user at round 1 (backup-lane substitution; the user's standing ruling retained the primary check-off before branch close)
 713: **Raw rounds:** docs/superpowers/plans/rounds/2026-07-25-flash-implementer/ (plan-round1-brief.md, plan-round{1,2}-transcript.txt — Kimi; sol-checkoff-round1-{brief,reply,header}.md/txt — Sol; round-2 files added at convergence)
 714: 
 715: Backup-lane note: Kimi ran read-only via a custom agent-file
 716: (ReadFile/ReadMediaFile/Glob/Grep/SetTodoList; pre-review write-probe
 717: refused) against a throwaway clone — the real tree was never exposed.
 718: Route evidence per round: `Using LLM model: provider='managed:kimi-code'
 719: model='k3-256k'` in ~/.kimi/logs/kimi.log (client-side class). Clone
 720: `git status` after round 2: sole untracked file was the session-authored
 721: brief (retained evidence: rounds/.../clone-status-after-round2.txt).
 722: Sol round 1 route: header block model gpt-5.6-sol / provider openai /
 723: sandbox read-only / effort high.
 724: 
 725: ### Resolved points
 726: 
 727: | # | Claim | Raised by | Outcome | Evidence |
 728: |---|-------|-----------|---------|----------|
 729: | 1 | Task 4 doctor text trips the plan's own literal sweep | Kimi r1 | accepted; doctor text reworded (5391455) | plan Task 4 vs test sweep globs |
 730: | 2 | Task 1 expected parity failure signature wrong (FileNotFoundError first) | Kimi r1 | accepted; expectation corrected | test_flash_implementer.py `_read` order |
 731: | 3 | README drop-ins absence pin vacuous (line-wrapped source) | Kimi r1 | accepted; presence pins added | README.md:132-133 line break |
 732: | 4 | Sweep omits evals/**/*.ps1 vs the reviewer sweep it mirrors | Kimi r1 | accepted; glob added | test_multi_model_verify.py:249 |
 733: | 5 | Flash Lane note misstates where the literal lives | Kimi r1 | accepted; reworded | implementer.md carries no gemini literal |
 734: | 6 | Drift-snapshot snippet violates carry-forward invariant; PS 5.1 parse risk | Kimi r1 | accepted; Task 4 Step 2 rewritten to carry-forward | check-drift.ps1:193-205 |
 735: | 7 | Red-probe expectation contradicts pinned dispatch contract | Kimi r1 | accepted; made disjunctive (superseded by #14) | plan Task 6 vs agent body pin |
 736: | 8 | Settings-deletion instruction pins unverifiable path spelling | Kimi r1 | accepted; made spelling-agnostic | settings file outside workspace |
 737: | 9 | Parity narrower than spec claim (headings unpinned in classic) | Kimi r1 | accepted; headings pinned in both files | spec section 1 vs test scope |
 738: | 10 | Corroboration evidence source wrong: --log-file log carries NO file actions; brain transcript does | Sol r1 | accepted; evidence source corrected to transcript_full.jsonl | probe9.log: 0 hits for probe-write4; brain/<cid>/.../transcript_full.jsonl: 5 hits |
 739: | 11 | Brief-file lifecycle unsafe (collision, interruption, unbaselined git status) | Sol r1 | accepted; unique name + collision preflight + guaranteed cleanup + clean-baseline preflight | agent body Dispatch/Preflight sections |
 740: | 12 | Debate record lacked required schema fields and session adjudication | Sol r1 | accepted; this record | frozen-plan-format.md:36-68, debate-protocol.md:63 |
 741: | 13 | Settings check had no path-matching algorithm | Sol r1 | accepted; conservative rule-class ban (any write_file( entry) | trust entry vs allow rule spelling mismatch on this machine |
 742: | 14 | Red probe vacuous (wrapper refusal passes without exercising failure path) | Sol r1 | accepted; split into raw-CLI probe + reachable-failure probe | plan Task 6 Steps 4a/4b |
 743: | 15 | Task 6 green run cannot satisfy the new clean-tree preflight | Sol r2 | accepted; scratch baseline committed, post-run commit + clean check | plan Task 6 Steps 2-3 |
 744: | 16 | Task 5 dry-check still tested the obsolete three-preflight contract | Sol r2 | accepted; Step 6 runs all five checks verbatim, unrelated rules escalate to user | plan Task 5 Step 6 |
 745: | 17 | F2/F4 amendments not actually pinned (loose keyword asserts) | Sol r2 | accepted; exact-sentence pins + ROUTE transcript pin added | test_flash_implementer.py dispatch/preflight tests |
 746: | 18 | Step 4b installs a functional bypass without interruption-safe restore | Sol r2 | accepted; nonmatching sentinel rule + save/restore-in-finally | plan Task 6 Step 4b |
 747: | 19 | Stale cross-references (log/tree, workdir-scoped, log-only ROUTE, #13 for #14) | Sol r2 | accepted; synchronized | plan header, spec Decisions + section 6, resolved point 7 |
 748: 
 749: ### Escalated points (user-decided)
 750: 
 751: | # | Question | Session position | Reviewer position | Owner's call |
 752: |---|----------|------------------|-------------------|--------------|
 753: 
 754: (none this debate; the consent-gated reroute mechanism records future
 755: blocked-task reroutes here)
 756: 
 757: ### Session adjudication (debate-protocol closing step)
 758: 
 759: Each Sol round-1 finding was verified against live evidence before
 760: acceptance: #10 by grepping the retained WRITE4 probe log (zero
 761: file-path hits) and the brain transcript (five hits); #11/#14 against
 762: the agent body and plan text as cited; #12 against
 763: frozen-plan-format.md:36-68; #13 against the live settings file's mixed
 764: path spellings. Kimi's rounds 1-2 were adjudicated at the time (all nine
 765: findings verified, fixes committed 5391455, round-2 re-verification by
 766: the reviewer). No finding was refuted; no point escalated. Convergence =
 767: Sol round-2 confirmation of these amendments (transcript retained under
 768: Raw rounds).
 769: 
 770: 
 771: 
 772: 
 773: 
 774: 
 775: 

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$files=@('"'agents/flash-implementer.md','evals/multi-model-verify/test_flash_implementer.py','docs/superpowers/specs/2026-07-25-flash-implementer-design.md'); foreach("'$p in $files){ Write-Output "FILE $p"; $lines=Get-Content -LiteralPath $p; for($i=1;$i -le $lines.Count;$i++){ '"'{0,4}: {1}' -f "'$i,$lines[$i-1] } }' in C:\Users\Brandon\Documents\parallax
 succeeded in 512ms:
FILE agents/flash-implementer.md
   1: ---
   2: name: flash-implementer
   3: description: Zero-judgment Flash implementer for frozen-plan tasks. Use when executing build tasks from a debate-frozen implementation plan - give it ONE task's verbatim text plus the plan's Global Constraints and a log-file path. It delegates ALL code-writing to Gemini 3.6 Flash via the Antigravity CLI headlessly, verifies route and authorship evidence, runs the task's verification itself, and reports. It never types repo code and never makes design decisions.
   4: model: haiku
   5: tools: Read, Grep, Glob, Bash
   6: ---
   7: 
   8: # Flash implementer (agy wrapper)
   9: 
  10: You supervise ONE task from a frozen implementation plan. Gemini 3.6 Flash
  11: does ALL the typing through the Antigravity CLI (`agy`); you do preflight,
  12: dispatch, evidence checks, verification, and honest reporting. You never
  13: edit or create repo files yourself — your tool grant has no Edit or Write,
  14: and using Bash to write repo content is equally forbidden: a changed file
  15: the brain transcript cannot account for fails the task. The one declared
  16: carve-out is the brief file below — the sole transient exception to the
  17: never-write rule, and it never survives to the evidence checks.
  18: 
  19: <!-- shared-contract:start -->
  20: ## The contract
  21: 
  22: - Build exactly what the task says: the files it lists, the code it shows,
  23:   the commands it specifies. Nothing else.
  24: - No improvements, no drive-by refactors, no added error handling, no scope
  25:   adjustments. A deviation is a defect even when it looks better — the diff
  26:   gets checked against the plan afterward, and unexplained drift fails it.
  27: - **INPUT GAP rule:** if the task references a file, interface, value, or
  28:   convention that is not in your brief and not discoverable at the exact
  29:   path the task names, STOP and report the gap. Never invent or guess the
  30:   missing piece.
  31: - Run the task's verification commands yourself and read the output. Never
  32:   claim completion without re-running verification — "should work" means
  33:   the task is not done.
  34: <!-- shared-contract:end -->
  35: 
  36: ## Inputs (from the dispatching controller)
  37: 
  38: - The task's verbatim text and the plan's Global Constraints.
  39: - The workspace directory (this cycle: the main checkout only).
  40: - A log-file path OUTSIDE the workspace (the controller owns it; you never
  41:   place logs in the repo tree).
  42: 
  43: ## Preflight (all five must pass BEFORE dispatch)
  44: 
  45: 1. `agy models` (binary at `$LOCALAPPDATA/agy/bin/agy.exe`) — output must
  46:    contain `gemini-3.6-flash-medium`. Anything else (missing binary,
  47:    sign-out, missing model) is blocked.
  48: 2. `~/.gemini/antigravity-cli/settings.json` — `trustedWorkspaces` must
  49:    contain the workspace directory. If not: blocked, and the report quotes
  50:    the fix ("run one interactive `agy` session in the workspace and approve
  51:    trust").
  52: 3. The same settings file must carry NO file-writing per-tool allow rule
  53:    at all — any `write_file(` entry, whatever path it names, is blocking.
  54:    A persisted settings allow rule is the durable, call-site-invisible
  55:    bypass class — its absence is the load-bearing permission control; path
  56:    spellings vary, so the rule CLASS is banned rather than path-matched.
  57:    If present: blocked, quoting the rule.
  58: 4. `git status --porcelain` in the workspace must be EMPTY. A dirty tree
  59:    makes authorship attribution impossible — blocked, quoting the paths.
  60: 5. No file matching `AGY-TASK-BRIEF-*` exists in the workspace — a stale
  61:    brief means an earlier dispatch died mid-cleanup: blocked.
  62: 
  63: ## Dispatch
  64: 
  65: 1. Write the brief to `<workspace>/AGY-TASK-BRIEF-<unique>.md` with a Bash
  66:    heredoc — `<unique>` is the dispatch log file's basename, so briefs
  67:    never collide. Content: the task's verbatim text, the Global
  68:    Constraints, and the exact files list. (stdin does not reach the model
  69:    in print mode — probed 2026-07-25; the workspace brief file is the
  70:    delivery mechanism.) This file is the sole transient exception to your
  71:    never-write rule.
  72: 2. Run (single line):
  73:    `agy -p "Read the file AGY-TASK-BRIEF-<unique>.md in the workspace and execute it exactly." --model gemini-3.6-flash-medium --add-dir <workspace> --log-file <log-path>`
  74: 3. Delete the brief file immediately after agy exits — on success, failure, and interruption alike — and always BEFORE any evidence check, so it never appears in `git status`. If your run is resumed after an interruption, delete any leftover brief FIRST.
  75: 
  76: ## Route and authorship checks (every run)
  77: 
  78: - On the log file: `Print mode: starting` line present containing
  79:   `model="gemini-3.6-flash-medium"`.
  80: - On the log file: `Propagating selected model override` line present
  81:   (presence only — its display label is not matched).
  82: - Transcript/tree corroboration: parse `conversationID="<uuid>"` from the
  83:   log's `Print mode: starting` line, then read the brain transcript at
  84:   `~/.gemini/antigravity-cli/brain/<conversationID>/.system_generated/logs/transcript_full.jsonl`
  85:   (the `--log-file` log itself carries NO file actions — probed). Every path git status reports changed must appear in the brain transcript as a successful file-changing action. A changed file the transcript never
  86:   mentions means someone other than Flash typed it — blocked, no matter
  87:   what the tests say. A missing transcript is blocked.
  88: - This evidence is client-side: report the route as **requested and
  89:   propagated**, never "used and confirmed". Server-side substitution is
  90:   not detectable from this evidence class.
  91: 
  92: ## Failure handling — loud, never silent
  93: 
  94: Blocked (quote the exact output) on: any preflight failure, the print-mode
  95: soft-deny line ("auto-denied"), nonzero exit, a missing or mismatched
  96: route line, a corroboration mismatch, or writes diverted to agy's internal
  97: scratch (expected files absent from the tree). Never retry with
  98: `--dangerously-skip-permissions` — that flag is forbidden in this lane, as
  99: is ANY approval-bypass flag or persisted per-tool allow rule added to agy
 100: settings. Never complete the work yourself: rerouting a blocked task to a
 101: Claude tier is the user's decision, recorded in the plan's Escalated
 102: points — not yours.
 103: 
 104: ## Report format (your final message)
 105: 
 106: - **STATUS:** done | blocked | INPUT GAP: <exactly what is missing>
 107: - **ROUTE:** the resolved model ID as requested and propagated, plus the
 108:   retained log file's path AND the brain transcript's path
 109: - **FILES CHANGED:** actual paths from `git status` — on blocked, STILL
 110:   list every path Flash already touched so the session can revert a
 111:   partial write
 112: - **VERIFICATION:** each command you ran yourself, with its real output
 113:   (condensed)
 114: - **DEVIATIONS:** must be "none" — anything else means you stopped and are
 115:   explaining why the task could not be built as written
 116: 
 117: ## Lane note
 118: 
 119: This agent pins the Flash implementation lane. Canonical model literal:
 120: `gemini-3.6-flash-medium` (Gemini 3.6 Flash, medium reasoning effort,
 121: Antigravity CLI resolved ID). The literal lives ONLY here;
 122: `implementer.md` pins its own lane's model in its frontmatter and Lane
 123: note — every other surface points at the agent files. Trust is per-directory and interactive-only, so this lane runs in
 124: the main checkout this cycle — a worktree trust story is future work.
FILE evals/multi-model-verify/test_flash_implementer.py
   1: """Contract pins for the Flash implementer lane (agents/flash-implementer.md).
   2: 
   3: Amended by design spec 2026-07-25 (advisory review B1-B8): these tests pin
   4: the agent file's contract text so drift in the dispatch recipe, route
   5: check, forbidden-bypass class, or report format fails offline with zero
   6: CLI calls. The two agent files are the only allowed homes for the
   7: implementer model literal.
   8: """
   9: import re
  10: from pathlib import Path
  11: 
  12: REPO = Path(__file__).resolve().parents[2]
  13: FLASH = REPO / "agents" / "flash-implementer.md"
  14: CLASSIC = REPO / "agents" / "implementer.md"
  15: CANONICAL_ID = "gemini-3.6-flash-medium"
  16: SHARED_START = "<!-- shared-contract:start -->"
  17: SHARED_END = "<!-- shared-contract:end -->"
  18: 
  19: 
  20: def _read(p):
  21:     return p.read_text(encoding="utf-8")
  22: 
  23: 
  24: def _frontmatter(text):
  25:     m = re.match(r"^---\n(.*?)\n---\n", text, re.DOTALL)
  26:     assert m, "missing frontmatter"
  27:     return m.group(1)
  28: 
  29: 
  30: def test_flash_frontmatter_pins_model_and_tools():
  31:     fm = _frontmatter(_read(FLASH))
  32:     assert re.search(r"^model: haiku$", fm, re.MULTILINE)
  33:     m = re.search(r"^tools: (.+)$", fm, re.MULTILINE)
  34:     assert m, "tools allowlist missing"
  35:     tools = [t.strip() for t in m.group(1).split(",")]
  36:     assert sorted(tools) == ["Bash", "Glob", "Grep", "Read"]
  37: 
  38: 
  39: def test_flash_dispatch_contract():
  40:     body = _read(FLASH)
  41:     assert "--model " + CANONICAL_ID in body
  42:     assert "--add-dir" in body
  43:     assert "--log-file" in body
  44:     # unique-suffix brief name + full lifecycle, pinned by exact sentence
  45:     # fragments so a regression cannot pass on loose keywords
  46:     # (Sol check-off round 2, finding 3)
  47:     assert "AGY-TASK-BRIEF-" in body
  48:     assert "sole transient exception" in body.lower()
  49:     assert "the dispatch log file's basename" in body
  50:     assert "on success, failure, and interruption alike" in body
  51:     # stdin is probed-dead in print mode; the body must not suggest it
  52:     assert "stdin" not in body.lower() or "does not reach" in body.lower()
  53: 
  54: 
  55: def test_flash_route_check_strings():
  56:     body = _read(FLASH)
  57:     assert 'Print mode: starting' in body
  58:     assert 'model="' + CANONICAL_ID + '"' in body
  59:     assert "Propagating selected model override" in body
  60:     assert "requested and propagated" in body
  61:     assert "used and confirmed" not in body.replace(
  62:         'never "used and confirmed"', "")
  63:     # transcript/tree corroboration (Sol check-off F1: the log carries no
  64:     # file actions; evidence lives in the brain transcript)
  65:     assert "transcript_full.jsonl" in body
  66:     assert "conversationID" in body
  67:     assert ("every path git status reports changed must appear in the "
  68:             "brain transcript as a successful file-changing action"
  69:             ) in body.lower()
  70: 
  71: 
  72: def test_flash_preflight_pins():
  73:     body = _read(FLASH)
  74:     assert "agy models" in body
  75:     assert "trustedWorkspaces" in body
  76:     # conservative rule-class ban + clean baseline + stale-brief check,
  77:     # pinned by exact sentence fragments (Sol check-off round 2,
  78:     # finding 3): loose keyword pins would still pass a regression to
  79:     # path-scoped matching or a dropped preflight
  80:     assert "any `write_file(` entry, whatever path it names" in body
  81:     assert "allow rule" in body
  82:     assert "git status --porcelain" in body
  83:     assert "No file matching `AGY-TASK-BRIEF-*`" in body
  84: 
  85: 
  86: def test_flash_route_report_carries_transcript():
  87:     body = _read(FLASH)
  88:     assert "AND the brain transcript's path" in body
  89: 
  90: 
  91: def test_flash_forbidden_bypass_class():
  92:     body = _read(FLASH)
  93:     assert "--dangerously-skip-permissions" in body
  94:     idx = body.find("--dangerously-skip-permissions")
  95:     window = body[max(0, idx - 200):idx + 200].lower()
  96:     assert "never" in window or "forbidden" in window
  97:     assert "persisted" in body and "settings" in body
  98: 
  99: 
 100: def test_flash_report_headings():
 101:     body = _read(FLASH)
 102:     for heading in ("**STATUS:**", "**ROUTE:**", "**FILES CHANGED:**",
 103:                     "**VERIFICATION:**", "**DEVIATIONS:**"):
 104:         assert heading in body, heading
 105:     # the four shared headings are pinned in BOTH files so a unilateral
 106:     # rename in implementer.md cannot pass the suite (ROUTE is
 107:     # lane-specific to the flash file)
 108:     classic = _read(CLASSIC)
 109:     for heading in ("**STATUS:**", "**FILES CHANGED:**",
 110:                     "**VERIFICATION:**", "**DEVIATIONS:**"):
 111:         assert heading in classic, heading
 112: 
 113: 
 114: def _shared_block(text, path):
 115:     assert SHARED_START in text and SHARED_END in text, (
 116:         "missing shared-contract markers in " + str(path))
 117:     return text.split(SHARED_START)[1].split(SHARED_END)[0]
 118: 
 119: 
 120: def test_shared_contract_parity():
 121:     # byte-identical shared block; ROUTE is lane-specific and lives
 122:     # outside the block (spec section 1)
 123:     assert _shared_block(_read(FLASH), FLASH) == _shared_block(
 124:         _read(CLASSIC), CLASSIC)
 125: 
 126: 
 127: def test_classic_lane_note_retired_stale_claim():
 128:     body = _read(CLASSIC)
 129:     assert "Nothing else in the plugin references" not in body
 130:     assert "flash-implementer" in body
 131: 
 132: 
 133: SWEEP_GLOBS = [
 134:     "skills/**/*.md", "commands/*.md", "tools/*.ps1", "hooks/*",
 135:     "evals/**/*.py", "evals/**/*.json", "evals/**/*.ps1",
 136:     "README.md", "CLAUDE.md", "agents/*.md",
 137: ]
 138: # The two agent files are the contract homes; this test file necessarily
 139: # carries the literal as its enforcement pin.
 140: ALLOWED = {FLASH.resolve(), CLASSIC.resolve(),
 141:            Path(__file__).resolve()}
 142: 
 143: 
 144: def test_flash_literal_single_source():
 145:     offenders = []
 146:     for pattern in SWEEP_GLOBS:
 147:         for p in REPO.glob(pattern):
 148:             if p.resolve() in ALLOWED:
 149:                 continue
 150:             if "gemini-3.6-flash" in p.read_text(encoding="utf-8",
 151:                                                  errors="replace"):
 152:                 offenders.append(str(p))
 153:     assert offenders == []
 154: 
 155: 
 156: def test_sonnet_implementer_literals_removed():
 157:     readme = _read(REPO / "README.md")
 158:     assert "currently `model: sonnet`" not in readme
 159:     # absence pins alone are vacuous against line-wrapped source; the
 160:     # presence pins below are the real oracles for Task 3's rewrites
 161:     assert "`haiku`/`opus` are drop-ins" not in readme
 162:     assert "any Claude tier is a drop-in" in readme
 163:     fpf = _read(REPO / "skills" / "multi-model-verify" / "references"
 164:                 / "frozen-plan-format.md")
 165:     assert "Sonnet 5" not in fpf
 166:     assert "the pinned lane in `agents/`" in fpf
FILE docs/superpowers/specs/2026-07-25-flash-implementer-design.md
   1: # Flash implementer lane (Antigravity CLI) — design
   2: 
   3: **Date:** 2026-07-25 · **Cycle:** 0.12.0 · **Status:** approved (Brandon, 2026-07-25);
   4: advisory-review amendments folded same day (see Review provenance)
   5: 
   6: ## Problem
   7: 
   8: The per-task SDD implementer lane is pinned to Sonnet (`agents/implementer.md`,
   9: `model: sonnet`). The user's roadmap (backlog item 4, order fixed 2026-07-25)
  10: replaces that build-task lane with Gemini Flash — "it does nothing but EXACTLY
  11: what you tell it to," a fit for a zero-judgment executor. Agent frontmatter
  12: only accepts Claude models, so Flash arrives by delegation: a cheap Claude
  13: tier holds the agent seat and drives a vendor CLI headlessly — the
  14: fable-advisor v3 `grok-implementer` pattern already documented in
  15: `implementer.md`'s Lane note.
  16: 
  17: ## Transport decision: Antigravity CLI, not gemini-cli (user, 2026-07-25)
  18: 
  19: The obvious transport died during probing:
  20: 
  21: - gemini-cli 0.52.0 OAuth for individuals is server-side dead ("This client
  22:   is no longer supported for Gemini Code Assist for individuals... migrate to
  23:   the Antigravity suite"); six open issues confirm (primary #28229, P1).
  24:   Google's transition post: free/consumer serving ended 2026-06-18; only
  25:   paid/enterprise API keys remain supported.
  26: - Worse: with a still-working consumer API key, gemini-cli **silently served
  27:   gemini-3.5-flash when pinned to gemini-3.6-flash** (stats block evidence),
  28:   while a garbage model ID errored loudly — a silent model substitution, the
  29:   exact route-integrity failure the codex lane's effective-route check exists
  30:   to reject.
  31: - Antigravity CLI (`agy`) is the official successor (Google Developers Blog,
  32:   "Transitioning Gemini CLI to Antigravity CLI"), installed and probed
  33:   end-to-end the same night. gemini-cli plays no part in this design.
  34: 
  35: ## Probe record (agy 1.1.7, Windows, 2026-07-25)
  36: 
  37: All probes ran headless in a scratch directory; log evidence retained in the
  38: session scratchpad (`agy-probe/probe*.log`).
  39: 
  40: - Auth: Google Sign-In (system keyring), one-time interactive. `agy models`
  41:   requires sign-in and lists `gemini-3.6-flash-{high,medium,low}` (plus 3.5
  42:   variants, `gemini-3.1-pro-*`, `claude-sonnet-4-6`, `claude-opus-4-6-thinking`,
  43:   `gpt-oss-120b-medium` — the CLI is multi-vendor).
  44: - Pinned headless run: `agy -p "..." --model gemini-3.6-flash-low --log-file F`
  45:   → correct reply, exit 0; log carries route-request evidence:
  46:   `Print mode: starting (... model="gemini-3.6-flash-low" ...)` and
  47:   `Propagating selected model override to backend: label="Gemini 3.6 Flash (Low)"`.
  48:   Both lines are CLIENT-side (what the CLI requested and propagated), not
  49:   server-side attribution of what was served.
  50: - Unknown model: rejected loudly at the CLI layer before any API call
  51:   ("invalid model selection ... not recognized"), available models listed.
  52: - Write path: print mode cannot prompt, so unapproved tools soft-deny loudly
  53:   ("jetski: ... auto-denied", tool confirmation "Edit"/CodeAction).
  54:   `--mode accept-edits` does NOT apply in print mode. Without `--add-dir`,
  55:   writes divert to the CLI's internal scratch workspace
  56:   (`~/.gemini/antigravity-cli/scratch/`) — files never reach the real tree.
  57: - Working combination (WRITE4-OK): one-time interactive trust of the target
  58:   directory (persists to `trustedWorkspaces` in
  59:   `~/.gemini/antigravity-cli/settings.json`; the interactive session also set
  60:   `allowNonWorkspaceAccess: true`) + headless `--add-dir <dir>` binding →
  61:   file created in the bound tree, no prompting.
  62: - File-action evidence does NOT live in the `--log-file` log (probed
  63:   2026-07-25, Sol primary check-off F1: the WRITE4 run's log contains zero
  64:   mentions of the file it wrote). It lives in agy's internal brain
  65:   transcript: parse `conversationID="<uuid>"` from the log's
  66:   `Print mode: starting` line, then read
  67:   `~/.gemini/antigravity-cli/brain/<conversationID>/.system_generated/logs/transcript_full.jsonl`
  68:   (the WRITE4 file appears there in successful file-changing actions).
  69: - `agy --help` (v1.1.7) lists `--dangerously-skip-permissions` ("Auto-approve
  70:   all tool permission requests without prompting"). Whether it takes effect
  71:   in print mode is UNPROBED (accept-edits provably does not).
  72: - Environment note: an "Orca" app has pre-planted hooks in BOTH
  73:   `~/.gemini/settings.json` (gemini-cli hook events) and agy's stop hooks
  74:   (`jsonhook__orca-status_Stop_0_0` failed non-fatally in probe logs). Inert
  75:   outside Orca sessions; watch in drift triage if it starts failing loudly.
  76: 
  77: ## Review provenance
  78: 
  79: Same-vendor advisory review (Opus 5, dispatched 2026-07-25, two rounds):
  80: initial verdict FIX with 8 blocking findings; after one factual correction
  81: (the skip-permissions flag IS an agy flag, per `agy --help`) and the
  82: amendments now folded into this text, revised verdict PASS conditional on
  83: the amendments living in the spec, which this revision satisfies. This pass
  84: is NOT cross-vendor verification and forms no part of the Sol gate: the
  85: plan debate (before implementation) and diff debate (before merge) remain
  86: required and are quota-gated to the ~Jul 29 codex reset.
  87: 
  88: Amended again 2026-07-25 after the PRIMARY reviewer's check-off (GPT-5.6
  89: Sol, codex window reset early; round 1 FIX, five blocking findings, all
  90: session-verified and accepted): F1 corroboration evidence source corrected
  91: to the brain transcript (the log carries no file actions — probed); F2
  92: brief-file lifecycle + clean-baseline preflight; F4 settings check made
  93: conservative (ban the write_file rule class, no path matching); F5 red
  94: probe split into raw-CLI and reachable-failure probes; F3 (debate-record
  95: schema) lands in the plan's Debate record, not this spec. Full round
  96: record in the plan's debate appendix.
  97: 
  98: ## Decisions (user, 2026-07-25)
  99: 
 100: - Architecture **A**: new `agents/flash-implementer.md`; `implementer.md`
 101:   kept as the direct-typing lane. Chosen over rewriting `implementer.md`
 102:   wholesale (B) after weighing the fable-advisor reference (which added its
 103:   vendor wrapper BESIDE the Claude lane), the untouched haiku transcription
 104:   lane, and the instant sonnet fallback against B's single-file tidiness.
 105:   C (controller shells the CLI directly) rejected: no isolation, no report
 106:   anchor. Known cost, accepted with mitigation: the two agent files share
 107:   contract sections that can drift; agent files cannot include each other,
 108:   so a byte-identity parity test is the only available enforcement and is
 109:   REQUIRED, not optional.
 110: - True-Flash-only, fails loudly: the wrapper never types repo code itself
 111:   and never completes work Flash failed. Enforced mechanically where
 112:   possible (tools allowlist, transcript/tree corroboration — see §1, §2), by
 113:   contract text where not.
 114: - Consent-gated reroute: a blocked task surfaces to the session AND the user;
 115:   rerouting to a Claude tier is an explicit user decision recorded in the
 116:   frozen plan's **Escalated points (user-decided)** section — the existing
 117:   artifact, not a new table. No automatic degradation — same ethos as the
 118:   debate lane's transport consent gate.
 119: - Scope: the Flash lane runs in the MAIN CHECKOUT ONLY this cycle. Trust is
 120:   per-directory and grantable only interactively, so fresh worktrees are
 121:   untrusted by construction; a worktree trust story is future-cycle work.
 122:   Coupling made explicit: with no disposable worktree, the
 123:   FILES-CHANGED-on-blocked rule (§4) is the recovery affordance for a
 124:   partially written tree.
 125: - Effort: ONE canonical variant this cycle, `gemini-3.6-flash-medium`. No
 126:   per-task effort choice by any seat, no effort slot in the plan format —
 127:   a zero-judgment lane gets zero dispatch-time judgment calls.
 128: - **Sol check-off required before this branch closes**: plan debate before
 129:   implementation, diff debate before merge, both after the ~Jul 29 reset;
 130:   spec and plan drafting proceed now.
 131: 
 132: ## Design
 133: 
 134: ### 1. Agent: `agents/flash-implementer.md`
 135: 
 136: `model: haiku` frontmatter (wrapper/supervisor) plus an explicit `tools:`
 137: allowlist — Read, Grep, Glob, Bash; **no Edit, no Write, no NotebookEdit** —
 138: so the ergonomic path for the wrapper to type code itself does not exist.
 139: (Bash remains a write primitive in principle; the transcript/tree corroboration in
 140: §2 is the control that catches that path.) If the long-brief probe (§2)
 141: forces brief-via-file, the brief is written with a Bash heredoc — Write
 142: stays out of the allowlist.
 143: 
 144: Same input contract as `implementer.md` (ONE task's verbatim text + the
 145: plan's Global Constraints), same INPUT GAP rule. Report format gains one
 146: lane-specific line:
 147: 
 148: - **STATUS:** done | blocked | INPUT GAP: <what is missing>
 149: - **ROUTE:** resolved model ID as requested-and-propagated, from the log,
 150:   plus the retained log file's path
 151: - **FILES CHANGED:** actual paths from `git status` — on `blocked` this
 152:   MUST still list every path Flash already touched, so the session can
 153:   revert a partial write
 154: - **VERIFICATION:** each command run by the wrapper itself, with real output
 155: - **DEVIATIONS:** must be "none" or an explanation of why the task could
 156:   not be built as written
 157: 
 158: The parity test (§6) pins the shared sections (INPUT GAP rule, report
 159: headings other than ROUTE) byte-identical across both agent files; ROUTE is
 160: declared lane-specific and excluded from parity.
 161: 
 162: ### 2. Dispatch recipe
 163: 
 164: - Preflight, all blocking, before any dispatch:
 165:   1. `agy models` output contains `gemini-3.6-flash-medium` (also proves
 166:      sign-in and binary presence);
 167:   2. `~/.gemini/antigravity-cli/settings.json` `trustedWorkspaces` contains
 168:      the workdir — if not, block with the exact re-trust instruction (one
 169:      interactive `agy` session in the workdir);
 170:   3. the same settings file carries NO file-writing per-tool allow rule
 171:      AT ALL (any `write_file(` entry, whatever path it names) — the
 172:      durable, call-site-invisible bypass class; this settings assertion is
 173:      the LOAD-BEARING permission control (a flag is at least visible
 174:      per-dispatch; a settings rule survives invisibly forever — tonight's
 175:      probing itself left one behind). Conservative by amendment (Sol
 176:      check-off F4): agy's path normalization is not offline-verifiable
 177:      (trust entries and allow rules already disagree on drive-letter
 178:      spelling on this machine), so the lane bans the rule CLASS rather
 179:      than attempting workdir path-matching;
 180:   4. `git status --porcelain` is EMPTY before dispatch (Sol check-off F2:
 181:      without a pre-dispatch baseline, pre-existing changes are
 182:      unattributable — a dirty tree is blocked, not absorbed).
 183: - Dispatch: `agy -p <brief> --model gemini-3.6-flash-medium
 184:   --add-dir <workdir> --log-file <fresh per-task log>`. Fresh log file per
 185:   attempt, written under the session scratchpad (never the repo tree — no
 186:   .gitignore dependency); each task's report carries its log path, and the
 187:   SDD execution record (the `.superpowers/sdd/<date>-<topic>/` workspace
 188:   the subagent-driven-development skill maintains) records per-task lane
 189:   provenance, which the diff-debate brief then carries. Adding an
 190:   implementer line to the frozen-plan debate record's Participants block is
 191:   declared scope for the plan debate to settle.
 192: - Brief-file lifecycle (Sol check-off F2): the brief is the SOLE transient
 193:   file the wrapper may create in the repo tree — a declared exception to
 194:   its never-write rule. Unique reserved name (`AGY-TASK-BRIEF-<unique>.md`,
 195:   unique suffix from the dispatch's log-file basename); collision
 196:   preflight (any existing `AGY-TASK-BRIEF-*` file in the workspace =
 197:   stale state = blocked); deletion guaranteed after agy exits — success,
 198:   failure, or interruption path alike — and always BEFORE any evidence
 199:   check, so the brief never appears in the attribution set.
 200: - Route check, every run, on the log file:
 201:   1. `Print mode: starting` line present with `model="gemini-3.6-flash-medium"`
 202:      (the RESOLVED ID is the comparand — same string §5 pins as canonical);
 203:   2. `Propagating selected model override` line present (presence-only; its
 204:      display label is a second namespace and is NOT matched);
 205:   3. transcript/tree corroboration (evidence source corrected by Sol
 206:      check-off F1 — the `--log-file` log carries NO file actions, probed):
 207:      parse `conversationID="<uuid>"` from the log's `Print mode: starting`
 208:      line, read the brain transcript at
 209:      `~/.gemini/antigravity-cli/brain/<conversationID>/.system_generated/logs/transcript_full.jsonl`,
 210:      and require every path `git status` reports changed to appear in that
 211:      transcript as a successful file-changing action. A changed file the
 212:      transcript never mentions means the wrapper (or something else) typed
 213:      it — blocked regardless of test results. A missing transcript is
 214:      blocked. The ROUTE report line carries the transcript path beside the
 215:      log path.
 216:   This evidence class is CLIENT-side: the check verifies the route was
 217:   **requested and propagated**, never "used and confirmed" (the doctor
 218:   check-4 language discipline, `commands/doctor.md`). Server-side
 219:   substitution is undetectable with today's evidence — named in Risks.
 220: - Probe task (pre-plan, throwaway harness, no agent file needed), pinned
 221:   before the agent body freezes:
 222:   1. long-brief mechanics — argument vs stdin vs file, quoting, size limits;
 223:   2. does agy emit ANY response-side model attribution (log tail, response
 224:      metadata; agy has no `--output-format` flag) — if yes, that artifact
 225:      becomes the route check and the client-side lines demote to
 226:      preconditions; the probe's answer is written into this section as the
 227:      final route-check contract text;
 228:   3. does `--dangerously-skip-permissions` take effect in print mode at all
 229:      (accept-edits provably does not) — if inert, the settings assertion is
 230:      the whole live defense, and §4's flag ban is belt-and-suspenders.
 231: 
 232: ### 3. One-time setup (documented in the agent file)
 233: 
 234: agy installed + signed in (done 2026-07-25). The repo root is trusted via
 235: one interactive agy session (same mechanism as the probe dir). An
 236: implementation task minimizes tonight's probe leftovers in agy's settings:
 237: remove the probe-dir `write_file` allow rule (required — §2's preflight
 238: bans the write_file( rule class outright), and attempt the narrowest working
 239: `allowNonWorkspaceAccess` config with a DECLARED fallback: if narrowing
 240: does not converge quickly, leave the value as-is and document it and why —
 241: the task must not stall, and the setting is shared with the Orca app's
 242: environment. Trust is per-directory; the main-checkout-only scope decision
 243: above is the worktree story for this cycle.
 244: 
 245: ### 4. Failure handling — loud, never silent
 246: 
 247: `STATUS: blocked` with the exact output quoted, on: any preflight failure
 248: (sign-out, missing model, untrusted workdir, any write_file( allow rule present),
 249: the print-mode soft-deny line, nonzero exit, route-check failure (missing
 250: route lines, wrong resolved ID, transcript/tree corroboration mismatch), files
 251: diverted to internal scratch. Forbidden by class in the agent body: the
 252: `--dangerously-skip-permissions` flag by name, any `--mode`/approval bypass,
 253: AND persisting any file-writing per-tool allow rule in agy settings —
 254: the durable settings-rule form is the load-bearing prohibition. On blocked,
 255: FILES CHANGED lists Flash's partial writes (per §1) as the recovery
 256: affordance. Reroute is consent-gated per Decisions.
 257: 
 258: ### 5. Model literal placement
 259: 
 260: The canonical implementer-lane literal is the RESOLVED ID
 261: `gemini-3.6-flash-medium`, living in `agents/flash-implementer.md` — the
 262: same string the preflight and route check compare against, so no
 263: requested/resolved ambiguity survives. After this cycle, no implementer
 264: model literal exists outside the two agent files. That claim is made true
 265: by enumerated edits, not assumption:
 266: 
 267: - `README.md` repo-map row (~line 66): "currently `model: sonnet`" literal
 268:   replaced by a pointer to the agent files; new row for the Flash lane.
 269: - `README.md` role-plug section (~lines 131-134): "`sonnet` today;
 270:   `haiku`/`opus` are drop-ins" reworded to point at the agent files without
 271:   naming tiers.
 272: - `agents/implementer.md` Lane note: gains the pointer to
 273:   `flash-implementer.md`; its "Nothing else in the plugin references the
 274:   implementer's model" sentence is RETIRED (already false today — README:66,
 275:   frozen-plan-format.md:4). Its own frontmatter/Lane-note literals stay:
 276:   agent files are the allowed homes.
 277: - `skills/multi-model-verify/references/frozen-plan-format.md` line 4:
 278:   "Sonnet 5" literal becomes a generic reference to the implementer agents.
 279: 
 280: Enforced by a new sweep test mirroring
 281: `test_reviewer_id_has_single_source`: scans the reviewer sweep's file set
 282: PLUS `agents/*.md`, with exactly the two agent files as the allowed homes —
 283: so a future third agent file is swept by default, not silently missed.
 284: 
 285: ### 6. Verification for this cycle
 286: 
 287: - Offline pytest (zero quota, both vendors): contract pins on the new agent
 288:   file — dispatch flags, resolved-ID comparand, route-check strings
 289:   (including the transcript/tree corroboration rule), forbidden-class phrasing,
 290:   report headings including ROUTE, `model: haiku` frontmatter, and the
 291:   `tools:` allowlist (no Edit/Write/NotebookEdit) — plus the §1 parity test
 292:   and the §5 implementer-literal sweep.
 293: - Pre-plan probes (throwaway harness, no agent file): the §2 probe task.
 294:   These are probes, not implementation — the plan-debate gate is not
 295:   crossed.
 296: - Live verification through the REAL agent happens during implementation,
 297:   AFTER the plan debate: one end-to-end dry-run task in a scratch repo
 298:   (trusted for the occasion), plus TWO failure probes (split by Sol
 299:   check-off F5 — the earlier single disjunctive probe was vacuous: a
 300:   wrapper refusing the contradictory brief would "pass" without ever
 301:   exercising the failure path): (a) a RAW agy probe, no agent, proving
 302:   the CLI's loud invalid-model rejection with no writes; (b) a
 303:   reachable-failure probe through the REAL agent — plant a scratch-scoped
 304:   `write_file` allow rule in agy settings, dispatch a trivial task,
 305:   require preflight check 3 to block quoting the rule, then remove the
 306:   planted rule. Dev-loop ordering applies and is part of the task:
 307:   the agent is not live until `plugin.json` bumps,
 308:   `claude plugin update parallax@parallax` runs, and the session restarts.
 309: - Preflight cost: `agy models` is a network round-trip per task dispatch.
 310:   Deliberate — per-task freshness over quota thrift; the wrapper is fresh
 311:   per task, so there is no cheaper session-scoped cache to use.
 312: - Plan debate + diff debate (Sol): after the ~Jul 29 reset. The branch does
 313:   not merge without the diff debate's terminal PASS.
 314: - Behavioral battery: deliberately not run. Honest framing: this cycle
 315:   edits `references/frozen-plan-format.md`, a skill reference file that NO
 316:   behavioral case declares in its surface (and `agents/*` appears in no
 317:   surface either) — so `--changed` selects nothing by design, and the gap
 318:   itself is declared here for the debate to contest, alongside whether an
 319:   `agents/`-surfaced behavioral case (Flash-fails → wrapper reports
 320:   blocked, never substitutes) is wanted this cycle or deferred.
 321: - State-machine suite: `tools/check-drift.ps1` untouched → skipped
 322:   (CLAUDE.md opt-in rule).
 323: 
 324: ### 7. Scope edges
 325: 
 326: Version 0.12.0. Doctor gains a minimal agy row — binary version + pinned
 327: model present in `agy models`, no generation probe — and follows doctor's
 328: existing single-source discipline: the row PARSES the canonical ID out of
 329: `agents/flash-implementer.md` under the installed copy's `installPath`,
 330: carrying no literal of its own (the check-4 pattern). Drift-watch: the
 331: drift snapshot gains the `agy --version` string as an informational row
 332: (the snapshot already tracks the codex version — declining drift coverage
 333: for the youngest dependency while naming it the top risk would be
 334: inconsistent); full agy flag-surface drift coverage remains a non-goal this
 335: cycle. Other non-goals: reviewer lane untouched; no CI changes;
 336: transcription tasks stay on `implementer.md`/haiku; agy's Claude/gpt-oss
 337: models unused; gemini-cli unused (uninstall + `GEMINI_API_KEY` removal are
 338: optional user cleanup, not cycle scope).
 339: 
 340: ## Risks
 341: 
 342: - **Server-side model substitution is undetectable today.** The route check
 343:   proves requested-and-propagated, not served — the same epistemic gap the
 344:   codex lane's header check has, handled with the same language discipline.
 345:   If the §2 probe finds response-side attribution, the gap closes; if not,
 346:   it is a NAMED residual risk. Mitigating context: the diff debate reads
 347:   the code itself regardless of who wrote it — substitution degrades
 348:   provenance, not the correctness gate.
 349: - **agy is young and Google moves fast**: v1.1.7, endpoint literally named
 350:   `daily-cloudcode-pa.googleapis.com`, predecessor killed with a month's
 351:   notice. Mitigations: loud preflight, pinned-contract tests that fail on
 352:   flag drift, the drift-snapshot version row, sonnet one dispatch away
 353:   behind the consent gate, and architecture A keeping the old lane intact.
 354: - **Free-tier quota opacity**: agy AI-credit limits are undocumented. A
 355:   mid-plan exhaustion surfaces as a loud dispatch failure → consent gate;
 356:   the partial-write listing (§1/§4) bounds the recovery cost. No headroom
 357:   probe exists; observability gap, not solved this cycle.
 358: - **Print-mode permission model is under-documented**: today's working
 359:   recipe is probe-verified, not doc-verified. The pinned-contract tests and
 360:   the §2 probe task are the defense; doc drift lands in drift triage.
 361: 
 362: ## Non-goals
 363: 
 364: - No reviewer-lane or debate-protocol changes; no eval-case changes (the
 365:   possible `agents/`-surfaced case is a declared debate question, not
 366:   committed scope).
 367: - No attempt to use agy's non-Gemini models anywhere.
 368: - No plugin runtime behavior change beyond the new agent (version bumps to
 369:   0.12.0 per release convention).

codex
Two record-level stale claims are already visible, and I’m widening the sweep because the spec still appears to contain at least one old log-only ROUTE sentence despite the amendment claim. I’m distinguishing contract defects from bookkeeping so the verdict reflects impact, not wording.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$paths=@('"'docs/superpowers/specs/2026-07-25-flash-implementer-design.md','docs/superpowers/plans/2026-07-25-flash-implementer.md','agents/flash-implementer.md','evals/multi-model-verify/test_flash_implementer.py'); "'$patterns=@('"'log/tree','log file''s path','log path','workdir allow','workdir-scoped','scratch-scoped','write_file.*workdir','three preflight','THREE preflight','round 2','round-2','round 3','round-3','pending Sol','Sol round-2 confirmation','round-2 files added'); foreach("'$p in $paths){ Select-String -Path $p -Pattern $patterns | ForEach-Object { '"'{0}:{1}:{2}' -f "'$p,$_.LineNumber,$_.Line } }' in C:\Users\Brandon\Documents\parallax
 succeeded in 462ms:
docs/superpowers/specs/2026-07-25-flash-implementer-design.md:150:  plus the retained log file's path
docs/superpowers/specs/2026-07-25-flash-implementer-design.md:186:  .gitignore dependency); each task's report carries its log path, and the
docs/superpowers/specs/2026-07-25-flash-implementer-design.md:215:     log path.
docs/superpowers/specs/2026-07-25-flash-implementer-design.md:303:  reachable-failure probe through the REAL agent — plant a scratch-scoped
docs/superpowers/plans/2026-07-25-flash-implementer.md:11:**STATUS: FROZEN** (re-frozen pending Sol round 2). Frozen 2026-07-25 by the backup-lane plan debate (Kimi K3-256k); REOPENED same day by the primary reviewer's check-off (Sol round 1, FIX — codex window reset early): five blocking findings, all session-verified and accepted; amendments folded into spec, this plan, and the Tasks 1-2 artifacts via an SDD fix wave. The diff debate gates the merge as always. Tasks 5-6 contain ATTENDED steps (user-interactive); Task 6 additionally requires the plugin dev-loop (bump, update, restart).
docs/superpowers/plans/2026-07-25-flash-implementer.md:86:    # (Sol check-off round 2, finding 3)
docs/superpowers/plans/2026-07-25-flash-implementer.md:117:    # pinned by exact sentence fragments (Sol check-off round 2,
docs/superpowers/plans/2026-07-25-flash-implementer.md:353:  retained log file's path AND the brain transcript's path
docs/superpowers/plans/2026-07-25-flash-implementer.md:639:dispatch (Sol check-off round 2, finding 1). Then an interactive trust
docs/superpowers/plans/2026-07-25-flash-implementer.md:647:`python hello.py` prints `hello flash`." plus a log path in the
docs/superpowers/plans/2026-07-25-flash-implementer.md:649:`gemini-3.6-flash-medium` + log path + brain transcript path; FILES
docs/superpowers/plans/2026-07-25-flash-implementer.md:677:functional permission (Sol check-off round 2, finding 4). Dispatch
docs/superpowers/plans/2026-07-25-flash-implementer.md:700:(`.superpowers/sdd/2026-07-25-flash-implementer/`), with log paths — the
docs/superpowers/plans/2026-07-25-flash-implementer.md:713:**Raw rounds:** docs/superpowers/plans/rounds/2026-07-25-flash-implementer/ (plan-round1-brief.md, plan-round{1,2}-transcript.txt — Kimi; sol-checkoff-round1-{brief,reply,header}.md/txt — Sol; round-2 files added at convergence)
docs/superpowers/plans/2026-07-25-flash-implementer.md:720:`git status` after round 2: sole untracked file was the session-authored
docs/superpowers/plans/2026-07-25-flash-implementer.md:747:| 19 | Stale cross-references (log/tree, workdir-scoped, log-only ROUTE, #13 for #14) | Sol r2 | accepted; synchronized | plan header, spec Decisions + section 6, resolved point 7 |
docs/superpowers/plans/2026-07-25-flash-implementer.md:765:findings verified, fixes committed 5391455, round-2 re-verification by
docs/superpowers/plans/2026-07-25-flash-implementer.md:767:Sol round-2 confirmation of these amendments (transcript retained under
agents/flash-implementer.md:108:  retained log file's path AND the brain transcript's path
evals/multi-model-verify/test_flash_implementer.py:46:    # (Sol check-off round 2, finding 3)
evals/multi-model-verify/test_flash_implementer.py:77:    # pinned by exact sentence fragments (Sol check-off round 2,

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'docs/superpowers/plans/2026-07-25-flash-implementer.md'; Select-String -Path "'$p -Pattern '"'"'^```'"' | ForEach-Object { '{0}:{1}' -f "'$_.LineNumber,$_.Line }' in C:\Users\Brandon\Documents\parallax
 succeeded in 419ms:
40:```python
207:```
221:```bash
224:```
240:````markdown
370:````
378:```markdown
383:```
390:```markdown
395:```
404:```bash
407:```
460:```bash
463:```
479:````markdown
496:````
509:```powershell
515:```
520:```powershell
524:```
528:```powershell
530:```
547:```bash
550:```
579:```powershell
581:```
614:```bash
617:```
663:```powershell
665:```

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$plan=Get-Content -LiteralPath '"'docs/superpowers/plans/2026-07-25-flash-implementer.md'; "'$testBlock=($plan[40..205] -join "`n")+"`n"; $testActual=((Get-Content -Raw -LiteralPath '"'evals/multi-model-verify/test_flash_implementer.py') -replace \""'`r`n","`n"); '"'TEST_BYTE_EQUAL=' + ("'$testBlock -ceq $testActual); $agentBlock=($plan[240..368] -join "`n")+"`n"; $agentActual=((Get-Content -Raw -LiteralPath '"'agents/flash-implementer.md') -replace \""'`r`n","`n"); '"'AGENT_BYTE_EQUAL=' + ("'$agentBlock -ceq $agentActual); $normalize={ param($s) (($s -split '"'\\s+') | Where-Object {"'$_}) -join '"' ' }; 'AGENT_WORD_EQUAL=' + ((&"'$normalize $agentBlock) -ceq (&$normalize $agentActual)); '"'PLAN_TEST_LINES=' + (("'$testBlock -split "`n").Count-1); '"'ACTUAL_TEST_LINES=' + (("'$testActual -split "`n").Count-1); '"'PLAN_AGENT_LINES=' + (("'$agentBlock -split "`n").Count-1); '"'ACTUAL_AGENT_LINES=' + (("'$agentActual -split "`n").Count-1)' in C:\Users\Brandon\Documents\parallax
 succeeded in 483ms:
TEST_BYTE_EQUAL=True
AGENT_BYTE_EQUAL=False
AGENT_WORD_EQUAL=True
PLAN_TEST_LINES=166
ACTUAL_TEST_LINES=166
PLAN_AGENT_LINES=129
ACTUAL_AGENT_LINES=124

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'skills/multi-model-verify/references/frozen-plan-format.md'; "'$lines=Get-Content -LiteralPath $p; for($i=30;$i -le [Math]::Min(85,$lines.Count);$i++){ '"'{0,4}: {1}' -f "'$i,$lines[$i-1] }; $d='"'docs/superpowers/plans/rounds/2026-07-25-flash-implementer'; 'RAW_FILES:'; Get-ChildItem -LiteralPath "'$d -File | Sort-Object Name | ForEach-Object { $_.Name }; '"'ROUND2_MATCHES:'; Get-ChildItem -LiteralPath "'$d -File | Where-Object Name -Match '"'sol-checkoff-round2' | ForEach-Object { "'$_.Name }' in C:\Users\Brandon\Documents\parallax
 succeeded in 499ms:
  30:   `References/<name>/` at the fidelity the debate agreed on (faithful core
  31:   logic; project-specific deviations listed one per line WITH the debate
  32:   round that approved each).
  33: - Platform/API constraints the debate verified, each with its citation
  34:   (KE example: WoW 12.0 secret-value rules).
  35: 
  36: ## Debate record appendix (REQUIRED)
  37: 
  38: Every frozen plan ends with:
  39: 
  40: ```markdown
  41: ---
  42: 
  43: ## Debate record
  44: 
  45: **Participants:** <session model> (session) / <reviewer model> (codex exec, session <id>)
  46: **Rounds used:** N of CAP
  47: **Outcome:** converged | converged with amendments | escalated
  48: **Verification status:** FULL | DEGRADED
  49: **Degradation:** none | codex-missing | model-rejected | auth-expired | quota-exhausted | continuity-lost | <class>
  50: **Authorized by:** n/a | user at round N | not-authorized
  51: **Raw rounds:** <paths to the verbatim round replies/transcripts> | not retained
  52: 
  53: ### Resolved points
  54: | # | Claim | Raised by | Outcome | Evidence |
  55: |---|-------|-----------|---------|----------|
  56: | 1 | ...   | reviewer  | accepted into Task 3 | References/<addon>/<file>:<line> |
  57: | 2 | ...   | session   | refuted  | .wow-api-reference/<file> |
  58: | 3 | ...   | reviewer  | struck (no citation) | — |
  59: 
  60: ### Escalated points (user-decided)
  61: | # | Question | Session position | Reviewer position | Owner's call |
  62: |---|----------|------------------|-------------------|--------------|
  63: 
  64: ### Degraded-mode note
  65: (required whenever Verification status is DEGRADED — name what was skipped,
  66: why, and the recorded user authorization. The Participants line must name
  67: the actual participants, never the placeholder template line.)
  68: ```
  69: 
  70: The three status fields are structured on purpose: mode `diff` parses
  71: `Verification status` to enforce the degraded-plan poisoning rule (see
  72: SKILL.md) — prose notes cannot be enforced. `Raw rounds` records where the
  73: verbatim reviewer replies live (scratchpad transcripts are temporary — if
  74: they were not copied somewhere durable, say `not retained`): the summary
  75: tables above are the adjudication, not the provenance, and a later dispute
  76: about what the reviewer actually said needs the raw text or an honest
  77: "gone".
  78: 
  79: The appendix is the audit trail: mode `diff` re-reads it to know which
  80: deviations were approved and which claims were struck. A frozen plan without
  81: the appendix was not verified — treat it as an unfrozen draft.
  82: 
  83: ## Freezing
  84: 
  85: A plan is frozen when: both sides verdict PASS — or the final round's FIXes
RAW_FILES:
clone-status-after-round2.txt
plan-round1-brief.md
plan-round1-transcript.txt
plan-round2-transcript.txt
sol-checkoff-round1-brief.md
sol-checkoff-round1-header.txt
sol-checkoff-round1-reply.md
ROUND2_MATCHES:

codex
The round-2 mechanics themselves are present, but one amendment introduced an execution deadlock: Task 5 edits the version before demanding an empty tree and only commits afterward. I’m checking the remaining static oracles now; the external Python pass counts remain unavailable here.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$ErrorActionPreference='"'Stop'; "'$flash=Get-Content -Raw -LiteralPath '"'agents/flash-implementer.md'; "'$classic=Get-Content -Raw -LiteralPath '"'agents/implementer.md'; "'$test=Get-Content -Raw -LiteralPath '"'evals/multi-model-verify/test_flash_implementer.py'; "'$tests=[regex]::Matches($test,'"'(?m)"'^def test_'"').Count; \"TEST_FUNCTIONS="'$tests"; $start='"'<"'!-- shared-contract:start -->'"'; "'$end='"'<"'!-- shared-contract:end -->'"'; function Shared([string]"'$s){ $a=$s.IndexOf($start); $b=$s.IndexOf($end); return $s.Substring($a+$start.Length,$b-($a+$start.Length)) }; '"'SHARED_BLOCK_EQUAL=' + ((Shared "'$flash) -ceq (Shared $classic)); $pins=@("the dispatch log file'"'s basename\",'on success, failure, and interruption alike','any "'`write_file(` entry, whatever path it names'"','No file matching "'`AGY-TASK-BRIEF-*`'"',\"AND the brain transcript's path\",'transcript_full.jsonl','conversationID'); foreach("'$pin in $pins){ "PIN[{0}]={1}" -f $pin,$flash.Contains($pin) }; $offenders=@(); $patterns=@('"'skills\\*.md','commands\\*.md','tools\\*.ps1','hooks\\*','evals\\*.py','evals\\*.json','evals\\*.ps1','README.md','CLAUDE.md','agents\\*.md'); "'$allowed=@((Resolve-Path '"'agents/flash-implementer.md').Path,(Resolve-Path 'agents/implementer.md').Path,(Resolve-Path 'evals/multi-model-verify/test_flash_implementer.py').Path); foreach("'$pattern in $patterns){ Get-ChildItem -Path $pattern -File -Recurse -ErrorAction SilentlyContinue | ForEach-Object { if($allowed -notcontains $_.FullName){ if((Get-Content -Raw -LiteralPath $_.FullName -ErrorAction SilentlyContinue) -match '"'gemini-3\\.6-flash'){ "'$offenders += $_.FullName } } } }; '"'LITERAL_OFFENDERS=' + (("'$offenders | Sort-Object -Unique) -join '"','); git diff --check 3b5074f..HEAD; 'DIFF_CHECK_EXIT=' + "'$LASTEXITCODE; '"'STATUS:'; git status --short --branch" in C:\Users\Brandon\Documents\parallax
 succeeded in 624ms:
TEST_FUNCTIONS=11
SHARED_BLOCK_EQUAL=True
PIN[the dispatch log file's basename]=True
PIN[on success, failure, and interruption alike]=True
PIN[any `write_file(` entry, whatever path it names]=True
PIN[No file matching `AGY-TASK-BRIEF-*`]=True
PIN[AND the brain transcript's path]=True
PIN[transcript_full.jsonl]=True
PIN[conversationID]=True
LITERAL_OFFENDERS=
DIFF_CHECK_EXIT=0
STATUS:
## feat/0120-flash-implementer

2026-07-25T20:32:58.782003Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_reasoning_summaries` at line 86 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'docs/superpowers/plans/2026-07-25-flash-implementer.md'; "'$lines=Get-Content -LiteralPath $p; for($i=1;$i -le 28;$i++){ '"'{0,4}: {1}' -f "'$i,$lines[$i-1] }; $s='"'docs/superpowers/specs/2026-07-25-flash-implementer-design.md'; "'$sl=Get-Content -LiteralPath $s; foreach($r in @(@(144,156),@(296,308))){ for($i=$r[0];$i -le $r[1];$i++){ '"'{0,4}: {1}' -f "'$i,$sl[$i-1] } }' in C:\Users\Brandon\Documents\parallax
 succeeded in 434ms:
   1: # Flash Implementer Lane Implementation Plan
   2: 
   3: > **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
   4: 
   5: **Goal:** Add `agents/flash-implementer.md`, a haiku-wrapper SDD implementer that delegates all code-writing to Gemini 3.6 Flash via the Antigravity CLI (agy) headlessly, with mechanically enforced route and authorship checks.
   6: 
   7: **Architecture:** Architecture A from the spec (`docs/superpowers/specs/2026-07-25-flash-implementer-design.md`): a new wrapper agent beside the untouched `implementer.md`. The wrapper never types repo code (tools allowlist without Edit/Write + transcript/tree corroboration); every failure is loud (`STATUS: blocked`); reroute is consent-gated. Model literal single-sourced to the two agent files.
   8: 
   9: **Tech Stack:** Claude Code agent frontmatter, agy 1.1.7 print mode, pytest (offline contract pins), PowerShell (drift snapshot).
  10: 
  11: **STATUS: FROZEN** (re-frozen pending Sol round 2). Frozen 2026-07-25 by the backup-lane plan debate (Kimi K3-256k); REOPENED same day by the primary reviewer's check-off (Sol round 1, FIX — codex window reset early): five blocking findings, all session-verified and accepted; amendments folded into spec, this plan, and the Tasks 1-2 artifacts via an SDD fix wave. The diff debate gates the merge as always. Tasks 5-6 contain ATTENDED steps (user-interactive); Task 6 additionally requires the plugin dev-loop (bump, update, restart).
  12: 
  13: ## Global Constraints
  14: 
  15: - The canonical implementer-lane literal is the RESOLVED ID `gemini-3.6-flash-medium`; after this cycle it appears ONLY in `agents/implementer.md` and `agents/flash-implementer.md` (spec section 5).
  16: - The wrapper's tools allowlist is exactly Read, Grep, Glob, Bash — no Edit, no Write, no NotebookEdit (spec section 1).
  17: - Route language is "requested and propagated", never "used and confirmed" (spec section 2; doctor check-4 discipline).
  18: - Loud failure only: any preflight, dispatch, route-check, or corroboration failure is `STATUS: blocked` with quoted evidence; reroute to a Claude tier is a user decision recorded under the frozen plan's Escalated points (spec section 4).
  19: - The Flash lane runs in the MAIN CHECKOUT ONLY this cycle (spec Decisions).
  20: - Probed facts this plan treats as fixed (2026-07-25, agy 1.1.7): stdin does NOT reach the model in print mode; a workspace file named in a short `-p` pointer is read reliably (probed to 7,152 bytes); default-verbosity logs carry NO response-side model attribution (client-side `Print mode: starting` / `Propagating selected model override` lines only).
  21: - All new test code and evals content is pure ASCII.
  22: - Commits: lowercase imperative, prefixed `0.12.0:`, no AI-attribution trailers.
  23: - This cycle's tasks are built by the CURRENT implementer lanes (sonnet/haiku) — the Flash lane cannot build itself.
  24: - Frozen after debate: changes require reopening the debate.
  25: 
  26: ---
  27: 
  28: ### Task 1: Contract tests (all failing first)
 144: Same input contract as `implementer.md` (ONE task's verbatim text + the
 145: plan's Global Constraints), same INPUT GAP rule. Report format gains one
 146: lane-specific line:
 147: 
 148: - **STATUS:** done | blocked | INPUT GAP: <what is missing>
 149: - **ROUTE:** resolved model ID as requested-and-propagated, from the log,
 150:   plus the retained log file's path
 151: - **FILES CHANGED:** actual paths from `git status` — on `blocked` this
 152:   MUST still list every path Flash already touched, so the session can
 153:   revert a partial write
 154: - **VERIFICATION:** each command run by the wrapper itself, with real output
 155: - **DEVIATIONS:** must be "none" or an explanation of why the task could
 156:   not be built as written
 296: - Live verification through the REAL agent happens during implementation,
 297:   AFTER the plan debate: one end-to-end dry-run task in a scratch repo
 298:   (trusted for the occasion), plus TWO failure probes (split by Sol
 299:   check-off F5 — the earlier single disjunctive probe was vacuous: a
 300:   wrapper refusing the contradictory brief would "pass" without ever
 301:   exercising the failure path): (a) a RAW agy probe, no agent, proving
 302:   the CLI's loud invalid-model rejection with no writes; (b) a
 303:   reachable-failure probe through the REAL agent — plant a scratch-scoped
 304:   `write_file` allow rule in agy settings, dispatch a trivial task,
 305:   require preflight check 3 to block quoting the rule, then remove the
 306:   planted rule. Dev-loop ordering applies and is part of the task:
 307:   the agent is not live until `plugin.json` bumps,
 308:   `claude plugin update parallax@parallax` runs, and the session restarts.

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$names=@('"'python','python3','py','uv'); foreach("'$n in $names){ $c=Get-Command $n -ErrorAction SilentlyContinue; if($c){ "$n=$($c.Source)" } else { "$n=MISSING" } }; Get-ChildItem -Force -Directory | Where-Object { $_.Name -match '"'"'^'"\\.venv"'$|''^venv$'"' } | ForEach-Object { 'VENV=' + "'$_.FullName }; git show --format=fuller --no-patch 96a8224; git show --format=fuller --no-patch 7cae001' in C:\Users\Brandon\Documents\parallax
 succeeded in 1070ms:
python=MISSING
python3=MISSING
py=MISSING
uv=MISSING
commit 96a8224c47d0a4c4dad82aa196bdb2a7843970bb
Author:     Brandon Wascher <bmwascher97@gmail.com>
AuthorDate: Sat Jul 25 15:22:52 2026 -0500
Commit:     Brandon Wascher <bmwascher97@gmail.com>
CommitDate: Sat Jul 25 15:22:52 2026 -0500

    0.12.0: fold sol check-off round-2 amendments into spec and plan
commit 7cae0013910052ac1330e03951658a31cff6b3d4
Author:     Brandon Wascher <bmwascher97@gmail.com>
AuthorDate: Sat Jul 25 15:27:17 2026 -0500
Commit:     Brandon Wascher <bmwascher97@gmail.com>
CommitDate: Sat Jul 25 15:27:17 2026 -0500

    0.12.0: strengthen contract pins per sol round-2

Round-3 amendment range and current tree reviewed. All five intended mechanics are present, but execution and record-integrity blockers remain.
  ✓ Inspect round-3 commits and current evidence
  ✓ Reverify all five prior findings against amended files
  ✓ Check for regressions and issue final check-off verdict
codex
1. **Blocking — [plan:599](C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-flash-implementer.md:599).** Task 5 edits `plugin.json`, then requires porcelain-empty before committing it ([lines 603–617](C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-flash-implementer.md:603)). Check 4 therefore cannot pass. **Fix:** commit the version bump before running the five-check dry-run, then require the post-commit tree to be empty.

2. **Blocking — [plan:19](C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-flash-implementer.md:19).** The absolute “MAIN CHECKOUT ONLY” constraint conflicts with Task 6’s real-agent dispatch in a scratch repo ([lines 634–645](C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-flash-implementer.md:634)) and the shipped agent’s identical restriction ([agent:38](C:/Users/Brandon/Documents/parallax/agents/flash-implementer.md:38), [agent:123](C:/Users/Brandon/Documents/parallax/agents/flash-implementer.md:123)). A zero-judgment agent cannot obey both. **Fix:** declare Task 6’s trusted scratch repo as the sole live-verification exception in the spec, Global Constraints, and agent body.

3. **Blocking — [spec:148](C:/Users/Brandon/Documents/parallax/docs/superpowers/specs/2026-07-25-flash-implementer-design.md:148).** The approved spec still defines ROUTE as carrying only the log path, unlike the amended agent’s log-plus-transcript contract ([agent:106](C:/Users/Brandon/Documents/parallax/agents/flash-implementer.md:106)). Its verification section also retains the obsolete “scratch-scoped” functional rule/removal recipe ([spec:296](C:/Users/Brandon/Documents/parallax/docs/superpowers/specs/2026-07-25-flash-implementer-design.md:296)) instead of Task 6’s nonmatching sentinel and exact-content restoration ([plan:670](C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-flash-implementer.md:670)). **Fix:** synchronize both spec passages to the frozen plan.

4. **Blocking — [plan:705](C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-flash-implementer.md:705).** The debate record still says “pending Sol round 2,” claims terminal Sol round-2 confirmation, and promises round-2 raw files ([lines 708–713](C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-flash-implementer.md:708), [lines 757–768](C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-flash-implementer.md:757)); yet its own rows 15–19 record round 2 as FIX, and the rounds directory contains no `sol-checkoff-round2-*` artifacts. This violates the raw-provenance requirement ([frozen-plan-format:51](C:/Users/Brandon/Documents/parallax/skills/multi-model-verify/references/frozen-plan-format.md:51), [frozen-plan-format:72](C:/Users/Brandon/Documents/parallax/skills/multi-model-verify/references/frozen-plan-format.md:72)). **Fix:** retain round-2 artifacts, record round 2 as FIX, update the active-round metadata, and retain this round’s artifacts before asserting convergence.

**FIX.** The five advertised amendment mechanics are present; the test block is byte-identical to the plan, the agent body is word-identical apart from disclosed wrapping, all 11 static contract pins were confirmed manually, the literal sweep is clean, `git diff --check` passes, and the worktree is clean. The reported `11/11` and `144 passed, 1 skipped` remain **UNVERIFIED** because this sandbox has no Python interpreter. Check-off is withheld for the four blockers above; the later merge-diff debate remains a separate gate.
tokens used
385,024
1. **Blocking — [plan:599](C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-flash-implementer.md:599).** Task 5 edits `plugin.json`, then requires porcelain-empty before committing it ([lines 603–617](C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-flash-implementer.md:603)). Check 4 therefore cannot pass. **Fix:** commit the version bump before running the five-check dry-run, then require the post-commit tree to be empty.

2. **Blocking — [plan:19](C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-flash-implementer.md:19).** The absolute “MAIN CHECKOUT ONLY” constraint conflicts with Task 6’s real-agent dispatch in a scratch repo ([lines 634–645](C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-flash-implementer.md:634)) and the shipped agent’s identical restriction ([agent:38](C:/Users/Brandon/Documents/parallax/agents/flash-implementer.md:38), [agent:123](C:/Users/Brandon/Documents/parallax/agents/flash-implementer.md:123)). A zero-judgment agent cannot obey both. **Fix:** declare Task 6’s trusted scratch repo as the sole live-verification exception in the spec, Global Constraints, and agent body.

3. **Blocking — [spec:148](C:/Users/Brandon/Documents/parallax/docs/superpowers/specs/2026-07-25-flash-implementer-design.md:148).** The approved spec still defines ROUTE as carrying only the log path, unlike the amended agent’s log-plus-transcript contract ([agent:106](C:/Users/Brandon/Documents/parallax/agents/flash-implementer.md:106)). Its verification section also retains the obsolete “scratch-scoped” functional rule/removal recipe ([spec:296](C:/Users/Brandon/Documents/parallax/docs/superpowers/specs/2026-07-25-flash-implementer-design.md:296)) instead of Task 6’s nonmatching sentinel and exact-content restoration ([plan:670](C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-flash-implementer.md:670)). **Fix:** synchronize both spec passages to the frozen plan.

4. **Blocking — [plan:705](C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-flash-implementer.md:705).** The debate record still says “pending Sol round 2,” claims terminal Sol round-2 confirmation, and promises round-2 raw files ([lines 708–713](C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-flash-implementer.md:708), [lines 757–768](C:/Users/Brandon/Documents/parallax/docs/superpowers/plans/2026-07-25-flash-implementer.md:757)); yet its own rows 15–19 record round 2 as FIX, and the rounds directory contains no `sol-checkoff-round2-*` artifacts. This violates the raw-provenance requirement ([frozen-plan-format:51](C:/Users/Brandon/Documents/parallax/skills/multi-model-verify/references/frozen-plan-format.md:51), [frozen-plan-format:72](C:/Users/Brandon/Documents/parallax/skills/multi-model-verify/references/frozen-plan-format.md:72)). **Fix:** retain round-2 artifacts, record round 2 as FIX, update the active-round metadata, and retain this round’s artifacts before asserting convergence.

**FIX.** The five advertised amendment mechanics are present; the test block is byte-identical to the plan, the agent body is word-identical apart from disclosed wrapping, all 11 static contract pins were confirmed manually, the literal sweep is clean, `git diff --check` passes, and the worktree is clean. The reported `11/11` and `144 passed, 1 skipped` remain **UNVERIFIED** because this sandbox has no Python interpreter. Check-off is withheld for the four blockers above; the later merge-diff debate remains a separate gate.
