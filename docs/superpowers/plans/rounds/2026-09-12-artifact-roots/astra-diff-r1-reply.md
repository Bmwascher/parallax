1. The eight declaration lines match Task 1 Step 5 exactly. The whole-region pin, registration, and label-count parser are present; missing or duplicate labels reach `Fail`, with no row defaults. Evidence: `skills/multi-model-verify/references/model-prompting-notes.md:783`, `docs/superpowers/plans/2026-09-12-artifact-roots.md:173`, `evals/multi-model-verify/test_artifact_roots.py:53`, `evals/multi-model-verify/test_contract_coverage.py:782`, `tools/artifact-roots.ps1:100`. **PASS.**

2. Amendment A correctly applies the spec. The guard strips the drive prefix and rejects the forbidden characters before resolving the assertion target; both regression inputs are present. I confirmed that the unsubstituted placeholder exits 2 with `ERROR:` on both hosts. The ledger authorizes this correction against the spec’s host-consistent path-error requirement. Evidence: `tools/artifact-roots.ps1:253`, `evals/multi-model-verify/test_artifact_roots.py:246`, `.superpowers/sdd/2026-09-12-artifact-roots/progress.md:29`, `docs/superpowers/specs/2026-09-12-artifact-roots-design.md:195`. The frozen tool text contains the defect this amendment repairs. **PASS.**

3. Amendment B correctly implements the amended spec. `-Expect` validates its argument and requires `-Assert`; membership mismatches name both roots and exit 1. The operating rule requires the intended root for both saves, and explicitly states the remaining frozen-plan-parent limitation. Evidence: `tools/artifact-roots.ps1:38`, `tools/artifact-roots.ps1:273`, `evals/multi-model-verify/test_artifact_roots.py:324`, `docs/superpowers/specs/2026-09-12-artifact-roots-design.md:217`, `skills/multi-model-verify/references/model-prompting-notes.md:833`. **PASS.**

4. The universal exit contract fails. With `TEMP=C:\bad|temp`, the shipped resolver exits **1 on PowerShell 5.1**, with an uncaught `GetFullPath` exception, and **0 on PowerShell 7**, printing `C:/bad|temp/<short-name>/`. The unguarded conversion is at `tools/artifact-roots.ps1:197`; further path conversions bypass `Resolve-Absolute` at `:149`, `:157`, and `:214`. The declaration’s `ReadAllText` also lacks exception handling at `:91`.

   Missing `-RepoRoot`, a missing `-DocsRoot` value, and an unknown parameter each exited **1 on both hosts**, without the required `ERROR:` output. Parameter binding at `tools/artifact-roots.ps1:23` causes these exits; the header’s exception at `:20` conflicts with the spec’s unconditional parameter-fault map at `docs/superpowers/specs/2026-09-12-artifact-roots-design.md:225`.

   Native-call handling, relative common-directory resolution, and ASCII encoding check out (`tools/artifact-roots.ps1:136`). **FIX:** validate TEMP and declaration-derived parents consistently, route path/read failures through `Fail`, and handle parameter faults through an entry point that can produce the specified exit and prefix. Add dual-host regressions for these cases.

5. The six expressions, restricted declaration exemption, controls, and printed search descriptions are present; my independent sweep found zero matches (`evals/multi-model-verify/test_artifact_roots.py:409`, `:420`, `:439`, `:476`). However, the claimed slashless-limit comment is absent from the sweep commentary at `:430`.

   Other missed forms include `.superpowers/sdd` without its trailing separator, and `<git-common-dir>/parallax/application-checkpoints/...`; neither matches the expressions at `:439`. The latter occurs in an actual writing instruction at `skills/multi-model-verify/references/application-checkpoint.md:75`. Name that existing instruction as a follow-up outside the certification unit. **FIX:** document the omitted spelling limits in the certified test’s sweep comment and correct the review record’s assertion that this documentation already exists.

6. The test implements the described checkpoint setup, writer calls, exact two-path set, resolver checks, final snapshot, and negative controls (`evals/multi-model-verify/test_artifact_roots.py:584`, `:591`, `:602`, `:610`, `:625`, `:629`). Its endpoint limitation is explicit at `:541`.

   This nevertheless drops required frozen-plan coverage: Task 4 expects the emitter to create `.git/parallax` and includes that directory in the observed set (`docs/superpowers/plans/2026-09-12-artifact-roots.md:1188`). The binding spec also requires observing both created directories (`docs/superpowers/specs/2026-09-12-artifact-roots-design.md:339`). Pre-creating the checkpoint removes that observation. The prior adjudication acknowledges the trade at `docs/superpowers/plans/rounds/2026-09-12-artifact-roots/README.md:263`. **FIX:** restore the original fresh-parent writer case and retain checkpoint binding as an additional case.

7. The three SKILL edits match the plan, and the citation swaps resolve under the coverage rule (`skills/multi-model-verify/SKILL.md:153`, `:323`, `:389`; `docs/superpowers/plans/2026-09-12-artifact-roots.md:910`; `evals/multi-model-verify/test_contract_coverage.py:842`).

   The stated character count is incorrect. Applying the linter’s frontmatter removal and line joining produces **25,985 characters**, or **6,496 estimated tokens**, below the ceiling (`evals/tools/skill_lint.py:170`, `:182`, `:134`). **FIX, record only:** correct 25,986 to 25,985 and update the shifted SKILL line citations.

8. The retained-record locations, ledger location, default-root output, backlog amendment, and documented mirror-location difference support the claim within its review-artifact scope. Evidence: `docs/superpowers/plans/rounds/2026-09-12-artifact-roots/artifact-roots.txt:4`, `.superpowers/sdd/2026-09-12-artifact-roots/progress.md:1`, `.superpowers/sdd/.gitignore:1`, `BACKLOG.md:115`, `docs/superpowers/plans/rounds/2026-09-12-artifact-roots/README.md:237`. The ledger is ignored by the shared SDD `.gitignore`. **PASS.**

**UNVERIFIED:** fresh execution of Group 3b and the Python suites; the available Python executable could not launch. The writer conclusions above are source findings (`evals/multi-model-verify/test_artifact_roots.py:564`). An unreadable-declaration exception was not reproduced dynamically; the missing handler is visible at `tools/artifact-roots.ps1:91`.

**File-content check:** no file instruction caused a pause. Two explicit spec requirements drove the contested findings: “Every path the tool resolves goes through one helper” (`docs/superpowers/specs/2026-09-12-artifact-roots-design.md:195`) and “plus the two directories the emitter creates for it” (`:340`). My interpretations require consistent handling of TEMP conversions and observation of the shared parent’s creation, respectively.

**Range verdict: FIX**, principally for the demonstrated host divergence and omitted required writer coverage in claims 4 and 6.