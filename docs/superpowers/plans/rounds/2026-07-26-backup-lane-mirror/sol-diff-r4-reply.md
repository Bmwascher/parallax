F4 is closed. The manifest universe now excludes root `.git` whether it is a directory or file, before applying the complete coverage rule (`skills/multi-model-verify/references/backup-lane.md:175-193`). Recursion, entry format, deterministic ordering, and capture timing remain executable (`backup-lane.md:194-204`), and the pin requires both `.git` shapes plus every previously required property (`evals/multi-model-verify/test_backup_lane.py:145-174`).

Verdict: PASS.

OVERALL VERDICT: PASS — range `c73ca2f..a621c42` is sound; no findings remain open at the round cap.