All verification complete. Here is the review.

---

**Claim 1 — Coverage mapping. PASS (with one noted partial-coverage observation).**
Every task line reference is exact: Task 1 at plan line 54, Task 2 at 253, Task 3 at 456, Task 4 at 574, Task 5 at 715, Task 6 at 993, Task 7 at 1474, Task 8 at 1525. Section-by-section: §3 → plan lines 265-319 (fable-reviewer.md: frozen plan/ledger/diff-package inputs, Strengths/Issues/triage/verdict report); §5 → plan lines 374-437; §4's pinned sentence → plan line 936; §6 → plan lines 471-557; §7 → plan lines 601-621; §8 notes → plan lines 757-833 (Fable 5 subsection 783-807, Opus 5 subsection 809-832 covering all seven enumerated points), record format → plan lines 659-678, backup brief bullet → plan lines 863-878; §9 routing → plan lines 894-955 plus the CLAUDE.md fix at 1435-1451 (both "private" instances confirmed at CLAUDE.md:39 and README.md:188); §10 → plan lines 1008-1433 (no Quickstart section — §14 respected); §11 → plan lines 69-220; §12 → plan lines 1492-1507 (five expectations covering invariant, subject revision, blind relay, per-lane evidence, record); §13 → Task 8 (bump 1536-1540, dev loop 1542-1549, dogfood 1562-1574, two-round smoke 1576-1612) plus ship flow at 1627-1630. §14: attestation schema explicitly unchanged (plan lines 672-673), no `codex exec --json`, no driver automation, no quickstart. Observation (not verdict-changing): spec §8 (spec line 232-233) lists "the no-unrequested-refactor snippet" among the notes re-distillation's NEW points, but Task 5's notes rewrite (plan lines 783-807) carries only the other four; the substance lives instead in escalation-implementer.md (plan lines 401-403: "No improvements, no drive-by refactors, no scope adjustments"). The discipline has a plan home and §11 pins no notes bullet for it, so coverage stands — flagging for the debate's awareness only.

**Claim 2 — TDD wiring. PASS.**
Byte-identity of every pinned string verified against the embedded artifacts: agents (plan 272/308 "raw reply is retained as a range-bound artifact", 272 "never replaces the cross-vendor gate", 328 "dispatch metadata" as a single physical line, 345 "the resume surface carries no model parameter", 344 "probed 2026-07-26", 352 "cite the subject revision", 381/390 "enumerated decision envelope", 433 "DEVIATIONS - must be `none`", 416 "only with user consent"); SKILL sentence (plan 936 == test lines 137-141 == spec line 72, byte-exact); panel pointer inserted twice (plan 911, 954) for `count == 2`; carve-out clause (plan 946); panels pins (plan 483, 485, 516 — each single-line); backup-lane paragraph (plan 695 == test 165-170); fallbacks pins (plan 602, 608, 615); format pins (plan 638, 666); notes headings (plan 758, 783, 809, 805). Arithmetic: static count gives 155 current pytest tests (test_multi_model_verify.py:107, test_attestation.py:27, test_backup_lane.py:10, test_flash_implementer.py:11 `def test_` occurrences) = 154 passed + 1 skipped baseline; the embedded file adds exactly 10 tests (plan lines 95-208), and REQUIRED_REFERENCE_FILES (test_multi_model_verify.py:27-34, ending in "backup-lane.md") gains panels.md, flipping test_reference_files_exist (line 84) and test_no_backslash_paths_anywhere (line 88) RED — 12 failed, 152 passed, 1 skipped (plan line 243) and 164 passed, 1 skipped (plan lines 1463, 1514) are consistent.

**Claim 3 — Containment class. PASS.**
Both reviewer agents carry `model: fable` and `tools: Read, Grep, Glob` in frontmatter (plan lines 273-274, 329-330) with no Bash and no write tools; the tests pin exactly this (plan lines 100-104: `assert "model: fable" in fm`, `assert "tools: Read, Grep, Glob" in fm`, `assert "Bash" not in fm`) — allowlist, not prose. Escalation-implementer correctly carries no tools line (full toolset, spec §5; plan line 382). Matches spec §3 (spec lines 39-45) and §6 (spec lines 152-153).

**Claim 4 — Required review sentence + routing. PASS.**
Single physical line at plan line 936, byte-identical to spec line 72, counted `== 1` (plan line 142); it names the retained range-bound artifact and the session's per-finding adjudications, and the surrounding inserted prose (plan 937-940) plus Task 8 step 4 (plan 1570-1573) require the round-1 brief to cite artifact + adjudications in the accept/refute/ESCALATE vocabulary (not "dispositioned", per spec lines 64-67).

**Claim 5 — Panels invariant. PASS.**
panels.md: ≥2 members (plan 474 "MORE than one reviewer lane"), cross-vendor invariant and all-Claude invalidity (485), exact composition enumeration (483), subject-revision rule — diff SHAs, plan-mode SHA-256 of the round's claims bytes with blob hash at freeze (509-514), terminal-verdict FINAL-revision binding (516) — all matching spec §6 (spec lines 122-143).

**Claim 6 — Lane-loss consent-first. PASS.**
Plan line 608 pins the consent-gate stop; line 609-611 the three gate options (continue/substitute/abort); lines 611-617 the remainder split — surviving cross-vendor may record FULL, Fable-only records DEGRADED with the poisoning rule; lines 617-620 OPEN carry-over of unresolved findings and the never-adjudicate-incomplete-round rule. Matches spec §7 (spec lines 172-193).

**Claim 7 — Attestation mapping. PASS.**
Plan lines 666-673: strictest-lane FULL condition pinned verbatim; `-Rounds` max lane count, `-Participants` driver + every lane, `-RouteNote` literal `effective route confirmed` only under the strictest-lane rule, "emitter and verifier schemas are unchanged" — matching spec §8 (spec lines 265-274) and the §14 deferral.

**Claim 8 — Decision envelope carve-out. PASS.**
Envelope enumeration and bounds (plan 394-408), DECISIONS one-entry-per-point (430-432), `DEVIATIONS - must be `none`` retained (433-435), plan-time and consent-gated reroute entries with SDD-ledger recording and fail-closed (412-418); carve-out sentence pinned in frozen-plan-format.md (638, test at 188-190) and in SKILL.md's spec-fidelity clause (946-948, test at 145-146). Matches spec §5 (spec lines 85-116).

**Claim 9 — Backup-lane panel participation. PASS.**
One paragraph, one physical line (plan 695), declaring the invocation itself the consent, containment/per-round evidence/write-probe unchanged, no failure class recorded — matching spec §6 (spec lines 148-151); panels.md cross-references it (plan 525-526).

**Claim 10 — Insertion safety. FIX.**
Anchor uniqueness verified by grep for every anchored edit: fallbacks.md:160 (unique), frozen-plan-format.md:3 and :85 (unique), backup-lane.md:11 (unique), model-prompting-notes.md:3-18/74/222 (unique), SKILL.md:20/102(with-indent)/142-150 (unique), CLAUDE.md:37-39 (unique). Existing pins undisturbed: the backup-lane pointer survives at SKILL.md:102 and in the mode-diff replacement (plan 952) keeping `count == 2` (test_backup_lane.py:139-141); both README pins carried byte-exact (plan 1099, 1130 vs. current README.md:80, 107; Global Constraints plan 34-37); `Canonical model id:` (notes.md:31) stays ahead of `Canonical backup reviewer model id:` (notes.md:218), untouched by all three notes edits. Two accuracy notes, one of them the FIX: (a) the claim's word "nine" matches no counting convention — there are 10 anchored edits across the five named files (4 insertions in Task 4; 2 replaces + 1 insertion in notes; 2 insertions + 1 replace in SKILL.md), 7 if only pure insertions count; the substance (every anchor unique) holds regardless. (b) **The defect:** Task 5 Step 1's extent parenthetical (plan lines 730-732) says the replaced block runs "through the end of the third bullet, `  mandatory; same-context self-critique rubber-stamps (see` / `  model-prompting-notes.md).`" — but that quoted text does not exist in model-prompting-notes.md; it lives in fallbacks.md:60-61 (grep-confirmed: the string occurs nowhere in notes.md). The actual third bullet ends at model-prompting-notes.md:18 ("  skeptic pass in a FRESH subagent, not inline."). The operative REPLACE block (plan 737-752) and the "i.e. everything before the blank line that precedes `## The reviewer lane...`" clause are both correct, so the edit is still executable — but a zero-judgment implementer cross-checking the parenthetical against the file will find a contradiction and hit a false INPUT-GAP stop. Specific fix: rewrite plan lines 730-732 to quote the true ending ("...must run the / `  skeptic pass in a FRESH subagent, not inline.`") or delete the misquoted parenthetical, keeping the exact block and the "i.e." clause.

**Claim 11 — Single-source discipline. PASS.**
New agents pin `model: fable` in frontmatter only (plan 273, 329, 382); no new `Canonical ... id` declarations; no `-m <literal>` anywhere added — the SKILL.md transport commands retain `<canonical-model-id>` placeholders (SKILL.md:93, 120, untouched), the Task 8 smoke uses no literal (plan 1589-1599), and the new README names "GPT-5.6 Sol"/"Kimi K3" descriptively without the swept literals `gpt-5.6-sol`/`kimi-code/k3-256k` (plan 1035-1036) that test_reviewer_id_has_single_source (test_multi_model_verify.py:226-260) and test_backup_literal_single_source (test_backup_lane.py:160-169) forbid outside notes.md.

**Claim 12 — Execution safety. PASS.**
Strict order 1→8 declared (plan 1623); RED window Tasks 1-6 with per-task flip expectations (1625-1626, 236-238, 442-445, 562-564, 960-961); long suites controller-run (46-48); fresh implementer per task via the mandated subagent-driven-development sub-skill (plan 3) with Tasks 2-7 unattended and Task 8 attended (1624); Task 8's two live verifications — dogfooding the required fable review on this branch (1562-1574) and the two-round Sol+Fable smoke exercising the agent-file resume variant the probe defers (1591-1602) — cover exactly the two surfaces the probe record left open (spec lines 359-361, 412-414, 362-368).

**UNVERIFIED:** (1) The literal pytest counts (154+1 baseline, 12/152/1 RED, 164/1 final) rest on a static `def test_` count of 155 and one environmental skip (test_run_state_machine, test_multi_model_verify.py:1810-1817); I cannot execute pytest, and conditional skips (pwsh, superpowers registry at lines 1282-1289) are machine-dependent — the arithmetic is internally consistent but not executed. (2) Byte-identity of pinned strings was verified by visual comparison of the files read, not by running the test suite.

**Verdict: FIX** — solely for the misquoted replace-block extent at plan lines 730-732 (text cited from fallbacks.md:60-61 as if it were model-prompting-notes.md's third-bullet ending); all other claims pass.

To resume this session: kimi -r 1108e314-a88a-4af8-84be-41df037b7a7f
