All three delta edits verified against the working tree. Round-3 review:

**Preliminary: the refuted residual was real.** The mechanic the brief cites exists exactly as described: tools/verify-attestation.ps1:6-10 and :161-200 accept only the pushed sha itself carrying an attestation (direct/fast-forward, line 161) or a merge whose parent2 is attested with base_sha == parent1 (lines 177-189), and explicitly reject when "extra commits or rebase since review" move the head (line 193). The plan's embedded README replacement says the same ("extra commits, a rebase, or a squash after the review all break the match and correctly force re-review" — docs/superpowers/plans/2026-07-26-seat-reshuffle.md:1390-1393, matching current README.md:320-323). So round 2's "commit the artifact with the debate record after the terminal verdict" would indeed have produced an unattested branch head; my round-2 verification only covered content commits before dispatch, missing the evidence commit itself. The refutation stands.

**Delta edit 1 — Step 5's ending. ACCEPTED, verified.** Step 5 now reads: "Do NOT commit the artifact — on this branch, ever: like the attestation, it is retained UNTRACKED at its rounds/ path through the diff debate, the attestation, the merge, and the push, so the reviewed head never moves out from under the review (the pre-push verifier accepts only the attested head itself or a merge whose second parent is the attested head — an evidence commit after the verdict would break the match and force a re-review). The SDD ledger records the artifact's path and SHA-256 hash; the attestation and the ledger are the durable verdict records" (plan lines 1632-1641). The round-2 instruction and its commit subject ("0.14.0: retain fable review artifact and diff debate record") are gone — grep for `fable-review|debate record|attest` across the plan shows no commit instruction for the artifact anywhere; the only Task 8 commit subjects remaining are the version bump (line 1550) and the pre-review smoke evidence (line 1605). The "like the attestation" analogy is accurate: attestations live untracked under `.git/parallax/attestations/` and "never ship" (plan lines 1379-1381), so the artifact's untracked retention mirrors an existing precedent rather than inventing one; the ledger (gitignored, line 1542) holding path + SHA-256 is the same durability class.

**Delta edit 2 — Files block split. ACCEPTED, verified.** Task 8 Files now reads "Create (evidence, committed in Step 4): ... smoke-* artifacts" and "Create (evidence, UNTRACKED through ship): ... fable-review.md" (plan lines 1540-1541) — the two evidence classes are split exactly as the brief states, and Step 4's procedure still commits the smoke artifacts before the review dispatches (lines 1601-1605).

**Delta edit 3 — execution-notes bullet. ACCEPTED, verified.** The bullet now reads "the artifact stays UNTRACKED through ship — diff-phase evidence never commits to the reviewed branch, so the attested head never moves" (plan lines 1659-1661), while still citing Task 8 Step 5's artifact as the diff debate's required input (lines 1658-1659).

**Post-verdict-commit sweep. CLEAN.** Every commit instruction in the plan lands before the review dispatch: Tasks 1-7's artifact commits (e.g. lines 239, 441, 576, 719, 997, 1480, 1531), Task 8 Step 1's version bump (1550), Step 4's smoke evidence (1605). Post-dispatch steps write only to the gitignored SDD ledger (1643-1646). The `## Debate record` appendix (1666-1670) is the mode-PLAN record appended at freeze — a pre-implementation, pre-review commit that is part of the reviewed content, not post-verdict evidence. No instruction anywhere commits post-verdict evidence onto the reviewed branch.

**Consistency spot-checks:** Step 5's retained-artifact flow still satisfies the new SKILL.md sentence it dogfoods — raw reply saved verbatim with base/head SHAs at the top, cited with adjudications in the round-1 brief (lines 1627-1631 vs. the pinned sentence at line 945); the observation-scope paragraph (1612-1616) and all other round-2 amendments are untouched by this delta.

**New risks:** none found with evidence. (Noted, not raised as a defect: untracked retention makes the artifact machine-local — but that is precisely the attestation lane's existing design class, cited as the 0.13.0 precedent, and relitigating it is outside this delta's scope.)

**UNVERIFIED:** the commit SHA 047dedfc99b85b88b04d60a2059d14f3e8631615 itself (no shell/git — working tree treated as the subject per the brief and verified by content).

**Verdict: PASS on the amended plan at subject revision 047dedfc99b85b88b04d60a2059d14f3e8631615.**

To resume this session: kimi -r 1108e314-a88a-4af8-84be-41df037b7a7f
