All amendments re-verified against the working tree. Round-2 review:

**Disposition 1 (my round-1 FIX — Task 5 Step 1 parenthetical). ACCEPTED, verified applied.** The parenthetical now reads "...through the end of the third bullet, `  skeptic pass in a FRESH subagent, not inline.` — i.e. everything before the blank line that precedes `## The reviewer lane (currently GPT-5.6 Sol via the codex CLI)`" (docs/superpowers/plans/2026-07-26-seat-reshuffle.md:738-743). The quoted ending matches model-prompting-notes.md:18 byte-exactly (two-space indent included), and the operative REPLACE block (plan lines 746-761) still matches model-prompting-notes.md:3-18. The fallbacks.md text is gone from the locator.

**Disposition 2 (no-unrequested-refactor snippet). ACKNOWLEDGED — no plan change, stands.** The spec's own sentence assigns the snippet to "backs the escalation lane's DECISIONS discipline" (spec line 232-233), and that is where the plan homes it: escalation-implementer.md's contract, "No improvements, no drive-by refactors, no scope adjustments" (plan lines 391-394).

**Disposition 3 (anchored-edit count). ACKNOWLEDGED.** Ten anchored edits across the five files (four Task-4 insertions; two replaces + one insertion in model-prompting-notes.md; two insertions + one replace in SKILL.md), each anchor verified unique in round 1 and re-confirmed at this revision for the moved/edited ones (fallbacks.md:160, frozen-plan-format.md:3 and :85, backup-lane.md:11, model-prompting-notes.md:3/74/222, SKILL.md:20/102/142-150).

**Amendment A (REQUIRED_REFERENCE_FILES moved Task 1 → Task 3). ACCEPTED, verified.** Task 1 Files/Steps no longer touch test_multi_model_verify.py (plan lines 56-58, 222-228, 239); Task 3 Files lists the Modify (line 451), Step 2 does the registration (lines 551-560), the same commit carries both files (line 576), and Step 3 correctly expects test_reference_files_exist/test_no_backslash_paths_anywhere to STAY green (lines 569-571). New RED baseline "10 failed, 154 passed, 1 skipped" (line 233) is arithmetically consistent: 154 passed + 1 skipped pre-existing baseline (static count: 155 `def test_` across test_multi_model_verify.py:107, test_attestation.py:27, test_backup_lane.py:10, test_flash_implementer.py:11) plus 10 failing new tests, with the registry tests never going red — honoring spec section 11's final bullet (spec line 340). Cascading expectations stay consistent: Task 2's "other seven still FAIL" (line 435), Task 5's "9 passed, 1 failed" (lines 968-970), the 164/1 final state (lines 1474, 1525), and the execution note "Only test_seat_reshuffle.py is RED between Tasks 1 and 6" (lines 1648-1649).

**Amendment B (conditional Participants sentence). ACCEPTED, verified.** The insertion now reads "the Participants line lists every lane that produced a terminal verdict, with its transport and session or agent id — a completed panel lists every member; a consented post-loss continuation lists only the surviving lanes, with the lost lane and its loss class in the failure prose" (plan lines 667-671), matching spec lines 189-190 ("The record's Participants field lists the lanes that produced terminal verdicts; the lost lane and its loss class live in the failure prose"). The strictest-lane FULL sentence below it (line 676) remains byte-identical to the Task 1 test pin (plan lines 184-187). Consistent with Task 7's expectation #5 (plan line 1515 — a completed panel, all lanes terminal).

**Amendment C (no-banner clause in both pin and insertion). ACCEPTED, verified byte-identical pair.** Test pin (plan lines 164-170, concatenated): "Panel participation: a user-invoked panel per references/panels.md is a second sanctioned entry route - the invocation itself is the consent, with no fallbacks banner (nothing degraded); containment, per-round evidence, and the write-probe apply unchanged, and no failure class is recorded because nothing substituted." — identical to the single-physical-line insertion (plan line 705). Covers spec lines 148-151 ("no fallbacks banner (nothing degraded)"). Additive placement after backup-lane.md:11-13 disturbs no 0.13.0 pin (test_backup_lane.py:75-109 pins only transport/evidence text elsewhere in the file; no backup literal introduced).

**Amendment D (README mermaid panel node). ACCEPTED, verified.** Node `P[/"panel option: user-invoked, any combo of<br/>Sol · Kimi · Fable, ≥1 cross-vendor<br/>(references/panels.md)"/]` dotted (`-.-`) to both debate nodes B and E (plan lines 1065-1066), and the fable end-review gate node R is in the same unified flowchart (line 1060) — satisfying spec lines 293-295. No test pins this mermaid; the pinned 0.13.0 edge in the second mermaid survives byte-exact (plan line 1141; test_backup_lane.py:145-146). Non-ASCII glyphs (≥, ·) follow existing README precedent (current README.md:107); the pure-ASCII constraint applies to test/eval content and test-pinned sentences only (plan lines 18-21), none of which this node is.

**Amendment E (Task 8 reorder). ACCEPTED, verified.** Smoke is now Step 4 (plan lines 1573-1615) with its artifacts committed inside the smoke procedure (lines 1601-1604); the dogfood review is Step 5, explicitly "runs LAST, after every content commit" against "the branch's immutable final head" (lines 1617-1624), and the artifact stays untracked through the diff debate, committing with the debate record after the terminal verdict (lines 1631-1635). The self-referential-range defect the amendment cites is real in the round-1 ordering and is genuinely removed: no content commit follows the review dispatch (Step 6 writes only to the gitignored SDD ledger — line 1541).

**Amendment F (execution-notes mislabel + observation scope). ACCEPTED, verified.** Execution notes now cite "Task 8 Step 5's fable-review artifact" (plan lines 1652-1655) — the "Task 4" mislabel is gone. The observation-scope paragraph (lines 1611-1615) keeps the dead-agent case observation-only, OPEN if no lane dies, never claimed covered — consistent with the probe record's narrowed claim (spec section 16, lines 455-457) and Global Constraints (plan lines 43-44).

**New risks:** none found. I specifically re-checked the amendment blast radius: Task 1's commit no longer includes test_multi_model_verify.py (line 239); no stale "12 failed, 152 passed" survives (line 233 updated); Task 8's title/Files/Interfaces still accurately name both artifacts (lines 1536-1545); all commit subjects remain imperative; no new `-m <literal>` or model-id literal introduced by any amended text.

**UNVERIFIED:** the commit SHA e44956e0479a164942b9bb47ed216b11f017fd12 itself (no shell/git — working tree treated as the subject per the brief and verified by content); pytest counts remain static-analysis-based, not executed.

**Verdict: PASS on the amended plan at subject revision e44956e0479a164942b9bb47ed216b11f017fd12.**

To resume this session: kimi -r 1108e314-a88a-4af8-84be-41df037b7a7f
